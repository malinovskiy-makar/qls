# Журнал сессии «Полировка «Стола» 24.09»

Ветка `feat/stol-polish-20260924` от `origin/fix/beta-hardening-20260924` (`9c3ab56`).
Карточка Notion: https://app.notion.com/p/3e5b11c92bc1816e93c5edfc64b63cc4

## Фаза −1. Сверка с реальностью

1. `git status` в `C:\Users\shipu\qls`: изменённых отслеживаемых файлов нет; лежат
   8 старых неотслеживаемых (`.txt`, `Claude outputs/`, `kachat_vektora.ps1` и т. п.),
   они были до сессии и в коммиты не попадают. Папка была на
   `claude/header-vp-section-ddf68c` (`8a329ab`).
   `git worktree list`: `qls-harden` занята `fix/beta-hardening-20260924` (`9c3ab56`),
   ещё `qls-fix-migration`, `qls-main-merge` (main), `qls-models`, `qls-olymp`,
   `qls-seo`, `qls-stolfix`.
2. `origin/fix/beta-hardening-20260924` = `9c3ab56` «журнал: Notion и следующий шаг —
   сессия завершена». В `origin/main` НЕ влита (`branch -r --contains` — только она сама).
   Ветка создана от неё.
3. Все опорные строки найдены: `_stol_help.html` — 9 из 9; `_stol_entry.html` — 3 строки,
   «складываются: «или»» дважды; `SITE_VERSION = 'Beta 1.0'`; учебник — фраза есть,
   перенесена на две строки («…до задач / заключительного этапа»); `CATALOG_FEATURES` —
   12 строк; `_clouds()` берёт бейджики из `problem.features` и `CATALOG_VIEW_LABELS`.
4. `manage.py check` — 0 ошибок. Локальная база (SQLite):
   - видимых задач (`_visible`: опубликована, не на ревью качества, не ждёт человека,
     текст в порядке) — **14 082**;
   - строк `ProblemFeature` — **44 848**;
   - задач с хотя бы одной особенностью кроме `с_реальной_олимпиады`: всего **27 490**,
     из них видимых **8 865**.
   Notion: прочитаны карточка задачи, пять решений 24.09, «Задачи» (В работе/Надо) и
   последние 8 «Решений». ADR 0131 занят, 0132 свободен.

## Фаза 1. Тексты и мелочи

Сделано: подписи ступеней («Поможет сделать первый шаг», «Обсуждение и проверка вашего
решения», «Эталонное решение» / «Свериться с «ключом»», карточка подтверждения «Открыть
эталонное решение?»); `.ladder-none` удалён, лестница рисуется только при хоть одной
ступени; вместо `.ladder-lead` — карточка-совет `#help-tipcard` + ⓘ `#help-info` в шапке
(оба `hidden` с сервера, `stol_task.js::bindTip` открывает одно по `weco_help_tip_seen`;
Esc перехватывается на `window` в фазе захвата — первый Esc гасит подсказку, а не фокус);
нижняя подсказка `#help-tip` и её строка в скрипте убраны; плейсхолдер «Вопрос по задаче
или решению». Вход: «Поиск ищет по смыслу…», «Enter – найти» и `.ask-kbd` убраны,
«складываются: «или»» убрано из шапки и подвала «Темы». Учебник — новый текст.
`SITE_VERSION = 'Beta 1.1'`, `test_nav` берёт версию из настроек (строку с Beta 9.9 не
трогал). `.fb-btn`: `height: 30px; box-sizing: border-box; padding: 0 10px` (узко — `0 7px`).

Замечено по ходу: без пояснения в подвале «Темы» у `space-between` остаётся один элемент,
и «Готово» уехало бы влево — добавлено `.se-dd-foot .se-done { margin-left: auto }`
(на телефоне сброшено в 0, там кнопка во всю ширину).

Инвариант grep по `catalog/templates`, `catalog/static`, `templates` — 0 у каждой из 9 строк:
«остаётся ИИ» 0, «Выделите фрагмент условия, чтобы обсудить его с ИИ» 0, «Enter – найти» 0,
«складываются: «или»» 0, «наведёт на первый шаг» 0, «теория, план, проверка» 0,
«в самом конце, когда» 0, «останется здесь и после перезагрузки» 0, «Вопрос по задаче или фото» 0.

Тесты: `test_stol_polish` (8), `test_stol_help`, `test_problem_page`, `test_catalog_redesign`,
`config.tests.test_nav`, `problems.tests.test_fb_button_style` — 117 тестов, код 0 после
одной правки фикстуры (у выпадашки «Сложность» нет данных — пояснения проверяются по шаблону).
Браузер: `test_stol_polish_browser` (новый `stol_polish_runner.mjs`) — совет: чистое
хранилище → карточка видна, ⓘ скрыт; «Понятно» → наоборот, ключ записан; перезагрузка и
смена задачи без перезагрузки → карточки нет; наведение → подсказка под кнопкой, текст
равен тексту совета; Esc → скрыта. Код 0. `test_css_comments` — 5, код 0. ruff — чисто.

## Фаза 2. Строка лимита ИИ

Сделано: `problems/ai/core.py::LIMIT_WARN_AT = 5` — единственное место числа; в контекст —
`limit_warn_at`, в `task_cfg` — `limitWarnAt`. `remaining` считается вошедшему, если
доступна проверка ИЛИ чат (раньше — только при `ai_available`, и при одном чате число было 0).
Строка `<p class="sv-limit" id="sv-limit">Запросов к ИИ на сегодня: осталось <b id="sv-remaining">N</b></p>`
рендерится вошедшему там же, где поле ввода, с `hidden` при `remaining > 5`.
`stol_task.js`: `t.limit(n)` — пишет число и `hidden = n > limitWarnAt`; зовётся после
ответа чата (`d.remaining`, при `error: 'limit'` — 0) и после проверки решения (так же).
Отказ по денежному потолку чата (`error: 'budget'`) счётчик не трогает — это не лимит обращений.

Тесты: `test_stol_polish.LimitLineTests` (8: 30 → hidden, 24 потрачено → hidden, 5 → видна
и текст ровно «…осталось 5», 0 → видна, только чат → число 3 верное, гость → строки нет,
`limitWarnAt` = `core.LIMIT_WARN_AT` = 5, ответ чата несёт `remaining`); переписаны
`test_attempt_api` (30 скрыта, 0 видна) и `test_no_slop` (новая подпись). Прогон
`test_stol_polish` + `test_attempt_api` + `test_no_slop` + `test_stol_help` — 52, код 0
(после правки фикстуры чата: без `AI_FAKE_REPLY` подставной чат отвечает ошибкой).
Браузер: остаток 6 → строка скрыта; реплика «Как решать?» → число 5, строка видна,
подпись «Запросов к ИИ на сегодня: осталось 5». Код 0.

## Фаза 3. «Свернуть» слева, тонкие полосы, искра пункта

Сделано: в `.help-head` порядок [свернуть ›] [искра] [«Помощь»/прогресс] … [«Решить заново»];
правило `.help-reset + .stol-strip-btn` убрано, `margin-left: auto` — у `.help-reset`.
Тонкие полосы: у существующего правила поля ИИ список селекторов расширен до `.help-body`
и `.rail-list` (и в `@supports not selector(...)` тоже). `.part-ask`: без рамки и фона,
`color: var(--accent-ink)`, искра 19 px, 32×32, скругление 8, наведение — `var(--accent-tint)`,
`top: 8px` (центр на прежних 24 px — уровень первой строки).

Ловушки замера: (1) Playwright в безголовом режиме запускает Chromium с `--hide-scrollbars`
— ширина полосы всегда 0; раннер полировки запускается с `ignoreDefaultArgs: ['--hide-scrollbars']`
(проверено зондом: без флага 5 px и 15 px у обычной полосы). (2) `.help-body` — flex-колонка:
пустой div-распорка сжимался до 0, переполнения не было; распорке дан `flex: none`.

Числа (обе темы, 1440×900): отступ «свернуть» от края панели ≤ 16 (padding 14), «Решить
заново» правее «Помощь»; `offsetWidth − clientWidth` = 5 у `.help-body` и `.rail-list`;
`.part-ask` — фон прозрачный, рамка 0px, svg 19, 32×32, цвет = `--accent-ink` темы.
Тесты: `test_stol_polish`, `test_stol_help`, `test_problem_page`, `test_css_comments` — 51, код 0;
браузер `test_stol_polish_browser` — код 0.

## Фаза 4. «Теги» на месте, особенности в строке свойств

Сделано: `.pp-sub` — сетка `minmax(0, 1fr) auto`: слева `.pp-sub-words` (характер, вид,
особенности, источник; переносятся внутри), справа кнопка `.pp-tags-btn`
(`aria-expanded`, `aria-controls="pp-tags-list"`), список `#pp-tags-list` — после `.pp-sub`
с `hidden`; стрелка через `[aria-expanded]`; переключатель — `stol_task.js::bindTags` в `init`
(через `once`). Особенности: `problems/enrich/features.py::CARD_HIDDEN = ('с_реальной_олимпиады',)`;
`_problem_context` берёт `feature_links.select_related('feature')` одним запросом, порядок
`Feature.order`; `_clouds()` получает их и больше не читает витрину. `CATALOG_VIEW_LABELS`
НЕ удалена: её читает `import_problem_attributes` (ключи витрины). Фразы «раскрываются без
скрипта» в `catalog/CLAUDE.md` не было — она была в комментарии шаблона, заменена там.

Тесты: `test_stol_polish.CardFeaturesTests` (5: ссылки «бизнесовое» и «на английском» с
`feature=`, «с реальной олимпиады» и «есть график» нет, переход по ссылке даёт каталог с этой
задачей; без особенностей — только «развёрнутая задача»; запрос к `problemfeature` ровно 1 и
число запросов с особенностями = без них; кнопка + скрытый список ниже, 2 ссылки-тега).
Переписаны `test_stol_api.test_tags_are_folded_for_everyone`, `test_problem_page` (класс
`pp-tags-btn`). Прогон `test_stol_polish` + `test_stol_api` + `test_problem_page` — 76,
первый прогон 1 падение (сравнивал разные задачи — у тегов свои запросы), после правки код 0.

Браузер (тест, синтетическая задача: 12 особенностей → 11 видимых, длинный источник, 3 тега):
кнопка до/после раскрытия/после сворачивания на месте, список ниже `.pp-sub`; 3 ширины ×
4 состояния панелей — «Теги» у правого края `.desk-col` ±1, слова внутри, прокрутки нет. Код 0.
Локальная база (самые длинные строки, руками раннером против runserver :8010): 54081
(6 особенностей, 4 тега, «Школково – банк задач по экономике») и 27618 (6 особенностей,
7 тегов, МатЭк 57) — 24/24 комбинации OK; кнопка стоит на 929,225 до и после клика;
список ниже строки на 8 px. Снимки светлой и тёмной темы — глазами в порядке.

## Фаза 5. Фокус: панели поверх

Сделано (`stol.js`): `state.focusPanels = {rail, help}` (в `localStorage` не пишется),
атрибуты `data-focus-rail` / `data-focus-help` на `.desk` ставит `paint()`. `overFocus()` —
фокус и не телефон: тогда `toggle()` переключает панель поверх, `openPanel()` открывает её
(лампочка, «Обсудить с ИИ», значок пункта, `/`, `j`/`k` идут через него), фокус не трогается.
`focusMode(false)` закрывает панели поверх; обычные панели и `weco_stol` не меняются.
Esc: сначала `closeFocusPanels()`, если закрывать нечего — выход из фокуса. Клик мимо
(не в ленте, не в помощи, не по `[data-stol]`, `[data-help-open]`, `[data-ask]`,
`[data-ask-part]`, `.ask-pop`, и не по узлу, уже убранному из разметки) закрывает панели поверх.
Задача, открытая из ленты поверх, — фокус остаётся, лента уходит (как шторка 1100–1399).
Телефон — прежняя ветка кода. CSS: при `min-width: 760px` в фокусе `.desk-side` открытой
панели снова `display: block` (нулевая колонка, `height: 0`, `position: static`), полоски
скрыты, сама панель `position: fixed`, 16 px от краёв, `var(--r-panel)`, `var(--surface)`,
`var(--shadow-pop)`, лента 316, помощь 392.

Браузер (1440×900): для «Помощи» и «Ленты» — F → пилюля: фокус остался, шапки нет, панель
видна, ширина 392 / 316 (±1), x `.desk-col` не изменился; Esc → панели нет, фокус есть;
второй Esc → фокуса нет. `]` в фокусе → помощь поверх; клик мимо → закрыта; выделение условия →
«Обсудить с ИИ» → помощь поверх, фокус включён. `weco_stol` до и после серии одинаков,
`data-help`/`data-rail` после выхода — как до входа. Код 0 (47,5 с). Снимок фокуса — в порядке.

## Фаза 6. Строки выдачи: метки у названия, ровные колонки

Сделано: `_rail_row.html` — `rail-line` = `rail-title` + группа `rail-marks` («тест» или
«✓ решение» с галочкой 13 px `var(--green-ink)`, ★ «сохранено» залитая 15 px `var(--accent-ink)`
с `aria-label="сохранена"`, «в N домашках» `var(--text3)` последней); группа пишется без
пробелов, пустая скрыта `:empty`. В `rail-meta` — только тема и звёзды. Широкая строка:
`grid-template-columns: auto minmax(0, 1fr) 240px 72px`, `rail-meta` — `display: contents`,
чип темы на всю колонку, подпись темы тает (`mask`), звёзды `justify-self: end`. Телефон —
`rail-meta` снова flex второй строкой, метки переносятся под название, если не влезли.
`stol_basket.js::markAdded` добавляет «в N домашках» в конец `rail-marks`. Комментарий
шапки шаблона обновлён.

Ловушки: (1) класс `.rail-head` уже занят шапкой ленты (`_stol_rail.html`, `justify-content:
space-between`) — первый вариант разнёс метки вправо; блок назван `.rail-line`. (2) Блок
названия сжимается до текста, и прежнее затухание 24 px съедало конец КОРОТКИХ названий;
теперь невидимый хвост `::after` 10 px и затухание на те же 10 px: короткое не тускнеет,
метки в 10 px от текста; длинное тает у обреза.

Тесты: `RailRowMarksTests` (4: решение и ★ в `rail-marks`, не в `rail-meta`; у теста «тест»
без «решение»; без темы и сложности группа на месте; репетитору «в 2 домашках» в группе).
Прогон `test_stol_polish` + `test_catalog_redesign` + `test_stol_teacher` + `test_problem_page`
— 86, код 0 (138 с). Браузер (выдача 15 строк, обе темы, 1440): левый край и ширина чипа темы
одинаковы, правый край звёзд одинаков, метка правее названия и левее темы, в `rail-meta` 0 меток,
фон соседних строк одинаков; документ не шире окна на 1440/1024/390; узкая лента 316, наложений 0.
Код 0 (58 с). Снимки 1440 светлая/тёмная и 390 — глазами в порядке.

## Фаза 7. Зубастость

Дефекты возвращались ПАЧКАМИ, каждый в свою проверку, откат — `git checkout -- <файлы>`
после каждого прогона (рабочая копия чистая перед и после). Так один прогон показывает, какая
проверка от какого дефекта покраснела; бюджет времени не позволял 30+ отдельных прогонов.

Серверные (`test_stol_polish`, 25 тестов — все покраснели):

| тест | дефект | красный |
|---|---|---|
| test_tip_card_and_info_button_once_each_both_hidden | карточка без `hidden` (S1) | да |
| test_guest_has_neither_card_nor_info | карточка у гостя (условие без `is_authenticated`, S1) | да |
| test_old_texts_gone_new_texts_present | «наведёт на первый шаг…» вернулась (S1) | да |
| test_bare_problem_has_no_empty_note_and_no_empty_ladder | лестница без правила нуля `{% if True %}` (S1) | да |
| test_catalog_entry_texts | «Enter – найти» вернулась (S1) | да |
| test_textbook_text | старый текст учебника (S1) | да |
| test_site_version_is_beta_1_1 | `Beta 1.0` (S1) | да |
| test_rule_sets_30px_without_vertical_padding | `padding: 5px 10px` (S1) | да |
| test_5_left_is_shown, test_0_left_is_shown, test_counted_with_chat_only | строка всегда `hidden` (S1) | да |
| test_task_cfg_carries_the_one_threshold | нет `limitWarnAt` в `task_cfg` (S1) | да (ошибка) |
| test_chat_answer_carries_remaining | ответ чата без `remaining` (S1) | да |
| test_card_shows_canonical_features_as_filter_links, test_card_hidden_constant_is_the_one_place | `CARD_HIDDEN = ()` (S1) | да |
| test_tags_are_a_button_and_a_hidden_list_below | список тегов без `hidden` (S1) | да (ошибка) |
| test_solution_and_saved_marks_sit_by_the_title_not_in_meta | ★ в `rail-meta` (S1) | да |
| test_test_has_test_mark_without_solution_mark | «тест» и «решение» вместе (S1) | да |
| test_tutor_sees_homework_count_inside_the_marks_group | «в N домашках» в `rail-meta` (S1) | да |
| test_30_left_is_hidden, test_6_left_is_still_hidden | строка никогда не скрыта (S2) | да |
| test_guest_has_no_line | строка лимита у гостя (S2) | да |
| test_features_cost_one_query_not_one_per_feature | без `select_related` — запрос на особенность (S2) | да |
| test_row_without_topic_and_difficulty_still_has_the_marks_group | группа меток только при метках (S2) | да |
| test_problem_without_features_has_only_kind | на карточке весь справочник `Feature` (S3) | да |

Браузерные (`test_stol_polish_browser`, проверки внутри одного теста):

| проверка | дефект | красный |
|---|---|---|
| совет: ключ, перезагрузка, смена задачи | «Понятно» не пишет `weco_help_tip_seen` (B1) | да |
| ⓘ: подсказка под значком | подсказка над значком (B6) | да |
| ⓘ: Esc закрывает | убран слушатель Esc (B6) | да |
| лимит после ответа чата | чат не зовёт `t.limit` (B1) | да (осталось 6) |
| «свернуть» у левого края | кнопка снова справа (B1) | да (отступ 346) |
| тонкие полосы 5 px | селекторы только у поля ИИ (B1) | да (15 px) |
| искра пункта: фон, рамка, 19 px, 32×32, цвет | прежний кружок (B1) | да (все 5) |
| «Теги» в первой строке и у правого края (12 комбинаций) | `.pp-sub` снова flex с переносом (B1) | да (24 строки) |
| «Теги» не прыгают, список ниже строки | список внутри строки с `flex-basis: 100%` (B2) | да (сдвиг 36 px) |
| фокус: пилюля, ширина, колонка на месте, Esc в два шага, `]`, клик мимо, выделение, `weco_stol` | `focusMode(false)` в `toggle`/`openPanel` (B1) | да |
| метки не в `rail-meta`, метка у названия | метки обратно в `rail-meta` (B1) | да |
| узкая лента без наложения | `.rail--narrow .rail-marks { margin-left: -30px }` (B6) | да (9 строк) |
| левый край и ширина чипа темы | прежняя раскладка: колонка `auto`, чип по тексту (B3) | да (937/958, 219/240) |
| правый край звёзд | тема и звёзды одним flex-блоком (B5) | да (981/1250) |
| без чередования | `nth-child(even)` с фоном (B6) | да |
| документ не шире окна (390) | `min-width: 440px` у входа (B6) | да |

Что пришлось переписать, чтобы проверки ловили: (1) раннер падал целиком на первом
дефекте (наведение на скрытую ⓘ) — секции обёрнуты в `section()`, падение секции — своя
красная строка `fail <имя>`, остальные меряются дальше; тест совета читает числа через `.get`.
(2) Проверка звёзд не краснела ни от одного дефекта — в фикстуре не было задачи без темы;
добавлена «Без темы» со сложностью 3, после этого B5 ловится. (3) Дефект B4 (звёзды без явной
колонки) оказался нерепрезентативным: авторазмещение сетки уводит звёзды во 2-ю колонку у всех
строк одинаково — заменён на B5.

## Фаза 8. Итоговая проверка

- `manage.py check` — 0 ошибок; `makemigrations --check --dry-run` — «No changes detected».
  Миграций в сессии нет.
- Прогон задетых модулей одной командой (код из лога, не через `| tail`): `test_stol_polish`,
  `test_stol_help`, `test_problem_page`, `test_attempt_api`, `test_catalog_redesign`,
  `test_stol_api`, `test_no_slop`, `config.tests.test_nav`, `problems.tests.test_fb_button_style`,
  плюс по поиску `test_stol_teacher` (метки репетитора) и `test_css_comments`, плюс браузерные
  `test_stol_polish_browser` и `test_stol_browser` — **212 тестов, OK, код 0, пропусков нет**, 646 с.
- `check_em_dash.py catalog templates config problems` — 0; `ruff` по всем тронутым `.py` — чисто.
- `catalog/CLAUDE.md` — абзац «Полировка 24.09.2026» в разделе «Стол»; ADR 0132 — ссылки на решения.
- Бюджет прогонов (~15 мин) превышен: всего около 40 мин, из них ~11 мин — итоговый прогон,
  ~15 мин — пачки зубастости (8 прогонов), остальное — по фазам. Полный прогон и пять джобов CI
  не запускались (по заданию).
- Для ручной проверки строки лимита (проверено на `admin`: 30 → 3 → 30): команды в отчёте.
