# Прогресс: сессия 5 ВП + редизайн профиля (22.09.2026)

**Ветка:** `claude/header-vp-section-ddf68c`
**Точка старта ветки:** `a2251488`
**Влитый main:** `3219570d` (витрина демо-ученика, 6 файлов, конфликтов нет)
**База тестов до правок:** 165 зелёных
(`vp.tests.test_landing vp.tests.test_take problems.tests.test_accounts`)
**Последняя миграция `problems` после слияния:** `0075_problem_progress`
→ новая в части B получит номер `0076`.

## Окружение
* Playwright есть (`node -e "require('playwright')"` — OK).
* PostgreSQL из `docker-compose.dev.yml` — проверяется в фазе A6.
* Длинное тире до правок: 13 штук в 5 файлах `vp/`
  (`intro.html` 8, `result.html` 2, `take.js` 1, `take.html` 1, `_take_item.html` 1) —
  это НЕ мой мусор, он был до сессии.

## Предположения, принятые без вопроса владельцу
1. **Локальная база разработчика** стояла на старой нумерации миграций ветки
   `feat/catalog-stol` (до переименования коммитом `c639594a`): таблицы
   `problems_searchlog`, `problems_problemprogress` существовали, а строки в
   `django_migrations` звались `0070_search_log`, `0071_feedback_pulse`,
   `0072_chat_turn_attachments`, `0073_problem_progress`. Переименовал четыре
   СТРОКИ в новые имена (`0072`…`0075`) — это не `migrate --fake`: схема реально
   применена, менялись только номера файлов. Резервная копия базы лежит в
   `scratchpad/db.sqlite3.before-vp-session5`. Прод и CI не затронуты.

## Фазы

| Фаза | Что | Статус |
|---|---|---|
| −1 | Сверка с реальностью | ✅ |
| 0 | Слияние main, макеты в ветку | ✅ |
| A1 | `is_ranked` + `vp/board.py` | — |
| A2 | Стена регистрации | — |
| A3 | Посадочная `/vp/` | — |
| A4 | `/vp/variants/` | — |
| A5 | `/vp/my/` + интро | — |
| A6 | Браузерные инварианты, зубастость A | — |
| B1 | Данные и формы профиля | — |
| B2 | Разметка и стили вкладки | — |
| B3 | Поведение: списки, аватар, обрезка | — |
| B4 | Тесты и зубастость B | — |
| C | Документация, штаб, локальные данные | — |

## Журнал
* **Фаза −1, 0 (22.09):** ветка переключена, `origin/main` влит без конфликтов,
  локальная база починена и промигрирована, макеты добавлены в индекс.
