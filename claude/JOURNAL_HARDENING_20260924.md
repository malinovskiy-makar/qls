# Журнал сессии «Укрепление беты 24.09»

Ветка `fix/beta-hardening-20260924` от `origin/main` @ `f50266d`, папка
`C:\Users\shipu\qls-harden` (junction на `venv313` и `node_modules` из `qls`).
Промпт сессии — 17 пунктов владельца, фазы −1…9. Карточка сессии в Notion:
https://app.notion.com/3e5b11c92bc1813eb587efa119842068

## Фаза −1. Сверка с реальностью — СДЕЛАНО

- `origin/main` = `f50266d` ровно, новее нет → файлы фаз никем не задеты.
- `manage.py check` в новой папке — 0 ошибок (без `.env`: `REDIS_URL` не
  задан, кэш в памяти — для тестов это норма).
- 9 из 9 фактов подтверждены чтением:
  1. `profile.html`: `{{ form.username }}` стр. 532, форма данных открывается
     на стр. 546 → поле вне формы.
  2. `profile.js` `start()`: `measure()` (228) и `crop.base` (229) до
     `dialog.showModal()` (235).
  3. `game/views.py:1868` `json.dumps(ctx['auto_set'])` → `game.html:2407`
     `{{ auto_set_json|safe }}`.
  4. `client_ip()` берёт первый из `X-Forwarded-For`; в nginx четыре
     `$proxy_add_x_forwarded_for` (стр. 196, 214, 359, 394).
  5. `catalog/views.py:524` — перед `rerank.apply()` только `seo.is_crawler`.
  6. `_stol_help.html:100` `help-sol-tpl` и 104 `help-part-answers-tpl` —
     без проверки входа.
  7. `teacher/picker.py` `cart_items()` (стр. 490):
     `Problem.objects.filter(pk__in=catalog_ids)` без видимости; ключей в
     `views_generate.py:484` сколько угодно.
  8. `game/leaderboard.py:78` и `:138` — `achieved=Min('created_at')`.
  9. `vp/board.py` `ranked_attempts()` — без `variant__is_published`.
- Notion прочитан: «Задачи» (В работе / Надо), 8 последних «Решений»
  (в т.ч. три от 24.09: «Решить заново», решения только вошедшим, умный
  поиск), карточки «Подозрительный трафик», «SEO-фикс», «Лидерборд по
  личным рекордам».

## Фаза 1. Безопасность — СДЕЛАНО

Коммиты: `0966c549` (XSS), `31bc6a6c` (IP + админка), `be0276bf` (потолки ИИ),
загрузки, `ad7027f8` (бэкап media), `6ec472ea` + правка (SECURITY.md).

- **1.1 XSS в наборе игры.** `game/views.py:1868` → `dumps_for_script`.
  Обход всего проекта: `|safe` внутри `<script>` — 8 мест; 7 уже шли через
  `dumps_for_script`/`script_json` (`calendar.html` ×2, `game.html` CFG,
  `stats.html`, `_stol_center.html`, `_catalog_modal.html`,
  `_typing_placeholder.html`), дырявое одно — `AUTO_SET`. `mark_safe(json…)`
  в Python — ноль. `added_ids_json` (`collection_detail.html`) без `|safe`,
  числа — безопасно.
- **1.2 Настоящий IP.** nginx: 4 места `X-Forwarded-For $remote_addr`
  (+ пояснение в основном `location /`); `client_ip()`: `X-Real-IP` →
  `REMOTE_ADDR`. Других читателей XFF в коде нет (grep — ноль).
  ⚠️ В репо лежит устаревший снимок `deploy/nginx/conf.d/weconomics.conf`
  (231 строка против 409, без блока dev) — на сервер не монтируется, не
  правил; вопрос в «Чистка: вопросы владельцу».
- **1.3 `/admin/login/`** — обёртка `views_auth.admin_login` тем же счётчиком
  `login`, маршрут выше `admin.site.urls`.
- **1.4 Потолки ИИ.** Виды, до которых дотягивается сайт: `search_rerank`
  ($0,50), `catalog_chat` ($1) — были; добавлены `catalog_check`,
  `catalog_ocr`, `homework_plan` — по $1, `.env`:
  `CATALOG_CHECK_/CATALOG_OCR_/HOMEWORK_PLAN_DAILY_CAP_USD`. Команды данных
  (`topic_tagging`, `answer_blind`, eval) — без потолка намеренно.
- **1.5 Загрузки** — 30/сутки по `FileAsset` (kind=student_work), 429.
- **1.6 Бэкап media** — `backup.sh`: tar.gz тома `weconomics_media`
  (alpine, том :ro, `gzip -t`), отправка обоих файлов при `BACKUP_S3_*`,
  ротация по видам. `bash -n`/`sh -n` чистые, `shellcheck` нет. Раздел
  «Восстановление media» в `docs/SERVER.md`.
- **1.7 Карточки Notion** — отложены до фазы 9 одним списком (их больше
  пяти: правило CLAUDE.md «массовая запись — после показа владельцу»).
  Почта Let's Encrypt уже есть карточкой («Надо», 07.09) — дубль не заводить.

Зубастость (дефект возвращён → красный → восстановлен → зелёный):
- `test_set_title_cannot_close_the_script_tag` — красный до правки (TDD).
- `ClientIpTests` ×3 — красные на `ratelimit.py` из `f50266d`.
- `AdminLoginRateLimitTests` ×3 (кроме контрольного «хороший вход») —
  красные с закомментированным маршрутом.
- `CostCapsTests` ×2 — красные без ключа `homework_plan`.
- `test_empty_pending_does_not_escape_the_daily_limit` — красный без проверки.

Инвариант: лейблы фазы (`test_auth_hardening`, `test_ai_cost_caps`,
`test_attempt_files`, `test_rerank`, `test_chat_api`, `test_xss_payloads`,
`game.tests.test_sets`, `config.tests`) — **256 тестов, OK, код 0**
(шаг B: 3 serial пропущены — браузерные config без node в этом наборе).

⚠️ `--scope-from-git` при правке `config/settings.py`/`urls.py` сам
расширяется до ПОЛНОГО прогона — запущенный было прогон остановлен (правило
сессии: полный — только CI). Дальше — явные лейблы.

## Фаза 2. Профиль — СДЕЛАНО

Коммит `291a3933`.

- **2.1 Обрезка.** `profile.js`: `showModal()` до `measure()`; «Сохранить»
  не отправляет при `!(scale > 0)` или без файла; «Другое фото» не закрывает
  окно. CSS телефона: `.pf-crop-dlg:not([open]){display:none}`.
- **2.2 Админка.** `UserProfileAdmin`: `exclude=['avatar']`, превью 96px +
  ссылка через `avatar`, действие «Удалить фото». Имя файла
  `avatars/<id>.jpg` (было `avatars/avatars/<id>[_суффикс].jpg`), старый
  файл удаляется перед записью. Старые файлы на бою не трогались.
  `NoOtherFileServingViewTests` зелёный без правок списка.
- **2.3 «Имя пользователя пустое».** Форма данных `id="pf-data-form"`, у
  `username` — `form="pf-data-form"`. Enter в поле логина отправляет форму
  (неявная отправка идёт владельцу поля по атрибуту `form`).
  Почему старые тесты не поймали: слали руками собранный словарь с
  `username`, а браузер собирает поля по разметке — поле вне формы в
  отправку не попадало.

Зубастость (5 файлов откатаны к `f50266d` → 7 из 7 новых проверок красные,
в т.ч. браузер: `scale` = 0 на 1440 и 390 — ровно боевой симптом):
- `test_every_profile_form_field_belongs_to_the_data_form` — красный.
- `test_unchanged_form_saves_and_keeps_the_username` — красный (200 вместо 302).
- `test_name_and_no_leftover_copies` — красный (`avatars/avatars/1_LAnime4.jpg`).
- `test_change_page_links_the_avatar_view_not_media` — красный.
- `test_admin_action_removes_the_photo` — красный.
- `test_crop_on_desktop_and_phone` (1440 и 390) — красный.

Инвариант: `test_profile_hardening` + `test_profile_account_tab` — 30 OK;
браузер обрезки — 1 OK (не пропущен); `test_media_route` +
`test_profiles_export` — 18 OK.

## Фаза 3. Умный поиск — СДЕЛАНО

Коммиты `cf9a31b0` (код и тесты), `docs/EMBEDDINGS.md`.

**3.0 Диагностика на бою (только чтение, SSH, 24.09 ~04:00 МСК).**
`AiUsageLog`, `kind=search_rerank`, потолок $0,50:

| Сутки | Вызовов | $ | Первый | Последний | Потолок выбран |
|---|---|---|---|---|---|
| 15.09 | 34 | 0,0405 | 00:00 | 23:30 | нет |
| 16.09 | 34 | 0,0537 | 00:32 | 23:06 | нет |
| 17.09 | 199 | 0,5004 | 00:37 | 20:42 | **20:42** |
| 18.09 | 205 | 0,5001 | 00:07 | 09:18 | **09:18** |
| 19.09 | 205 | 0,5010 | 00:00 | 07:35 | **07:35** |
| 20.09 | 24 | 0,0535 | 00:12 | 22:17 | нет |
| 21.09 | 17 | 0,0400 | 09:28 | 21:33 | нет |
| 22.09 | 1 | 0,0024 | 18:22 | 18:22 | нет |
| 23.09 | 14 | 0,0288 | 02:14 | 23:16 | нет |
| 24.09 | 8 | 0,0182 | 00:29 | 03:21 | нет |

После выкатки 19.09 (UA + robots) потолок не выбирался ни разу. `SearchLog`
за 10 суток: `fallback` 569, `rerank` 244. Строк «исчерпан суточный бюджет»
в логах `web` — 0 (контейнер перезапущен 4 ч назад, логи короткие). nginx за
72 ч: 52 обращения к платным адресам (`/catalog/?q=` 21, `filter-state` 23,
`map/?q=` 8) — все с браузерными UA (Chrome/Safari) плюс 1
`facebookexternalhit`; Amazonbot 41 раз ходил по другим адресам с `q=`.
Строки расхода пишутся без пользователя (`with_user=0`) — так устроено
`record_usage`, не ошибка.

- **3.1** robots: `/catalog/*?q=`, `/catalog/*&q=`, `/catalog/api/`; карта с
  `q` — `noindex` (только он, без заголовков темы).
- **3.2** `_catalog_context(paid=False)`: полная загрузка → `deferred`
  (краулер — `bot`), журнал не пишется, кука посетителя ставится; разметка
  `data-search-deferred`, CSS прячет порядок «по словам» только при
  `@media (scripting: enabled)`, `stol.js` сразу `busy(true)` +
  `F.refresh({log:true})`. `api_filter_state` — `paid=True` через
  `catalog/rerank_gate.py`. Заголовок `X-Weco-Search: 1` — в
  `catalog_filters.js` (refresh) и `stol.js` («Показать ещё»).
  ⚠️ Промпт называл `stol.js ~201` местом, где заголовок «уже ставится», —
  его там не было ни в одном из двух файлов: добавлен в оба.
- **3.3** общий кэш — кэш Django, ключ `rerank:v2:<sha256>` (запрос + ноги,
  глубина, потолок пула, пачка, модель, `semantic:index_version`), 24 ч,
  только удачи.
- **3.4** квоты `SMART_SEARCH_QUOTA_VISITOR/USER/IP` = 40/80/150; у вошедшего
  квота посетителя не применяется (иначе 40 срезало бы 80).
- **3.5** +24 маркера. ⚠️ `headless` делает ботом и Playwright с UA по
  умолчанию — браузерный тест ставит обычный UA.
- **3.6** статусы `deferred/bot/quota/budget`; WARNING раз в сутки;
  `AiUsageLog` в админке только на чтение; `rerank_spend`.
- **3.7** `record_usage` в `finally` — пишется и при упавшей пачке.

Зубастость: `seo.py`, `rerank.py`, шаблон, CSS, два JS откатаны к `f50266d`,
развилка во `views.py` возвращена к старой → **20 проверок красные**
(`test_rerank_gate` + браузер); отдельно `noindex` карты — красный без строки.

Инвариант «бот тратит $0 при любом UA и адресе»: `test_rerank_gate` —
полная загрузка `/catalog/?q=` и `/catalog/map/?q=` с UA браузера, Googlebot и
пустым → `apply` не вызван ни разу; `api_filter_state` без заголовка / без
куки / с UA бота / с пустым UA → не вызван. Браузер: до ответа видимых строк
«по словам» 0, запрос скрипта 1 (с заголовком), платный вызов 1, итог
отсортирован; без JS список виден (4 строки).
Прогон: весь `catalog` — **627 + 6 serial, OK, код 0**.

Владельцу после выкатки (только чтение): `manage.py rerank_spend` — в столбце
«потолок выбран» «нет»; `curl -sI -A 'Mozilla/5.0 (compatible; Googlebot/2.1)'
'https://weconomics.ai/catalog/?q=спрос' | grep X-Smart` → `bot`; админка →
«Расход на ИИ» → вид `search_rerank`, сегодня.

## Фаза 4. Защита банка — СДЕЛАНО

Коммиты: teacher-утечки, `46c526eb` (решения гостю), браузерный тест решения,
подборки, `69f2f5ec` (nginx), квота разных задач, ADR 0128/0129 + SECURITY.md,
правка двух старых тестов.

Путей массовой выгрузки было **5**, закрыто **5**: (1) решение/ответы/«Почему
так» в HTML страницы и (2) в `?pane=1`; (3) корзина репетитора
`cart/rows`; (4) «Целиком» и предпросмотр репетитора; (5) подборка —
добавление скрытых задач и `.tex` с ответами без счёта. Шестой путь —
выкачивание открытых условий подряд — замедлен (квота разных задач + nginx),
не закрыт: условия открыты для SEO осознанно.

- **4.1** решения/ответы к пунктам/«Почему так»/верный вариант теста — в
  разметке нет ни у кого; вошедшему — `POST /catalog/api/solution/<id>/`
  (партиалы `_help_solution.html`, новый `_help_part_answer.html`); гостю —
  карточка `help-invite-tpl` (текст как у ВП, `?next=` на задачу);
  `test-reveal` гостю — 403 → в интерфейсе приглашение.
- **4.2** `teacher`: `visible_catalog_ids()` — `base_queryset('tutor')` + свои
  работы; корзина ≤ 100 (400 сверх); предпросмотр через тот же шлюз (раньше
  без `content_status`). Подборки: добавление через шлюз страницы задачи,
  ≤ 50; `.tex`/PDF с ответами — вошедшему, 20/сутки (сверх — 429).
- **4.3** nginx: `map` по `$uri` → 5 зон `limit_req`, всё в одном
  `location /`; `deploy/nginx/html/429.html`. `nginx -t` локально НЕ прогнан
  (Docker Desktop выключен) — в файле выкатки `nginx -t` до `reload`.
- **4.4** `catalog/scrape_guard.py`: отметка «видел» + счётчик на окно (час,
  сутки) в кэше; роботы — обратный+прямой DNS в пуле потоков с таймаутом
  1,5 с, вердикт сутки, сбой — обычные квоты на 10 мин. `scrape_report`.
  ⚠️ В тестах выключена (`SCRAPE_GUARD_ENABLED` по `_TESTING`), иначе тысячи
  открытий задач с `127.0.0.1` упирались бы в неё посреди чужих тестов.
  ⚠️ Тестовому LocMem нужен `MAX_ENTRIES` побольше (300 по умолчанию
  вытесняют отметки) — на бою Redis.
- **4.5** `docs/SECURITY.md` «Защита от выгрузки банка», ADR 0129.

Зубастость: teacher 4/4; решения 5/5 (+1 контроль зелёный); браузер
решения — красный на старом `stol_task.js`; подборки 3/3; квота 4/4 +
отдельно «проверенный Googlebot» красный при сломанной проверке DNS.

Инвариант: `catalog` + `teacher` + 7 модулей `problems` (корзина и сборка
работ) — шаг A **852**, шаг B **7**; два старых теста утверждали решение в
гостевой разметке — переписаны под вход; после правки — 33/33.
Браузер «Стола» (`test_stol_browser`) — зелёный после смены JS решения.

## Фаза 7. Лидерборды Wecon Rush и ВП — СДЕЛАНО

Коммиты: 7.4/7.5 (`game/leaderboard.py`), ВП 7.7–7.9, `277bc1ee` (7.1–7.3, 7.6).

**7.0 Диагностика на бою до правок (только чтение, 24.09, 30 суток):**
- `game_start` 107, `game_end` 94, `GameResult` 164.
- «Потерянные» (есть `game_end`, нет результата того же режима в окне −3/+0,5
  мин): **0 из 94**. ⚠️ У 69 из 94 очки в событии `game_end` не совпадают с
  серверными — событие пишет очки клиента до поправки на точность; это не
  потеря результата. Брошенных (старт без конца) ≈ 13.
- `GameResult` по зачётности: `too_few_correct` 53, зачётных 49, `anonymous` 23,
  `quit` 21, `set_run` 10, `quota_exceeded` 4, `time_overrun` 2,
  `mistakes_run` 2. По концу: `lives` 111, `time` 29, `quit` 22, `set_done` 2.
- Игрок×режим в таблице: 14, дата в таблице ≠ дате рекорда: **2**.
- ВП: открытых зачётных с истёкшим временем **1**; зачётных сданных на
  неопубликованных вариантах **1**; людей с попыткой на время без зачётной —
  0 из 9; всего попыток 25, зачётных 14.

- **7.1** финиш: 3 повтора (1,5 / 3 / 4,5 с), `no_run` — без повтора; не
  дошёл — `summary.unsaved` и строка «Результат не сохранён…» на итоге.
- **7.2** `pagehide` → `fetch(..., {keepalive:true})` с `reason:'quit'` и
  `run_id`; сервер: новый старт сохраняет прежний раунд с ответами
  (`_save_abandoned_run`: законченный по жизням — `lives`, иначе `quit`;
  набор/дуэль/практику — нет); `ended_at` пишется при конце по жизням и при
  финише, `wall_ms` считается до него.
- **7.3** старт через `queueRun`; `run_id` в ответе старта, в `answer` и
  `finish`; несовпадение — 409 `run_mismatch` (старый клиент без `run_id` —
  без проверки).
- **7.4/7.5** `records()` — `Window(RowNumber)` по игроку (значение, время,
  id), дата и тай-брейк — строки рекорда; `my_row` — место по тому же
  порядку.
- **7.6** все 9 причин — целой фразой с числами из настроек (текст «Не в
  таблице: …»); отсчёт «с фильтрами – зачёт только по очкам» (доски
  «верных» и «скорости» такие раунды не берут; логика не менялась).
- **7.7** `board.close_lapsed_ranked()` в начале `rows()`: кандидаты одним
  запросом (`expires_at < now`), сдаёт то же правило (`views._lapsed` с
  запасом + `_finalize(auto=True)`).
- **7.8** `ranked_attempts()` — `variant__is_published=True`.
- **7.9** Выбор: в `start` проверка «прежняя попытка с таймером» учитывает
  только попытки с пустым `session_key` — у попыток вошедших он пустой, у
  перенесённых гостевых (`vp/signals.py`) — старый ключ сессии. Самая узкая
  правка: данные не трогаются, зачётность прошлых попыток не пересчитывается,
  касается только попыток до стены 22.09. Отвергнуто: помечать перенесённые
  попытки полем (миграция ВП за два дня до тура) и чистить `session_key` при
  переносе (затронуло бы `_owns` гостя).

Зубастость: 7.4/7.5 — 2/2 красные; ВП — 3/3 (+1 контроль); игра — 9 из 11
(2 контроля «лишнего не сохранять» зелёные на старом коде, как и должны),
включая браузер `test_browser_freeze` (новые случаи `finish_retry_saved`,
`finish_unsaved_plate`).

Инварианты: `vp` целиком — **359 + 1 serial (пропущен самим тестом), OK**;
`game` целиком — **990 шаг A** (1 падение — сторож полей сводки заметил
клиентское `unsaved`, добавлено в исключения рядом с `offline`, 40/40) +
**11 шаг B OK**; `test_browser_freeze` отдельно — прошёл, не пропущен.

## Фаза 5. «Стол»: раскладка и тексты — СДЕЛАНО
## Фаза 6. Чат — СДЕЛАНО

Коммит `398cc2e3` (обе фазы: общие файлы). Миграция **одна**:
`problems/0077_chat_quote_progress_reset` — 5 `AddField` с умолчаниями
(`ChatTurn.quote`, `quote_source`; `ProblemProgress.reset_at`,
`hints_before_reset`, `solution_before_reset`); `makemigrations --check` —
«No changes detected».

- **5.1** `body:has(.stol-app[data-view="stol"]) .site-version{display:none}`
  — следует за видом «Стола» без перезагрузки; `--top:48px`, к `100vh` —
  строка с `100dvh` (`.stol-app`, `.desk`, `.desk-side`).
- **5.2** `.rail-title`, `.rail--wide .rail-preview`, `.se-pill-l` —
  `clip` + `mask-image`; `weco.plainText` (срезает `.katex-mathml`) — в
  `titleOf`, цитате пункта, тексте выделения. Safari — в ручной чек-лист.
- **5.3** тексты и стартовый блок: `ladder-lead` и `help-tip` — только
  вошедшему с чатом и пока лента пуста; прячутся первым `t.feed`;
  `help_used` учитывает реплики чата (после `reset_at`). `.ai-note` удалён
  вместе с двумя правилами CSS.
- **5.4** «Спросить об этом» → «Обсудить с ИИ». **5.5** регистр.
- **5.6** `sync()` не меряет скрытое поле, `overflowY` по высоте, `focus`;
  полоса 5 px без стрелок, стандартные свойства — под `@supports not selector`.
- **5.7** вкладка «Лента» (`data-rail-tab="results"` не менялся; «Выдача» в
  `teacher/` и `problems/tests/` не тронута).
- **6.1** `selectionHit()`: условие → «Обсудить с ИИ», `#help-feed
  .ai-msg--ai` → «Ответить»; `quote_source ∈ {statement, reply}`, иное — 400;
  подпись в промпте; цитата в `ChatTurn` и в истории; `chat_export`.
- **6.2** история — до 200 от самых новых, по порядку, с цитатами. ⚠️
  Порядок реплик относительно подсказок после перезагрузки по-прежнему
  приближённый (у подсказок нет времени открытия) — карточка в Notion уже
  есть («Панель помощи: порядок ленты после перезагрузки приближённый»).
- **6.3 СДЕЛАНА** — легла лаконично: иконка `refresh` в шапке помощи перед
  «свернуть», подтверждение карточкой в ленте, `POST
  /catalog/api/progress/<id>/reset/` (вход, CSRF, повтор безопасен), после —
  перезагрузка страницы. Кто читает `ProblemProgress`: только
  `catalog/progress.py` и `catalog/views.py` — статистика репетитора его не
  читает вовсе. Экран обнуляется (подсказки, решение, статус), факты до сброса
  — в двух полях-архивах (максимум подсказок, «смотрел решение»); чат и
  попытки на экране — после `reset_at`; `ChatTurn`, `CatalogAttempt`,
  `LearningEvent` не трогаются. `weco.track('help_reset')`.

Зубастость: код фаз 5–6 откатан к предыдущему коммиту (модели оставлены) →
**13 проверок красные** (9 FAIL + 4 ERROR), включая браузер «Стола».
Инварианты: `test_chat_beta` + `test_stol_help` + `test_problem_page` +
`test_chat_api` — 85 OK; браузер «Стола» — низ панелей = низ окна на
1440×900 и 900×1200, «Beta 1.0» скрыта на задаче и при переходе без
перезагрузки, телефон `top==120, bottom==0` — зелёный.

## Фаза 8. Безопасная чистка — СДЕЛАНО

Перед каждым удалением — grep по всему репозиторию (код, шаблоны, `docs/`,
`deploy/`, `scripts/`, `.github/`); после каждого шага — `manage.py check`.

| Шаг | Коммит | Что | Строк |
|---|---|---|---|
| 1 | `f148a5d7` | мёртвые функции: `visible_groups`, `eq_solution_steps`, `_figure_axes`, `duel_present`, `find_attempt` | −50 |
| 2 | `9451e5e5` | повторный `import os` в `config/settings.py` (`sys` там нужен — оставлен) | −1 |
| 3 | `ff405fd6` | шаблоны `student/progress.html`, `student/exam_result.html`, `calendar_student.html`, `calendar_teacher.html` (+ пометка в `docs/SECURITY-MATRIX.md`) | −394 |
| 4 | `c2e61d6e` | `django-redis`, `pdfminer.six` из `base.in`, дубль `PyYAML` из `local.in`; lock-файлы `pip-compile` без `--upgrade` — дифф только убирает эти два пакета и `charset-normalizer` (исключительная зависимость pdfminer) из `base.txt`, смен версий 0; `pip-audit base.txt` — «No known vulnerabilities» | −34 |
| 5 | `541c265c` | удалены `.playwright-mcp/` (32 файла) и `polish_screenshots/` (22); `git mv` в `claude/archive/root_20260924/`: 2 макета, 2 промпта, PDF «описание проблем в калке 2», папка `2025/` (4 PDF) | −15 107 (снимки/логи) |
| 6 | `4ef8dca6` | `_with_features_text`, `_deserialize`, `block_label` (+ константа `_BLOCK_LABEL`), теги `filter_param`/`set_param`/`remove_param`; осиротевшие импорты `TextField`, `Cast`, `fmt_num`; `catalog/CLAUDE.md` | −70 |

Кода Python/шаблонов удалено ≈ 545 строк; вместе со снимками инструментов —
≈ 15 650. Инвариант: `catalog game olympiads student calendar_stub config` —
**шаг A 1870 OK (skipped=1), шаг B 21 OK (skipped=4)**, код 0.

**Оставлено с причиной:**
- `Procfile` — промпт предполагал «две строки документации», на деле на него
  опираются 5 мест `docs/MIGRATION-CHECKLIST.md` и 1 в `ARCHITECTURE.md` как на
  описание запуска gunicorn; правка меняла бы смысл документа.
- Давние неиспользуемые импорты (`typing.*` в `game/generators/base.py`,
  `Fraction` в `_market.py`, `Q` в `catalog/filters.py`) — не мои, ruff
  проекта F401 не включает; не трогал.
- `student/submission_detail.html` — тоже осиротел по SECURITY-MATRIX, но в
  списке кандидатов не было.
- Пакет передачи Аничу, `scripts/anich_*`, `docker-compose.scoped-tests.yml`,
  `venv/`/`venv312`, флаги, `.claude/`, `olympiads/services.py` — не трогал.

**Management-команды, на имя которых нет ни одной ссылки вне своего файла
(только список, не трогал):** `audit_megarecords`, `batch2_ns_sweep`,
`batch2_unblock_post_apply_sample`, `blind_experiment_score`,
`build_audit_links`, `build_econ_terms_df` (поиск остановлен на первых
шести — полный список собрать отдельно, если понадобится).

### Чистка: вопросы владельцу
1. `Procfile` — удалять вместе с переписыванием `MIGRATION-CHECKLIST.md`
   (Render отключён) или оставить как историю?
2. `deploy/nginx/conf.d/weconomics.conf` в репозитории — устаревший снимок
   (231 строка против 409 в `available/django.conf`, без dev-блока), на
   сервер не монтируется. Удалить?
3. `student/submission_detail.html` — удалить вслед за остальными?

## Фаза 9. Финиш — проверки

- `ruff check .` — All checks passed (код 0).
- `bandit -r … -ll` — код 0, находок medium+ нет.
- `manage.py check` — 0 ошибок; `makemigrations --check --dry-run` — «No
  changes detected».
- Итоговый `run_tests.py --scope-from-git f50266d` раскрывается в ПОЛНЫЙ
  прогон (задет `config/settings.py`) — по правилу сессии не запускался;
  вместо него все 9 приложений прогнаны по частям после своих правок (см.
  фазы) + `problems calc2` отдельно в конце. Полный набор — CI после пуша.
- `problems calc2`: шаг A **4717, 19 FAIL + 4 ERROR**, шаг B 3 OK. Разбор:
  - 18 — известные с 09.09 (`embeddings_transfer` 10, `embedding_formula` 6,
    `corpus_diagnostics` 2; активна спецификация `v2_focus_repeat`);
  - `test_exams` (1), `test_obzor_build` (1), `test_work_settings` (1),
    `test_typography` (calc2, 2 тире) — **красные и на `f50266d`** (проверено
    временным worktree `qls-base-check` тем же набором модулей, 4 FAIL);
  - **`test_review_bundle` — мой**: офлайн-пакет ревью снимал страницу задачи
    гостем и ждал решение в разметке. Починено `e7bb0def`: пакет сам вставляет
    карточку решения тем же партиалом; заодно — мои «—» (страницы 429, подпись
    экспорта, причины «не в таблице») заменены на «–», сторож тире вернулся к
    счёту `main`. После правки: 142 теста задетых модулей — OK, кроме давнего
    `test_typography` (calc2, 2).

## Notion — записано 24.09 (список одобрен владельцем перед записью)

- «Задачи»: карточка сессии → **«На проверке»** + раздел «Сделано 24.09»;
  разделы «Сделано/Дополнено 24.09» — «Профиль: редизайн вкладки „Аккаунт“»,
  «Подозрительный автоматический трафик…» (статус «Готово» не трогал);
  дописки — CSP, HSTS, «Подключить внешнее хранилище бэкапов».
- «Задачи», новые «Надо» (6): `game:duel_new` на GET; гостевой просмотр пишет
  сессию и LearningEvent; жёсткие лимиты у провайдеров ИИ (владельцу); лимиты
  nginx для класса — решить до выкатки; вопросы чистки (Procfile, conf.d,
  submission_detail); `game_end` пишет очки клиента.
- «Решения»: новая — ADR 0131 (Wecon Rush); дописки — «SEO-фикс…» (ADR 0128),
  «Wecon Rush: глобальный лидерборд…» (ADR 0131). Три решения владельца от
  24.09 (решения гостям, «Решить заново», умный поиск) реализованы — не
  дублировались.
- «Результаты»: раздел «24.09.2026 — Укрепление беты» в начале страницы.
- Дубли не заводились: HSTS, CSP, внешнее хранилище, почта Let's Encrypt уже
  были карточками.

## Предположения

- `.env` в новую папку не копировал (секреты); для тестов не нужен.
- Потолок $1/сутки у трёх новых видов ИИ — значение по умолчанию из промпта.
- Notion-карточки 1.7 — в фазе 9 одним списком.
- Квота посетителя (40) у вошедшего не применяется — только его 80 и IP.
- Нет куки посетителя при заголовке скрипта — статус `bot` (скрипт без куки — не браузер человека).
- «Решить заново» сбрасывает и статус экрана («Как прошло?») — «как у новой задачи»; статус репетитор не видит.
- После «Решить заново» страница перезагружается целиком — проще и надёжнее, чем пересобирать панель на месте.

## Следующий шаг

Сессия завершена. Пуш — командой владельца:
`git -C C:\Users\shipu\qls-harden push -u origin fix/beta-hardening-20260924`,
дальше CI (полный набор), визуальная приёмка, решение по лимитам nginx,
выкатка по `claude/DEPLOY_HARDENING_20260924.md`.
