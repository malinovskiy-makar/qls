# Журнал дневной сессии «Стол» — 18.09.2026

- Ветка: `feat/catalog-stol` (поверх `feat/beta-prep`, та поверх `main` = `origin/main` = `83fdd67`)
- `main..feat/beta-prep` — 19 коммитов; `feat/beta-prep..feat/catalog-stol` — 26 коммитов ночи
- Промпта в репозитории не было — создан первым коммитом `70369dfc`
- Окружение: Docker — `qls_postgres_dev` (55432), `qls_redis_dev` (56379) подняты; `manage.py check` — 0;
  Playwright (node) есть; порты 8000 и 8611 свободны
- Рабочая локальная база — SQLite `db.sqlite3` (как ночью: `DATABASE_URL` не задан); PostgreSQL в
  Docker — для тестов. Каталог видит 14 082 задачи; `tutor@test.local`, `student1@test.local` есть
- Notion прочитан: «Задачи» (В работе — «Стол» 81b3; Надо — 38 карточек, из них по теме:
  баг «+ В домашку» 81ce, PDF/TeX 8154, числа карты 818d, копия решения 817a, фильтр тем
  конструктора 819e), «Решения» — все 11 решений от 17.09 «Действует», 81b9 «Пересмотрено»
- Базовый прогон `manage.py test catalog teacher` (PostgreSQL): **567 тестов, OK, 3,3 мин**, красных нет
  (ночной журнал называл 568; красного нет, разница в один тест не мешает — записано)
- Ответы владельца: `feat/beta-prep` — пуш и слияние позже, в отчёте S8; сервер на 8000 — да, в фоне

Куда продолжать: S4.1 · монтируемый движок карты (`_stol_map.html`, `stol_map.js`, переход по §6)

⚠️ РЕЖИМ С 18.09 (решение владельца после S1): остановок после визуальных фаз нет; стоп только перед
необратимым (S7, записи в данные, слияние, пуш) и один раз — переход в карту в S4. После каждой фазы —
3–5 строк в чат и сразу следующая. После каждой фазы — `test_stol_browser` (замеры числами на свежей
странице, без кликов), источник истины — README §1–§9 и `shots/` по числу и составу.

## Чек-лист

- [x] −1 Сверка, инвентарь, базовый прогон, вопросы владельцу
- [x] S1.1 Шаблон `stol.html` и адрес `/catalog/`
- [x] S1.2 Карточка поиска и заголовок
- [x] S1.3 Чипы и выпадашки (одно состояние с окном «Все фильтры»)
- [x] S1.4 Лента входа широкими строками, «Продолжить», «Случайная задача»
- [x] S1.5 Карта-фон (`topic_map_preview.js` с параметрами и выделением)
- [x] S1.6 Убрать галерею, модалку «Условие», процент близости
- [x] S1.7 Тесты `test_stol_entry.py`, переписанные старые, снимки → остановка
- [x] S2.1 Панель задачи одним ответом `?pane=1`
- [x] S2.2 Вид «стол» в том же шаблоне, `stol_task.js` с `init(root)`
- [x] S2.3 Переключение видов и адреса без перезагрузки
- [x] S2.4 Лента у задачи: Выдача / Похожие / Мои, «Дальше», соседи
- [x] S2.5 Ширины и панели
- [x] S2.6 Тесты `test_stol_pane.py`, `test_stol_browser.py`, снимки, видео → остановка
- [x] S3.1 Центр задачи
- [x] S3.2 Лестница и лента помощи
- [x] S3.3 Попытка из панели помощи
- [x] S3.4 Тест (README §5)
- [x] S3.5 Тесты, снимки → остановка
- [ ] S4.1 Монтируемый движок карты
- [ ] S4.2 Переход вход ↔ карта
- [ ] S4.3 Выбор = фильтры
- [ ] S4.4 Тесты, видео, снимки → остановка
- [ ] S5.1 Корзина учителя в интерфейсе
- [ ] S5.2 «В домашку», «PDF или TeX», «Новая домашка»
- [ ] S5.3 Баг одиночной «+ В домашку» (81ce)
- [ ] S5.4 Тесты, снимки → остановка
- [ ] S6.1 Телефон: вход, шторки, задача, помощь, карта, тест
- [ ] S6.2 Тесты, снимки → остановка
- [ ] S7.1 Стоп-гейт удаления → удаление
- [ ] S7.2 Сторожа
- [ ] S7.3 Документы и ADR
- [ ] S7.4 Notion
- [ ] S8.1 Полный прогон
- [ ] S8.2 Пять джобов CI
- [ ] S8.3 Полный набор снимков, таблица README → доказательство
- [ ] S8.4 Приёмка владельца
- [ ] S8.5 Отчёт

## Инвентарь ночной работы (файл → что в нём → раздел README)

С `feat/beta-prep` (часть A):
| Файл | Что | README |
|---|---|---|
| `catalog/search_log.py`, `SearchLog` (0070), `templates/_search_rating.html` | журнал поиска, `log=1`, `data-search-log`, плашка оценки | §2 (поиск) |
| `templates/_corner_stack.html`, `_pulse.html`, `_feedback.html` | стек плашек угла `#corner-stack` | §7, §8 (над полосами) |
| `catalog/chat.py`, `catalog/chat_files.py`, `ChatTurn.attachments` (0072) | чат с TeX, история `api_chat_history`, `quote`, до трёх файлов, вьюха вложения | §4 |
| `problems/ai/prompts.py` | промпт чата: TeX, «деньги словами» | §4 |
| `scripts/beta_shots.mjs` | снимки Playwright по наборам | S1–S8 снимки |
| `problems/tests/test_beta_smoke.py` | смок | – |

С `feat/catalog-stol` (часть B, ночь):
| Файл | Что | README |
|---|---|---|
| `catalog/preview.py` (+55) | `solution_is_statement_copy`, `strip_statement_retell`, `strip_score_tails` | §4, §5 |
| `catalog/progress.py`, `ProblemProgress` (0073), `POST /catalog/api/progress/<id>/` | статусы, «Как прошло?», подсказки/решение для восстановления, `continue_for`, `statuses_for` | §2 статусы, §3, §4, §5 |
| `catalog/map_numbers.py` | `db` у узлов и живое `c` | §6 |
| `catalog/views.py` (+124) | `api_rail_similar`, `api_rail_saved`, `api_progress`, контекст «Стола» в `problem_detail`, «Продолжить» в `problem_list` | §2, §3 |
| `catalog/templates/catalog/stol/_rail_row.html` | общий партиал строки ленты | §2, §3 |
| `catalog/templates/catalog/stol/_stol_css.html` | стили «Стола» (сетка, полоски, фокус, телефонная панель) | §1, §3, §8 |
| `catalog/static/catalog/js/stol.js` (170) | панели, `localStorage`, «Фокус», клавиши | §1 |
| `catalog/templates/catalog/problem_detail.html` (+70) | разметка «Стола» поверх старой страницы | §3 |
| `catalog/templates/catalog/problem_list.html` (+16) | «Продолжить» | §2 |
| `catalog/templates/catalog/_problem_test.html` | «Дальше ›» главной, клавиши скрыты на телефоне | §5 |
| `catalog/static/catalog/js/topic_map.js` (+22) | «Показать задачи» → `topic`/`tag` | §6 |
| `catalog/static/catalog/js/problem_page.js` (+23) | история разговора при загрузке | §4 |
| `teacher/views.py` (+50), `teacher/urls.py` | `POST /teacher/api/assignment/<pk>/add_problems/` | §7 |
| `docs/adr/0119`, `0120` | прогресс; «Стол» первым шагом | – |
| тесты: `test_stol_api.py` (369 строк), `test_preview.py`, `teacher/tests/test_basket_add.py`, `test_chat_beta.py` | | |

Расхождений журнала ночи с репозиторием нет.

## S1 — вход E3 (коммит a9f6df94 и следом)

- `problem_list` → `catalog/stol.html` (`view='entry'`), партиал `catalog/stol/_stol_entry.html`,
  стиль `catalog/static/catalog/css/stol.css`. `problem_list.html` ни одним адресом не используется.
- Чипы: выпадашки «Тема / Сложность / Задачи и тесты» нарисованы теми же атрибутами
  (`data-topic`, `data-diff`, `data-kind`, `data-count`), что окно «Все фильтры»; скрипт окна
  ловит их во всём документе → одно состояние без второго скрипта. `catalog_filters.js`:
  `weco.filters = {state, refresh, buildQuery}`, события `weco:filters-start` / `weco:filters` /
  `weco:filters-error`, `refresh({log:true})` для поиска, адрес меняется только на входе.
- Поиск без перезагрузки (`api_filter_state?…&log=1`), фразы ожидания, скелет из шести строк
  (с задержкой 250 мс у фильтра, сразу у поиска), «Показать ещё» на месте, плашка оценки
  поиска зовётся после подмены (`weco.searchRating.consider`).
- Строки — `stol/_rail_row.html` широкой плотности: статус (только у вошедшего), название,
  строка условия, чип темы, звёзды, «решение»/«тест», звезда «сохранено» (`_cards_with_marks`:
  статусы и сохранённые одним запросом на страницу). Появление лесенкой при поиске сохранено.
- Галерея, модалка «Условие», «+ В домашку» на карточке, процент близости — со входа убраны;
  `?view=gallery|table` → строки, параметр дальше не несётся; `_card()` без `score`.
- Карта-фон: `topic_map_preview.js` + `inert`, `zoom`, `nodeScale`, `pitch`, `cy`, `dim`,
  `selected`, `select()`, `getAngle()`; выбранные узлы — кольцо, подпись с ореолом, яркая связь
  тега с темой (`drawChosen`). Параметры фона: zoom 1,55, узлы ×1,1, наклон −0,26, центр 0,42, dim 0,5.
- Значки `network` и `sparkle` в оба набора (`_icon.html`, `_icons.html`).
- Снимки: `scripts/beta_shots.mjs <префикс> <порт> stol[:фаза]` → `reports/stol_20260918/shots/`;
  облако фона стартует лениво — сцена дожидается данных и досчитывает раскладку `pump()`.
- Тесты: `test_stol_entry` 12 новых. Зубастость — да, 4 дефекта: заголовок всегда → красный
  `test_title_only_on_a_calm_entry`; статусы потеряны → `test_guest_has_no_status_column_student_has`;
  чужой ключ варианта в выпадашке → `test_chip_and_window_give_one_address`; модалка на входе →
  `test_no_condition_modal_and_no_card_buttons`. После отката 12 OK.
- Задетые модули (11 модулей): 259 → после правок OK.
- `manage.py test catalog teacher` (PostgreSQL): 579 тестов, 7,8 мин — 2 красных, оба мои и
  починены: `test_problem_page.test_page_uses_its_own_wide_column` (класс полосы входа теперь
  `stol stol--entry`) и `test_contrast` — в тёмной теме звёзды 4,28 и «решение» 4,11 на песочной
  плашке ленты, бледная «☆» 2,23–2,37. Метки строки переведены на `--amber-ink` / `--green-ink`,
  бледность «☆» снята. После правки оба модуля — 15 OK.
- Локально (SQLite) ответ `api_filter_state` с двумя темами идёт ~6,5 с — это подсчёт чисел по
  группам на SQLite, а не новый код; на PostgreSQL в тестах — доли секунды.

## S1.x — водяные «W» (правка владельца 18.09, 13:50)

- Узор — `body::before/::after` в `templates/_tokens.html`. Гасится атрибутом
  `<body data-bg-pattern="off">` (правило рядом с узором); в `catalog/base.html` появился блок
  `body_attrs`. `stol.html` ставит атрибут для видов `entry` и `map`, задача (`stol`) — без него;
  при смене вида без перезагрузки — `weco.stol.setView(view)` в `stol.js` (единая точка смены вида,
  ей же пользуется S2/S4). Старый шаблон `/catalog/map/` (до S4) — тоже без узора.
- Сторожей узора в тестах не было — исключать нечего.
- Тест `test_stol_bg_pattern` (4). ⚠️ Первый вариант ловил ЛОЖНО: регулярка находила
  `<body …>` в тексте моего же комментария в `_tokens.html` (печатается в `<head>`). Комментарий
  переписан, тест ищет тег после `</head>`. Зубастость — да, 4 дефекта (правило гашения, вход с
  узором, карта с узором, `setView` включает узор не на том виде) → по 1 красному; после отката 4 OK.
- Решение записано в Notion «Решения» (не в «Решил сам» — решение владельца).
- Быстрый круг `--scope-from-git main` (скрипт выбрал полный набор): шаг A 6 489 тестов, 34,8 мин,
  `failures=17, errors=4` — ровно известное: 18 в трёх модулях отпечатка эмбеддингов + 3 теста
  конструктора работы (`test_exams`, `test_obzor_build`, `test_work_settings`, красные на `main`).
  Шаг B скрипт после красного шага A не запускает — отдельно в S8.

## S1-правки владельца (18.09, после первой приёмки)

1. **Облако не рисовалось при загрузке.** Первый кадр зависел от цепочки «видимость блока →
   простой браузера → кадры анимации»; в неактивной или фоновой вкладке (в панели браузера —
   `visibilityState: hidden`) она не доходила до кадра, пока его не перерисовало что-то постороннее.
   Починка — `eager` в `topic_map_preview.js`: фон входа стартует сразу, раскладку досчитывает
   таймером и рисует кадр напрямую; вращение — обычными кадрами. ⚠️ Мои снимки S1 ВРАЛИ: сцена
   «дожидалась облака» и сама звала `pump()` — то есть дорисовывала его. `pump` из скрипта снимков
   убран, сцена только ждёт `drawn > 0`.
2. **Ярче §6.** Прозрачность ставилась на каждый узел, и в местах наложения альфа доходила до
   183–213 из 255. Теперь облако рисуется непрозрачным в буфер и кладётся на экран одним слоем с
   прозрачностью 0,5: максимум альфы ровно 128 (замер). Выбранные узлы — поверх, полной яркости.
3. **Отклик чипов.** При выборе — сразу класс `is-loading`: скелет шести строк без задержки,
   «Обновляю…» с крутилкой в подвале выпадашки, числа приглушены; галочка ставится до запроса.
4. **Раннер замеров** `catalog/tests/stol_runner.mjs` + `test_stol_browser.py` (serial, причина
   записана): свежая страница, ни одного клика до замера. Сейчас меряет: облако (закрашено > 0,
   альфа ≤ 130; светлая/тёмная, с «уменьшить движение» и без), вход на 1280/1440/1600/1920 и
   360/390/430 (ширина документа ≤ окна, заголовок 34/26, чип 32/36, карточка поиска 820, колонка
   1120, текст поля 19), выпадашки 400/250/280 и ≤ 430 в окне 800, отклик выбора при ответе
   состояния с задержкой 3 с (через 150 мс: `is-loading`, «Обновляю…», 6 строк скелета, галочка).
   Зубастость — да, 4 дефекта: нет `draw()` в `settleNow` → «облако не нарисовано» (reduce);
   без буфера → «альфа 255 из 255»; скелет через 250 мс без `is-loading` → «без мгновенного
   отклика»; шапка гостя без правки → «вход 360: шире окна (366 > 360)». После отката OK.
5. **Раннер сразу нашёл дефект шапки сайта**: на 360 ряд гостя (обратная связь, тема, «Войти»,
   «Создать аккаунт», ☰) шире окна на 6 px — на ВСЕХ страницах. На ≤ 400 px «Создать аккаунт»
   уехал из шапки в панель ☰ (`templates/_nav.html`); вход, главная, игра на 360 — ровно 360.
- Задетые модули (topic_map, stol_entry, stol_bg_pattern, contrast, no_slop, config.test_nav) — 140 OK.
- Стоимость кадра фона в headless (программная отрисовка, DPR 2): ~13 мс против 0,4 мс до буфера —
  цена одного `drawImage` на 2880×1200. В настоящем браузере это работа видеокарты; если на слабых
  ноутбуках будет тяжело — буфер можно держать в DPR 1 (фон бледный). Записано, не менял.

## S2 — один экран: задача без перезагрузки

- Сервер: `problem_detail` собирает контекст задачи одной функцией `_problem_context`; прямая
  ссылка рисует `stol.html` (`view='stol'`), `?pane=1` отдаёт JSON `{id, title, url, center_html,
  help_html, similar_html, meta:{position,total,prev,next,saved,status,is_test}}` теми же
  партиалами. Шлюз качества один (404). Позиция и соседи на сервере — по фильтрам адреса
  (без `q`, порядок `-id`, как у входа); при поиске — `null`, считает сценарий по своему списку
  (второй прогон смыслового поиска ради соседей — секунды). `_similar_cards` удалена: блока
  «Похожие» под условием нет, похожие — вкладка ленты (`similar_cards` в контексте).
- Шаблоны: `stol.html` рисует и вход, и `_stol_desk.html` (лента · задача · помощь); на входе
  каркас пустой и скрытый. Партиалы: `_stol_rail.html`, `_stol_center.html` (README §3: строка над
  задачей 34 px, заголовок 26, свойства, условие 16,5/1,7, «Как прошло?» с «Дальше ›», соседи с
  названиями), `_stol_help.html` (полоска помощи и панель; пока чат — лестница в S3),
  `_stol_similar_rows.html`. Стили страницы задачи перенесены в `stol.css` без старого каркаса.
- Сценарий: `stol.js` — вид, панели (`localStorage`, первое открытие: лента свёрнута, помощь
  открыта), «Фокус», клавиши, лента «Выдача / Похожие / Мои ★» (Выдача — строки блока результатов
  входа, один источник), позиция «N из M», стрелки, соседи и «Дальше» по текущей вкладке, подмена
  задачи `?pane=1`, `pushState`, «назад» (вход не перерисовывается — скрыт, прокрутка
  возвращается), поиск и «Фильтры» ленты = состояние входа. `stol_task.js` — всё внутри задачи с
  идемпотентным `init(root)` (узлы помечаются `data-bound`, общие слушатели документа — один раз
  через `cur`). `track.js`: `weco.trackPage()` — просмотр экрана без перезагрузки.
- Замеры в браузере (`test_stol_browser`, свежая страница): смена задачи без перезагрузки
  (`__stolBoot` тот же), адрес `/catalog/problem/<id>/`, первое открытие «лента свёрнута, помощь
  открыта», позиция «1 из 26», «Дальше» = следующая по выдаче, «назад» ×2 → вход с тем же числом
  строк без перезагрузки; колонка условия 680 на 1280/1440/1600/1920 при всех четырёх состояниях
  панелей; лента 316, помощь 392, полоски 52; заголовок 26, условие 16,5; ширина документа ≤ окна
  (и 360/390/430). Первый прогон нашёл дефект: граница 0,5 px съедала пиксель (315/391/51) —
  граница заменена тенью.
- Зубастость — да: `test_stol_pane` (без шлюза → 404-тест красный; соседи наоборот → 2 красных;
  без `help_html` → красный) и `test_stol_browser` (клик не перехвачен → «перезагрузил»; колонка
  700 → красный; лента 300 → «лента 300, по README 316»). После отката всё зелёное.
- `test_stol_pane` — 7 тестов. Переписанные старые — в разделе ниже.
- Видео `reports/stol_20260918/s2_no_reload.webm` (вход → клик → задача → «Дальше» → «назад» ×2).

## S3 — помощь, центр, тест

- Центр (README §3): у каждого подпункта кнопка «спросить ИИ про пункт» (только при чате и входе) —
  в поле «Пункт а): », цитата пункта над полем, фокус в поле; всплывающая «Спросить об этом» у
  выделения в условии. Карточка попытки из центра ушла в помощь (режим «Проверь решение»).
- Помощь (README §4): пустое состояние — лестница «Берите столько помощи, сколько нужно, и не
  больше.»; ступени только при данных (подсказки, ИИ при ключе, ответ к пункту, решение/ответ
  теста); в банке ничего — строка «остаётся ИИ». После первого действия — лента по времени:
  карточки подсказок, ответы ИИ, решение; шапка «подсказок 2 из 3 · решение». Перезагрузка
  возвращает открытые подсказки и решение (сервер из `ProblemProgress`). Решение — через
  подтверждение текстом README. Без ключа чата — ни поля, ни ступени ИИ; подсказки и решение есть.
  Ответ ИИ — целиком, безопасной разметкой (`rich()`: абзацы, списки построчно, жирный, KaTeX).
  `api_hint` и перезагрузка отдают подсказку одной формой (`_hint_view`).
- Тест (README §5): «не то» — вариант красный и второй раз не выбирается; после верного — «Верно
  со N-й попытки», «вы выбирали», «верно», одна строка кнопок (Дальше · Ещё тест · Почему так ·
  Пройти заново). «Почему так» без повтора верного варианта (`preview.strip_correct_repeat`) и без
  разбалловки `( $4$ балла)` (`_RX_SCORE_TAIL`). Данные: 342 из 1 975 тестов начинают пояснение с
  повтора верного варианта.
- Замеры в браузере (`test_stol_browser`, дописано): ответ ИИ 5 000 знаков — все 147 шагов на
  экране, без обрезки; «спросить про пункт»; тест после ошибки и после верного. Всё S1/S2 — зелёное.
- Зубастость — да. `test_stol_help`: лестница всегда скрыта → красный (первый заход показал дыру:
  тест не проверял, что лестница ВИДНА, — утверждение добавлено, повтор красный); перезагрузка без
  подсказок → красный; `strip_correct_repeat` выключена → 3 красных. `test_stol_browser`: ответ
  режется до 1 200 знаков → «35 из 147 шагов»; ошибочный вариант снова выбирается → «тест после
  ошибки»; без «Пункт а): » → «кнопка у пункта». После отката — всё зелёное.
- `test_stol_help` — 15 тестов. Снимки S3 — 28 штук (демо-сервер 8611, поддельный ИИ).

## Решил сам

- S1: выпадашки чипов — серверная разметка с теми же `data-*`, что в окне, а не второй
  скрипт состояния: одно состояние держит `catalog_filters.js`. Откат: вернуть `pressed()` на `dlg`.
- S1: ряд выбранных чипов под поиском показывает только теги и то, у чего нет своего чипа
  (источник, особенности, характер); тема/сложность/вид/решение видны в самих чипах-кнопках.
  Откат: условие в `_catalog_chips.html`.
- S1: «Показать ещё» подменяет выдачу на месте (без перезагрузки); без скрипта — обычная ссылка.
- S1: скелет строк у фильтра — только если ответ дольше 250 мс (иначе мигание); у поиска — сразу.
- S1: подписи чипов («Темы · 2», «4★ 5★», «Тесты») считает скрипт из состояния, сервер рисует
  «Тема»/«Сложность» — вспышка на время загрузки скрипта (defer) незаметна. Откат: подписи на сервере.
- S1: карта-фон — `zoom 1,55` и узлы ×1,1 вместо «1,32» README: 1,32 — масштаб камеры перехода
  (§6), а при обзоре по высоте блока 600 px облако на 1,32 выходило вдвое уже макета 01. Подобрано
  по снимкам обеих тем. Откат: параметры в `stol.js`.
- S1: учитель между S1 и S5 не видит «+ В домашку» на строках входа (выпадашка убрана вместе с
  кнопками строк; корзина — S5). На странице задачи одиночная кнопка остаётся.
- S2: по прямой ссылке окна «Все фильтры» нет: оно тянет подсчёт чисел по всем группам (на
  SQLite — секунды), а задаче он не нужен; «Фильтры» и поиск ленты по прямой ссылке ведут во вход
  обычной ссылкой/формой. Вкладка «Выдача» по прямой ссылке скрыта (выдачи нет — правило нуля).
  Откат: включить `_catalog_modal.html` в `stol.html` для вида `stol`.
- S2: позиция и соседи при поиске — только на клиенте (по списку ленты). Откат: не выйдет без
  кэша выдачи поиска на сервере.
- S2: режимы чата подписаны по README («Теория / Как решать / Проверь решение»), текст реплики без
  набранного вопроса — в `data-prompt` («Объясни теорию» и т. д.), как было.
- S2: «Сохранить» — звезда без подписи (README §3, снимок 09), состояние в `aria-pressed`/`title`.
- S2: удалена `_similar_cards` и «Все похожие в каталоге» (поиск по началу условия): похожие —
  вкладка ленты; README этой ссылки не называет.
- S1: вернул в строки ленты анимацию появления выдачи поиска (решение 15.09) — иначе функционал
  терялся при переходе на общий партиал.
- S3: ступень «ответ к пункту» почти не встречается — у видимых задач ответы по пунктам есть
  только у 8. Ступень оставлена по README и правилу нуля (есть данные — есть ступень).
- S3: файлы к вопросу/попытке держатся в поле и уходят на сервер при отправке, а не при выборе:
  убрал файл до отправки — на сервере ничего не осталось.
- S3: порядок ленты после перезагрузки приближённый: подсказки → последняя попытка → решение →
  история чата. Точного времени открытия подсказок в `ProblemProgress` нет (только число), новую
  миграцию ради этого не заводил. Точный порядок потребует поля времени — отдельным решением.
- S3: демо-настройки `scripts/stol_demo_settings.py` (поддельный ИИ) — только для снимков на 8611.
- S3: кнопка Telegram и стопка плашек угла на ≥1100 сдвинуты левее панели помощи — иначе лежали
  поверх поля ввода.
- S3: правило `.stol [hidden]{display:none!important}` — стили кнопок перебивали `hidden`, и обе
  строки теста были видны разом.
- В банке у части решений буквальные «\n» вместо переноса строки — это данные, не правлю (запрет
  писать в данные задач); видно в «Почему так» отдельных тестов.

## Смотрел глазами

- s1_entry_empty light 1440 — как макет 01: заголовок, карточка поиска с воздухом, 5 чипов, пилюля
  «Карта тем: 29 тем, 343 тега», облако фоном до середины, «Случайная задача» справа. До правки
  облако в кадре не успевало появиться (ленивый старт) — сцена теперь ждёт данных.
- s1_entry_empty dark 1440 — до правки узлы крупные и яркие, спорили с заголовком; после zoom 1,55 /
  узлы ×1,1 — спокойно, заголовок читается.
- s1_entry_search light 1440 — шапка сжата, запрос в поле, плашка «Ищем по словам с умной
  сортировкой…» (локально нет плотного сервиса), «Похожих задач: не меньше 522 · из 14 082»,
  строки без процента, формулы в строке условия набраны.
- s1_entry_topic_dd light/dark 1280 — выпадашка у чипа, блок «Микро» с точкой, числа справа; до
  правки низ с «Готово» уходил за край окна 800 px — теперь высота по окну.
- s1_entry_filters light/dark 1440 — «Все фильтры 3», тёмный «Темы · 2», «Сбросить» в строке (до
  правки переносился), чип тега «# Оптимум монополиста: MR = MC ×», пилюля «Карта тем: выбрано 3»,
  на фоне две темы и тег с кольцами, подписями и яркой связью тега с темой; счётчик «1 403 задачи по
  темам «…», «…»» (до правки было коряво «задач под фильтры по темам…»).
- s1_entry_continue light 1280 — ученик: «Продолжить» с пилюлей (серое кольцо «открывал»),
  пустые кольца статусов в строках, «Случайная задача» справа.

- w_entry light 1440 / dark 1280 — «W» нет, фон — только облако; в тёмной не ярко, в светлой не
  сливается с бирюзовой карточкой.
- w_map dark 1440 (старая страница карты до S4) — «W» нет, фон — сама карта.
- w_problem light 1440 — «W» на месте, как на остальных страницах.

- s3_help_ladder light/dark 1280/1440 — как макет 09: лестница из трёх ступеней, «пока ничем не
  пользовались», поле с режимами внизу.
- s3_help_hints_ai — две подсказки карточками, ответ ИИ списком с формулами (до правки список внутри
  абзаца не разбирался — стал построчный разбор); лента прокручивается сама, страница — нет (до
  правки `scrollIntoView` двигал всю страницу).
- s3_help_confirm — текст подтверждения README §4; s3_help_solution — решение в ленте, кнопка
  решения ушла, шапка «… · решение».
- s3_help_check — карточка проверки решения в ленте (макет 12); до правки кнопка Telegram лежала на
  поле ввода — сдвинута.
- s3_test_wrong / s3_test_right — как макеты 17–19: красный «не то», серый «вы выбирали», зелёное
  «Верно со 2-й попытки», одна строка кнопок; до правки были видны обе строки кнопок (`hidden`).

## Переписанные и удалённые старые тесты

S3 (попытка и подтверждение переехали в помощь, подсказка теста — тоже; факт тот же). Первый прогон
`catalog teacher` после S3 дал 16 красных — все на старой разметке:
- `test_catalog.CurrencyEscapeFrontendTests` (2): решение рендерится `math(t.feed(sol))`.
- `test_attempt_api` (2): гостю — одна ссылка «Войти, чтобы спросить» и нет `data-mode="check"`;
  вошедшему — режим «Проверь решение», счётчик `sv-remaining`, заглушка `help-busy-tpl`.
- `test_attempt_files`: кнопка файла — `ai-clip` (у гостя нет).
- `test_chat_api` (2): карточка — `help-panel`; без ключа чата нет разговора и режимов
  «Теория»/«Как решать», поле остаётся для проверки решения.
- `test_problem_test_page` (4): «Показать ответ» — одна кнопка в центре и ступень `reveal` в помощи;
  правило «выберите один» не пишется (README §5: правило только у множественного); «не то —
  попробуйте другой вариант» в разметке; подсказка теста — в панели помощи, не в строке игры;
  тест без игры — обычная задача со ступенью «решение».
- `test_problem_page` (4): подтверждение — `help-confirm-tpl` с `data-confirm`; попытка одна на
  задачу (одно поле помощи, нет `sv-text`); «Показать ответ» один в центре; признак чата —
  подсказка «Выделите фрагмент условия».
- `test_stol_api.TestNextButtonTests`: «Дальше» со значком-стрелкой вместо «›».

S2 (страница задачи переехала в партиалы `stol`; факт тот же):
- `test_problem_page`: полоса `stol stol--stol`; облачка — `pp-stars` (звёзды с темами) и
  `pp-w` (вид словами), тест — «тест · один верный»; пустые свойства — нет `pp-stars`/`pp-tags`;
  текст подтверждения решения — README §4; похожие — 4 строки во вкладке ленты, скрытая не
  попала, «Похожих задач в банке нет.» у голой; «Сохранить» — `tb-btn is-on` и `aria-pressed`;
  карточка чата — `aria-label="Чат с ИИ по этой задаче"`.
- `test_chat_api`: карточка чата по `aria-label`; режимы `Теория / Как решать / Проверь решение`
  с `data-prompt`.
- `test_catalog.ProblemDetailTests.test_similar_block_excludes_hidden`,
  `test_semantic_degradation…похожих…`: `context['similar']` → `context['similar_cards']`.
- `test_catalog.CurrencyEscapeFrontendTests` (2): раскрытие решения идёт общим конвейером
  `renderMathIn` (`math(sol)` в `stol_task.js`), который маскирует и чистит эскейпы.
- `test_catalog_math_and_sources.SourceLinkTests`: разметка чипа источника сохранена
  (`pp pp--src`, `pp-ext` 12 px) — тесты не менялись.
- `test_stol_api.PhoneBarTests.test_bare_problem…`: «Дальше» в нижней панели стоит скрытым.


S1 (вход перешёл с `problem_list.html` на `stol.html`; факт тот же, разметка новая):
- `test_catalog_redesign`: `_card_html` ищет `rail-row`; подпись карты — `Карта тем: N темы, M тегов`
  в `_stol_entry.html`; `StripTests` — модуль собирает все пять чипов в прежнем порядке, в ряду под
  чипами только `source`, тема/сложность/вид/решение — `aria-pressed="true"` в чипах; крестик — у
  чипа источника; `CardTests` — «Галерея» отсутствует, `?view=table|gallery` → строки, заголовок —
  `rail-title`, звёзды — `★★★★<span class="rail-star-off">☆</span>`, тема — `rail-topic`, тест —
  метка «тест»; `ScoreTests` — процента нет и при поиске, порядок по близости сохранён.
- `test_filter_state_api`: `chips_html` не повторяет тему и сложность; `test_view_mode_and_query_ride_along`
  → `test_query_rides_along_and_gallery_is_gone`.
- `test_catalog_modal.ModalMarkupTests`: модалка «Условие» и `toggleHwDropdown` со входа ушли.
- `test_catalog_math_and_sources`: `…_and_in_modal` → `…_and_show_more` (формулы после «Показать ещё»).
- `test_search_busy`: появление лесенкой — `rail-row ct-appear`.
- `test_stol_api.ContinueRowTests`: класс блока `se-continue`.
- `test_topic_map.PreviewModeTests.test_preview_draws_no_labels_at_all` → `…_labels_only_the_chosen_nodes`
  (текст на холсте только в `drawChosen`).
- `test_no_slop`: обход шаблонов с подпапками (`catalog/stol/`), подпись карты «Карта тем: …»,
  на пустой базе нет `rail-row`.
