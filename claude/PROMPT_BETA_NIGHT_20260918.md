Привет! Прочитай CLAUDE.md, затем прочитай текущие задачи в Notion-штабе — обе базы, и «Задачи», и «Решения».

# Ночная автономная сессия «Бета»: правки перед бета-тестом (часть A) + каталог «Стол» (часть B) + финальная приёмка (часть C)

Машина: Windows, папка `C:\Users\shipu\qls`, команды через `venv313\Scripts\python.exe manage.py <команда>`.
База локально: PostgreSQL 17 в Docker (`docker compose -f docker-compose.dev.yml`, порт 55432); тесты на PostgreSQL – `--settings=config.settings_test_pg`.

Ветки (две, по порядку):
- `feat/beta-prep` – от `main` (ожидается `83fdd67`). Часть A. Утром сливается ПЕРВОЙ, даже если каталог не закончен.
- `feat/catalog-stol` – от `feat/beta-prep`. Часть B.

Файлы этой сессии в репозитории (положены владельцем, проверь в фазе −1):
- `claude/PROMPT_BETA_NIGHT_20260918.md` – этот промпт (единственная память после сжатия контекста);
- `claude/BETA_RECON_20260918.md` – разведка «Проблемы перед бета-тестом» (диагнозы, точные места в коде, что чего не трогать). Этот промпт – главнее разведки; где они расходятся, действует промпт;
- `claude/mockups/catalog_stol_20260917/` – макеты «Стола»: `README.md` (спецификация экранов), 4 доски `.dc.html`, `shots/` (31 снимок);
- журнал: `claude/JOURNAL_BETA_NIGHT_20260918.md` (создаёшь ты);
- отчёты и снимки: `reports/beta_20260918/` (часть A и C), `reports/catalog_stol_20260917/` (часть B).

Карточки в Notion (база «Задачи» `39bb11c9-2bc1-81c4-b770-000b48caa895`):
- часть A: `3deb11c9-2bc1-81a8-8889-fa521ca03823` («Бета-подготовка (ночь 18.09)…», статус «В работе»);
- часть B: `3deb11c9-2bc1-81b3-9a40-f5323da333d9` («Стол»).

Часы сессии (по часам машины, `Get-Date -Format HH:mm` в PowerShell или `date +%H:%M`):
- `ГРАНИЦА_B = 05:30` – если после приёмки части A (A8) время позже, часть B НЕ начинать, идти в часть C;
- `ГРАНИЦА_C = 08:00` – в любой момент части B, если время позже: закончить текущую подфазу, закоммитить, идти в часть C.
Владелец может поменять эти два значения до запуска.

## 0. Зачем и что должно получиться

Утром стартует закрытая бета с живыми людьми. Две цели ночи, в этом порядке:

**A. Сайт готов принимать людей и собирать данные.** Семь правок из разведки: снимок экрана в обратной связи (сейчас в админке всегда главная), карточка Telegram в профиле, поля профиля и выгрузка профилей, журнал поисковых запросов с оценкой выдачи и выгрузкой, плашка «Всё ли нравится?» для вернувшегося, начинка помощника ИИ (формулы в TeX, ответ не обрывается, несколько файлов, просмотр своего вложения, PDF), окно фильтров Wecon Rush в стиле каталога. Плюс смок-тест и снимки экранов, которые ты смотришь сам.

**B. Каталог «Стол».** Один экран вместо двух: вход с поиском-героем и декоративной картой тем, лента · задача (680 px) · панель помощи, карта тем как фильтр, корзина учителя, телефон. Спецификация – `claude/mockups/catalog_stol_20260917/README.md`, решения владельца – в Notion (список в фазе −1).

**C. Финал.** Полный прогон и все пять джобов CI на `feat/beta-prep` (и на `feat/catalog-stol`, если B закончена), снимки, документы, Notion, отчёт с командами пуша.

Главный критерий ночи: **ничего не сломать.** Правка, которую нельзя проверить числом или тестом, не делается. Функционал не теряем: поиск по смыслу с переранжированием, окно «Все фильтры», сохранение, «Ошибка в задаче?», подсказки, решение с подтверждением, тест как игра, чат ИИ с режимами и файлами, проверка решения, домашки, подборки, шлюз качества, тёмная тема, работа без скрипта по прямым ссылкам, игра целиком.

## 1. Правила ночи

Сессия автономная: владелец спит.

1. **Вопросов не задавать и ответа не ждать.** Любая развилка: выбери вариант, который ближе к разведке, README макетов и решениям в Notion, запиши в журнал в раздел «Решил сам» (что, почему, как откатить) и иди дальше.
2. **Жёстких остановок пять** (причина в журнал, журнал закоммитить, остановиться и написать отчёт):
   - рабочее дерево грязное чужими правками, или текущая ветка не `main` / `feat/beta-prep` / `feat/catalog-stol`;
   - нет `claude/PROMPT_BETA_NIGHT_20260918.md` или `claude/BETA_RECON_20260918.md` в репозитории;
   - для части B: нет `claude/mockups/catalog_stol_20260917/README.md` и четырёх `.dc.html` (часть A от этого не зависит – её делать);
   - `manage.py check` красный ДО твоих правок, или PostgreSQL в Docker не поднимается (`docker compose -f docker-compose.dev.yml up -d` не помог);
   - базовый прогон `manage.py test problems catalog game --settings=config.settings_test_pg` (см. фазу −1) красный больше чем в трёх известных модулях из CLAUDE.md (`test_corpus_diagnostics`, `test_embedding_formula`, `test_embeddings_transfer`).
3. **Журнал** `claude/JOURNAL_BETA_NIGHT_20260918.md` – одна память на всю ночь. Структура: шапка (ветка, время старта, версии), чек-лист ВСЕХ подфаз (`- [ ] A1.2 …`, `- [ ] P3.1 …`), после каждой подфазы – галочка, цифры, что решил сам, что не смог, какие тесты покраснели-позеленели. Плюс разделы «Решил сам», «Смотрел глазами» (по одной строке на снимок), «Удалённые и переписанные старые тесты», «Куда продолжать». Журнал коммитится вместе с кодом подфазы.
4. **Сжатие контекста.** Контекст будет сжат автоматически, возможно несколько раз за ночь – это штатно. После сжатия ты помнишь только сводку. Поэтому:
   - перед каждым большим чтением (файл больше 1 000 строк, `git diff` большой) – сначала закончи и закоммить текущую подфазу;
   - **первое действие после любого сжатия**: перечитать `claude/PROMPT_BETA_NIGHT_20260918.md` целиком, затем журнал, затем `git status`, `git branch --show-current`, `git log -8 --oneline`; продолжать с первой подфазы без галочки на текущей ветке. Если в журнале есть «Куда продолжать» – начать оттуда;
   - в журнал перед сменой подфазы всегда писать строку «Куда продолжать: <ветка> · <подфаза> · <следующий шаг одной фразой>»;
   - не держать в контексте лишнего: не читать `game/templates/game/game.html` целиком (7 200 строк) – только нужные диапазоны по `grep -n`.
5. **Возобновляемость.** Этот же промпт могут запустить повторно. Если ветка и журнал уже есть: фаза −1 заново (коротко), затем первая подфаза без галочки.
6. **Git.** `git add` только по именам файлов (никогда `.` и шаблоны). Коммит после каждой подфазы, сообщения вида `A4.2: плашка «Нашли, что искали?» – карточка и правила показа`. **Не пушить** (пуш отклоняется системой разрешений): точные команды – в итоговом отчёте. В `main` ничего не сливать. Ветку `feat/catalog-stol` создавать только от `feat/beta-prep` после A8.
7. **Данные.** Никаких правок существующих данных задач, тегов, пула игры. Схему меняют только миграции, описанные ниже; `makemigrations` только с именем приложения и с `-n <имя>`. На бой не ходить, ничего не деплоить, `.env` не трогать.
8. **Тесты.** Внутренний круг после каждой подфазы – модуль, который правил (`manage.py test problems.tests.test_feedback --settings=config.settings_test_pg`). В конце каждого раздела A1…A7 и каждой фазы P – быстрый круг `venv313\Scripts\python.exe scripts\run_tests.py --scope-from-git main --settings=config.settings_test_pg`. Полный прогон и пять джобов CI – ОДИН раз, в части C (в фоне, см. там). Без ручного `--parallel` в `manage.py test`. Команды дольше 10 минут запускать в фоне с выводом в файл и читать файл, не обрывать.
9. **Зубастость.** Для каждого нового теста: сначала коммит, потом временно верни дефект, убедись, что тест КРАСНЫЙ, верни код (`git checkout -- <файл>`), убедись, что зелёный. В журнал: «тест X краснеет на дефекте Y – да».
10. **Чистый код** (это будут читать люди):
    - новая логика – в своём модуле с докстрингом «зачем» в стиле проекта (`catalog/search_log.py`, `problems/pulse.py`, `problems/management/commands/profiles_export.py`), вьюхи тонкие;
    - константы с именами, без «магических чисел» в теле функций; лимиты – модульные константы рядом с существующими (`REPLY_MAX`, `FEEDBACK_MAX_SCREENSHOT`);
    - один источник правды на каждое правило: правило показа плашки живёт в одном месте, список вариантов поля – в модели, а форма и шаблон читают оттуда;
    - никакого копирования кода, кроме одного оговорённого случая (окно фильтров игры, A7) с комментарием-обещанием;
    - `ruff check <файлы>` чист по каждому коммиту; мёртвого кода и `TODO` без карточки в Notion нет;
    - тесты называются по поведению (`test_tall_screenshot_is_downscaled_not_dropped`), один факт на тест;
    - JS без фреймворков, без новых глобалей (одно пространство имён на файл), без `innerHTML` с сырыми данными;
    - комментарии объясняют «почему», не «что».
11. **Правила сайта** (из CLAUDE.md и `catalog/CLAUDE.md`, нарушать нельзя):
    - шлюз качества в КАЖДОМ новом ответе сервера: задача с `needs_quality_review`, `hidden_pending_review`, не `PUBLISHED` или не `ContentStatus.OK` не попадает ни в ленту, ни в панель, ни в «Похожие», ни в «Мои», ни в корзину, ни в `top_ids` журнала поиска;
    - правило нуля: число, имя, список на экране – только из данных; нет данных – нет элемента; слов-обещаний нет (`catalog/tests/test_no_slop.py`);
    - цвета только токенами `var(--…)` из `templates/_tokens.html`; карточке явный `background`; тёмная тема через `[data-theme="dark"]`;
    - иконки только SVG из `templates/_icons.html` + `templates/_icon.html` (оба набора держит `test_icons.py`); эмодзи в интерфейсе нет;
    - длинного тире в шаблонах нет (`scripts/check_em_dash.py`);
    - текст с формулами – партиал `_math_text.html`, KaTeX один конвейер `renderMathIn(root)`, `$$` раньше `$`;
    - фильтры – один модуль `catalog/filters.py`; имена параметров – `filters.PARAM`; адреса строит `filters.query()`;
    - **P0**: поля профиля (старые и новые) и текст поисковых запросов НИКОГДА не уходят в модель ИИ;
    - `teacher/picker.py`, `_filters.html`, `_filters_body.html`, `_filters_css.html` не трогать; в `game/` трогать только то, что названо в A7;
    - ADR на каждое архитектурное решение (`docs/adr/`, следующие свободные номера после последнего в папке), принятые ADR не переименовывать.
12. **Notion.** Ничего не удалять. Перед созданием карточки – поиск дубля. Статус «Готово» не ставить: максимум «На проверке». Направления брать из существующих: «Техническое», «Каталог и поиск», «Онлайн-игры», «Кабинеты», «Дизайн».

## 2. Карта сессии

| Шаг | Ветка | Что | Ориентир |
|---|---|---|---|
| −1 | main → feat/beta-prep | сверка, базовый прогон, журнал | 20 мин |
| A0 | feat/beta-prep | диагноз на данных, только чтение | 10 мин |
| A1 | feat/beta-prep | снимок обратной связи | 45 мин |
| A2 | feat/beta-prep | Telegram в профиле | 15 мин |
| A3 | feat/beta-prep | профиль: класс, уровни, поля, выгрузка | 60 мин |
| A4 | feat/beta-prep | журнал поиска, плашка оценки, выгрузка | 75 мин |
| A5 | feat/beta-prep | плашка «Всё ли нравится?» | 40 мин |
| A6 | feat/beta-prep | ИИ: TeX, длина, файлы, вложение, PDF, история | 75 мин |
| A7 | feat/beta-prep | фильтры Wecon Rush | 60 мин |
| A8 | feat/beta-prep | смок-тест, снимки глазами, документы, чекпоинт | 45 мин |
| B | feat/catalog-stol | P0…P10 «Стол» (только если время < ГРАНИЦА_B) | до ГРАНИЦА_C |
| C | обе | полный прогон + 5 джобов CI (в фоне), снимки, Notion, отчёт | 90 мин |

## Фаза −1. Сверка с реальностью

1. `git status`, `git branch --show-current`, `git log -3 --oneline`, `git fetch`; сравни `main` и `origin/main`. Если локальная `main` отстаёт и своих коммитов в ней нет – `git merge --ff-only origin/main` (не остановка, в журнал). Если `main` не на `83fdd67` – в журнал список коммитов после него и `git diff 83fdd67..main --stat`; если среди изменённых есть `templates/_feedback.html`, `problems/views_platform.py`, `problems/models_platform.py`, `catalog/chat.py`, `problems/ai/prompts.py`, `game/templates/game/game.html`, `game/views.py` – перечитай эти места перед правкой (номера строк из разведки могут сдвинуться). Создай `feat/beta-prep` от `main` (или переключись, если есть).
2. Проверь файлы сессии: промпт, разведка, папка макетов (см. остановки).
3. Версии и окружение: Python 3.13, Django 5.2; `docker compose -f docker-compose.dev.yml ps` (PostgreSQL 55432, Redis 56379; если не подняты – `up -d`); `venv313\Scripts\python.exe manage.py check`; `node -e "require('playwright')"` (нужен для снимков); `venv313\Scripts\python.exe -c "import fitz, PIL"`.
4. Убедись, что на месте: `templates/_feedback.html` (`grabShot`), `problems/views_platform.py` (`api_feedback`, `_feedback_screenshot`, `FEEDBACK_MAX_SCREENSHOT`, `api_track`, `TRACK_COOKIE`), `problems/models_platform.py` (`UserProfile`, `Feedback`, `ChatAttachment`, `ChatTurn`), `problems/forms_accounts.py` (`ProfileForm`), `problems/templates/platform/profile.html`, `problems/ai/prompts.py` (`catalog_chat`), `problems/ai/core.py` (`run(..., max_tokens=None, ...)`), `catalog/chat.py` (`REPLY_MAX`, `answer`, `save_attachment`, `derived_images`, `BAD_PDF`), `catalog/views.py` (`_catalog_context`, `problem_list`, `api_filter_state`, `api_chat`, `api_chat_upload`), `catalog/templates/catalog/_catalog_results.html`, `game/views.py` (`pool_tags`, `pool_counts_for`, `api_pool_counts`), `game/templates/game/game.html` (`#fmodal` около строки 2148), `problems/management/commands/analytics_export.py`, `scripts/run_tests.py`, `scripts/rush_shots.js` (образец снимков).
5. Локальная база: сколько задач видит каталог (`filters.base_queryset('catalog').count()`), есть ли демо-аккаунты (`seed_platform_demo`: `tutor@test.local`, `student1@test.local`; если нет – прогони команду, это локальная база). Цифры в журнал.
6. Базовый прогон на PostgreSQL: `venv313\Scripts\python.exe manage.py test problems.tests.test_feedback problems.tests.test_track problems.tests.test_accounts catalog.tests.test_chat_api catalog.tests.test_chat_upload catalog.tests.test_attempts game.tests.test_filters game.tests.test_filter_window --settings=config.settings_test_pg` – число тестов, время, красные. Красное до правок – в журнал поимённо, в конце не считать своим.
7. Прочитай в Notion карточку части A (`3deb11c9-2bc1-81a8-8889-fa521ca03823`) и решения от 15–17.09 про аналитику беты (`3dcb11c9-2bc1-81dd-86ea-ca02f167cb4b`) и обратную связь. Для части B – решения от 17.09.2026 (все «Действует», id начинаются с `3deb11c9-2bc1-`): `8183-a8b2-f502a47abf1a` («Стол»), `816c-8024-dbbcd7ea89f8` (панели S1), `81ce-b174-ebfff9d01eab` (вход E3), `8184-b1d2-e067ea43700f` (карта = фильтры), `8113-9e54-f880c2eff8ab` (теги свёрнуты), `8170-aee2-d29f9ac6c562` (под ученика), `81dc-b8b0-fb313edd6cac` (ответ по пункту), `818f-b834-ddc11f9eda62` (первое открытие), `81ae-86a1-c5c609bffe00` (галерея убрана), `8106-aea8-fd8fe1d06ba9` (тест без потока), `8189-9899-dc3aed86b964` (корзина). Карточка `81b9-b6e5-d6e01f09255a` – «Пересмотрено», не выполнять.
8. Создай журнал: шапка, полный чек-лист подфаз A0…A8, P0…P10, C1…C5, время старта, «Куда продолжать: feat/beta-prep · A0».

Результат: раздел «Фаза −1» с фактами и цифрами; ветка `feat/beta-prep`; журнал закоммичен первым коммитом.

---

# ЧАСТЬ A. Ветка `feat/beta-prep`

Каждый раздел A1…A7 заканчивается одинаково: тесты раздела зелёные и зубастые; быстрый круг `--scope-from-git main` зелёный (кроме известных красных из фазы −1); `manage.py check`; `makemigrations --check --dry-run` молчит; `ruff check` по изменённым файлам; `scripts/check_em_dash.py`; коммит; журнал с цифрами. Ничего из раздела не оставлять «наполовину»: если раздел не удаётся закончить, откати его целиком (`git checkout -- <файлы>`, миграцию удалить), запиши причину и иди дальше – утром сливается только целое.

## A0. Диагноз на данных (только чтение, 10 минут)

Отчёт `reports/beta_20260918/DIAG.md`:
1. **Снимки обратной связи.** В `manage.py shell` на локальной базе: `Feedback.objects.count()`, `exclude(screenshot='').count()`, `page_key` у записей со снимком, `screenshot_note` у записей без снимка. Ожидание (по коду): со снимком – почти всегда `home`; без снимка – `ok`. Если записей меньше пяти – диагноз на данных не подтверждается и не опровергается, это нормально: подтверждение даст браузерная проба в A1.1.
2. **Профили.** Сколько `UserProfile`, у скольких заполнен `grade`, распределение значений, сколько `city` непустых. Эти числа – инвариант миграции A3.
3. **Пул игры.** Сколько тегов возвращает `game.views.pool_tags()` сейчас; сколько из них `kind='canonical'`; сколько уникальных имён тем в `GameQuestion.topics` и сколько из них НЕ совпадают с именами канонических `Topic` (нестрогое сравнение: регистр, «ё», тире, кавычки). Списки несовпавших – в отчёт. Эти числа – инвариант A7 и материал для карточки в Notion.
4. **Чат.** Сколько `ChatTurn`, у скольких `len(reply)` ровно 900 или 1200 (обрезанные), у скольких вложение. Число обрезанных – в карточку `3deb11c9-2bc1-81e1-9c75-e8fec5c734b2`.

Коммит `A0: диагноз на данных`.

## A1. Снимок экрана в обратной связи (разведка §1)

Зачем: сейчас клиент снимает ВЕСЬ документ (`html2canvas(document.documentElement, …)`), сервер молча выбрасывает снимок выше 4 000 px и тяжелее 2,5 МБ, а `screenshot_note` остаётся клиентским `ok`. Доживают только короткие страницы – главная.

**A1.1 Клиент снимает видимую область.** `templates/_feedback.html`, `grabShot()`. Точный набор ключей html2canvas сверь с `static/vendor/html2canvas-1.4.1/` (в минифицированном исходнике ищи `windowWidth`, `scrollX`, опции `x`, `y`, `width`, `height`) – по памяти не писать. Кандидаты: (а) `{x: window.scrollX, y: window.scrollY, width: clientWidth, height: clientHeight, scrollX: 0, scrollY: 0, windowWidth, windowHeight}`; (б) то же, но `scrollX: window.scrollX, scrollY: window.scrollY`. Какой верный – решает проба, не мнение: скрипт `scripts/feedback_shot_probe.mjs` (Playwright, по образцу `scripts/rush_shots.js`, сервер на порту 8611) открывает `/catalog/` при 1280×800, вставляет в документ полосу `position:absolute; top: 2010px; height: 40px; background: #ff0066` и полосу `top: 0; background: #0066ff`, прокручивает на 2 000 px, вызывает тот же код, что `grabShot` (вынеси сборку опций в функцию `shotOptions()` рядом с `grabShot`, чтобы проба и кнопка использовали одно), декодирует JPEG и проверяет: высота снимка ≤ `clientHeight` + 2; пиксель на 20-й строке снимка розовый (`#ff0066` ± 40), а синего в кадре нет. Кандидат, прошедший пробу, остаётся; результат обеих проб – в журнал. Правило «снимок снимается ДО открытия окна» – оставить.

**A1.2 Сервер уменьшает, а не выбрасывает.** `problems/views_platform.py`:
- `FEEDBACK_MAX_SCREENSHOT = 5_000_000`;
- `_feedback_screenshot(shot)` возвращает пару `(blob | None, note)`, `note` из `'ok' | 'tall' | 'bad'`: не открывается как картинка – `(None, 'bad')`; длинная сторона больше 12 000 px – `(None, 'tall')`; больше 4 000 px – `image.thumbnail((4000, 4000))` и дальше как раньше (ширина 1 600, JPEG); иначе как раньше;
- в `api_feedback`: файл тяжелее потолка – `screenshot_note='big'`; ответ функции не `ok` – `screenshot_note` = её `note`; принят – оставить клиентское значение. Клиентское значение по-прежнему режется до 16 знаков;
- колонка `screenshot_note` в списке админки уже есть (`problems/admin_platform.py`, `list_display`) – убедись, что новые значения видны; `shot_link`/`screenshot_view` не трогать.

**A1.3 Тесты** в `problems/tests/test_feedback.py` (генерируй картинки Pillow в памяти):
- 1 600×5 000 → запись со снимком, высота сохранённого ≤ 4 000, `screenshot_note` клиентское;
- 1 600×13 000 → без снимка, `screenshot_note='tall'`;
- валидная картинка ~4 МБ (PNG с шумом, размер подбери) → принята; файл 6 МБ (содержимое неважно: отбой по размеру идёт до декодирования) → без снимка, `note='big'`;
- не картинка → сообщение сохранено, `note='bad'`; существующий `test_broken_screenshot_does_not_lose_the_message` зелёный;
- `shotOptions()` присутствует в разметке `_feedback.html` и `document.documentElement` больше не единственный аргумент (тест на шаблон, как `ScreenshotNoteScriptTests`).

Инварианты в журнал: до/после – число тестов в модуле; проба A1.1 – высота снимка и цвет пикселя числами.

## A2. Telegram в профиле (разведка §6)

`problems/templates/platform/profile.html`: карточка «Мы в Telegram» под основной формой (и на `?welcome=1` – это та же страница): значок `telegram` (добавь контурный SVG в ОБА набора `templates/_icons.html` и `templates/_icon.html`, `test_icons.py` сверяет их посимвольно), текст «Разборы задач и новости платформы», ссылка `https://t.me/weconomics_ru` (без `?direct`), `target="_blank" rel="noopener"`. Стиль карточки – как у существующих карточек профиля, цвета токенами. Кружок `.tg-fab`, `login.html`, `register.html` не трогать.

Тест в `problems/tests/test_accounts.py` (или где живут тесты профиля): вошедший видит на `/profile/` ссылку `https://t.me/weconomics_ru"` и в этой карточке нет `?direct`; `/login/` не изменился (ссылка с `?direct` на месте).

## A3. Профиль: класс, уровни, новые поля, выгрузка (разведка §2)

**A3.1 Модель** `problems/models_platform.py`, `UserProfile`:
- `grade` → `CharField('Класс', max_length=8, blank=True, default='', choices=Grade.choices)`, `Grade(TextChoices)`: `LE7='le7','7 класс и младше'`, `G8='8','8 класс'`, `G9='9','9 класс'`, `G10='10','10 класс'`, `G11='11','11 класс'`, `NONE='none','Уже не школьник'`;
- `Level` – коды те же, подписи: `novice` «Ещё не участвовал», `basic` «Школьный или муниципальный этап», `region` «Региональный этап ВсОШ или отбор перечневой», `final` «Заключительный этап ВсОШ или призёр перечневой»; `LEVEL_HINTS` переписать под ось «дальше всего дошёл» (по одной фразе, без обещаний);
- новые поля, все необязательные: `prep_mode = JSONField('Как готовлюсь', default=list, blank=True)` со списком допустимых кодов `PREP_MODES = (('self','Сам'),('tutor','С репетитором'),('club','В кружке или школе'),('course','На курсах'))`; `hours_week = CharField(max_length=8, blank=True, default='', choices=HoursWeek)`: `lt1` «Меньше 1», `1_3` «1–3», `3_6` «3–6», `gt6` «Больше 6»; `source_channel = CharField(max_length=16, blank=True, default='', choices=SourceChannel)`: `telegram` «Telegram-канал», `teacher` «Преподаватель», `friend` «Друг», `search` «Поиск в интернете», `other` «Другое»; `olympiad_history = JSONField(default=list, blank=True)` с `OLYMPIAD_HISTORY = (('vsosh_school','ВсОШ, школьный'),('vsosh_municipal','ВсОШ, муниципальный'),('vsosh_region','ВсОШ, региональный'),('vsosh_final','ВсОШ, заключительный'),('listed','Перечневые'),('none','Ни одной'))`;
- `city` и `goal` уже есть – только форма.

**A3.2 Миграция** `makemigrations problems -n beta_profile_fields`, затем открой файл и сделай `grade` тремя операциями: `AlterField` в `CharField(max_length=8, null=True, blank=True)`, `RunPython(forward, backward)`, `AlterField` в итоговое поле (`null=False, default=''`). `forward`: `'5'|'6'|'7'` → `'le7'`, `'8'..'11'` как есть, `NULL` → `''`; `backward`: `'le7'` → `'7'` (потеря 5/6 – в докстринг миграции), `'none'` и `''` → `NULL`, остальное как есть. Остальные поля – обычными операциями в той же миграции. Проверка обратимости на локальной базе (это база разработки, не бой): число профилей с непустым классом до (A0) = после; `migrate problems <предыдущая>` → числа вернулись; `migrate problems` снова. Числа в журнал. `makemigrations --check --dry-run` молчит.

**A3.3 Все места чтения `grade`** (разведка нашла): `profile.html:~262`; `teacher/templates/teacher/groups/list.html:~113` – `{{ profile.grade }} класс` → `{{ profile.get_grade_display }}` (иначе «none класс»); `problems/management/commands/seed_platform_demo.py` (четыре места с числами → строки `'9'`, `'le7'` и т. п.); тесты `grade=` в `problems/tests/` и `teacher/tests/` (`grep -rn "grade=" problems/tests teacher/tests`). `grep -rn "\.grade\b" --include=*.py --include=*.html` – убедись, что ничего не сравнивает с числом.

**A3.4 Форма и экран.** `ProfileForm.Meta.fields = ('grade','school','city','level','goal','prep_mode','hours_week','source_channel','olympiad_history','phone')`; множественные – `forms.MultipleChoiceField(choices=UserProfile.PREP_MODES, widget=CheckboxSelectMultiple, required=False)` с `clean_*`, который оставляет только известные коды и отдаёт список; одиночные – `select` с пустым «не указано». В `profile.html` – в существующем стиле формы, подписи по-русски, над блоком новых вопросов одна строка: «Необязательно. Помогает нам понять, кто пользуется платформой во время беты.» Порядок: класс, школа, город, уровень, цель, как готовлюсь, часов в неделю, какие олимпиады писал, откуда узнали, телефон.

**A3.5 P0-сторож.** Существующие тесты `catalog/tests/test_attempts.py::test_prompt_carries_the_problem_but_no_profile_fields` и `catalog/tests/test_chat_api.py::test_reply_comes_back_and_prompt_has_no_profile_fields` расширить: заполнить ВСЕ поля профиля маркерами (`city='ГородМаркер'`, `goal='ЦельМаркер'`, `prep_mode=['tutor']`, `school='ШколаМаркер'` …) и проверить, что ни один маркер и ни одно имя нового поля не попало ни в системный блок, ни в текст запроса. `grep -rn "profile" problems/ai/` – ничего нового не должно появиться.

**A3.6 Выгрузка** `problems/management/commands/profiles_export.py` по образцу `analytics_export` (`--since`, `--until` по `user.date_joined`, `--out`, CSV `utf-8-sig`). Колонки: `user_id, date_joined, role, grade, level, city, school, hours_week, source_channel, prep_mode, olympiad_history, goal`; списки – коды через `|`; `city` – `strip().lower()`; **без** телефона, логина, почты, имени. Тест: два пользователя → две строки, заголовок точный, нет колонки `phone`, город в нижнем регистре, `--since` отсекает.

Тесты формы: сохранение всех новых полей и чтение обратно; неизвестный код в `prep_mode` отбрасывается; `grade='none'` показывается как «Уже не школьник» в списке групп учителя; `LEVEL_HINTS` покрывает все коды `Level`.

## A4. Умный поиск: журнал запросов, плашка «Нашли, что искали?», выгрузка (разведка §4)

Самый важный раздел по данным: сейчас текст запроса не сохраняется нигде.

**A4.1 Модель** `SearchLog` в `problems/models_platform.py`: `ts` (auto_now_add, индекс), `user` (FK, null, SET_NULL), `visitor` (CharField 64, blank, индекс), `session_key` (40, blank), `query` (CharField 300), `status` (16, blank – `smart_search_status`), `ms` (PositiveIntegerField, null), `total` (PositiveIntegerField, default 0), `degraded` (Boolean), `top_ids` (JSONField, default list), `rating` (CharField 4, blank, choices `yes`/`no`), `rated_at` (null), `rating_text` (CharField 300, blank). `Meta.indexes`: `(visitor, ts)`. Регистрация в админке `problems/admin_platform.py`: список по `ts`, поиск по `query`, фильтр по `rating`, только чтение. Миграция `makemigrations problems -n search_log`.

**A4.2 Запись – на сервере, при полном рендере.** Новый модуль `catalog/search_log.py`: `log_search(request, context) -> int | None` – зовётся ТОЛЬКО из `problem_list` после `_catalog_context` при `context['searched']` (не из `_catalog_context`, не из `api_filter_state`). Склейка: если у того же посетителя (`visitor` из `request.COOKIES[TRACK_COOKIE]`, а для вошедшего – `user`) та же нормализованная строка (`strip().lower()`) была меньше `DEDUP_SECONDS = 30` назад – вернуть id той строки, новую не заводить. `top_ids` – первые 10 id из `context['cards']` (они уже прошли шлюз качества). Всё в `try/except` как `log_problem_event`: упавший журнал не ломает страницу. Параметр `LOG_PARAM = 'log'`: `api_filter_state` тоже зовёт `log_search`, но только при `request.GET.get(LOG_PARAM) == '1'` – этим будет пользоваться «Стол» (часть B), в нынешнем каталоге его никто не шлёт. В `_catalog_results.html` рядом с `data-search-status` – `data-search-log="{{ search_log_id }}"`, только когда id есть.

**A4.3 Оценка.** `POST /api/search-rating/` (`config/urls.py` рядом с `api/feedback/`, вьюха в `problems/views_platform.py`, тонкая, логика в `catalog/search_log.py::rate`): поля `log` (id), `rating` (`yes`|`no`), `text` (≤ 300, иначе 400). Сверка владельца: `visitor` совпал с кукой, или вошедший и `user` совпал; иначе 404 (не раскрывать существование). Повторная оценка перезаписывает `rating`, `rated_at`, `rating_text`. CSRF как у `api_feedback`. Ответ `{ok: true}`.

**A4.4 Плашка.** Партиал `templates/_search_rating.html` (разметка + стили + скрипт, пространство имён `weco.searchRating`), включается в `catalog/templates/catalog/problem_list.html`. Общий контейнер угла `#corner-stack` (`templates/_corner_stack.html`, включается в `catalog/templates/catalog/base.html` перед `_feedback.html`): `position: fixed`, правый нижний угол, НАД `.tg-fab` и кнопкой обратной связи (высоту их ряда возьми из `_feedback.html` и вынеси в переменную `--corner-offset`), ширина карточки 320 px, на телефоне (< 760) – от края до края над рядом кнопок; в стеке одна карточка за раз. Правила показа (в `localStorage['weco_sr'] = {n, last}`): при загрузке выдачи с `data-search-log` счётчик `n` растёт на 1; показать, если `n % 3 == 0` и `now - last ≥ 20 мин`; момент показа – через 15 с на выдаче (таймер). В нынешнем каталоге клик по задаче уводит на другую страницу, поэтому второе условие разведки «или открыл хотя бы одну задачу» здесь не реализуется – его включит «Стол» (часть B), где экран один. Карточка: «Нашли, что искали?» · кнопки «Да» / «Нет» · крестик. «Да» → POST, текст «Спасибо», скрыть через 1,5 с. «Нет» → POST сразу (оценка не теряется), затем одна строка «Что не так? (необязательно)» + «Отправить» → второй POST с текстом. Крестик → скрыть, `last = now`. Пока карточка в стеке, плашка A5 не показывается.

**A4.5 Выгрузка** `problems/management/commands/search_export.py` (`--since`, `--until`, `--out`): `ts, user_id, visitor, query, status, ms, total, degraded, rating, rated_at, rating_text, top_ids` (`|`). `utf-8-sig`.

**Тесты** `problems/tests/test_search_log.py`:
- `GET /catalog/?q=монополия` (нужна хотя бы одна опубликованная задача в фикстуре) → ровно 1 строка с `query`, `status`, `total`, `top_ids` ≤ 10;
- затем 5 вызовов `api_filter_state` с тем же `q` и разными фильтрами → по-прежнему 1 строка (**главный инвариант**);
- тот же запрос тем же посетителем через 10 с → 1 строка; через 31 с (`freezegun` или подмена `timezone.now`) → 2; другой запрос → 2;
- `api_filter_state` с `log=1` → пишет (с той же склейкой);
- гость без куки → строка с `visitor=''`;
- оценка: своя строка → 200, сохранено; чужая кука → 404; повтор перезаписывает; `text` 301 знак → 400; `rating='maybe'` → 400;
- `data-search-log` есть в разметке при поиске и отсутствует без поиска;
- зафлагованная задача не попадает в `top_ids`;
- `search_export`: заголовок, строки, `--since`;
- `test_no_slop` покрывает `_search_rating.html` и `_corner_stack.html`.

## A5. Плашка «Всё ли нравится?» для вернувшегося (разведка §3)

**A5.1 Сервер.** `Feedback.Kind.PULSE = 'pulse', 'Пульс'` (миграция `makemigrations problems -n feedback_pulse` – `AlterField` из-за `choices`). В `api_feedback` – отдельная ветка ДО правила «хотя бы одна галочка»: для `pulse` `choices` должно быть ровно `['like']` или `['dislike']`, иначе 400; `comment` ≤ 300 (иначе 400); `other_text` игнорируется; снимок не ожидается. Ветки `problem` и `idea` не менять (настроены 17.09). Логика проверки – в `problems/pulse.py::validate(post) -> (choices, comment) | error`, вьюха только зовёт.

**A5.2 Клиент.** `templates/_pulse.html` (разметка, стили, скрипт `weco.pulse`), включается во все базовые шаблоны рядом с `_feedback.html` (найди все места включения `_feedback.html`: `grep -rn "_feedback.html" --include=*.html`), но НЕ на `/login/`, `/register/` и восстановлении пароля (передавай флаг из контекста или проверяй `location.pathname` в скрипте – выбери одно, запиши). Правила в `localStorage['weco_pulse'] = {visits, shown: [ts…]}` и `sessionStorage['weco_t0']` (время первого экрана в этой сессии браузера): `visits` растёт на 1 раз за сессию браузера; показать, если `visits ≥ 2`, `shown.length < 2`, последний показ ≥ 7 дней назад, `now - t0 ≥ 60 с`, стек угла пуст. Карточка: «Всё ли нравится?» · «Нравится» / «Не очень» (текстовые кнопки со значками SVG `thumb-up`/`thumb-down` – добавить в оба набора иконок) · крестик. Выбор → показать строку «Что улучшить? (необязательно)» + «Отправить»; ОДИН POST на `/api/feedback/` с `kind=pulse`, `choices=[…]`, `comment`, `url` – при «Отправить», при крестике после выбора и по таймеру 20 с без действий. Крестик до выбора → событие `track('pulse_dismiss')`, показ засчитан. После отправки – `track('pulse', {choice})`, «Спасибо», скрыть. Стек угла из A4.4.

**Тесты**: `api_feedback` с `pulse`/`like` → сохранён `kind='pulse'`, `choices=['like']`; `choices=['meh']` → 400; `comment` 301 → 400; `kind='problem'` без галочек по-прежнему 400 (существующие тесты зелёные); разметка `_pulse.html` есть на `/catalog/` и `/`, отсутствует на `/login/` и `/register/`; `test_no_slop`.

## A6. Помощник ИИ: начинка (разведка §7)

Окно чата целиком переделает часть B; здесь – сервер и минимальный клиент в нынешнем `problem_page.js`, чтобы бета могла выйти и без части B.

**A6.1 Формулы и длина в промпте.** `problems/ai/prompts.py`, профиль `catalog_chat`, пункт 3 заменить на: «3. Формулы и обозначения записывай в TeX: в строке – `$…$`, отдельно стоящую формулу – `$$…$$` на своей строке. В длинной выкладке – по одной формуле на строку, между смысловыми шагами пустая строка. Отвечай живым языком и по делу.» Убрать «до 900 знаков» и из блока режима `check` фразу «В этом режиме ответ до 1 200 знаков». Правила 1, 2, 4, 5 не трогать (они не дают выдать решение и ответ). Тест: собранный системный блок содержит `$…$` и `$$…$$`, не содержит `900` и `1 200`; P0-тесты чата зелёные.

**A6.2 Обрезка.** `catalog/chat.py`: `REPLY_MAX = 8000`, `CHECK_REPLY_MAX = 10000`, `CHAT_MAX_TOKENS = 3000`; функция `cut_reply(text, limit)`: короче лимита – как есть; иначе срез по последней границе предложения (`. ! ? \n`) до лимита плюс « …»; если границы нет – по последнему пробелу. `turn.reply = cut_reply(reply, …)`. В `core.run(...)` для реплики передавать `max_tokens=CHAT_MAX_TOKENS` явно (сигнатура `run(profile, user_text, schema, user, max_tokens=None, …)`). Тесты (`catalog/tests/test_chat_api.py`, заглушка провайдера): ответ 5 000 знаков доходит целиком; 9 000 знаков → ≤ 8 000, конец на границе предложения, последнее слово не разрезано; провайдер получил `max_tokens=3000`. Валюта: прогони существующий `scripts/dollar_render_compare.js` (или его аналог в `scripts/`) на реплике «Цена 5 долларов, а $MC = 2q$» – в журнал, что формула набралась, а «долларов» не сломало текст.

**A6.3 Несколько файлов.** `ChatTurn.attachments = ManyToManyField(ChatAttachment, blank=True, related_name='turns')`; старое `attachment` оставить (журнал беты). Миграция `makemigrations problems -n chat_turn_attachments`. `api_chat` принимает `attachment_ids` (список, ≤ `FILES_PER_TURN = 3`, иначе 400 с текстом «Не больше трёх файлов к одной реплике»), старый `attachment_id` продолжает работать; `answer(… attachments=[…])` – картинки со всех вложений подряд, общий потолок `VISION_PAGES_MAX = 5` страниц; если страниц больше – в текст модели и в JSON-ответ клиенту поле `note: 'Смотрю первые пять страниц.'`; `UPLOADS_PER_DAY` считается по файлам (как сейчас). Клиент (`problem_detail.html` `#ai-file` → `multiple`; `problem_page.js`): список чипов с крестиком, до трёх, отправка `attachment_ids`, в пузыре – имена файлов ссылками (A6.4). Тесты (`test_chat_upload.py`/`test_chat_api.py`): три вложения → у `ChatTurn` три `attachments`; четыре → 400; две PDF по 3 страницы → модель получила 5 картинок и `note` в ответе; чужое вложение в `attachment_ids` → 400/404 как сейчас для `attachment_id`.

**A6.4 Просмотр своего вложения.** `GET /catalog/chat/attachment/<id>/` (`catalog/views.py`, отдельная функция `chat_attachment`): владелец или staff → `FileResponse` с `content_type` из поля `mime` (не из запроса), путь из поля `file` (не из адреса), заголовки `Content-Disposition: inline; filename="…"`, `X-Content-Type-Options: nosniff`, `Cache-Control: private, max-age=0`; иначе 404 (гостю тоже 404, не редирект). `docs/SECURITY.md`: раздел «Третья файловая вьюха: вложение чата» – кто видит, почему 404 а не 403, почему тип из модели. Клиент: в пузыре ученика вместо `[файл: имя]` – ссылка на вьюху (`target="_blank"`), для картинок миниатюра ≤ 120 px тем же адресом. Тесты: свой → 200, `Content-Type` = `mime`, `nosniff` есть; чужой → 404; гость → 404; несуществующий → 404; staff → 200.

**A6.5 PDF.** Ничего не переписывать. Тесты на `derived_images`: PDF на 7 страниц (сгенерируй PyMuPDF в памяти) → 5 картинок; PDF с паролем → `ValueError(BAD_PDF)`; пустой файл с расширением `.pdf` → `BAD_PDF`. Через `api_chat_upload` – честное сообщение `BAD_PDF` в ответе.

**A6.6 История и цитата (сервер для части B).** `GET /catalog/api/chat/history/<problem_id>/` – последние 20 удачных реплик ЭТОГО пользователя по ЭТОЙ задаче: `{turns: [{message, reply, mode, ts, attachments: [{id, name, mime, url}]}]}`; гость → 401 JSON; чужие реплики никогда. `api_chat` принимает необязательную `quote` (≤ 300 знаков, только текст, длиннее → 400) – уходит модели в тексте реплики строкой «Фрагмент условия: «…»», в системный блок не попадает. Тесты: чужая история пуста; гость 401; `quote` в тексте запроса и не в системном блоке; `quote` 301 → 400.

Коммит по подфазам. В Notion после раздела: `3deb11c9-2bc1-81e1-9c75-e8fec5c734b2` (чат режет ответ) → «На проверке» с числом обрезанных из A0.

## A7. Фильтры Wecon Rush в стиле каталога (разведка §5)

Ветка `feat/wecon-rush-redesign` уже в `main` (P8.3 = `83fdd67`), конфликта нет.

**A7.1 Отчёт** – числа из A0 (неканонические теги и темы пула) в `reports/beta_20260918/GAME_POOL.md`. Данные пула не чинить (решение владельца 18.09): создай карточку «Надо» в «Задачи» (направление «Онлайн-игры»): «Пул Wecon Rush: N неканонических тегов и M имён тем вне канона – привести к справочнику» со списками.

**A7.2 `pool_tags()`** (`game/views.py:~361`) – только `kind='canonical'`. Инвариант: `pool_counts_for(f)` для трёх наборов фильтров (все · только микро · одна тема + один канонический тег) даёт ТОЧНО те же числа до и после (сними числа в `shell` до правки, запиши, сравни после). Тест `game/tests/test_pool_tags_canonical.py`: в пуле тег `kind='legacy'` → в `pool_tags()` его нет, а вопрос с ним по-прежнему считается в `pool_counts_for` для своей темы.

**A7.3 Окно `#fmodal`** (`game/templates/game/game.html`, блок ~2148–2255 и его CSS/JS – найди по `grep -n "fmodal"`; читай диапазонами). Перерисовать по приёму окна каталога (`catalog/templates/catalog/_catalog_modal.html`, `_catalog_modal_block.html`, `catalog_filters.js`): две колонки; темы – сворачивающиеся разделы (по умолчанию все свёрнуты) с числом тем и вопросов в заголовке, внутри плитки с галочкой и счётчиком; теги – поиск плюс теги выбранных тем; внизу живой счётчик «Показать N вопросов» (число из `api_pool_counts`, той же функцией, что выбирает вопросы – это правило не нарушать) и кнопки «Сбросить» / «Применить». Пресеты «все / микро / макро / снять всё» сохранить. Фильтр «задачи или тесты» НЕ добавлять. Общий партиал НЕ заводить (окно каталога переписывается частью B): скопировать структуру, классы и поведение, в начале блока комментарий: «Приём скопирован из окна каталога 18.09.2026; свести в общий партиал после того, как «Стол» устаканится (карточка в Notion)». Цвета токенами, иконки SVG, без эмодзи. Логику `_filter_matches`, `pool_counts_for`, `_candidate_rows` не трогать. `git diff --stat game/templates/game/game.html` – только этот блок; ничего не переформатировать.

Существующие `game/tests/test_filter_window.py` и `test_browser_modals.py` – прогнать; что покраснело из-за новой разметки – переписать под новую (каждый переписанный – строкой в журнале: что проверял, чем заменён), не удалять.

Снимки (в A8): окно фильтров игры 1440 и 390, светлая и тёмная.

## A8. Приёмка части A

**A8.1 Смок-тест** `problems/tests/test_beta_smoke.py` (быстрый, без браузера, < 1 мин): гость – `/`, `/catalog/`, `/catalog/?q=монополия`, `/catalog/problem/<id опубликованной>/`, `/game/`, `/login/`, `/register/`, `/healthz/` → 200; ученик (фабрики из `problems/tests/factories.py`) – `/profile/` 200, POST профиля со всеми новыми полями → 302 и сохранено, `/catalog/problem/<id>/` 200, `/api/feedback/` с `pulse` → 200; учитель – `/teacher/` 200; `/api/search-rating/` на чужую строку → 404; страница задачи вошедшего содержит `multiple` у `#ai-file`.

**A8.2 Снимки глазами.** `scripts/beta_shots.mjs` (по образцу `scripts/rush_shots.js`; сервер `runserver 8611` в фоне, локальная база, вход демо-учеником и демо-учителем; выход `reports/beta_20260918/shots/<префикс>_<имя>_<тема>_<ширина>.png`; viewport 1440×900 и 390×844; темы `light` и `dark` через `data-theme`; для плашек ставь `localStorage`/`sessionStorage` через `page.addInitScript` так, чтобы правила показа сработали – никаких отладочных параметров в боевом коде). Набор: главная; каталог с поиском и плашкой «Нашли, что искали?» (обе стадии: «Да/Нет» и строка комментария); каталог с плашкой «Всё ли нравится?»; профиль с новыми полями и карточкой Telegram; страница задачи с чипами двух файлов; игра – стартовый экран и окно фильтров (раздел раскрыт); окно обратной связи поверх середины каталога. Скрипт печатает для каждого экрана `document.documentElement.scrollWidth <= innerWidth` (на 390 – обязательно `true`) и ошибки консоли (`pageerror`, `console.error`) – их должно быть 0.

Затем **открой каждый PNG инструментом Read и посмотри**: плашки не перекрывают кружок Telegram и кнопку обратной связи; текст не обрезан; в тёмной теме нет белых пятен; на 390 ничего не выезжает за край; окно фильтров игры похоже на окно каталога. По одной строке на снимок в раздел журнала «Смотрел глазами»; найденное – чинить и переснимать. Сервер на 8611 после снимков остановить.

**A8.3 Документы.** `CLAUDE.md` (коротко: команды `profiles_export`, `search_export`; модель `SearchLog`; `Feedback.Kind.PULSE`; вложения чата M2M; третья файловая вьюха), `docs/SECURITY.md` (из A6.4), ADR: «Журнал поисковых запросов пишется сервером при полном рендере, склейка 30 с» и «Вложение чата: файловая вьюха с проверкой владельца» (следующие свободные номера). `docs/TESTING.md` – одна строка про смок-тест.

**A8.4 Чекпоинт.** `manage.py check`; `makemigrations --check --dry-run`; быстрый круг `--scope-from-git main` зелёный; `ruff check .` без новых замечаний; `scripts/check_em_dash.py`; `git log main..feat/beta-prep --oneline` в журнал; запись «Часть A готова к слиянию: да/нет, что не вошло». Notion: карточка `3deb11c9-2bc1-81a8-8889-fa521ca03823` → комментарий с цифрами по разделам (статус остаётся «В работе» до части C); `3ddb11c9-2bc1-817c-8169-c0754d9f80ce` и `3dcb11c9-2bc1-81ba-b2ee-f9dd9fdad79a` (снимок обратной связи) → «На проверке» с одной строкой «что сделано»; `3dcb11c9-2bc1-81e8-9cb7-c7f1f29e65a4` (аналитика) → комментарий про `SearchLog` и `profiles_export`.

**Проверь часы.** Позже `ГРАНИЦА_B` – в журнал «Часть B не начата: время», сразу часть C. Иначе – часть B.

---

# ЧАСТЬ B. Ветка `feat/catalog-stol` (от `feat/beta-prep`)

`git checkout -b feat/catalog-stol feat/beta-prep`. Журнал тот же файл (продолжаешь на новой ветке). Спецификация экранов – `claude/mockups/catalog_stol_20260917/README.md`: **прочитай целиком до первой строки кода** и держи открытой; снимки `shots/` – как выглядит; `.dc.html` – вёрстка, размеры, тексты и логика состояний; раскладку облака карты и демо-данные из них не переносить.

Что уже сделано в части A и здесь НЕ повторяется: сервер чата (обрезка, `max_tokens`, TeX в промпте, несколько вложений, вьюха вложения, история `GET /catalog/api/chat/history/<id>/`, `quote`), журнал поиска с `data-search-log` и параметром `log=1`, плашки угла (`#corner-stack`). «Стол» обязан это использовать, а не ломать: новый блок результатов сохраняет `data-search-log`; поиск из ленты и с входа (нажатие Enter/кнопки, не смена фильтра) идёт с `log=1`; после открытия задачи со свежей выдачей плашка «Нашли, что искали?» может показаться сразу (условие «открыл хотя бы одну задачу» из разведки); стек угла стоит над полосой корзины учителя (P8) и над нижней панелью телефона (P9); кнопку обратной связи на телефоне в каталоге прячем, стек – нет.

Правила части B: дополнительная остановка – папка макетов отсутствует. Все правила из раздела 1 действуют. Тесты по фазам – `manage.py test catalog --settings=config.settings_test_pg` и точечно `teacher`. `ГРАНИЦА_C` проверяй после каждой подфазы.

## P0. Аудит данных (только чтение)

Отчёт `reports/catalog_stol_20260917/AUDIT.md`, кода сайта не менять. На локальной базе, в границах `filters.base_queryset('catalog')`:
1. **Карта ↔ справочник.** Сколько из 29 тем и 343 тегов `topic_map.json` находят каноническую `Topic`/`Tag` по названию (нестрогое сравнение: регистр, «ё», тире, кавычки). Ожидание: 29 из 29; по тегам – число и список ненайденных.
2. **Числа карты ↔ фильтры.** Таблица «тема · число в данных карты · число в фильтре каталога». Известно: «Производство и издержки фирмы» 7 434 против 1 989. Числа карты собраны из статичного дерева (`catalog/taxonomy_map.py`), а не из базы – подтверди или опровергни.
3. **Решения.** Сколько задач с решением; у скольких решение почти равно условию (доля общих слов ≥ 0,9 и длина решения ≤ 1,3 длины условия); у скольких решение НАЧИНАЕТСЯ с пересказа условия (первые 200 знаков совпадают с началом условия на ≥ 0,8), а дальше настоящее решение. Первые 50 id каждого вида. Пример первого вида: 63315.
4. **Ответы.** У скольких задач непустой общий ответ; у скольких заполнены ответы по пунктам (по проверке 17.09 – похоже, ни у одной).
5. **Подсказки.** Сколько задач с подсказками, распределение по числу.
6. **Тесты.** Сколько тестов играбельны (`testplay`), у скольких есть «почему так».

Результат: шесть таблиц с выводами в три строки; цифры в журнал; в Notion – комментарии в `3deb11c9-2bc1-818d-bc2f-da44a86acbfa` (числа карты) и `3deb11c9-2bc1-817a-8fbc-e40c551f7b9b` (решение = копия условия).

Следствия для кода: «решение = копия условия» – при показе такая задача считается задачей БЕЗ решения (значок, ступень помощи); «начинается с пересказа» – пересказ срезается при показе (функция в `catalog/preview.py` рядом с `looks_like_statement_cut`, тесты на реальных примерах из аудита). Данные не трогать.

## P1. Данные и серверные ответы

**P1.1 Прогресс ученика.** Модель рядом с `SavedProblem` (`problems/models_platform.py`), `ProblemProgress`: пользователь, задача, `status` (`opened` / `solved_self` / `solved_hint` / `failed`), `hints_opened`, `solution_viewed`, `updated_at`; уникальность (пользователь, задача). `makemigrations problems -n problem_progress`. Правила: открытие задачи вошедшим создаёт `opened`, если строки нет (неблокирующе, как `log_problem_event`); `POST /catalog/api/progress/<id>/` (вход обязателен; гостю 401 JSON) принимает `status` (`self`/`hint`/`failed`/`null` – снять отметку), `hints_opened`, `solution_viewed: true`; `solved_self` при `solution_viewed` – 400 с понятным текстом; тест ставит статус сам на сервере в `api_test_check`/`api_test_reveal`: верно с первой попытки без подсказок – `solved_self`, иначе `solved_hint`; «показать ответ» – `failed`.

**P1.2 Панель задачи одним ответом.** `GET /catalog/problem/<id>/?pane=1` → JSON `{title, url, center_html, help_html, meta}`; те же партиалы рисуют прямую ссылку `/catalog/problem/<id>/` целиком на сервере (без скрипта экран рабочий). Шлюз качества – как в `problem_detail`.

**P1.3 Ленты.** Один партиал строки на широкую и узкую ленту (плотность решает CSS контейнера). Источники: выдача – нынешний `api_filter_state` (контракт с окном фильтров не ломать, `test_filter_state_api.py`); `GET /catalog/api/rail/similar/<id>/` – нынешние «Похожие»; `GET /catalog/api/rail/saved/` – сохранённые (вход). Строки вошедшего аннотируются статусом одним запросом на страницу строк. Процент близости не выводить.

**P1.4 Чат** – сделано в A6. Здесь только клиент (P5).

**P1.5 Данные карты.** `topic_map_data`: к каждому узлу `db` (ключ канонической `Topic`/`Tag`, найденной как в P0.1; не нашлась – `null`) и живое число `c` из тех же подсчётов, что в фильтрах каталога. Кэш 10 минут (ключ – ETag файла + размер корпуса). Подпись «29 тем, 343 тега» из данных.

Инварианты (журнал + тесты `catalog/tests/test_stol_api.py`): панель зафлагованной задачи – 404; `solved_self` после `solution_viewed` – 400; у 29 из 29 тем есть `db`; живое число темы на карте равно числу этой темы в `_topic_options` без фильтров.

## P2. Каркас «Стол»

Один шаблон `catalog/templates/catalog/stol.html` и один скрипт `catalog/static/catalog/js/stol.js` (без фреймворков) на три адреса: `/catalog/` (вход), `/catalog/problem/<id>/` (стол с задачей), `/catalog/map/` (вход сразу в режиме карты). Свой `main_class` на всю ширину.

Состояние экрана (вид, открытая задача, панели, фокус, вкладка ленты, фильтры), `history.pushState`/`popstate`, загрузка панели через P1.2 с заменой центра и помощи и `renderMathIn`, сброс прокрутки центра при смене задачи, память панелей в `localStorage` (первое открытие: лента свёрнута, помощь открыта), полоски 52 px, «Фокус» (скрывает панели, полоски и шапку; `Esc` выходит), клавиши из README §1 (не в полях ввода), ширины: ≥ 1400 панели сдвигают; 1100–1399 лента выезжает поверх и закрывается после выбора; < 1100 обе панели шторками; < 760 – телефон (P9). Колонка условия всегда 680 px по центру свободного места – при открытии панелей строки НЕ перевёрстываются. `prefers-reduced-motion` – без анимаций.

Результат: три адреса отвечают 200; со входа клик по строке открывает задачу без перезагрузки, «назад» возвращает; прямая ссылка рисуется сервером целиком и работает без скрипта; панели, полоски, фокус и клавиши – как на снимках 09–15 и 20.

## P3. Вход (README §2, снимки 01–04, 08)

Карточка поиска в центре; заголовок «Это умный каталог.» только без запроса и фильтров; сжатие шапки при первом фильтре; чипы «Все фильтры / Тема ▾ / Сложность ▾ / Задачи и тесты ▾ / С решением / Сбросить» с выпадашками у чипа, числа из `api_filter_state`; ряд чипов выбранных тегов; пилюля «Карта тем: …»; «Продолжить» (до трёх: «не получилось» и «открывал», свежие первыми; нет – нет строки) и «Случайная задача» (`random_problem` понимает фильтры); блок результатов широкими строками с `data-search-log`; «Показать ещё 20»; скелет на время запроса; фразы ожидания, «не меньше N», подсказка «снимите фильтр» (`relief`) – сохранить. Плашки угла (A4, A5) на входе работают.

Окно «Все фильтры» (`_catalog_modal.html` + `catalog_filters.js`) остаётся и работает с тем же состоянием, что чипы. «Галерея» и модалку «Условие» (`_problem_modal.html`, `openCatalogModal`) убрать; `?view=gallery`/`?view=table` открывают ленту без ошибки. Публичный `/catalog/api/problem/<id>/` оставить.

Инварианты: счётчик ленты = `filtered_total`/`total` из `_catalog_context`; выбор темы чипом и в окне «Все фильтры» дают один адрес; в разметке входа нет `%` близости и нет эмодзи; поиск с входа создаёт ровно одну строку `SearchLog`.

## P4. Задача и лента рядом (README §3, снимки 09–14)

Центр: строка над задачей, заголовок по правилу `show_title`, строка свойств, теги свёрнуты у всех и раскрываются «Теги · N ▸» (облачка – ссылки в каталог через `filters.query()`), условие с подпунктами, «спросить ИИ про пункт», всплывающая «Спросить об этом» по выделению (уходит как `quote` из A6.6), «Как прошло?» (P1.1; нет у тестов и у учителя), «Дальше», соседи с названиями, строка из игры при `game_stat`. Сохранить, ссылка, «Ошибка в задаче?» (`_problem_report.html`) – на месте.

Лента 316 px: поиск карточкой (Enter – `log=1`), вкладки «Выдача / Похожие / Мои ★», «Фильтры · N» со сводкой, узкие строки со статусами, рамка у открытой задачи, «весь каталог». По прямой ссылке – вкладка «Похожие». Блока «Похожие задачи» под условием больше нет.

Инварианты: «3 из 506» совпадает с местом задачи в выдаче; при закрытии и открытии любой панели высота карточки условия не меняется; у гостя нет колонки статусов и «Как прошло?».

## P5. Панель помощи (README §4, снимки 09, 12, 16)

Лестница в пустом состоянии (ступени только при данных; «Ответ по одному пункту» – только если заполнены ответы по пунктам, без акцента), единая лента по времени, карточки подсказок (`api_hint`, пометка про ИИ-подсказки как сейчас), разговор (режимы «Теория / Как решать / Проверь решение», скрепка с чипами до трёх файлов и ссылками на вложения из A6, лимиты), результат проверки (`_attempt_result.html`) карточкой при `ai_available`, подтверждение и показ решения (текст из README; `solution_needs_review` прячет решение, ответ остаётся; срез пересказа из P0), строка «что уже использовано». Ответы ИИ: безопасная лёгкая разметка (экранирование, затем абзацы, списки, жирный) + KaTeX (`$$` блоком на своей строке – посмотри глазами на снимке). Восстановление после перезагрузки: подсказки и решение – из прогресса, разговор – из `api/chat/history/`. Отдельной карточки «Решение» с полем под условием больше нет; `POST /catalog/api/attempt/` и файлы к попытке работают из панели. Режим наводящих вопросов для задачи из активной домашки – без изменений; подсказки и решение не прятать.

Инварианты: ответ чата не обрывается посреди слова (тест с длинной заглушкой – уже есть в A6, здесь проверить рендер); после перезагрузки число карточек подсказок = `hints_opened`; без ключа чата карточек разговора и поля нет (правило нуля), подсказки и решение остаются.

## P6. Тест (README §5, снимки 17–19)

Партиал теста в новом виде: одна главная кнопка на состояние, контрастные сообщения, запрет повторного выбора ошибочного варианта, «Дальше» после ответа, «Ещё тест по этой теме» при наличии, «Пройти заново», «Почему так» с вырезанием хвостов «( 4 балла)» и повтора верного варианта (только при показе), клавиши 1–9/Enter, статус сам (P1.1). `testplay`, «всё или ничего», счётчик попыток, `CatalogAttempt` – не менять. Тест без вариантов – обычная задача.

## P7. Карта тем (README §6, снимки 01, 05–08, 33)

1. **Фон входа** – встраиваемый режим `topic_map_preview.js`: параметры (масштаб, высота растворения снизу, выделенные узлы с подписью, отдача угла). Курсор не слушает.
2. **Переход** на том же экране: по кнопке лениво подгрузить полный движок (`topic_map.js` ~127 КБ – на вход его не тянуть), смонтировать в слой поверх и сыграть анимацию из README §6 (длительности, задержки, кривые – точно по README). Движок сделать монтируемым: `window.TMAP.mount(root, {data, selection, onChange, onClose, startAngle})` вместо `getElementById` при загрузке; разметку `topic_map.html` – в партиал внутри `stol.html`. Если передать угол и масштаб из фона дорого – перекрёстное растворение двух канвасов за 300 мс без скачка масштаба и положения. Обучение, поиск, дерево разделов, масштаб, хождение по линиям – сохранить.
3. **Выбор = фильтры.** Клик по теме/тегу меняет те же `topic`/`tag`, что чипы (темы «или», теги «и»); адрес обновляется; любой выход оставляет фильтр; повторный вход показывает выбор; узлы без `db` не выбираются (бледнее, без курсора-руки). Старую передачу тегов через `/catalog/?q=…` убрать.
4. **Числа** живые (P1.5).
5. **Телефон**: при узком контейнере (ширина/высота < 0,8) оси раскладки меняются местами, растяжение по вертикали ~1,3, вписать в ширину, подписи у правого края – влево, длинные – до 28 знаков.
6. `/catalog/map/` открывает каталог в режиме карты; «×», «В каталог», `Esc`, «назад» ведут в каталог с сохранённым выбором.

Инварианты: «выбрал тему → В каталог» – адрес содержит `topic=<id>`, счётчик ленты равен числу темы на карте; после возврата узел отмечен; полный движок не загружается до нажатия кнопки (проверка по разметке входа или сетевым запросам).

## P8. Учитель (README §7, снимки 21–25)

Галочки в строках обеих лент, «+ В корзину», корзина в `localStorage` (ключ с id пользователя), полоса корзины (стек угла над ней), список выбранного, «В домашку ▾» (`_teacher_assignments`: название + сколько задач и кому), зелёная плашка после добавления. Сервер: `POST /teacher/api/assignment/<pk>/add_problems/` `{problem_ids: […]}` на логике и проверке владельца `api_assignment_add_problem`; ответ – сколько добавлено, сколько уже было. «В N домашках» – один запрос на страницу строк по работам ЭТОГО учителя. «PDF или TeX»: `POST /catalog/collection/from-basket/` создаёт `Collection` и ведёт в конструктор (сборку PDF не чинить – карточка `3deb11c9-2bc1-8154-8fef-e33f23e6f3fa`). «Новая домашка из корзины»: `/teacher/assignment/create/?preselect=1,2,3`, если `teacher/picker.py` принимает список малой правкой чтения параметра; иначе кнопку не рисовать и записать. Старую выпадашку `_hw_dropdown_js.html` из каталога убрать, где ещё используется – оставить.

Инварианты: ученик и гость не получают ни галочек, ни полосы, ни эндпоинтов (403/404); чужая работа – 403; зафлагованная задача в корзину не добавляется и честно называется.

## P9. Телефон (README §8, снимки 30–35)

Вход, шторки фильтров снизу, строки в две строки, задача на весь экран, нижняя панель из четырёх кнопок, помощь шторкой от 120 px, карта вертикальная с нижней карточкой выбора, тест. Кнопку обратной связи на экранах каталога скрыть; стек угла (плашки) – над нижней панелью. Проверь 360, 390, 430 px: документ не шире окна (формулы прокручиваются внутри себя).

## P10. Зачистка, документы, Notion

1. Удалить мёртвое: галерея, модалка «Условие», процент близости, старые `problem_list.html`/`problem_detail.html` и их скрипты – только когда новый экран закрывает всё, что они делали (включая минимальный клиент файлов из A6.3). **Каждый удалённый или переписанный старый тест – строкой в журнале.**
2. Сторожа: `test_no_slop.py` (новые шаблоны), `check_em_dash.py`, контраст `test_contrast.py` – новый экран в обеих темах.
3. Документы: `catalog/CLAUDE.md` («Шаблоны», «Карта тем», новый раздел «Стол»), корневой `CLAUDE.md` (коротко), ADR: «Стол – один экран на каталог и задачу», «Прогресс ученика по задаче», «Карта и фильтры – одно состояние».
4. Notion: `3deb11c9-2bc1-81b3-9a40-f5323da333d9` → «На проверке» с цифрами; `…818d-bc2f-da44a86acbfa`, `…817a-8fbc-e40c551f7b9b` – что сделано в коде, что осталось в данных; всё, что решил сам и не смог – карточками «Надо» («Каталог и поиск»).
5. Снимки «Стола» через `scripts/beta_shots.mjs` (добавь набор `stol`: вход, стол 1280 и 1440, фокус, карта, учитель с корзиной, телефон 390 – обе темы) и посмотреть глазами, как в A8.2.

---

# ЧАСТЬ C. Финал

**C1. Полный прогон на `feat/beta-prep`** (это то, что сливается утром). `git checkout feat/beta-prep` (дерево чистое – всё закоммичено). В фоне, вывод в файл: `venv313\Scripts\python.exe scripts\run_tests.py --settings=config.settings_test_pg --summary > reports\beta_20260918\full_run_beta_prep.log 2>&1`. Ориентир по CLAUDE.md: шаг A ~31 мин, шаг B ~5 мин. Пока идёт – делай C3 и C4, каждые ~10 минут заглядывай в лог; не обрывай. Известные красные до тебя: `test_corpus_diagnostics`, `test_embedding_formula`, `test_embeddings_transfer` (14 падений + 4 ошибки, отпечаток `v1`/`v2_focus_repeat`) и то, что записано в фазе −1. Любое другое красное – твоё, пока не доказано обратное: доказательство – тот же модуль красный на `main` в отдельном дереве (`git worktree add ..\qls-main main`, прогнать модуль там, дерево удалить `git worktree remove ..\qls-main`). Своё красное чинить и коммитить в `feat/beta-prep`; после починки повторно гнать только упавшие модули.

**C2. Пять джобов CI** на `feat/beta-prep`, поимённо, статус каждого в отчёт: `ruff check .`; `bandit -r problems catalog teacher student game calc2 config olympiads -ll`; `pip-audit -r requirements/base.txt` (сетевой – если не дотянулся из-за VPN, так и написать, не считать красным); `manage.py migrate --noinput --settings=config.settings_test_pg` и `makemigrations --check --dry-run --settings=config.settings_test_pg`; `manage.py check --deploy --fail-level WARNING` с переменными окружения из джоба `deploy-check` в `.github/workflows/ci.yml` (прочитай, задай в той же оболочке).

**C3. Если часть B закончена целиком** (все P с галочками): то же C1 + C2 на `feat/catalog-stol` после зелёного `feat/beta-prep`. Если B не закончена: на `feat/catalog-stol` только `manage.py test catalog teacher --settings=config.settings_test_pg`, и в журнале «B незавершена: сделано …, следующий шаг …» – ветку не сливать.

**C4. Notion.** Карточка A `3deb11c9-2bc1-81a8-8889-fa521ca03823` → «На проверке» с цифрами (тесты добавлено/всего, снимков посмотрено, миграций, инварианты); запись в «Результаты» (`39bb11c9-2bc1-81da-91c5-e890da24d3c5`): «2026-09-18 Ночная сессия «Бета»: часть A …, часть B …, полный прогон …»; всё, что решил сам и не смог – карточки «Надо». Ничего не удалять, «Готово» не ставить.

**C5. Отчёт** (журнал и последний ответ).

## Отчёт

По каждой фазе (−1, A0…A8, P0…P10 или «не начата», C1…C3): **что сделано / результат с цифрами / ошибки и чего не смог / что записано в Notion**. Затем:
- статус пяти джобов CI на `feat/beta-prep` (и на `feat/catalog-stol`, если гнал), с числами тестов и временем; известные красные отдельно от своих;
- список «Решил сам» с тем, как откатить;
- список удалённых и переписанных старых тестов;
- список снимков, которые смотрел, и что починил по ним;
- точные команды: `git push -u origin feat/beta-prep`, затем `git push -u origin feat/catalog-stol`; порядок утреннего слияния: сначала `feat/beta-prep` в `main`, миграции `problems` на бою после деплоя (`manage.py migrate problems`), потом – когда владелец примет – `feat/catalog-stol`;
- сценарии ручной приёмки владельца на `http://127.0.0.1:8000/` (по 1–2 строки): обратная связь с середины каталога → снимок в админке показывает середину; профиль – класс списком, новые вопросы, Telegram-карточка; поиск «монополист с двумя заводами» → строка в `/admin/problems/searchlog/`, третий поиск → плашка, «Нет» + текст → оценка в строке; второй визит через минуту (`localStorage` можно подправить) → «Всё ли нравится?»; задача 63315 → вопрос ИИ с формулами набран в TeX, длинный ответ не оборван, два файла к реплике, клик по имени файла открывает картинку; игра → окно фильтров как в каталоге, пресеты работают, счётчик совпадает; `profiles_export` и `search_export` → CSV открываются в Excel; если B делалась – сценарии «Стола» из README §9; тёмная тема и 390 px на всех шагах.

---
Рекомендуемая модель: **Opus, effort High**. Часть A – много мелких точных правок с миграциями, часть B – архитектурная. Если сессия остановится – запусти этот же промпт ещё раз, он продолжит с первой подфазы без галочки.
