# Журнал ночной сессии «Бета» — 18.09.2026

- Ветка: `feat/beta-prep` от `main` = `origin/main` = `83fdd67` (ff не понадобился)
- Старт: 03:16 по часам машины
- Python 3.13.12, Django 5.2.17, Playwright (node) есть, `fitz` и `PIL` есть
- Docker: PostgreSQL 17 (55432), Redis 7 (56379), search — подняты `up -d` (были выключены)
- ГРАНИЦА_B = 05:30, ГРАНИЦА_C = 08:00

Куда продолжать: feat/beta-prep · A5 · Feedback.Kind.PULSE, problems/pulse.py, _pulse.html

## Чек-лист

- [x] −1 Сверка с реальностью
- [x] A0 Диагноз на данных
- [x] A1.1 Клиент снимает видимую область (проба)
- [x] A1.2 Сервер уменьшает, а не выбрасывает
- [x] A1.3 Тесты снимка
- [x] A2 Telegram в профиле
- [x] A3.1 Модель профиля
- [x] A3.2 Миграция `beta_profile_fields`
- [x] A3.3 Места чтения `grade`
- [x] A3.4 Форма и экран
- [x] A3.5 P0-сторож
- [x] A3.6 `profiles_export`
- [x] A4.1 Модель `SearchLog`
- [x] A4.2 Запись на сервере
- [x] A4.3 Оценка `/api/search-rating/`
- [x] A4.4 Плашка «Нашли, что искали?»
- [x] A4.5 `search_export`
- [ ] A5.1 Сервер pulse
- [ ] A5.2 Клиент pulse
- [ ] A6.1 Промпт: TeX, без длины
- [ ] A6.2 Обрезка по границе предложения, max_tokens
- [ ] A6.3 Несколько файлов
- [ ] A6.4 Вьюха вложения
- [ ] A6.5 PDF-тесты
- [ ] A6.6 История и цитата
- [ ] A7.1 Отчёт пула + карточка
- [ ] A7.2 `pool_tags()` только канонические
- [ ] A7.3 Окно `#fmodal`
- [ ] A8.1 Смок-тест
- [ ] A8.2 Снимки глазами
- [ ] A8.3 Документы, ADR
- [ ] A8.4 Чекпоинт
- [ ] P0 … P10 «Стол» (только если время < 05:30 после A8)
- [ ] C1 Полный прогон `feat/beta-prep`
- [ ] C2 Пять джобов CI
- [ ] C3 `feat/catalog-stol`
- [ ] C4 Notion
- [ ] C5 Отчёт

## Фаза −1

- `git status`: отслеживаемых изменений нет; неотслеживаемые — файлы сессии
  (промпт, разведка, макеты) и старые чужие черновики (`.txt`, `Claude outputs/`,
  `New Text Document.txt`, `kachat_vektora.ps1`, `session_c15_…`, `t1341_local.txt`,
  два старых `claude/*.md`). Это не правки — не остановка; чужие черновики не трогаю
  и не коммичу. Файлы сессии (промпт, разведка, папка макетов) коммичу в ветку.
- Файлы сессии на месте: промпт 383 строки, разведка 614, макеты: README + 4 `.dc.html` + `shots/`.
- `manage.py check`: 0 ошибок.
- Рабочая локальная база — SQLite `db.sqlite3` (не PostgreSQL: `config/settings.py`,
  DATABASE_URL не задан); PostgreSQL в Docker — только для тестов. Промпт называл
  базой PostgreSQL — работаю по факту.
- Каталог видит 14 082 задачи; демо-аккаунты `tutor@test.local`, `student1@test.local` есть.
- Последний ADR — 0116; мои — с 0117.
- Notion: карточка A прочитана (статус «В работе»), решение 15.09 про аналитику
  (своя таблица событий, ADR 0103) прочитано. Решения для части B прочитаю, если до
  неё дойдёт время (по оценке части A — вряд ли).
- Базовый прогон восьми модулей (PostgreSQL): 188 тестов, OK, 1 мин 41 с, красных нет.

## A0

Отчёт `reports/beta_20260918/DIAG.md`. Коротко: Feedback 5 (со снимком 4 — мало
для диагноза); профилей 24, класс у 5 (NULL 19, 9→2, 10→1, 11→2), город у 6;
пул игры 8 610 вопросов, 305 тегов — все канонические, 4 имени тем из 17 вне
канона (1 800 вопросов); ChatTurn 0.

## A1 (коммит 1d527a37)

- Ключи кадра сверены с `html2canvas.min.js`: `x`, `y`, `width`, `height`, `scrollX`,
  `scrollY`, `windowWidth`, `windowHeight` есть.
- **Настоящий корень «всегда главная» глубже разведки:** html2canvas 1.4.1 падает
  с «unsupported color function "color"» на любой странице, где Chrome отдаёт
  вычисленный `color-mix()` как `color(srgb …)`. Замер вычисленных стилей:
  `/catalog/` — 35 элементов, задача 63315 — 6+5, `/game/` — 1; главная — 0.
  Снимок в Chrome получался ТОЛЬКО на главной (на остальных `note='error'`).
  Лечение: `onclone: flattenModernColors` — в клоне документа `color(srgb …)` →
  `rgba(…)` по 16 цветовым свойствам. Библиотеку не трогал.
- Проба `scripts/feedback_shot_probe.mjs` (1280×800, прокрутка 2 000 px):
  вариант «а» (`x/y` = прокрутка, `scrollX/Y = 0`) — снимок 1280×800, окно 800,
  пиксель строки 20 = rgb(255,0,102), синих 0 → прошёл; вариант «б»
  (`scrollX/Y` = прокрутка) — те же числа, тоже прошёл. Оставлен «а».
  Задача 63315 и `/game/` — снимок 1280×800 без ошибки.
- Сервер: потолок 5 МБ, выше 4 000 px — `thumbnail`, выше 12 000 — `tall`,
  не картинка — `bad`, тяжелее потолка — `big`. Константы модульные.
- Тесты модуля: 37 → 45. Зубастость: на старом коде краснеют 7 из 8 новых;
  `test_downscaled_screenshot_keeps_the_client_note` зелёный и на старом коде
  (он сторожит, что сервер не перетирает клиентское значение при успехе).

## A2 (коммит после A1)

- Карточка «Мы в Telegram» под формой профиля (вкладка «Данные»; `?welcome=1` — та же
  страница). Значки `telegram`, `thumb_up`, `thumb_down` добавлены в оба набора.
- Тесты: +5 (`TelegramChannelCardTests`). Зубастость: без карточки краснеют 3 из 5;
  `/login/` с `?direct` и сверка наборов иконок — сторожа, зелёные на обоих кодах.

## A3 (коммит 589dec62)

- `grade` → `CharField` с `Grade`; миграция `0069_beta_profile_fields` тремя шагами для
  класса + 4 новых поля + подписи `level`. Обратимость на локальной SQLite:
  24 профиля, класс у 5 → после 0069: 5 (`''` 19, 9→2, 10→1, 11→2) → откат 0068: 5
  (NULL 19) → снова 0069: 5. `makemigrations --check` молчит.
- Места чтения класса: `teacher/.../groups/list.html` → `get_grade_display`;
  **`templates/_student_facts.html` — разведка его не нашла**: там было бы «класс none»;
  метка «класс» убрана, значение `get_grade_display`. `seed_platform_demo` — строки.
  Мёртвый ключ контекста `grades: range(5, 12)` в двух местах `views_platform` удалён.
- Форма: 10 полей по порядку промпта; у студента теперь виден и телефон
  (раньше поле было в форме, но не на экране — сохранение его стирало).
- `profiles_export` переиспользует `_cell`/`_day` из `analytics_export` (импорт).
- P0: `problems/tests/profile_markers.py` — один список маркеров для двух сторожей.
  Зубастость: утечка `city` в `build_prompt` чата → сторож красный.
- Тесты: `BetaProfileFieldsTests` 7, `GradeMigrationTests` 1, `test_profiles_export` 6.
  Зубастость формы: на старой форме краснеют 3 из 7.
- Прогон 9 модулей (248 тестов) — зелёный после двух починок теста (кодировка
  `os.devnull` в Windows, роль `teacher` вместо `tutor`).
- ⚠️ Урок ночи: два прогона тестов в фоне одновременно убили друг другу тестовую базу
  `test_qls`. Дальше — строго по одному.

## A4 (коммиты 038a8fff и следом тест)

- `SearchLog` + миграция `0070_search_log`, админка только чтение.
- `catalog/search_log.py`: `log_search` (только `problem_list`; `api_filter_state` —
  по `log=1`), склейка 30 с (сравнение `strip().lower()` в Python — на SQLite `iexact`
  не понимает кириллицу), `rate` (своя строка: кука или пользователь; иначе 404).
- Стек угла `templates/_corner_stack.html` включён в `_feedback.html` (одна точка на
  все экраны), там же `weco.corner.post/load/save` — общие для двух плашек.
- Плашка `_search_rating.html`: каждый 3-й поиск, пауза 20 мин, 15 с на выдаче.
- Тесты `test_search_log`: 22. Зубастость: (1) журнал в `api_filter_state` всегда →
  красный `…without_log_param_writes_nothing`; (2) без склейки → 2 красных;
  (3) без проверки владельца → `…someone_elses_row_is_not_found` красный.

## Решил сам

- A1: `weco.feedback = {shotOptions}` — пространство имён на файл, чтобы проба
  звала тот же код, что кнопка. Откат: убрать две строки.
- A1: html2canvas + `onclone`-нормализация цветов вместо замены библиотеки
  (форк html2canvas-pro — новая зависимость ночью). Откат: убрать `onclone`.
- A3: множественный выбор — своё поле `KnownCodesField` (неизвестный код отбрасывается),
  а не `clean_*`: штатное поле на чужой код валит всю форму. Откат: вернуть
  `MultipleChoiceField`.
- A3: у класса в `_student_facts.html` убрана метка «класс» (подпись уже «11 класс»).
- A4: значки называются `thumb_up`/`thumb_down` (как `arrow_left`), не через дефис.
- A4: стек угла включается из `_feedback.html`, а не из `catalog/base.html` — плашке
  «Всё ли нравится?» он нужен на всех экранах. Откат: перенести include.
- A4: плашка ставит `last` в момент показа (иначе «не чаще раза в 20 минут» держалось
  бы только на крестике).

## Смотрел глазами

## Удалённые и переписанные старые тесты

- `test_feedback.AcceptTests.test_huge_screenshot_does_not_lose_the_message`:
  3 МБ → 6 МБ случайных байт (потолок поднят до 5 МБ; с 3 МБ тест проверял бы уже
  «не картинка», а не «слишком большой»).
