# Журнал ночной сессии «Бета» — 18.09.2026

- Ветка: `feat/beta-prep` от `main` = `origin/main` = `83fdd67` (ff не понадобился)
- Старт: 03:16 по часам машины
- Python 3.13.12, Django 5.2.17, Playwright (node) есть, `fitz` и `PIL` есть
- Docker: PostgreSQL 17 (55432), Redis 7 (56379), search — подняты `up -d` (были выключены)
- ГРАНИЦА_B = 05:30, ГРАНИЦА_C = 08:00

Куда продолжать: ночь закончена — утром владелец: пуш двух веток, слияние feat/beta-prep, миграции на бою (раздел «Итог» внизу)

## Чек-лист

- [x] −1 Сверка с реальностью
- [x] A0 Диагноз на данных
- [x] A1.1 Клиент снимает видимую область (проба)
- [x] A1.2 Сервер уменьшает, а не выбрасывает
- [x] A1.3 Тесты снимка
- [x] A2 Telegram в профиле
- [x] A3.1 Модель профиля
- [x] A3.2 Миграция `beta_profile_fields`
- [x] A3.3 Места чтения `grade`
- [x] A3.4 Форма и экран
- [x] A3.5 P0-сторож
- [x] A3.6 `profiles_export`
- [x] A4.1 Модель `SearchLog`
- [x] A4.2 Запись на сервере
- [x] A4.3 Оценка `/api/search-rating/`
- [x] A4.4 Плашка «Нашли, что искали?»
- [x] A4.5 `search_export`
- [x] A5.1 Сервер pulse
- [x] A5.2 Клиент pulse
- [x] A6.1 Промпт: TeX, без длины
- [x] A6.2 Обрезка по границе предложения, max_tokens
- [x] A6.3 Несколько файлов
- [x] A6.4 Вьюха вложения
- [x] A6.5 PDF-тесты
- [x] A6.6 История и цитата
- [x] A7.1 Отчёт пула + карточка
- [x] A7.2 `pool_tags()` только канонические
- [x] A7.3 Окно `#fmodal`
- [x] A8.1 Смок-тест
- [x] A8.2 Снимки глазами
- [x] A8.3 Документы, ADR
- [x] A8.4 Чекпоинт
- [x] P0 Аудит данных
- [~] P1 Данные и серверные ответы (P1.1, P1.3, P1.5 — да; P1.2 панель одним ответом — нет)
- [~] P2 Каркас «Стол» — первый шаг на странице задачи (без SPA и без stol.html на три адреса)
- [~] P3 Вход — только «Продолжить» (до трёх задач) на нынешнем входе каталога
- [~] P4 Задача и лента — теги свёрнуты, «Как прошло?», лента «Похожие/Мои»; нет «Выдачи», соседей, «Дальше», блок «Похожие» под условием ещё на месте
- [~] P5 Панель помощи — только история разговора после перезагрузки
- [~] P6 Тест — только срез «(N баллов)» в «Почему так» (25 тестов банка)
- [~] P7 Карта тем — живые числа и `db` (P1.5), «Показать задачи» = фильтр topic/tag вместо ?q=
- [~] P8 Учитель — только сервер: POST /teacher/api/assignment/<pk>/add_problems/
- [~] P9 Телефон — нижняя панель на странице задачи
- [~] P10 — документы (ADR 0119, 0120, catalog/CLAUDE.md) и Notion; зачистки нет
- [x] C1 Полный прогон `feat/beta-prep`
- [x] C2 Пять джобов CI
- [x] C3 `feat/catalog-stol` — B не закончена: `test catalog teacher` 568 OK
- [x] C4 Notion
- [x] C5 Отчёт

## Фаза −1

- `git status`: отслеживаемых изменений нет; неотслеживаемые — файлы сессии
  (промпт, разведка, макеты) и старые чужие черновики (`.txt`, `Claude outputs/`,
  `New Text Document.txt`, `kachat_vektora.ps1`, `session_c15_…`, `t1341_local.txt`,
  два старых `claude/*.md`). Это не правки — не остановка; чужие черновики не трогаю
  и не коммичу. Файлы сессии (промпт, разведка, папка макетов) коммичу в ветку.
- Файлы сессии на месте: промпт 383 строки, разведка 614, макеты: README + 4 `.dc.html` + `shots/`.
- `manage.py check`: 0 ошибок.
- Рабочая локальная база — SQLite `db.sqlite3` (не PostgreSQL: `config/settings.py`,
  DATABASE_URL не задан); PostgreSQL в Docker — только для тестов. Промпт называл
  базой PostgreSQL — работаю по факту.
- Каталог видит 14 082 задачи; демо-аккаунты `tutor@test.local`, `student1@test.local` есть.
- Последний ADR — 0116; мои — с 0117.
- Notion: карточка A прочитана (статус «В работе»), решение 15.09 про аналитику
  (своя таблица событий, ADR 0103) прочитано. Решения для части B прочитаю, если до
  неё дойдёт время (по оценке части A — вряд ли).
- Базовый прогон восьми модулей (PostgreSQL): 188 тестов, OK, 1 мин 41 с, красных нет.

## A0

Отчёт `reports/beta_20260918/DIAG.md`. Коротко: Feedback 5 (со снимком 4 — мало
для диагноза); профилей 24, класс у 5 (NULL 19, 9→2, 10→1, 11→2), город у 6;
пул игры 8 610 вопросов, 305 тегов — все канонические, 4 имени тем из 17 вне
канона (1 800 вопросов); ChatTurn 0.

## A1 (коммит 1d527a37)

- Ключи кадра сверены с `html2canvas.min.js`: `x`, `y`, `width`, `height`, `scrollX`,
  `scrollY`, `windowWidth`, `windowHeight` есть.
- **Настоящий корень «всегда главная» глубже разведки:** html2canvas 1.4.1 падает
  с «unsupported color function "color"» на любой странице, где Chrome отдаёт
  вычисленный `color-mix()` как `color(srgb …)`. Замер вычисленных стилей:
  `/catalog/` — 35 элементов, задача 63315 — 6+5, `/game/` — 1; главная — 0.
  Снимок в Chrome получался ТОЛЬКО на главной (на остальных `note='error'`).
  Лечение: `onclone: flattenModernColors` — в клоне документа `color(srgb …)` →
  `rgba(…)` по 16 цветовым свойствам. Библиотеку не трогал.
- Проба `scripts/feedback_shot_probe.mjs` (1280×800, прокрутка 2 000 px):
  вариант «а» (`x/y` = прокрутка, `scrollX/Y = 0`) — снимок 1280×800, окно 800,
  пиксель строки 20 = rgb(255,0,102), синих 0 → прошёл; вариант «б»
  (`scrollX/Y` = прокрутка) — те же числа, тоже прошёл. Оставлен «а».
  Задача 63315 и `/game/` — снимок 1280×800 без ошибки.
- Сервер: потолок 5 МБ, выше 4 000 px — `thumbnail`, выше 12 000 — `tall`,
  не картинка — `bad`, тяжелее потолка — `big`. Константы модульные.
- Тесты модуля: 37 → 45. Зубастость: на старом коде краснеют 7 из 8 новых;
  `test_downscaled_screenshot_keeps_the_client_note` зелёный и на старом коде
  (он сторожит, что сервер не перетирает клиентское значение при успехе).

## A2 (коммит после A1)

- Карточка «Мы в Telegram» под формой профиля (вкладка «Данные»; `?welcome=1` — та же
  страница). Значки `telegram`, `thumb_up`, `thumb_down` добавлены в оба набора.
- Тесты: +5 (`TelegramChannelCardTests`). Зубастость: без карточки краснеют 3 из 5;
  `/login/` с `?direct` и сверка наборов иконок — сторожа, зелёные на обоих кодах.

## A3 (коммит 589dec62)

- `grade` → `CharField` с `Grade`; миграция `0069_beta_profile_fields` тремя шагами для
  класса + 4 новых поля + подписи `level`. Обратимость на локальной SQLite:
  24 профиля, класс у 5 → после 0069: 5 (`''` 19, 9→2, 10→1, 11→2) → откат 0068: 5
  (NULL 19) → снова 0069: 5. `makemigrations --check` молчит.
- Места чтения класса: `teacher/.../groups/list.html` → `get_grade_display`;
  **`templates/_student_facts.html` — разведка его не нашла**: там было бы «класс none»;
  метка «класс» убрана, значение `get_grade_display`. `seed_platform_demo` — строки.
  Мёртвый ключ контекста `grades: range(5, 12)` в двух местах `views_platform` удалён.
- Форма: 10 полей по порядку промпта; у студента теперь виден и телефон
  (раньше поле было в форме, но не на экране — сохранение его стирало).
- `profiles_export` переиспользует `_cell`/`_day` из `analytics_export` (импорт).
- P0: `problems/tests/profile_markers.py` — один список маркеров для двух сторожей.
  Зубастость: утечка `city` в `build_prompt` чата → сторож красный.
- Тесты: `BetaProfileFieldsTests` 7, `GradeMigrationTests` 1, `test_profiles_export` 6.
  Зубастость формы: на старой форме краснеют 3 из 7.
- Прогон 9 модулей (248 тестов) — зелёный после двух починок теста (кодировка
  `os.devnull` в Windows, роль `teacher` вместо `tutor`).
- ⚠️ Урок ночи: два прогона тестов в фоне одновременно убили друг другу тестовую базу
  `test_qls`. Дальше — строго по одному.

## A4 (коммиты 038a8fff и следом тест)

- `SearchLog` + миграция `0070_search_log`, админка только чтение.
- `catalog/search_log.py`: `log_search` (только `problem_list`; `api_filter_state` —
  по `log=1`), склейка 30 с (сравнение `strip().lower()` в Python — на SQLite `iexact`
  не понимает кириллицу), `rate` (своя строка: кука или пользователь; иначе 404).
- Стек угла `templates/_corner_stack.html` включён в `_feedback.html` (одна точка на
  все экраны), там же `weco.corner.post/load/save` — общие для двух плашек.
- Плашка `_search_rating.html`: каждый 3-й поиск, пауза 20 мин, 15 с на выдаче.
- Тесты `test_search_log`: 22. Зубастость: (1) журнал в `api_filter_state` всегда →
  красный `…without_log_param_writes_nothing`; (2) без склейки → 2 красных;
  (3) без проверки владельца → `…someone_elses_row_is_not_found` красный.

## A5 (коммит после A4)

- `Feedback.Kind.PULSE` + миграция `0071_feedback_pulse`; проверка — `problems/pulse.py::validate`,
  ветка в `api_feedback` после правил `problem`/`idea` (их не трогал).
- `templates/_pulse.html` — в стеке угла; включается из `_feedback.html` с серверным условием
  `url_name not in (login, register)` (решил: флаг на сервере, а не `location.pathname`).
- Все правила показа — в одной функции `due()`; один POST (Отправить / крестик после выбора /
  20 с); крестик до выбора — `pulse_dismiss`.
- Тесты +10. Зубастость: без ветки `pulse` краснеют 6 из 7 серверных; без условия
  включения — `test_card_is_not_on_login_and_register`.

## A6 (коммит 8212bcbe)

- Промпт `catalog_chat`: пункт 3 — TeX (`$…$`, `$$…$$` на своей строке, выкладка по строке,
  пустая строка между шагами), «деньги словами» (добавил сам, см. «Решил сам»); из `check`
  убрана фраза про 1 200 знаков. Правила 1, 2, 4, 5 не тронуты (тест).
- `REPLY_MAX 8000`, `CHECK_REPLY_MAX 10000`, `CHAT_MAX_TOKENS 3000`, `cut_reply` по `. ! ? 
`,
  иначе по пробелу, метка « …». `FILES_PER_TURN 3`, `pages_note` («Смотрю первые пять
  страниц.»), `quote` ≤ 300 текстом реплики. Миграция `0072_chat_turn_attachments`
  (M2M `attachments`, `related_name='turns_all'` — `turns` занят старым FK; плюс поле
  `ChatAttachment.name` для подписи).
- Вьюха вложения — `catalog/chat_files.py` (отдельный модуль ради сторожа
  `NoOtherFileServingViewTests`), картинка inline, PDF скачиванием. История —
  `api_chat_history`. SECURITY.md — раздел «Третья файловая вьюха», ADR 0118.
- Валюта (боевой конвейер на странице задачи): «Цена 5 долларов, а $MC = 2q$» — 1 формула,
  0 ошибок, текст цел; «$$MR = MC$$» — блочная формула; «Цена $5, а … $q = 10$» — ломается
  (старое ограничение конвейера), отсюда «деньги словами» в промпте.
- Тесты: `test_chat_beta` 30. Зубастость: старый промпт → 2 красных; срез вместо
  `cut_reply` и без `max_tokens` → 2; без проверки владельца вложения → 1.

## A7 (коммит 2f14d41f)

- `pool_tags()` — только `kind='canonical'` + `topics` у тега. Инвариант `pool_counts_for`
  (все / микро / тема+тег) совпал до и после точно.
- Окно `#fmodal`: копия приёма каталога (`fl-acc`/`fl-tile`), две колонки, разделы свёрнуты,
  бейдж выбранных тем, «Теги выбранных тем», «Показать N вопросов» = `availFor(mode)`.
  Пресеты на месте, «задачи/тесты» не добавлял. `git diff --stat game.html`: +103 −18.
- Зубастость: без `kind='canonical'` → `test_legacy_tag_is_not_listed` красный.
- Карточка Notion «Пул Wecon Rush…» (Надо, Онлайн-игры) — 3dfb11c9-2bc1-8147-ad33-f90279264684.

## A8.1–A8.2

- Смок `test_beta_smoke` 6 тестов, < 5 с.
- Снимки `scripts/beta_shots.mjs`: 48 PNG (1440/390 × light/dark), шире окна на 390 — 0,
  ошибок консоли — 0 (после починки ниже).
- **Снимки нашли два бага, оба починены:**
  1. `POST /api/search-rating/` → 404 у первого поиска нового браузера: кука `weco_vid`
     ставится `track.js` уже после ответа, строка журнала была без владельца. Теперь
     сервер сам выдаёт id и ставит куку (коммит e773f6d9, тесты +3, зубастость — да).
  2. После выбора в плашке ряд кнопок не прятался (`display:flex` сильнее `[hidden]`);
     и кружок Telegram ложился на «Показать N вопросов» в окне игры на 390 (коммит 12624304).
- Живая проверка обратной связи: запись №6 в локальной базе, снимок = середина каталога
  (прокрутка 2 400 px), формулы набраны.
- ⚠️ Быстрый круг `--scope-from-git main` решил гнать полный набор (задеты общие шаблоны):
  69,6 мин, **недействителен после ~03:48** — мой параллельный прогон на той же базе
  `test_qls` снёс её. До сноса 169 модулей зелёные; красные — три известных и
  `test_exams` (1 тест, красный и на `main`: фильтр темы в конструкторе работы после
  решения 17.09 — не мой) и `test_glm_ramp_probe` (перепроверен отдельно — зелёный).
  Дальше свои модули — только на `TEST_DATABASE_NAME=test_qls_night`.

## A8.3–A8.4 — часть A готова к слиянию: ДА

- Документы: CLAUDE.md (абзац «Бета 18.09», команды `profiles_export`, `search_export`),
  ADR 0117 (журнал поиска) и 0118 (вложение чата), docs/SECURITY.md, docs/TESTING.md
  (смок, ловушка двух прогонов), problems/ai/CLAUDE.md (снято «до 900 знаков»).
- Чекпоинт 05:02: `check` — 0; `makemigrations --check` — молчит; `ruff check .` — чисто;
  `check_em_dash.py` — 0; `git log main..feat/beta-prep` — 16 коммитов.
- Полный прогон C1 запущен в 05:04 в отдельном дереве `../qls-beta` (detached на
  `8ef89437`), лог `reports/beta_20260918/full_run_beta_prep.log`; своё на части B —
  только `TEST_DATABASE_NAME=test_qls_night`.
- Notion: карточка A — комментарий с цифрами; 3ddb…817c и 3dcb…81ba → «На проверке»;
  3dcb…81e8 (аналитика) — комментарий; 3deb…81e1 (чат режет ответ) → «На проверке».
- Часы: 05:05 < ГРАНИЦА_B 05:30 — часть B начата.

## Часть B — ход

- 05:05 ветка `feat/catalog-stol` от `feat/beta-prep` (`ffa13692`).
- **P0** (коммит 74e30572): `reports/catalog_stol_20260917/AUDIT.md` + скрипт. Карта 29/29 тем,
  343/343 тега в каноне; числа карты статичны (сумма тегов справочника) — расходятся у 29/29;
  решений 7 055: копия условия 4 (правило длины 0,7–1,3 добавил, иначе «Дед прав» из 27019 —
  копия), пересказ всего условия в начале 21 (63315 — здесь); ответы по пунктам у 4 104 задач
  (проба 17.09 ожидала «ни у одной»); подсказки у 6 982; тестов 7 515, играбельных 5 505,
  с «почему так» 1 974. В коде: `preview.solution_is_statement_copy`,
  `preview.strip_statement_retell` → `_solution_block` и `has_solution` карточки. Тесты 6,
  зубастость — да (2 дефекта → 2 красных). Notion: комментарии в 818d и 817a.
- **P1.1** (f932ebef): `ProblemProgress` (миграция 0073), `catalog/progress.py`,
  `POST /catalog/api/progress/<id>/`, открытие задачи → `opened`, подсказка → `hints_opened`,
  тест → статус сам. ADR 0119. Тесты 14, зубастость — да.
- **P1.5** (7d16de82): `catalog/map_numbers.py` — у узлов `db` и живое `c`; локально 29/29 тем
  и 343/343 тега с ключом; «Производство и издержки фирмы» 2 015 = фильтр. Кэш 10 мин, у
  ответа `max-age=600` вместо суток. Тесты 3, зубастость — да.
- ⚠️ Полный прогон C1, запущенный в 05:04, испорчен: первые модули с базой упали с нулём
  тестов (база `test_qls` была занята хвостом убитого по ошибке прогона), а в 05:14 я ещё и
  прогнал модуль на той же базе. Остановлен, тестовых баз в контейнере не осталось,
  перезапущен в 05:17 в `../qls-beta`.

- **P1.3** (dfbffe0c): `GET /catalog/api/rail/similar/<id>/`, `GET /catalog/api/rail/saved/`
  (гостю 401), партиал строки `catalog/stol/_rail_row.html`, статусы одним запросом, шлюз
  `views._visible`. Тесты 7, зубастость — да.
- **P2/P4, первый шаг** (9e09af3d, 5f…): на странице задачи сетка «Стола» (лента 316 ·
  условие 680 · помощь 392), полоски 52 px, «Фокус» (F, Esc), `[`/`]`, J/K/стрелки/Enter,
  1100–1399 лента поверх задачи, клик мимо закрывает; теги в `<details>`; «Как прошло?»
  (не у теста, не у учителя, «Решил сам» гаснет после открытого решения). Замер колонки
  условия: 1440 первое открытие 680, лента открыта 680 (после сужения промежутков с 24 до
  16 — было 652), 1280 680, фокус 680, 390 — одна колонка 374, шире окна — нет. Ошибок
  консоли 0. Тесты 6 + переписан 1 (`pd-wrap stol-wrap`). Зубастость — да.
- **P5, кусок** (05b096bd): разговор с ИИ поднимается из `ChatTurn` при загрузке; проверено
  пробной репликой (удалена): `$$…$$` блоком, `$p_1$` в строке.
- **Документы** (dbae3435): ADR 0120, раздел «Стол» в catalog/CLAUDE.md, строка в CLAUDE.md.

- **P6, кусок** (b41132bb): `preview.strip_score_tails` в `_test_context` — 25 видимых тестов
  с «(N баллов)» в «Почему так». Тесты 3 (живые хвосты 62601, 63234, 63244), зубастость — да.
- **P8, сервер** (21f03e7e): `POST /teacher/api/assignment/<pk>/add_problems/` — позиции
  `AssignmentItem` после существующих (+ старый M2M), скрытые — в `refused`, чужая работа 404
  (так же, как одиночный эндпоинт и правило SECURITY «404, а не 403»). Тесты 5, зубастость — да.
  Попутно найдено: одиночная «+ В домашку» кладёт только в M2M — карточка Notion (Надо).
- **P3, кусок** (3ae4d8b4): «Продолжить» на чистом входе каталога — `progress.continue_for`,
  «не получилось»/«открывал», свежие первыми, за шлюзом. Тесты 4, зубастость — да. Снимок в
  тёмной теме — пилюля над результатами.
- **P7, кусок** (000ca854): «Показать задачи» на полной карте ставит `topic=`/`tag=` по `db`, а не
  текстовый поиск `?q=`. Данные карты локально: 372 узла, у всех `db`, `max-age=600`. Тест на
  исходник, зубастость — да.
- **P4** (29184d28): блок «Похожие задачи» под условием спрятан от 1100 px (там лента), на
  узких экранах остаётся до телефонного вида; «Все похожие в каталоге» — в ленте.
- **P9, первый шаг** (ae715c96): нижняя панель на странице задачи ≤ 759 px — «Подсказка ·
  Спросить ИИ · Решение/Ответ · Дальше» (каждая при данных), зовёт кнопки страницы; кружок
  Telegram скрыт, плашки угла над панелью. «Дальше» — первая похожая (ленты выдачи нет).
  Эмодзи 💡 и 👁 заменены SVG/текстом. Проверено на 390 в обеих темах: 4 кнопки, подсказка
  выдаётся, шире окна нет, ошибок 0. Тесты 3, зубастость — да.
- **C3** (`../qls-stol`, `dbae3435`): `manage.py test catalog teacher` — **549 тестов, OK
  (skipped=1)**, 6,3 мин. Перезапущен на `21f03e7e` в 05:47.
- ⚠️ В отдельных деревьях нет `node_modules`: браузерные тесты падают «раннер не отдал
  результат» (`test_design_canon` в C1). В `../qls-beta` сделана ссылка-junction на
  `../qls/node_modules`, там `test_design_canon` — 4 теста OK. **Удалять дерево только после
  `rmdir` ссылки**, иначе удаление пройдёт в настоящий `node_modules`.

## C3 — `feat/catalog-stol` (B не закончена → только `catalog teacher`)

- `dbae3435`: 549 тестов, OK (skipped=1), 6,3 мин.
- `21f03e7e`: 557, OK (skipped=1), 5,7 мин.
- `413689a6` (голова, с `test_scripts_import_safe`, с `node_modules`): **568 тестов, OK**, 14 мин.
- Дерево `../qls-stol` удалено (ссылка `node_modules` снята `rmdir` до удаления; настоящий
  `node_modules` цел).

## C1 — почему полный прогон шёл часами (найдено в 06:05)

Строки «NN.Ns  N  ok/FAIL  модуль» в логе были НЕ прогоном на PostgreSQL, а выводом
`scripts/test_timing.py`: с коммита `dade0c54` (`--scope-from-git`) папка `scripts` — пакет,
и прогон **без меток** (`run_tests.py --settings=…`, как в промпте ночи) находит
`scripts/test_timing.py` по шаблону `test*.py` и импортирует его, а код верхнего уровня
гонит все 341 модуль по одному на SQLite (~2,5 ч) и переписывает
`reports/calc2_22aug/timing_plain.json` (отсюда и «грязный» `timing_plain.json` в stash от
12.09). Первые два прогона ночи шли так же. CI не задет: джоб `tests` запускает с метками
приложений. Починка (коммит `c910affe` на `feat/beta-prep`, cherry-pick `efa7418a` в
`feat/catalog-stol`): тело скрипта в `main()` под `if __name__ == '__main__'`; сторож
`problems/tests/test_scripts_import_safe.py` (на старом скрипте красный); строка в
docs/TESTING.md. C1 перезапущен в 06:09 с метками приложений, как джоб CI.

## C1 — полный прогон `feat/beta-prep` (дерево `../qls-beta`, `c910affe`, с метками приложений)

**Шаг A:** 6 353 теста за 30,7 мин, `FAILED (failures=18, errors=4, skipped=1)`:
- `test_embedding_formula` 6, `test_embeddings_transfer` 6 + 4 ошибки, `test_corpus_diagnostics` 2 —
  **ровно известные 14 падений и 4 ошибки** (спецификация отпечатка `v1`/`v2_focus_repeat`,
  CLAUDE.md);
- `test_exams`, `test_work_settings`, `test_obzor_build` — по 1: нет `data-fl="topic"` в
  конструкторе работы — известная карточка Notion «Подбор задач в работу: фильтр тем пропал
  из панели — три теста красные на main» (3deb…819e), на `main` красные (проверено для
  `test_exams` в отдельном дереве);
- `config.tests.test_nav.VersionBadgeTests.test_version_stands_at_the_bottom_of_a_short_page` —
  **мой**: после метки «Beta 1.0» встали стек плашек угла и `<template>` плашек. Они вне потока
  (fixed и инертный шаблон) — тест переписан честно: вырезает `<template>` и явно разрешает
  закреплённый стек (коммит `0c330c61` в beta-prep, cherry-pick `b4f23034` в stol). Модуль — 28 OK.
- После шага A скрипт упал на печати сводки (`UnicodeEncodeError` cp1251 при выводе в файл),
  шаг B не стартовал — запущен отдельно `--only-serial` с `PYTHONIOENCODING=utf-8`.

## C2 — джобы CI на `feat/beta-prep` (дерево `../qls-beta`, `8ef89437`), 05:34

| Джоб | Итог |
|---|---|
| lint `ruff check .` | чисто |
| security `bandit -ll` | 0 находок, код 0 |
| security `pip-audit -r requirements/base.txt` | «No known vulnerabilities found» |
| migrations `migrate` на пустой базе `qls_ci_night` + `makemigrations --check` | код 0, «No changes detected»; база удалена |
| deploy-check `check --deploy --fail-level WARNING` (переменные из ci.yml) | 0 проблем (1 silenced) |
| tests | шаг A 6 353 за 30,7 мин — только известное красное; шаг B 20 за 6,8 мин OK |

## Решения по части B (Notion, прочитаны в 03:58)

«Стол» (A, лента+задача+помощь, «Фокус»); S1 — свёрнутая панель полоской с иконками,
открытая сдвигает колонку 680 px; E3 — вход: поиск в центре, карта фоном, анимированный
переход; карта = фильтры (одно состояние, темы «или», теги «и»); теги свёрнуты у всех;
под ученика; «Ответ по пункту» — только при данных, без акцента; первое открытие — лента
свёрнута, помощь открыта, дальше запоминается; галерея убрана; тест без потока, статус сам;
корзина учителя вместо выпадашки.

## Решил сам

- A1: `weco.feedback = {shotOptions}` — пространство имён на файл, чтобы проба
  звала тот же код, что кнопка. Откат: убрать две строки.
- A1: html2canvas + `onclone`-нормализация цветов вместо замены библиотеки
  (форк html2canvas-pro — новая зависимость ночью). Откат: убрать `onclone`.
- A3: множественный выбор — своё поле `KnownCodesField` (неизвестный код отбрасывается),
  а не `clean_*`: штатное поле на чужой код валит всю форму. Откат: вернуть
  `MultipleChoiceField`.
- A3: у класса в `_student_facts.html` убрана метка «класс» (подпись уже «11 класс»).
- A4: значки называются `thumb_up`/`thumb_down` (как `arrow_left`), не через дефис.
- A4: стек угла включается из `_feedback.html`, а не из `catalog/base.html` — плашке
  «Всё ли нравится?» он нужен на всех экранах. Откат: перенести include.
- A6: PDF-вложение ученику отдаётся скачиванием, а не `inline` (промпт просил inline) —
  как в админке, чтобы PDF не открывался на нашем домене. Откат: `INLINE_TYPES` + pdf.
- A6: в промпт добавлено «деньги пиши словами» — голый `$5` ломает формулы. Откат: одна фраза.
- A6: `ChatAttachment.name` — новое поле (не было в промпте), без него в пузыре и истории
  нечего подписать. Откат: поле и `name=` в `save_attachment`.
- A8.2: сервер ставит куку `weco_vid`, если её нет, при записи журнала поиска. Откат:
  `visitor_for` → только чтение куки.
- B/P8: чужая работа в пакетном эндпоинте — 404, не 403 (промпт просил 403): так у
  одиночного эндпоинта и по правилу docs/SECURITY.md. Откат: `PermissionDenied`.
- B/P2: «Стол» сделан первым шагом на странице задачи, без `stol.html` на три адреса (ADR 0120):
  иначе ветка заменила бы рабочий каталог недоделанным. Откат: убрать include `_stol_css.html`,
  `stol.js` и разметку «Стола» из `problem_detail.html`.
- B/P4: блок «Похожие задачи» под условием не снят (на нём тесты), дублирует ленту.
- A4: плашка ставит `last` в момент показа (иначе «не чаще раза в 20 минут» держалось
  бы только на крестике).

## Смотрел глазами

## Смотрел глазами

- search_rating_ask light 1440 — плашка над кружком Telegram, не перекрывает; текст цел.
- search_rating_why dark 1440 — строка комментария, фокус виден, пятен нет.
- pulse_ask light 390 — от края до края над кружком, кнопки со значками.
- pulse_why dark 1440 — до починки ряд «Нравится/Не очень» не прятался; после — только
  вопрос, поле и «Отправить».
- profile_fields light 1440 — уровни одной осью, галочки плитками, списки «Не указано».
- profile_telegram dark 390 — карточка Telegram под формой, тёмная тема ровная.
- problem_chips light 1440 — два чипа с крестиком над полем, не выезжают.
- game_filters dark 390 — до починки кружок Telegram на кнопке «Показать…»; после — нет.
- game_filters light 390 — раздел «Микро» раскрыт, числа справа, кнопка в две строки.
- feedback light 1440 — окно с середины каталога, «Снимок экрана приложен».

## Удалённые и переписанные старые тесты

- `test_feedback.AcceptTests.test_huge_screenshot_does_not_lose_the_message`:
  3 МБ → 6 МБ случайных байт (потолок поднят до 5 МБ; с 3 МБ тест проверял бы уже
  «не картинка», а не «слишком большой»).


## Итог (C5, 06:50)

**`feat/beta-prep` — готова к слиянию.** 19 коммитов от `main`, миграции `problems` 0069–0072.
Полный прогон: шаг A 6 353 теста / 30,7 мин, красное — только известное (14 + 4 в трёх модулях
отпечатка; 3 теста конструктора работы без фильтра тем, красные и на `main`); шаг B 20 / 6,8 мин OK.
CI: ruff, bandit, pip-audit, миграции с нуля + `makemigrations --check`, `check --deploy` — чисто.

**`feat/catalog-stol` — первый шаг, НЕ сливать без приёмки.** Миграция 0073. `test catalog teacher`
568 OK; config, игра, канон дизайна, смок, обратная связь, журнал поиска — 254 OK.

### Команды утром (пуш в этой сессии запрещён)

```bash
git push -u origin feat/beta-prep
git push -u origin feat/catalog-stol
```

Порядок: сначала `feat/beta-prep` в `main` (после слияния на бою — `manage.py migrate problems`,
0069–0072), потом — когда владелец примет «Стол» — `feat/catalog-stol` (миграция 0073).

### Ручная приёмка на http://127.0.0.1:8000/

1. `/catalog/`, прокрутить до середины, «Проблема или предложение» → отправить → в
   `/admin/problems/feedback/` снимок = середина каталога (не главная).
2. `/profile/` учеником: класс списком, новые вопросы, «Мы в Telegram» под формой;
   сохранить → в админке профиль с кодами.
3. Поиск «монополист с двумя заводами» → строка в `/admin/problems/searchlog/`; третий поиск
   (или `localStorage.weco_sr = {"n":2,"last":0}`) → через 15 с плашка; «Нет» + текст → оценка в строке.
4. Второй визит (`localStorage.weco_pulse = {"visits":1,"shown":[]}`, новая вкладка) → через минуту
   «Всё ли нравится?» → «Нравится» → запись вида «Пульс» в обратной связи.
5. Задача 63315 учеником: вопрос ИИ с формулами — TeX; длинный ответ не оборван; два файла к
   реплике; клик по имени файла открывает картинку (PDF скачивается).
6. `/game/` → «Все фильтры»: разделы тем, «Теги выбранных тем», «Показать N вопросов» = число
   на вкладке режима; пресеты работают.
7. `manage.py profiles_export --since 2026-09-18 --out p.csv` и `search_export … --out s.csv`
   → открываются в Excel.
8. (ветка `feat/catalog-stol`) задача 63315: лента «Похожие/Мои» (клавиша `[`), «Фокус» (F),
   «Как прошло?», теги под «Теги · N»; на телефоне — нижняя панель; на `/catalog/` —
   «Продолжить»; на `/catalog/map/` выбор → «Показать задачи» ставит фильтр.
9. Тёмная тема и ширина 390 на всех шагах.
