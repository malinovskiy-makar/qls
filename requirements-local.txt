# Full dependencies for local development (includes ML stack)
-r requirements.txt

# ML / embeddings (only needed locally for build_embeddings, night_embeddings)
sentence-transformers==5.1.2
torch==2.8.0
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==0.36.2
scikit-learn==1.6.1
scipy==1.13.1
joblib==1.5.3
threadpoolctl==3.6.0
regex==2026.1.15
tqdm==4.68.1
filelock==3.19.1
fsspec==2025.10.0
packaging==26.2
sympy==1.14.0
mpmath==1.3.0
networkx==3.2.1

# Playwright (for headless rendering tools)
# npm install playwright separately

# Other local tools
PyYAML==6.0.3
cryptography==48.0.0

# Подбор домашки по описанию (Часть C сессии 3). На проде НЕ нужен:
# без ключа ANTHROPIC_API_KEY функция сама выключается.
anthropic>=0.113
