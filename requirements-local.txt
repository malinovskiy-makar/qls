# УКАЗАТЕЛЬ, а не список. Настоящий лок — requirements/local.txt
# (боевые зависимости + ML-стек для эмбеддингов + anthropic).
#
# ⚠️ Версии sentence-transformers, transformers и tokenizers там ЗАКРЕПЛЕНЫ:
# ими посчитаны все векторы корпуса. Подробности — requirements/local.in.
-r requirements/local.txt
