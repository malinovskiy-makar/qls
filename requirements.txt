# УКАЗАТЕЛЬ, а не список. Настоящий лок — requirements/base.txt: он собран
# pip-compile на Python 3.13 и содержит точные версии всех зависимостей,
# включая транзитивные.
#
# ⚠️ После отказа от Render (2026-08) ничего в репозитории больше не ставит
# зависимости из ЭТОГО файла — CI использует requirements/base.txt напрямую,
# а команда сборки, которая раньше ставила именно requirements.txt
# (buildCommand: pip install -r requirements.txt), жила только в удалённом
# render.yaml. Возможно, файл осиротел; удалять его — отдельное решение
# владельца (см. requirements/README.md про переходный период с Python 3.9
# на 3.13). Менять состав — в requirements/base.in, потом пересобирать лок:
#     pip-compile requirements/base.in -o requirements/base.txt
-r requirements/base.txt
