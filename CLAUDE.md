# CLAUDE.md — рабочая память проекта (техническая)

Платформа задач по олимпиадной экономике (российской). Этот файл — **актуальный
рабочий контекст** для сессий Claude Code: стек, структура, модели, команды,
текущее состояние, рабочие памятки и ближайшие планы.

**Где что лежит (важно):**
- `CLAUDE.md` (этот файл) — ТОЛЬКО техника: стек, структура, модели, команды,
  движки, деплой, дизайн-токены, известные ловушки. Держим коротким.
- **Notion-штаб** — ВСЁ остальное: задачи, баги, идеи, решения, результаты,
  стратегия, дорожная карта.
  Главная: https://app.notion.com/p/39ab11c92bc181f7bbe1efa8a89e38a9
- `CLAUDE_ARCHIVE.md` — история завершённых сессий. Справка «как это было сделано».
- `STRATEGY.md`, `PRODUCT.md` — ЗАМОРОЖЕНЫ. Актуальные версии — в Notion.
- `DESIGN.md` — живой, техническая дизайн-система. В Notion лежит зеркало.

**Правило: одно и то же не пишем в двух местах.** Планы, статус, баги и
обоснования решений — только в Notion. Техника — только здесь.

Преподаватель (не программист) ведёт проект и принимает решения; код пишет Claude
Code. Объяснять простыми словами, комментарии в коде — по-русски.

---

## Текущий фокус

<!-- NOTION-SYNC:START — генерируется из Notion, руками не править -->
**Обновлено: 2026-08-10 (вечер)** · [Notion-штаб](https://app.notion.com/p/39ab11c92bc181f7bbe1efa8a89e38a9)

**В работе:**
- Просмотреть предпросмотр переносов строк по источникам — Макар
- Чистка дефектов ILE по вердиктам ревью Анича (482 задачи) — Claude Code
- calc2: зрелость интерфейса, общие механизмы, прокатывание точки, блок КПВ/КТВ — Андрей

**На проверке:**
- **Ревью на 55 пунктов: недоделанные фазы 11.5, 12.1–12.3, 13, 14.4, 15 — СДЕЛАНО ЦЕЛИКОМ** — Макар (ревью) / Claude Code
- **Экран проверки показывает часть LaTeX сырым кодом — починено** — Макар (ревью) / Claude Code
- **Отдельная база для браузерных сценариев — сделана (`config/settings_check.py`)** — Макар (ревью) / Claude Code
- Ревью кабинета на 55 пунктов: фазы 0–10, 11.1–11.4, 12.0, 14.1–14.3 — Макар (ревью) / Claude Code
- Фиксы после приёмки ключевого экрана — Макар (ревью) / Claude Code
- Ночная сессия: кабинет преподавателя — 25 замечаний владельца — Макар (ревью) / Claude Code
- Сессия 4: приёмка — тесты, разбор вариантов, утверждение ответов, подбор, печать — Макар (ревью) / Claude Code
- Статистика, геймификация и контрольные — визуальная приёмка — Макар (ревью) / Claude Code

**Надо (ближайшее):**
- Разбор вердиктов ревью Сборника АА (1 593 задачи) — Claude Code
- У тестовых задач Сборника АА пропали варианты ответа — системный класс
- clean_latex вырезает quad внутри формул — текст слипается
- Посчитать покрытие банка тестами по темам — Макар
- Отметить ошибки: связать список с темой задачи — Кабинеты
- Собрать .tex задания в Overleaf обоими движками (проверка починки) — Макар
- Переподнять прод: истёк пробный период Render, сайт недоступен — Макар

**Последние решения:**
- Ревью на 55 пунктов: решения, принятые в сессии 8
- Ревью на 55 пунктов: решения стоп-гейтов, принятые в сессии
- Ревью кабинета репетитора: решения перед объединённой ночной сессией
<!-- NOTION-SYNC:END -->

---

## Стек

- Python **3.9** + Django 4.2 (LTS) + SQLite локально / **PostgreSQL на проде**.
- Проект: `config/`. Основное приложение: `problems/`. Локальная база — `db.sqlite3`.
- Кастомный пользователь `problems.User` с ролью (teacher / student / parent — план).
- Виртуальное окружение в `./venv/`. Все команды — через `./venv/bin/python manage.py …`.

⚠️ **Совместимость Python 3.9.** Синтаксис `int | None` (union через `|`) появился
только в 3.10 и даёт `TypeError` при импорте. Всегда использовать
`from typing import Optional` → `Optional[int]`. Никогда не писать `int | None`.

## Как запустить

```
./venv/bin/python manage.py runserver
```

⚠️ **Браузер для проверки У НАС ЕСТЬ.** В `node_modules` стоит **playwright**
(`package.json`), браузеры скачаны (`~/Library/Caches/ms-playwright/`, chromium).
Значит фронтенд можно проверять НЕ на глаз: поднять `runserver` на свободном порту,
прогнать сценарий скриптом (`node script.js` — запускать из корня проекта, иначе
`require('playwright')` не разрешится), снять скриншоты, прочитать консоль
браузера. Так на ветке `feat/econ-rush-lives-results` нашлись два бага, которых
не видели ни `check`, ни тесты. Это НЕ отменяет визуальную приёмку преподавателем —
вкус и «красиво ли» тесты не проверяют.
⚠️ `runserver --noreload` не перечитывает python-код: правишь `views.py` во время
прогона — перезапусти сервер, иначе браузер проверяет СТАРЫЙ бэкенд (шаблоны при
этом перечитываются на каждый запрос, и расхождение выглядит как «мистика»).
Сайт: http://127.0.0.1:8000/ → главная. Админка: `/admin/`.
Логины (тестовые): `admin/admin12345`, `teacher1/teacher12345`, `student1/student12345`.
Наполнить примерами: `./venv/bin/python manage.py seed_demo` (без дубликатов).

---

## Карта сайта — приложения и страницы

**Django-приложения:** `problems` (ядро: все модели, импорт-команды, движки),
`catalog` (публичный каталог + подборки + экспорт), `student` (кабинет ученика),
`teacher` (панель преподавателя + группы), `graphs` (старый Desmos-калькулятор),
`calc2` (новый собственный калькулятор), `calendar_stub` (заглушка календаря).

**Публичные страницы:**
- `/` — главная: «ЭкЗадачи», поиск по центру, живая статистика, 3 карточки
  (Каталог / Калькулятор / Случайная).
- `/catalog/` — каталог: фильтры (21 канон. тема, сложность, источник, тип, есть
  решение), поиск по тексту, пагинация, 3 вида (строки/таблица/галерея), атлас тем
  как нулевое состояние, чипы фильтров. Шлюз качества прячет зафлагованные задачи.
- `/catalog/<id>/` — страница задачи: условие, подпункты, «Показать ответ»,
  «Показать решение» (скрыто при `solution_needs_review`), блок «Похожие задачи».
- `/catalog/random/` — случайная опубликованная задача.
- `/catalog/collection/new/`, `/catalog/collection/<token>/` — конструктор подборок
  (двухколоночный, AJAX, хранится по токену без логина).
- `/catalog/collection/<token>/export/` — экспорт в `.tex`/PDF (на проде PDF
  отключён — только `.tex` + Overleaf).
- `/catalog/api/problem/<id>/` — публичный JSON для модала.
- `/catalog/smart-search/` — семантический поиск «Подобрать похожие» (в линии
  ветки `feat/dirty-text-cleanup`, в `main` не влит — см. ниже).

**Кабинет ученика (`/student/`):** дашборд (домашки, дедлайны, прогресс), страница
домашки (поля ответов, подсказки), просмотр проверенной работы, `/student/progress/`
(две независимые шкалы: открытые задачи — средний балл; тесты — % верных).
Автопроверка тестов мгновенная.

**Панель преподавателя (`/teacher/`):** дашборд (бейджи «N ждут проверки»),
`/teacher/assignment/<pk>/` (таблица решений), `/teacher/submission/<pk>/review/`
(форма оценки), `/teacher/assignment/create/` (конструктор домашек, двухколоночный,
sessionStorage), группы (`/teacher/groups/...`), `/teacher/student/<pk>/progress/`,
JSON-эндпоинты `/teacher/api/...`.

**Калькуляторы:** `/desmos/` — старый на Desmos API (приложение `graphs`), **НЕ
трогаем**; `/calc2/` — собственный движок D3.js + Math.js.

**Прочее:** `/admin/` (управление контентом, импорт, эмбеддинги, дедуп, шлюз);
`/calendar/` (заглушка); `/join/<token>/` (ссылки-приглашения — план); логин
(регистрация на проде закрыта).

## Модели (`problems/models.py`) — сводка

- **Контент:** `User` (роль), `Topic`/`Subtopic`, `Tag`, `Source`/`SourceReference`
  (`grade` — поле SourceReference), `FileAsset`, `Problem`, `ProblemPart` (подпункты
  а/б/в), `ProblemVersion`.
- **`Problem`** — ядро. Поля: `title`, `statement`, `answer` (осн.), `solution`
  (необязат.), `problem_type` (тип; тесты с префиксом «тест:»), `difficulty` +
  `difficulty_native`, `status` (draft/needs_review/published/archived/hidden/
  duplicate), `embedding` (BinaryField, numpy float32), `content_hash`,
  `similar_problems` (M2M), `needs_quality_review` (флаг шлюза), `solution_needs_review`.
  M2M: `topics`, `tags`, `skills`, `mistakes`.
- **Педагогика:** `Skill`, `StudentSkillProgress`, `MistakeTag`, `Hint` (упорядоченные
  подсказки к задаче ИЛИ подпункту), `Rubric`/`RubricCriterion`, `TheoryPage`
  (мини-теория по теме).
- **Цикл ученик↔учитель:** `Lesson` (три отдельных M2M к Problem с разными
  related_name), `Assignment`, `Submission` (unique student+assignment+problem),
  `TeacherFeedback` (OneToOne к Submission), `StudentTopicProgress`, `StudentGroup`.
- **Инфраструктура:** `Collection` (+ токен), `Job`, `Template` (LaTeX), `ExportRecord`,
  `ImportSession`, `DuplicateCandidate` (инвариант `a.id < b.id`), `DesmosGraph`,
  `AutoTopicAssignment`, `InviteLink` (план).

**Решение по M2M «задача↔навык/ошибка»:** объявлены ОДИН раз на стороне `Problem`
(`related_name='problems'`), доступны с обеих сторон. Редактирование — со страницы
задачи (filter_horizontal).

---

## Движки и команды (всё локально, без внешнего API)

**Эмбеддинги (семантика):**
- Модель **`BAAI/bge-m3`** (sentence-transformers, русский, CPU, ~1.4 ГБ). Имя
  вынесено в единую константу `EMBEDDING_MODEL_NAME` в `problems/embedding_config.py`
  — все команды и поиск обязаны импортировать оттуда.
  ✅ Полный пересчёт на BGE-M3 выполнен 2026-07-04: **31 472 вектора**, кэш похожих
  перелит (105 245 связей). Лог — `reports/recompute_2026-07/recompute.log`,
  замер «после» — `reports/bge_benchmark/after_bge_m3.txt`.
  ⚠️ На Mac с BGE-M3 обязательно `--device cpu`; на 8 ГБ RAM — `--encode-batch 8`.
- `build_embeddings` — строит эмбеддинги батчами. `--limit N` (первые N без
  эмбеддинга), `--reset` (пересчитать все).
- `night_embeddings --ids-file <файл>` — пересчёт по списку id (см. ловушку ниже).
- `find_similar --problem-id N --limit 10` — топ-N похожих по косинусу.
- `cache_similar` — заливает похожие в M2M `similar_problems` (прод работает по кэшу,
  без модели).

**Дедупликация:** `find_duplicates` — `--threshold 0.95` (по умолч.), `--reset`.
Заполняет `DuplicateCandidate`; разметка через админку (подтвердить/отклонить).

**Темы:** `auto_assign_topics` (kNN по эмбеддингам, порог 0.70), `apply_topic_mapping`
(21 каноническая тема).

**Шлюз качества:** `quality_gate --apply` / `--revert` (обратимый). Детекторы читаются
из `reports/quality_audit/gate_*.txt` и смежных. Прячет зафлагованные задачи из
публичной выдачи (`needs_quality_review`).

## Как строятся эмбеддинги

**Формула `problem_to_text`** (в `build_embeddings.py`), 7 блоков:
`title` + `statement[:500]` + подпункты `ProblemPart.statement` (≤500 символов
суммарно) + `«Темы: …»` (канонические, кроме технической «Тест») + `«Навыки: …»`
+ `ai_blurb[:400]` (суть из Батча 1) + `«Теги: …»` (только канонические из
`CANONICAL_TAG_NAMES`). Решение и ответ в отпечаток НЕ входят. Пустые блоки не
добавляются. Требует `prefetch_related('parts', 'topics', 'skills', 'tags')`.
Все 31 472 эмбеддинга в базе посчитаны именно этой формулой (пересчёт 2026-07-04).

⚠️ **Ловушка done-файла (ВАЖНО).** `night_embeddings` вычитает из очереди все id, уже
записанные в `reports/night_session/embeddings_done_ids.txt`. Если у задачи изменился
текст, но её id уже в done-файле — повторный запуск **молча пропустит её** (вывод
«осталось 0» обманчив). Флага `--force` нет. Правильный порядок:
1. Удалить нужные id из done-файла.
2. Запустить `night_embeddings --ids-file <файл>`.

```bash
# Удалить id из done-файла перед пересчётом:
python3 -c "
parts = set(open('ids_to_recompute.txt').read().split())
kept = [l for l in open('reports/night_session/embeddings_done_ids.txt') if l.strip() not in parts]
open('reports/night_session/embeddings_done_ids.txt', 'w').writelines(kept)
print('removed', len(parts))
"
```

После любого патча, меняющего `statement` или `parts`, обязательно пересчитать
эмбеддинги изменённых id (иначе поиск похожих хуже для разбитых задач).

---

## calc2 — собственный графический движок

Стратегия: свой движок на открытых библиотеках вместо переламывания Desmos.
Старый Desmos (`graphs/`, `/desmos/`) НЕ тронут.

**Стек:** D3.js v7 (рисование SVG, оси, перетаскивание) + Math.js 12.4.3 (разбор/
вычисление формул, численные методы). Без npm/сборки — всё в одном шаблоне через
`<script src>`. Приложение `calc2`, страница `/calc2/` (`Calc2View`, TemplateView,
`@login_required`), шаблон `calc2/templates/calc2/calc2.html`. **Моделей/миграций
НЕТ** (сохранение графиков — будущее).

**Принципы:**
- Кривые задаются как **P = f(Q)** (Math.js принимает Q и x). Только первая четверть
  (Q≥0, P≥0); оси/сетку рисуем сами через D3; кривые/заливки обрезаются `clipPath`.
- Вся экономика **численная**: равновесие D=S — сканирование сетки + **бисекция**;
  площади CS/PS — **метод трапеций** (∫), а не формула треугольника. Так нелинейные
  кривые работают без переписывания.

**Сцены/режимы:** равновесие, налог/субсидия, потолок/пол, монополия (с областями),
издержки фирмы, КПВ (одна и сумма двух стран), торговля (КТВ), рынок труда (с МРОТ),
неравенство. Редизайн «Окно сценариев + рабочее место Сцена».

**Живой пульт (ветка `feat/calc2-live-pult`, готова к слиянию):** панель
«Инструменты» свёрнута по умолчанию; ключевые регуляторы — в горизонтальной
ленте-пульте `#pult` у нижнего края графика. Перетаскиваемые панели (`makeDockable`),
реестровый перенос DOM-узлов (`syncPultRegulators` — `appendChild`, не clone, чтобы
сохранить listeners). Слайдер пульта и drag кривой используют один сеттер
`setCurveFreeTerm`. Слайдеры строятся только для линейных кривых (`c.linear !== null`).
Плавность осей — `animateRanges` (твин через requestAnimationFrame, ~0.45с),
дебаунс/тротл, уважается `prefers-reduced-motion`. Frosted-glass панели.

**Как проверять без браузера:** `manage.py check` (0 ошибок) + рендер `/calc2/` → 200.
JS-математика — node-харнесс: извлечь инлайн-скрипт, подгрузить с заглушками DOM/D3 и
настоящим Math.js, вызвать реальные функции (`findEquilibrium`/`integrate`/`recompute`)
на контрольных числах. Тесты математики — в `calc2/tests/calc2_math.mjs`.
**Визуальную проверку (перетаскивание, заливки, темы) всегда делает преподаватель
в браузере.** «Тесты зелёные» доказывают математику, НЕ внешний вид.

**Контрольные числа (эталон математики):** `100-Q`@30=70; равновесие D:100-Q,S:Q →
Q*=50,P*=50; нелинейное D:100-Q,S:Q²/100 → Q*≈61.80; CS=PS=1250; налог t=20 →
Q1=40,Pb=60,Ps=40,Tx=800,DWL=100; субсидия s=20 → Q1=60,расход −1200,DWL=100.

## Старый калькулятор Desmos (`graphs`, `/desmos/`)

Работает, **не трогаем**. Шаблоны: `graphs/templates/graphs/desmos.html`. Шаблоны
кривых (спрос/предложение, КПВ, монополия, изокванта, эластичность), анализ
(благосостояние, вмешательство государства), экспорт PNG/PDF/LaTeX. Desmos API v1.8,
публичный демо-ключ. Модель `DesmosGraph` (сохранение графиков).

## Econ Rush — игра на скорость (`game`, `/game/`, ветки `feat/game-modes` → `feat/game-generators` → `feat/econ-rush-lives-results`)

Публичная (без логина) игра-таймер по тестовым вопросам — инструмент привлечения.
**Четыре режима** (как контроли времени в шахматах), параметры — `game/config.py::MODES`
(значения = предмет тюнинга):

| Режим | Ключ | Тип вопроса | Время | Верно | Жизни |
|---|---|---|---|---|---|
| Пуля | `bullet` | `boolean` — данетка Верно/Неверно | 60 с | +2 с | 3 |
| Блиц | `blitz` | `single` — один из вариантов | 120 с | +5 с | 3 |
| Рапид | `rapid` | `multi` — несколько из вариантов | 300 с | +15 с | 3 |
| Классика | `classic` | `numeric` — числовой ответ вводом | 600 с | +30 с | 3 |

⚠️ **Наказание — ЖИЗНЬ, а не время** (с ветки `feat/econ-rush-lives-results`).
`time_wrong = 0` и `time_skip = 0` у всех режимов: за ошибку минус жизнь и комбо
в ноль, время НЕ трогаем (одно наказание, не два); пропуск полностью бесплатный
(жизнь цела, комбо цело, время цело) — иначе угадать выгоднее, чем честно
пропустить. Третья ошибка завершает забег.

Очки одинаковые во всех режимах: 100×комбо-множитель (×2/×3/×4 на сериях 3/6/9).
Неверный ответ рвёт комбо; **скип комбо НЕ рвёт**. Рапид засчитывается только при
полном совпадении множества выбранных с правильным. Классика: точное равенство
значений через `fractions.Fraction` (`views.py::parse_exact_number` — целые,
десятичные с запятой/точкой, дроби a/b; 0,1 = 0.1 = 1/10 верно, 1/3 ≠ 0.33;
мусорный ввод = неверно, не 500).

⚠️ **Кто что считает (изменилось).** Раньше очки/комбо/время считал КЛИЕНТ, сервер
отдавал только дельту времени. Теперь **очки, комбо и жизни считает сервер** и
отдаёт готовыми числами — клиент их только рисует. Иначе серверный журнал и
`GameResult` разошлись бы с экраном (урок «Штриха» v1: две записи одного и того же
всегда разъезжаются). У клиента остался **только таймер** — сервер не тикает,
поэтому конец по времени (`reason: 'time'`) сообщает клиент, но перебить сервер
не может: закрытый по жизням забег остаётся `'lives'`.

**Состояние забега** (Django session, бэкенд `db` — `SESSION_ENGINE` не задан,
куки бы не вместили): `econ_rush` = `{mode, topic, seen, answered, lives, score,
streak, best_streak, ended, log, queue?, result_code?}`; `econ_rush_seen` =
`{режим: [id]}` между забегами; `econ_rush_last` = `{mode, log}` завершённого
забега (переживает старт нового — с него живёт работа над ошибками).

**Журнал и сводка.** `state['log']` — запись на каждый сыгранный вопрос
(`question_id, number, topics, difficulty, question_type, outcome, elapsed_ms,
running_score, running_combo, lives_after`); темы и сложность берутся из
`GameQuestion` (денормализованы) — сводке не нужно ходить в базу за вопросом,
которого может уже не быть. `build_summary(state)` — **чистая функция журнала**:
точность, разбивка по темам, группы сложности, макс. комбо, кривые счёта и комбо,
гистограмма времени, причина конца, жизни. Отдельных счётчиков не заводим —
разошлись бы с журналом. **Точность считается от ПОПЫТОК** (верные+неверные):
пропуск не ответ и точность не портит. `elapsed_ms` шлёт клиент (он знает момент
показа карточки) из единой точки `postAnswer`; мусор/отрицательные/огромные → 0.

**Работа над ошибками** (`GET api/session/start_mistakes/`): целевой забег из
`config.MISTAKES_RUN_SIZE` (10) вопросов по темам ошибок ПРЕДЫДУЩЕГО забега,
в пропорции по сырому числу ошибок, **методом наибольшего остатка**
(`allocate_quotas`; контроль: 2 ошибки в A и 1 в B → 7/3). Режим — тот же
(разбирать ошибки «Пули» вопросами «Классики» бессмысленно). Дальше это ОБЫЧНЫЙ
забег с курированным пулом (`state['queue']` + `_pick_from_queue`).
- Вопрос с двумя темами даёт ошибку ОБЕИМ: какая подвела — неизвестно, делить
  пополам = выдумка.
- ⚠️ Вопросы без тем в работу над ошибками не идут (целиться в «Без темы» нечем).
  Поэтому сводка отдаёт **`mistake_topics`** — ровно то, что соберёт сервер, тем
  же подсчётом; экран рисует полосу пропорции оттуда и НЕ фильтрует сам, иначе
  обещал бы разбор темы, которой не будет. В `topic_rows` «Без темы» при этом
  есть — это честная строка точности. Инвариант держится тестом.
- Недобор: сначала из других тем ошибок, потом из любых вопросов того же типа;
  меньше 10 в пуле — забег короче, это не ошибка.

**Экран результатов** — порядок блоков = порядок важности: чем кончилось (сердца +
«выбыл на вопросе N» / «время вышло») → метрики → **работа над ошибками**
(единственный акцентный блок) → точность по темам → свёрнутые графики → шеринг →
кнопки. Рисуется целиком из серверной сводки. Ошибок нет → зелёное «Чисто!»
с предложением режима сложнее.
- ⚠️ Экран красится РОВНО один раз (из колбэка `finish`): второй проход
  проиграл бы count-up счёта заново, а бейдж рекорда, уже записанного в
  localStorage, перестал бы быть рекордом сам себе. Рекорд считается в `endRun` —
  это событие, а не состояние.
- Сеть отвалилась — остаётся запасная сводка из счётчиков клиента (`localSummary`,
  поле `offline`): не второй расчёт, а те же числа, что прислал сервер. Разбивок
  и графиков в ней нет — их честно считает только сервер.

**Графики** (`Подробная статистика`, свёрнуты, строятся лениво при раскрытии):
динамика счёта + комбо ступенькой, гистограмма времени, кольцевая сложностей,
сравнение с прошлыми играми. ⚠️ **Библиотеки НЕТ, SVG рисуем руками** — не ради
размера: цвета идут через `var(--rush-*)`, поэтому переключатель темы сайта
перекрашивает графики сам; canvas-библиотека потребовала бы перерисовки на каждую
смену темы и держала бы цвета в JS в обход токенов. Янтарь в кольцевой — в своём
законном смысле (шкала сложности).

**localStorage:** `econ_rush_best_<режим>` (рекорд; старый общий ключ
`econrush_best` мигрирует в блиц) и **`econ_rush_history`** (последние 30 забегов
`{mode, score, accuracy, at}` — истории раньше не было вообще). График сравнения
показывает 12 последних забегов ТОГО ЖЕ режима (счёт «Пули» и «Классики»
несравним). Целевой забег в историю не пишем.

**Публичная страница результата** `/game/r/<код>/` (`GameResult`, миграция 0005):
- `code` — 8 символов Crockford base32 (без I, L, O, U: код читают вслух и
  переписывают руками), 32^8 ≈ 1,1 трлн. Порядковый id выдавал бы число забегов.
- Строку создаёт `finish`, идемпотентно (код в `state['result_code']`). Ссылка —
  **абсолютная** через `build_absolute_uri`.
- Это **НЕ лидерборд**: записи ничьи и несравнимы (авторизации нет, счёт из сессии
  игрока). Смысл один — у ссылки, которой делятся, должно быть что показать.
- Страница read-only, без логина, сводку рисует СЕРВЕР; JS только на кнопках
  шеринга — без него страница цела. Мини-график — `<polyline>`, точки считает
  вьюха (`_curve_points`). `require_safe`, не `require_GET`: перед разворачиванием
  превью мессенджер дёргает HEAD.
- Open Graph + Twitter (`summary_large_image`), все URL абсолютные; счёт — в
  тексте `og:title`. Картинка превью **статичная одна на все результаты**:
  `game/static/game/og_default.png`, пересобирается командой
  `manage.py make_og_image` (Pillow, шрифт ищется среди системных с кириллицей;
  не нашёлся — заливка без текста, но не падаем). Заменить на дизайнерскую можно
  просто положив новый PNG.
- Шеринг с экрана результатов: карточку-картинку рисует **canvas в браузере**
  (кириллицу отрисует он сам, серверу рендер не нужен) → `navigator.share({files})`
  на мобильных, скачивание PNG на десктопе; плюс копирование ссылки и нативное
  «Поделиться» (кнопка только там, где `navigator.share` есть).
  ⚠️ **Ловушка Web Share (чинили 2026-07-16).** В `navigator.share` рядом с
  `files` НЕЛЬЗЯ класть `text`/`url`: системное окно «Поделиться» на десктопе
  берёт текст и ВЫБРАСЫВАЕТ картинку — кнопка «поделиться картинкой» уходит
  подписью. Отдаём только `{files, title}`; ссылка не теряется — она нарисована
  на самой карточке. Отказ `share` (кроме `AbortError` — игрок передумал) уводит
  в скачивание PNG, никогда в текст. Держится тестами в `test_page_js.py`.

- **`GameQuestion`** (кэш, пересобирается `build_game_pool`, контент-таблицы не
  трогаются): FK на Problem (+ nullable FK `part` — задел под данетки-пачки,
  `UniqueConstraint(problem, part)`), `question_type`, `options`,
  `correct_index` / `correct_indices` / `correct_value` (по типу), difficulty
  (в основном эвристика), темы денормализованы, `lang`; `stage`/`year`/`grade`
  заполнены у вопросов ВсОШ (денормализовано из `SourceReference`) — задел под
  фильтр «только регион».
- **Пул (локально, после пере-импорта ВсОШ 2026-07-13): 2 917** — boolean 380
  (ru), single 2 225 (ru 801 + en 1 424, en в игре не используется), multi 248
  (ru), **numeric 64 (ru) — «Классика» жива**; источник numeric — тесты
  ВсОШ-регион 2016–2025 (см. «Импорт ВсОШ-регион»). Вопросов с заполненными
  `stage`/`year`/`grade` (ВсОШ) в пуле: **165** — задел под фильтр «только
  регион». Таблицы (реконструированные `$$\begin{array}…$$`) и кусочные
  функции (`\begin{cases}…\end{cases}`) не бракуются — часть таких вопросов
  вошла в пул (numeric-тип с лимитом длины 700), часть отсеялась по общему
  лимиту длины (300 у boolean/single/multi) из-за длинного условия сценария —
  это НЕ связано с таблицей/cases как таковыми (проверено: даже после замены
  таблицы/cases на плейсхолдер условие всё равно длиннее лимита).
  Разведка данеток (`reports/game_modes/01_boolean_recon.md`): пачек с разметкой
  по подпунктам в базе НЕТ — «тест: верно/неверно» это одиночные утверждения
  (текст в `statement`, подпункты = варианты Верно/Неверно), конвертация 1:1;
  «тест: все верные» → multi, буквы `Problem.answer` через `normalize_label`,
  несопоставленная буква = брак. Фильтры качества прежние (длина ≤300 (≤700
  numeric), рисунки, битый LaTeX, непарные $, конфликт сигналов ответа = брак;
  таблица без реконструкции — брак, с реконструкцией — пускаем).
- **Схлопывание повторов на сборке пула** (`build_game_pool.py::pool_dedup_key`/
  `pool_dedup_wins`, контент-таблицы Problem/ProblemPart НЕ трогает — только
  какие GameQuestion попадут в кэш): ключ = тип + нормализованный текст +
  нормализованные варианты **в исходном порядке** (+ `correct_value` для
  numeric — одинаковый текст с разным числовым ответом это разные вопросы, не
  дубликат). Совпадение ключа → побеждает более свежий `year`, при равенстве —
  меньший `problem_id`; проигравший считается в `duplicate_in_pool` и не
  попадает в пул. При пере-импорте ВсОШ 2026-07-13 схлопнуто **84** повтора
  (в основном межгодовые повторы вопросов ВсОШ, см. «Хвосты» ниже). Тесты —
  `PoolDedupTests` в `game/tests/test_game.py`.
- API: `session/start?mode=&topic=` (без mode — blitz; неизвестный — 400;
  тайминги режима в ответе), `question`, `answer` (POST: `{question_id,
  choice|choices|value}` по типу, null/отсутствие = скип; повтор = 409).
  Анти-чит: правильный ответ (`correct_*`) НИКОГДА не входит в payload
  вопроса — только в ответ на ответ. Числовой вопрос (Классика) несёт `unit`
  в payload, если у задачи есть единица ответа (напр. «%»).
- **Выдача — случайная, но «сначала невиданные»** (прогрессии сложности больше
  нет): session-ключ `econ_rush_seen` `{mode: [id…]}` живёт МЕЖДУ забегами
  (последние 1500 id на режим); пул режима исчерпан → список чистится, цикл
  по кругу. Внутри забега повторы исключены строго.
- **Клавиатура — первична:** 1–5 ответ (в рапиде — переключение выделения,
  Enter — отправить), пробел пропуск, Enter старт/ещё раз; в классике ввод
  в поле (`inputmode=decimal`, символы `0-9 , . / -`), Enter — отправить.
  Рекорды — localStorage `econ_rush_best_<mode>` (старый общий ключ мигрирует
  в блиц). Слой токенов `--rush-*`; полоса времени малиновый→амбер(<15с)→
  красный(<7с) — задокументированное исключение из «сигналы священны».
- Тесты: `game/tests/test_game.py` — **141** (извлекатели по типам,
  parse_exact_number, 4 режима, multi/numeric-проверки, анти-чит, «невиданные»,
  дедуп пула, жизни и две концовки, агрегатор сводки на синтетическом журнале,
  квоты работы над ошибками, публичная страница и OG) + `test_generators.py` 35
  + **`test_page_js.py` 5**. Всего по игре 181, по проекту 307.
  ⚠️ `test_page_js.py` ловит класс опечаток, невидимый и для `check`, и для
  рендера (страница отдаёт 200 с любой ошибкой в JS, а ломается уже у игрока):
  `node --check` инлайн-скрипта, каждый `getElementById` имеет элемент в
  разметке, **имена полей сводки сверяются с реальным `build_summary`**
  (разъехалось имя → «undefined» на экране, ни один питон-тест не увидит),
  нет мёртвых функций, числа механики берутся из конфига.
- **Не сделано:** звук; глобальный лидерборд (рекорд только локальный;
  `GameResult` для него не годится — записи ничьи и несравнимы);
  деплой (ветка не в `main` — решение о выкатке за преподавателем);
  английская часть пула не используется; фильтр «только регион» в UI (данные
  в пуле уже есть — `stage`/`year`/`grade` заполнены у 165 вопросов);
  **тюнинг жизней и таймингов замером** (3 жизни и бонусы времени поставлены
  на глаз — как пороги «Штриха», снимать замером, а не спорить).
- **Планы:** возврат вопросов с ошибками, ежедневный вызов, фильтр
  «только регион» в UI (рандомизацию чисел в классике закрыли генераторы),
  дизайнерская OG-картинка вместо сгенерированной заглушки.
- **Хвост генераторов:** 13 архетипов из 17 ещё не на эталоне —
  `shift_equilibrium`, `price_control`, `elasticity_point`, `elasticity_arc`,
  `surplus`, `costs_tc`, `comp_firm`, `perfect_price_discrimination`,
  `ppf_joint`, `comparative_advantage`, `mpc_multiplier`, `labor_minwage`,
  `price_index`. Порядок переработки: сначала графические
  (`price_control`, `surplus`, `ppf_joint`) — у них уже есть куда положить
  чертёж; общие декорации `_market.setup_*` и товары `_ppf.PPF_GOODS` живы
  только ради них и уйдут вместе с последним непеределанным архетипом.

## Генераторы вопросов Econ Rush (`game/generators/`, ветка `feat/game-generators`)

Параметрические генераторы «архетипов»: сначала сэмплируется «красивый»
ответ (целое в ≥80%, иначе дробь со знаменателем ≤10 / десятичная с одним
знаком), параметры условия вычисляются обратным ходом — ответ верен по
построению, ИИ в математике нет. Стиль текстов — по памятке
`reports/generators/style_notes.md` (манера ВсОШ-региона, дословных
заимствований нет).

**Эталон качества (утверждён 2026-07-16, решение в Notion; пример —
монополия «Аквалайн»).** Переработанные архетипы обязаны держать планку:
сюжет солидным абзацем с мотивацией; **БИБЛИОТЕКА сюжетов** (у каждого свой
текст, а не один шаблон с подставленными словами); ровно ОДИН числовой
вопрос в Классике; полное решение с рассуждением «почему так», единицами и
смыслом; целочисленные/просто-дробные ответы; график у графических.
⚠️ **Единицы, товары и вид вмешательства берутся ИЗ СЮЖЕТА** (кофе меряют в
кг, паром продаёт поездки, «субсидия на газировку ради борьбы с ожирением» —
бессмыслица). Сюжет выбирается в `sample()` и кладётся в `params['story']`,
иначе движок выберет обёртку ПОСЛЕ единиц и они разъедутся.
**На эталоне 4 из 17:** `monopoly`, `equilibrium`, `tax_subsidy`, `ppf_single`
(список продублирован в `test_generators.py::ETALON_ARCHETYPES` и
`preview_generated.py::ETALON_KEYS`). Остальные 13 — прежние, банальные,
ждут переработки; держатся выключенным флагом. Планка проверяется тестами
`EtalonQualityTests` (длина сюжета, один вопрос с единицами, глубина решения,
точность ответа, разнообразие сюжетов, единицы из сюжета, валидность чертежа).

- **Движок** `game/generators/base.py`: `Archetype` (методы `sample(rng)`,
  `solve(params)`, `error_variants(params, solved, asked)`, `wrappers()`,
  `asked_values(params)`, `solution(...)`, `figure(...)` — необязательный);
  преобразование в 3 типа вопросов (numeric/single/boolean) — в движке один
  раз (`generate_question`/`generate_batch`). Дистракторы только вычисленные,
  уникальные, ≠ ответу, неотрицательные; <3 красивых → пересэмплирование.
  Всё точно на `Fraction`.
- ⚠️ **Сложность задаётся ЯВНО** — `Asked(difficulty=1..5)`, по экономической
  глубине вопроса. Без неё работает старая оценка по числу шагов решения
  (`_difficulty`), и это неверно для развёрнутых решений: подробный разбор
  «почему так» длиннее не потому, что задача труднее — все переработанные
  архетипы стали бы сложностью 5. **Многословность ≠ сложность.** Старое
  поведение оставлено фолбэком для непеределанных архетипов.
- **График — только к numeric** (его смотрят в разборе, где есть место; в
  карточке Блица/Пули он был бы лишним). Схема — `game/generators/_figure.py`:
  ДЕКЛАРАТИВНАЯ геометрия (линии/заливки/точки/засечки в координатах задачи,
  роли вместо цветов). Питон считает числа, браузер рисует SVG. Добавить
  график новому архетипу = вернуть геометрию из `figure()`, рисователь не
  трогается.
- **Реестр** `game/generators/registry.py` — 17 архетипов (Блок А:
  equilibrium, shift_equilibrium, tax_subsidy, price_control,
  elasticity_point, elasticity_arc, surplus; Блок Б: costs_tc, comp_firm,
  monopoly, perfect_price_discrimination; Блок В: ppf_single, ppf_joint,
  comparative_advantage; Блок Г: mpc_multiplier, labor_minwage, price_index).
  Недоделанный архетип в реестр не включать — это и есть исключение из боя.
- **Хранение:** миграция 0002 — у `GameQuestion` появились `is_generated`
  (индекс), `generator_key`, `gen_params` (JSON), `gen_solution`; FK
  `problem` → nullable (только для сгенерированных). Миграция **0006** —
  `figure` (JSON, nullable): параметры чертежа. `build_game_pool`
  пересборкой сгенерированные НЕ трогает (закреплено тестом).
- **Команды:** `generate_game_questions --per-archetype N [--confirm]
  [--seed S] [--only key,key]` (без `--confirm` — dry-run; повторный запуск
  сначала удаляет прежние записи тех же generator_key);
  `purge_generated` — полный откат (пул возвращается к базовым 2 917);
  `preview_generated --per 10` → `reports/generators/preview.html`
  (в базу не пишет, генерирует свежие примеры).
- **Выдача:** флаг `GAME_GENERATED_ENABLED` — True в `settings.py`,
  False в `settings_production.py` (на проде сгенерированных нет). Анти-чит
  прежний; numeric несёт `unit`. UI: чип «Тренировочный» на карточке,
  в «Разобрать ошибки» вместо ссылки в каталог — «Показать решение»
  (разворачивает `gen_solution` + чертёж, KaTeX).
- ✅ **Флаг герметичен (проверено 2026-07-16).** Единственная точка входа —
  `views._pool_qs()`; все поверхности выдачи ходят только через неё (счётчики
  и чипы тем стартовой страницы, выбор вопроса, очередь работы над ошибками).
  Три `GameQuestion.objects.get(id=…)` не дыры: два берут id из уже
  отфильтрованных списков, третий (`api_answer`) закрыт проверкой «вопрос не
  выдавался» → 404. Держится классом `FlagHoldsEverywhereTests` на СМЕШАННОМ
  пуле (тесты проверены на зубастость: при снятом фильтре краснеют все).
  **Вердикт: ветку можно мёржить в main с выключенным генератором.**
- **Рисователь чертежей** — `game/static/game/figure.{js,css}`, ОБЩИЙ у
  страницы игры и HTML-предпросмотра (`preview_generated` инлайнит те же
  файлы). Второй рисователь «только для превью» разошёлся бы с боевым, и
  превью показывало бы не то, что видит игрок. Наружу торчит одна
  `window.drawFigure(fig)`. Палитра — из calc2, но под своими именами
  `--gfig-*` и только внутри `.mi-figure` (у calc2 свой изолированный мир
  токенов). Как в calc2, цвета кривых зафиксированы в обеих темах, по теме
  меняются только структурные — поэтому чертёж перекрашивается сам.
  ⚠️ В `game.html` уже есть свой `svgEl` для графиков статистики — не
  объявлять второй в той же области видимости (побеждает позднее объявление).
- **Тесты:** `game/tests/test_generators.py` — 60 (контрольные числа всех
  архетипов прямой подачей в `solve()`, по 500 сэмплов свойств на архетип,
  хранение, выдача, `EtalonQualityTests` — планка качества,
  `FlagHoldsEverywhereTests` — герметичность флага, `FigureServingTests` —
  чертёж только после ответа) + `test_page_js.py` 12. Полный локальный
  прогон: **330/330** (по игре 204).
- **Состояние пула (локально, 2026-07-16):** 8 017 = базовые 2 917
  (boolean 380, single 2 225, multi 248, numeric 64) + 5 100 сгенерированных
  (17 архетипов × 100 × 3 типа). Классика 64 → 1 764 numeric. Из
  сгенерированных **400 несут чертёж** (numeric четырёх эталонных архетипов).
- **Предпросмотр:** `manage.py preview_generated --per 6` →
  `reports/generators/preview.html` (306 примеров, KaTeX + чертежи; архетипы
  на эталоне помечены бейджем, остальные — «ждёт переработки»).

## Импорт ВсОШ-регион (тесты первого тура) — ✅ 2016–2025 в базе, пере-импорт 2026-07-13

Все 10 годов регионального этапа импортированы: **218 задач** (после
вычитки и пере-импорта 2026-07-13; исходно было 225 — см. «Почему 225→218»
ниже), Source «ВсОШ — региональный этап» (id=25) один на все годы, различие
в `SourceReference` (year/grade/problem_number/url). Списки id — в
`materials/vsosh_region/<год>/imported_ids.txt`; главные отчёты —
`reports/vsosh_region/proofread_summary.md` (вычитка 257 вопросов, 25
mechanical-фиксов, 0 расхождений ответа) и `reports/vsosh_region/
structure_audit.md` (таблицы/cases/подписи графиков, см. ниже).

**Конвейер года:** PDF (качает преподаватель вручную — сеть машины к
российским сайтам заблокирована) → нормализация имён `test_answers_<кл>.pdf`
→ при битой кодировке `fix_vsosh_tounicode --year N` → `parse_vsosh_region
--year N` (span-парсинг PyMuPDF: правильный вариант = полужирный маркер;
типы boolean/single/multi/numeric; LaTeX с восстановлением этажных дробей,
кусочных функций и реконструкцией таблиц — см. ниже) → `preview_vsosh_region`
(HTML с KaTeX) → `check_vsosh_gates` (шлюзы: unparsed ≤3 и ≤10%, диапазон
12–60, валидность ответов, парность $/{}) → `import_vsosh_region --year N
[--confirm]` (двухфазная; дедуп = условие **плюс варианты**, точное
совпадение нормализованного текста против ВСЕЙ базы — генерические стемы не
съедают новые вопросы, но и настоящие межисточниковые дубли не плодятся).
Правило «ничего не выдумывать»: неоднозначный ответ/тип/нерасшифрованный
глиф → вопрос в unparsed.

**Годоспецифика вёрстки (профили в `parse_vsosh_region.PROFILES`):**
- 2020–2023 `zadanie`: секции «Задание N», данетки в №1, numeric в №4;
  у 2021 в конце сетка «Правильные ответы» (стоп-строка).
- 2016–2019 `chast_flat`: секции «Часть N» со старыми типами и СКВОЗНОЙ
  нумерацией вопросов 1–20 (защита: номер = предыдущий + 1).
- 2024–2025 `chast`: «Часть N», без данеток, numeric в №3, «Часть 4» —
  задачи второго тура (стоп-секция, не тест).
- Общие файлы: 2016–2017 один PDF на 9–11; 2018–2020 один на 10–11
  (копия файла в слоты классов, merge схлопывает в `grades`).

**Битые кодировки (`fix_vsosh_tounicode`):** PDF 2024 (весь) и 2021 (11
класс) — CID-шрифты без ToUnicode / с врущим ToUnicode, текст извлекается
кракозяброй. Референсные карты глиф→юникод снимаются `texttrace` с 2023/2025
(те же Libertinus, Identity-H ⇒ код = глиф оригинала), в PDF вписываются
корректные CMap; недостающие глифы размечены вручную в `MANUAL_GLYPHS`
(каждый проверен по рендеру страницы). Ловушки: texttrace обрезает имена
шрифтов ~31 симв. (SemiboldItalic маскируется под Semibold — лечится
italic-битом flags); непокрытые глифы MuPDF отдаёт как chr(gid), и мусор
маскируется под ASCII — фиксер принудительно маппит их в U+FFFD, а парсер
отправляет такие вопросы в unparsed. Литеральный доллар (валюта) хранится
как `$\$$` — KaTeX рендерит знак, голые $ спаривались бы в псевдоформулу.

**Вычитка (257 вопросов, `proofread_summary.md`):** узкая построчная сверка
всех вопросов против PDF по классам дефектов (винкулум-гард, глифы,
курсив-сдвиг, дроби только при наличии черты, подписи осей, гомоглифы,
задвоенные знаки и т.п.) — 25 mechanical-находок починены, 0 расхождений
ответа. Отдельным заходом — **структурный класс** (`structure_audit.md`):
вычитка на битые символы пропускала таблицы/списки, размазанные PDF-парсером
в строку («валидный» текст, KaTeX не ругается):
- **Таблицы** (`extract_tables`/`reconstruct_table`/`apply_tables` в
  `parse_vsosh_region.py`): распознаются по геометрии PDF (горизонтальные
  линейки booktabs), реконструируются в `$$\begin{array}{|l|c|…|}\hline…$$`
  (формат Б — LaTeX-массив, а не HTML `<table>`: `problem_detail.html` не
  использует `|safe`, HTML-тег показался бы текстом; массив едет по
  существующему KaTeX-конвейеру везде без правки шаблонов). Реконструировано
  **4** таблицы (2018 №12, 2022 №3.2 и №4.5, 2025 №3.2). Игра больше не
  бракует вопрос с реконструированной таблицей (`\begin{array}` в
  `TALL_MATH_ENVS`, `\hline` внутри массива не путается с псевдотаблицами).
- **Кусочные функции** (Класс В, `_assemble_cases` в `postprocess_text`):
  литеральная уплощённая `\{значение1, условие1; значение2, условие2.`
  собирается в `\begin{cases}…\end{cases}` (признак — открытая `\{` без
  `\}`, ветви через `;`, каждая «значение, условие»; множество `\min\{A,B\}`
  со `\}` не трогается). Собрано **5** записей (4 вопроса: 2020 №4.3, 2021
  №3.5[9]/[10,11], 2022 №3.4[9]). `cases` уже в `TALL_MATH_ENVS` — игра
  берёт такие вопросы.
- **Подписи осей графика** (Класс Г, `drop_figure_labels`/
  `_polyline_figure_marks`): графики, нарисованные полилинией (короткие
  прямые сегменты, не кривые Безье), теперь детектируются; короткие подписи
  (≤4 симв.) у края зоны графика вычищаются из решения; владелец графика
  атрибутируется по потоку чтения (page, y) — ловит график на стыке страниц.
  Пометка «в оригинале рисунок»: 2018 №18, 2021 №3.5[10,11], 2024 №2.2.
- **Остаточные плашки** «математика линеаризована» (нечиненные floor-fraction
  в решениях): **7** — 2017 №13/№14, 2019 №11, 2020 №3.1, 2021 №4.5, 2022
  №4.2, 2024 №2.5 (плашка зовёт сверить с PDF).

**Почему 225 → 218 при пере-импорте (2026-07-13, полностью протрассировано,
0 неожиданностей):** удаление старых задач года по `imported_ids.txt` →
`import_vsosh_region --year N --confirm` для всех 10 лет → сверка каждой
пары (год, номер, классы) со снимком базы до пере-импорта: **0 расхождений
ответа** на всех 217 сохранившихся вопросах. Пропали 8 старых записей
(появилась 1 новая = чистый минус 7):
- **2019 №19** — два старых вопроса (классы `[9]` и `[10, 11]`) схлопнулись
  в один общий `[9, 10, 11]`: младший класс получил тот же текст, что и
  старшие, после нормализации; аналогично пропало отдельное **2019 №9
  [10,11]** — совпало с уже существующей задачей в другом источнике после
  того, как `0{,}5` (LaTeX-запись без математики) нормализовалось в `0,5`.
- **2016 №17, 2017 №8** — вычитка убрала приклеенный подвал страницы
  («Первый тур. Тест Всероссийская олимпиада школьников (2016 г.)»), после
  чего текст стал байт-в-байт совпадать с уже существующей в банке задачей
  из другого источника (Overleaf-архив / Сборник тестов АА) — дубль
  корректно отфильтрован тем же механизмом дедупа, что и раньше.
- **2017 №11** — та же история, но подвал был приклеен к тексту ВАРИАНТА
  ответа, не условия.
- **2023 №3.5[11]** — вычитка восстановила дефис («врачей-рентгенологов»,
  раньше слова были склеены), после чего текст совпал с существующей
  задачей.
- **2024 №2.1[11]** — вычитка исправила гомоглиф («долгоcрочном» с латинской
  `c` → «долгосрочном»), после чего текст совпал с существующей задачей.

Во всех случаях — существующий дедуп-механизм `import_vsosh_region.py`
(точное совпадение нормализованного текста+вариантов против всей базы)
сработал ПРАВИЛЬНО: контент не потерян (та же формулировка уже есть в банке
под другим id из другого источника), просто у конкретно этого вхождения
ВсОШ не появилось отдельной записи — то же поведение, что уже применялось к
другим дублям при первом импорте.

**Хвосты:** разметка почти-дублей (`reports/vsosh_region/near_dups_all.md`,
195 пар ≥ 0.90 — межгодовые повторы ВсОШ + копии из смешанных источников);
2 вопроса с текстовыми ответами не импортированы (2018 №16 «Монополия»,
2019 №20 «Нью-Йорк»); фильтр «только регион» в игровом UI (данные в пуле
уже есть); решение о выкатке игры и обновлённого банка на прод.

## Импорт ВсОШ-муниципал (Москва) — конвейер (ветка `feat/vsosh-municip-import`)

Материалы: `materials/vsosh_municip/<год>/` (zip качает преподаватель;
2016/2024/2025 пустые). Конвейер: `parse_vsosh_municip [--year N]` →
`check_vsosh_municip_gates` → `preview_vsosh_municip` →
`import_vsosh_municip --year N [--confirm]`. Выход парсера —
`materials/vsosh_municip/<год>/parsed.json`; превью —
`reports/vsosh_municip/preview.html` (все годы, навигация сверху);
разведка — `reports/vsosh_municip/00_recon.md`, dry-run —
`reports/vsosh_municip/dry_run.md`. Механика span-разбора/LaTeX/таблиц
импортируется из `parse_vsosh_region` (регион не тронут). В муниципе НЕТ
boolean и multi — только single (тест 5 шт.), numeric (краткий ответ),
open (развёрнутые с решением жюри, есть в 2017–2020).

**Три семейства вёрстки** (`PROFILES` по годам):
- **A. Word-PDF «Решения и критерии»** (2017, 2018, 2020, 2022, 2023):
  вопрос = полужирный маркер «N.». Ответ теста: полужирная строка варианта
  (2017, 2022), «Таблица ответов» (2018, 2020; 2017 — сверка обоих
  механизмов, расхождение = unparsed), полужирный текст буллет-варианта
  без букв (2023). Нумерация сквозная (2017–2020) или с 1 в каждой секции
  (2022–2023). Вариантов может быть 4 или 5.
- **B. Экспорт онлайн-платформы** (2021, файлы `otvety`): сетка Roboto,
  ВСЕ варианты полужирные — ответ помечает ТОЛЬКО синяя радиокнопка
  (вектор fill≈(0.29,0.55,0.93), 2 элемента на вопрос); серые кольца =
  якоря вариантов; кластеризация по y-зазору >150pt = вопросы. Краткий
  ответ 2021 — отдельный Word-PDF `resheniya` (механика A).
- **C. DOCX** (2019): варианты — элементы word-списка (букв в тексте НЕТ,
  буква таблицы ответов ↔ позиция; формат нумерации обязан быть
  `russianLower`); формулы — OLE/WMF-объекты, в тексте отсутствуют →
  вопрос честно в unparsed (22 из 60 — год держится шлюзом HELD).

**Ловушки муниципа:**
- 2017/2020: Word рвёт порядок чтения формул («TC aQ b = +») — строки одной
  базовой линии пересобираются по x (`_merge_same_baseline`) с пометкой
  «формула Word собрана по координатам»; вариант из 2-й колонки
  (порядок чтения «а в б г») НЕ сливать — собирается по буквам.
- 2017: формулы SymbolMT в PUA (F0xx = Adobe Symbol, F8xx = куски больших
  скобок) — статические карты `SYMBOL_PUA`/`SYMBOL_PUA_F8`; непокрытый код
  → U+FFFD → unparsed. Куски скобок систем (⎧⎨⎩) вычищаются с пометкой.
- 2022: в `ans`-файлах задвоенные глифы Cambria Math С ПОТЕРЕЙ второй буквы
  («TC»→«TTTT», не «TTCC») — восстановить нельзя (детектор
  `has_doubled_math`); условия/варианты берутся из чистых `tasks`-файлов
  (двухфайловая сборка), задвоенное решение выбрасывается с пометкой.
- Таблица ответов читается из СЫРЫХ строк ДО реконструктора таблиц (иначе
  станет `$$array$$`); у 2017 реконструктор таблиц отключён
  (`tables: False`) — принимает формулы решений за таблицы и съедает
  соседние строки.
- «Ответ на вопрос N:» в развёрнутых (2020) — разбор жюри, идёт в решение,
  не в канонический ответ.
- Слияние классов внутри года — нормализация ё→е + хвостовая пунктуация
  (`_norm_merge`), иначе «вперёд»/«вперед» не сливаются.

Импорт: Source «ВсОШ — муниципальный этап (Москва)», stage='муниципальный',
url пуст (сеть закрыта, не выдумываем — файл+страница в note). Статусы:
published только тесты с надёжным ответом без пометок о тексте условия;
draft — открытые задачи и линеаризованная/пересобранная математика.
Тесты парсера — `problems/tests/test_parse_vsosh_municip.py` (31).

---

## Дизайн-токены и правила публичных страниц

Акцент сайта — **малиновый** (`#BE185D` светлая / `#FF4D94` тёмная). Токены — в
`templates/_tokens.html` (подключается ПЕРВЫМ в `<head>` каждой публичной базы:
`catalog/base.html`, `student/base.html`, `teacher/base.html`, login, calendar_stub).
`:root` (светлая) + `[data-theme="dark"]` + анти-мигание (скрипт ДО CSS ставит
`data-theme` по `localStorage['theme']`). Переключатель тем ☾/☀ — в `_nav.html`.

⚠️ `/calc2/` имеет СВОЙ изолированный мир токенов (`--curve-*`, `--cost-*`, свой
bootstrap темы) — не трогать его общими токенами. Единый ключ темы для всего сайта —
`localStorage['theme']` (calc2 тоже на него переведён).

**Правила (соблюдать на любой новой публичной странице):**
- Каждый цвет — через `var(--…)`. Хардкод hex ломает тёмную тему (исключение — цвета
  типов событий календаря и `rgba(0,0,0,…)` тени).
- **Сигналы священны:** амбер = звёзды сложности, зелёный = «решение ✓». Никогда не
  делать их акцентом. (`--green #1d7e45`, `--amber #b26b00` — затемнены под WCAG AA.)
- **Кнопки «Сцены»:** крупные действия (отправить/создать/сохранить/найти/скачать) —
  графитовые `var(--btn-bg)` + `var(--on-btn)`. Малиновый сплошной — ТОЛЬКО на
  мелочах/активном (логотип, активный nav, фильтр, ссылки, фокус-кольца, чипы).
  Акцент ≤ ~10% экрана.
- **Иерархия поверхностей:** страница — серая (`--bg`); карточки-контент — белые
  (`--surface`); `--surface-2` — только рецессивное (рельсы, инпуты). Карточка без
  явного `background` сольётся с фоном — задавать `var(--surface)`.

**Каталог (Фаза 2):** доп. логика в `problem_list` (catalog/views.py) — только
additive: `sort`, `view` (rows/table/gallery), нулевое состояние `is_zero_state` +
`atlas` (21 тема), `source`/`grade` в карточке. Теги в `catalog_filters.py`:
`set_param`, `remove_param`.

**Процент в шаблоне:** выводить через `{% widthratio pct 1 100 %}` (даёт integer).
`{{ pct|floatformat:0 }}` НЕ использовать — возвращает строку с запятой.

---

## Деплой и работа с продом

🌐 **Боевой сайт: https://qls-platform.onrender.com** — ЖИВОЙ.
Регистрация на проде ЗАКРЫТА (живых пользователей нет, показывается сосоздателям).

- Хостинг **Render.com** (free, регион Frankfurt). БД — **PostgreSQL** `qls-db`
  (free, 90 дней). Репозиторий приватный `github.com/malinovskiy-makar/qls`, ветка
  `main`. Python 3.11 на проде. Docker НЕ используется (нет TeX → PDF-экспорт отключён,
  только `.tex`). `sentence-transformers` на проде НЕ грузится (эмбеддинги посчитаны
  локально, лежат в базе; похожие работают по кэшу M2M).
- Файлы деплоя: `requirements.txt` (без ML), `requirements-local.txt` (с ML),
  `config/settings_production.py`, `Procfile`, `render.yaml`, `.gitignore`.

**Деплой = пуш в `main`** (Auto-Deploy On Commit, 2–4 мин; Start Command прогоняет
`migrate` + `collectstatic`). Откат: Render → Events/Deploys → Rollback.

**Рабочее правило:** недоделанное — в отдельных ветках; в `main` пушим только то, что
преподаватель одобрил глазами по скриншотам.

⚠️ **Ограничения free-tier (выяснено в Г1):**
- **Нет Shell** на free — команды на проде напрямую НЕЛЬЗЯ. Только локально против
  внешней базы или временно через `startCommand`.
- `startCommand` обязан открыть порт за ~3 мин (`gunicorn` первым), иначе деплой
  `Timed Out`. Долгие операции до gunicorn убивают деплой.
- 1 общий CPU / 512 МБ; сон после 15 мин без HTTP (просыпается ~30 с).

**Загрузка/обновление данных в прод (рабочий способ).** Внешнее `loaddata` непригодно
(часы, обрывы). Используем bulk_create локально против внешней базы:
```
# 1. собрать порционные gzip-фикстуры (исключают embedding, чистят NUL-байты)
./venv/bin/python manage.py dump_for_deploy --outdir deploy_fixtures --chunk 2000
gzip -f deploy_fixtures/*.json
# 2. залить (ignore_conflicts → идемпотентно)
SECRET_KEY=любой DATABASE_URL="<EXTERNAL_DB_URL>?sslmode=require" \
DJANGO_SETTINGS_MODULE=config.settings_production \
./venv/bin/python manage.py bulk_load_fixtures --dir deploy_fixtures
```
EXTERNAL_DB_URL — Render → qls-db → Connections → External Database URL.

⚠️ **`bulk_load_fixtures` ТОЛЬКО ВСТАВЛЯЕТ новые записи** (`ignore_conflicts=True`) и
молча пропускает существующие по pk → **правки в существующих задачах на прод так НЕ
попадают.** Для обновления существующих — команда **`update_prod_problems`** (точечный
`bulk_update` по списку id из `reports/ai_cleanup_ile/affected_ids.txt`; пересобирает
подпункты; задачи с привязанными Hint пропускает; одна транзакция; `--dry-run`).
Последовательность: (1) бэкап `pg_dump "<URL>?sslmode=require" -Fc -f backup.dump`
(`brew install libpq`), (2) `--dry-run`, (3) боевой.

⚠️ **Мина: отставшие sequence в PostgreSQL.** `bulk_load_fixtures` грузит явные id, но
НЕ двигает счётчик таблицы → при вставке без явного id → `UniqueViolation`. На SQLite
не воспроизводится. `update_prod_problems` выравнивает sequence для ProblemPart через
`setval`. **TODO:** выровнять sequence ВСЕХ таблиц перед следующей крупной заливкой
новых записей (`sqlsequencereset problems` против прода).

**Технический долг деплоя:**
- ⚠️ Сменить пароли `admin`/`teacher1`/`student1` на проде (пришли с дев-паролями,
  публично известны) — до показа кому-либо.
- PDF-экспорт отключён (нет TeX без Docker) — вернуть на платном тарифе.
- Сон free-tier (15 мин) и лимит Postgres (90 дней) — уйдут на платном.

---

## Текущее состояние платформы (цифры, 2026-07-13)

*Снимок на дату. Живые цифры и статус — в Notion «Результаты».*

| Параметр | Значение |
|---|---|
| Задач в базе | **31 690** (published 21 267) |
| Видимых в каталоге | **18 861** (published, не зафлагованных) |
| — из них тестов (`problem_type` «тест:») | 4 156 (published+незафлагованных; всего тестов в базе 4 950) |
| ВсОШ-регион (Source id=25) | **218** задач, годы 2016–2025 (было 225 до пере-импорта 2026-07-13 — см. «Импорт ВсОШ-регион») |
| Флагов шлюза (`needs_quality_review`) | 3 518 |
| `solution_needs_review` (решение скрыто) | 410 |
| Подпунктов (`ProblemPart`) | ~63 000 |
| С решением | 6 448 (из них 1 097 — вынесены ИИ, `solution_ai_extracted`) + решения жюри ВсОШ |
| С ответом | 5 445 + 218 ВсОШ |
| Эмбеддинги | все 31 690 на BGE-M3, формула с ai_blurb+тегами (218 ВсОШ пересчитаны 2026-07-13) |
| Тесты | **1235/1235** ✅ (снимок 2026-08-10; в таблице выше — цифры на 2026-07-13) |
| Игровой пул (`GameQuestion`) | **8 017** = базовые **2 917** (boolean 380, single 2 225, multi 248, numeric 64; из них ВсОШ — 165) + **5 100** сгенерированных (17 архетипов, ветка `feat/game-generators`) |
| Автотем (kNN, порог 0.70) | ~3 425 |

Источников ~24. AI-чистка ILE (2 584) применена и доехала на прод. Батч 1 (поля)
и Батч 2 (чистка текста, 4 930 задач) применены локально. `fix_latex_junk`
применён (834). **На прод изменения Батчей/чисток НЕ заливались** — локальная
база впереди прода.

## Правовой контур (152-ФЗ) — до запуска

Владелец/оператор — **в России** (прежняя запись про Финляндию была неверной;
трансграничный риск ЕС↔РФ снят). Но действует **152-ФЗ**: с 2025 ч. 5 ст. 18
запрещает хранить/обрабатывать персданные граждан РФ в базах за пределами России;
штрафы до ~18 млн ₽; за данные несовершеннолетних — в т.ч. уголовная ответственность;
нужна регистрация оператором в РКН. Текущий Render (США) — **несоответствие, как
только появятся живые пользователи (тем более дети)**.

**До живого запуска:** перенести мастер-базу на российскую инфраструктуру (Яндекс
Облако / SberCloud / VK Cloud / колокейшн в РФ), оформить политику конфиденциальности
и согласия, российский приём платежей (ЮKassa). Не юр-консультация — нужна сверка со
специалистом, но риск конкретный.

Учебник: оригинальный самостоятельно написанный текст — допустимо; узнаваемо
производный пересказ чужих учебников — нет.

---

## Семантический поиск — проработка + Стадия 1 (АКТИВНАЯ ветка)

Фича: пользователь описывает задачу словами → система возвращает похожие задачи из
банка по смыслу. **Главный сдвиг в позиционировании:** это НЕ «найди ровно ту самую
задачу» (недостижимо и не нужно), а «**подбери 15–20 похожих**» для подборки/листка —
и в этом качестве уже работает. Фича названа «Подобрать похожие задачи».

**Путь:** ввод (текст + фильтры) → (опц.) HyDE → эмбеддинг локальной моделью →
косинусный поиск (грубый перебор numpy по ~31к векторов — векторная БД не нужна) →
жёсткие фильтры (тема/сложность/решение) ранжируют → карточки.

**Две стороны проблемы качества (диагностика из ручных тестов):**
- *Сторона запроса:* сухое описание ≠ форма олимпиадного условия. Лечится **HyDE**
  (дешёвая модель придумывает гипотетическое условие как «магнит»; доказано — подняло
  задачу с «не найдено» до 88%). Эффективен на «чистых» задачах, слабее на сюжетных.
- *Сторона задачи:* олимпиадные условия НАМЕРЕННО сюжетные (бабушка, ресторан) — это
  неустранимое свойство домена, не дефект данных. Сюжет забивает экономический смысл в
  отпечатке. Лечится **ИИ-резюме сути** (сухая строка «о чём задача», эмбеддить её) —
  это **главный рычаг**. Разовая трата ~$20–65 на корпус, сначала пилот 300 задач за <$1.

### Стадия 1 — локальный прототип ✅ РАБОТАЕТ на BGE-M3

Страница `/catalog/smart-search/` («Подобрать похожие»). Код и все последующие
правки (фильтр Задачи/Тесты, апгрейд BGE-M3, ссылка в навигации) — в линии ветки
`feat/dirty-text-cleanup` (в `main` НЕ влито). Эмбеддинги пересчитаны — поиск
полноценно работает локально.

**Новые файлы:**
- `catalog/semantic.py` (ядро): ленивый синглтон (модель + индекс грузятся при первом
  запросе, живут в памяти); `get_model()` — использует `EMBEDDING_MODEL_NAME` из
  `problems/embedding_config.py`; `_build_index()` — numpy-матрица нормализованных
  эмбеддингов published-задач без флага (⚠️ BinaryField даёт `memoryview` → нужна
  обёртка `bytes(blob)`); `search(query_text, topic_id, difficulty, has_solution,
  limit)` → `[{'problem', 'score'}]` (косинус = скалярное произведение, топ-K×10 через
  `np.argpartition`, затем ORM-фильтры); `invalidate_index()`.
- `catalog/templates/catalog/smart_search.html` — форма + бейдж схожести (зелёный ≥70%,
  жёлтый ≥50%) + KaTeX-превью + нулевое/пустое состояние.
- `catalog/management/commands/benchmark_search.py` — команда замера качества (5
  запросов, топ-10). Эталон «до» на MiniLM: `reports/bge_benchmark/before_minilm.txt`.

**Изменённые:** `catalog/urls.py` (маршрут `smart_search`), `catalog/views.py`
(view `smart_search`, `@login_required`; обогащает результат `preview` + `topics_display`;
при отсутствии `sentence_transformers` — внятная ошибка без краша).

**Сделано после Стадии 1:** фильтр «Задачи/Тесты/Всё» (учитывает и тему «Тест»);
апгрейд на BGE-M3 + полный пересчёт + `cache_similar` + замер «после»
(`reports/bge_benchmark/after_bge_m3.txt`); ссылка «Подобрать похожие» в навигации;
HTML-отчёт бенчмарка с KaTeX.

**НЕ сделано:** HyDE (доказан вручную — 88%, автоматизация отложена); деплой
(модель ~1.4 ГБ не лезет на free-tier); пилот ИИ-резюме сути (главный рычаг
качества, 300 задач за <$1 — когда преподаватель готов на трату).

**Проверка:** в ветке `feat/dirty-text-cleanup` (или её потомках) → `runserver` →
`/catalog/smart-search/` (нужен логин). Первый запрос грузит модель ~6–7 с.

---

## ИИ-обогащение задач (Batch API) — ✅ ЗАВЕРШЁН весь пайплайн

Полные детали — в `CLAUDE_ARCHIVE.md` («ИИ-обогащение (Батчи 1–2)»). Выжимка:

- **Батч 1 (Haiku, поля) применён:** `ai_blurb` (внутреннее поле, на сайте не
  показывается), канонические теги (~210, `SYNONYM_MAP` в `apply_batch1.py`),
  флаги `multiple_problems` (~437, очередь на пере-импорт `batch1_reimport_queue.txt`),
  `not_a_problem`, «потерян рисунок» — списки на ревью, ничего не удалялось.
- **Батч 2 (Sonnet, чистка текста) применён: 4 930 задач** — чистка условия 3 489,
  вынесено решений 1 097 (`solution_ai_extracted=True`, фильтр в админке),
  переписаны подпункты 1 656. Пропуски: флаги `missing_figure`/`truncated` 1 772
  (очередь на пере-импорт), конфликт решения 118, неизвестная метка 106,
  подозрительное сокращение 26. Отчёт: `reports/batch2/apply_report.md`.
- **Разблокировка группы Б (250 пропущенных правок) — автосекции ПРИМЕНЕНЫ**
  (2026-07-19, ветка `feat/batch2-unblock`, в `main` не мержилось). Общая
  логика — `problems/batch2_unblock.py` (одна библиотека на превью
  `preview_batch2_unblock` и применение `apply_batch2_unblock` — урок
  склейки нарезки про два расходящихся генератора). Конвейер применения:
  правка Sonnet → БЕЗ solution (эта сессия его не пишет никогда) →
  нормализация кавычек (парные `<<...>>` → «ёлочки», `normalize_quotes`) →
  фильтр палочек (`introduces_pipe_table` — карточка целиком в остаток,
  если правка вносит `|` как разделитель вне формул, которого не было;
  суррогат таблицы не нужен, реконструкция — отдельный этап) →
  `glue_field()`. Три политики: конфликт решения — `solution` НИКОГДА не
  трогаем; неизвестная метка — детерминированный маппинг подпунктов по
  СХОДСТВУ ТЕКСТА (`difflib`, не по написанию метки — Sonnet путает
  латиницу/кириллицу, в базе есть испорченный ярлык «Ь» вместо «б»),
  однозначно только когда КАЖДАЯ запись Sonnet получила уникальный
  уверенный и монотонный по порядку адресат, и только для полей, где новый
  текст РЕАЛЬНО отличается (находка пост-контроля 2026-07-19: Sonnet иногда
  включает в патч неизменённый пункт); подозрительное сокращение — без
  автоматики, весь остаток. Переоценки идут против ТЕКУЩЕЙ базы, не снимка
  2026-07-04 (`apply_batch2.py` останавливается на первом блокере —
  объяснение расхождений в `reports/batch2_unblock/inventory.md`).
  **Применено: 95 из 97** (44 statement + 177 подпунктов; 2 исключены
  фильтром палочек — `excluded_pipes.md`). Бэкапы — `backups/
  before_batch2_unblock_*.sqlite3` + `reports/batch2_unblock/
  backup_apply_20260719.json`. Контроль: идемпотентность склейки 0,
  `solution` не тронут ни у одной задачи, рендер headless — 0 ошибок
  консоли, KaTeX-ошибок ПОСЛЕ не больше ДО. Эмбеддинги 95 задач пересчитаны.
  **26 «подозрительное сокращение» — в остатке**, ждут отдельного вердикта/
  этапа. Найдено попутно: #35031 — подозрение на утерянный при импорте
  рисунок (`\caption` без `\includegraphics`), #40131 — не задан страйк
  пута в условии (виден только из решения); оба — кандидаты в очередь
  пере-импорта, флаги не менялись (`incomplete_suspects.md`). Полный отчёт
  применения — `reports/batch2_unblock/apply_report.md`.
- **Ревизия по ревью соавтора (2026-07-20) — свип подмен содержания.**
  Ручное ревью нашло 3 реальных случая подмены сути механической чисткой
  на 95 применённых задачах: подменённый знак (#48915, «negative»→
  «positive»), подменённые числа (#6118, «40/40»→«80/3 и 0»), два разных
  подпункта с ОДНИМ И ТЕМ ЖЕ текстом вопроса вместо содержательного ответа
  (#27756). Принцип: механическая чистка не имеет права менять числа/
  знаки/суть — откатывается всегда, даже если правка выглядит «умной».
  `sweep_field()` (`problems/batch2_unblock.py`) сравнивает ДО/ПОСЛЕ по
  ПОСЛЕДОВАТЕЛЬНОСТИ числовых/знаковых токенов всего поля, не по чанкам
  посимвольного диффа — «negative»/«positive» делят общий хвост «itive», и
  посимвольная разбивка (`difflib`) режет слово мимо границ регулярки,
  подмена проходит незамеченной. LaTeX-скобка десятичной запятой
  («0{,}5» = «0,5») нормализуется до сравнения — иначе каждая такая
  запятая ловилась бы как пропавшее число. Нашёл ещё 68 подмен на тех же
  95 задачах (команда `batch2_unblock_sweep --confirm`, автооткат;
  `--exclude-part-pk` — вывести конкретный подпункт под отдельную ручную
  проверку). Восстановленные Sonnet предложения (числа не подменены, есть
  ТОЛЬКО в ПОСЛЕ) — не откатываются, идут в `reconstructed_review.html` на
  вердикт. Итог свипа: откачено 72 поля, 49 из 95 задач (51.6%) — подробности
  и оговорка про leaked-solution-false-positives в
  `reports/batch2_unblock/sweep_digit_sign_reverts.md`.
  ⚠️ **Тихая порча рендера KaTeX не ловится по `.katex-error`.**
  Несуществующая команда (`\myarray`, #27420) не даёт видимой ошибки — KaTeX
  с `throwOnError: false` молча выбрасывает команду и парсит остаток как
  обычную математику, соседние числа СЛИПАЮТСЯ без признака ошибки («31»+
  «8p»→«318p»). Если находите похожий `\custom{...}` макрос в LaTeX задач,
  проверяйте не только `.katex-error`, но и фактический отрендеренный ТЕКСТ
  на предмет слипшихся чисел. Очередь ручной починки (готовые Sonnet-фиксы
  на `\begin{cases}` уже лежат в бэкапе применения, не нужно сочинять
  заново) — `reports/batch2_unblock/manual_macro_queue.md` (4 случая).
- **Визуальная доводка по глазам Макара (2026-07-20) —
  CSS-коллизия `.text`/`mord text`.** Макар глазами нашёл 2 поломки,
  которых НЕ видит headless-проверка по ошибкам консоли (наложение глифов
  и развал вёрстки на боксы — не ошибка KaTeX, `.katex-error`=0).
  Playwright-скриншоты боевых страниц локализовали: **#7865 «Rich-to-Poor»
  была сломана ТОЛЬКО в превью** — корень в том, что KaTeX сам генерирует
  `<span class="mord text">` для `\text{...}`, а наш CSS-класс `.text`
  (контейнер колонки ДО/ПОСЛЕ в общем шаблоне превью, `preview_head`)
  матчится CSS по ТОКЕНУ класса и протаскивал рамку/паддинг/
  `white-space: pre-line` ПРЯМО В ФОРМУЛУ — инлайн-элемент с рамкой при
  переносе строки рисует отдельный бокс на каждый фрагмент.
  ⚠️ **Правило для новых HTML-генераторов превью с KaTeX: никогда не
  называть свой CSS-класс так, чтобы он совпал с классом внутри
  `.katex-html` (`text`, `mord`, `base`, `strut`, `mfrac`, `sqrt` и
  т.п.) — CSS матчит по токену, не по полной строке.** Класс переименован
  `.text` → `.field-text` в ЕДИНОЙ точке (`preview_head`/`diff_block`/
  `_preview_card`, `glue_pdf_lines.py`+`preview_batch2_unblock.py`) — чинит
  разом все страницы линии (`preview.html`/`post_apply_sample.html` обеих
  сессий, `reconstructed_review.html`). 2 regression-теста, гоняющих через
  `_preview_card`/`diff_block` и проверяющих отсутствие `class="text"` в
  выводе. #27420 [в]/[г] (pk 34817/34818) — оверлей `\begin{cases}`
  воспроизвести не удалось нигде (ни боевая страница, ни исправленное
  превью, ни программная проверка перекрытия рамок), но по прямому
  указанию откачены к ДО (`\myarray`) как к честному известному состоянию.
  **Вывод: для формульных правок скриншот-проверка ОБЯЗАТЕЛЬНА — ошибки
  консоли такой класс дефектов не ловят.** Тесты —
  `problems/tests/test_batch2_unblock.py` (57) +
  `problems/tests/test_glue_pdf_lines.py` (49). Полный прогон 435/435. Тот
  же риск (числа, знаки, тихая порча рендера) не проверен на остальных
  4 930 задачах Батча 2 (применены 2026-07-04) — карточка в Notion «Идеи».
- **Свип Батча 2 целиком — все 4 930 задачи (2026-07-21, ветка
  `feat/batch2-sweep`, в `main` не мержилось).** Карточка из «Идей»
  закрыта: тот же `sweep_field`, но ДО не JSON-снимок (такого не делали
  для основного батча), а полный бэкап sqlite перед применением
  (`backups/db_backup_before_batch2_apply.sqlite3`) — команда
  `batch2_full_sweep` (без `--confirm` — только замер; `--confirm` —
  откат) реклассифицирует записи `batch2_parsed.jsonl` против ЭТОГО
  бэкапа 1:1 повторяя `apply_batch2._classify_record` (0 аномалий,
  100% покрытие, 6 411 полей). digit_sign_change — **1 476 задач из
  4 254 покрытых (34.7%)**, ниже стоп-порога 40% (зашит в команде —
  если бы превысил, откат не выполнился бы даже с `--confirm`,
  насторожило бы на ошибку детектора, а не масштаб). Откачено 1 762
  поля (1 130 statement + 632 подпункта).
  ⚠️ **`fix_latex_junk.clean_text` никогда не тестировался на сыром
  до-Sonnet тексте (только на уже прошедшем июньскую чистку) — на сыром
  тексте он БЬЁТ.** Откат обязан переприменить `clean_text` + `glue_field`
  поверх ДО (иначе откат стирает и июльскую чистку) — но на 47 полях
  из 1 762 сам `clean_text` внёс НОВУЮ подмену числа: паттерн
  `junk_comment` режет от `%` до первого `\n`, а в сыром, не прошедшем
  склейку тексте (абзацы одной строкой без переносов) этот `\n` может
  найтись на сотни символов дальше настоящей границы комментария и
  утащить реальный кусок условия (#4357 — потеряло целый абзац с «в 2
  раза»). Предохранитель shrink-ratio (>50%) это НЕ поймал (ушло 46%).
  **Правило: если переприменяешь `fix_latex_junk`/любую другую механику
  к тексту, для которого она не создавалась и не тестировалась — обязателен
  пересвип КАЖДОЙ попытки против ДО, а не разовая проверка на выборке.**
  `batch2_full_sweep` держит трёхуровневый fallback (полный конвейер →
  только `glue_field` → чистый ДО без переприменения), принимая первую
  попытку, чей пересвип не находит digit_sign_change — итог 0 подмен
  после отката на всех 1 762 полях (проверено повторным прогоном).
  Превью 30 случайных new_sentence (361 всего, не откатываются) —
  `batch2_sweep_new_sentence_preview` → `reports/batch2_sweep/
  reconstructed_preview.html`. Эмбеддинги 1 476 задач пересчитаны.
  Отчёты — `reports/batch2_sweep/` (coverage_report.md, sweep_stats.md,
  recleaning_report.md). Полный прогон 435/435.
  ⚠️ **Параллельные сессии Claude Code в ОДНОЙ папке ломают друг друга
  через git.** На этой сессии другая параллельная сессия (ревью-
  инфраструктура, `feat/review-snapshots`) работала в том же working
  tree (не `git worktree`) — её `git checkout` дважды утащил мои
  незакоммиченные файлы в свои коммиты. `db.sqlite3` не под git —
  не задет, но код пришлось вручную вычленять (`git checkout <их-коммит>
  -- <path>`) обратно на свою ветку. Параллельные сессии — только через
  `git worktree` (`EnterWorktree`/изолированные копии), не общий каталог.
- **`problem_to_text` обновлён** (+ ai_blurb + канонические теги) и **полный
  пересчёт BGE-M3 выполнен** (31 472 вектора + `cache_similar`, 2026-07-04).
- Хвосты пайплайна: очередь пере-импорта 1 772 + КСИГМА (системная склейка);
  ревью тем-подсказок `batch1_topic_review.csv`; фильтр по тегам в каталоге
  (интерфейс); заголовки — `apply_batch1_titles` (~7 098 кандидатов, не применялась).

---

## Чистка текстов задач — состояние и очередь

**Диагностика отображения (полный скан, 2026-07)** — команды `diagnostic_sample`
и `full_defect_scan`, отчёт `reports/diagnostic_sample/full_scan_summary.md`:
- **73% каталога (13 603 задачи, 16 источников) — жёсткая построчная PDF-нарезка.**
  Глобальный `white-space: pre-line` включать НЕЛЬЗЯ до склейки строк (сейчас
  браузер прячет нарезку; pre-line разорвёт предложения посередине). ILE (2 401,
  структурные переносы) можно на pre-line уже сейчас.
- «Обрывы» — в основном не обрывы, а **повисшие служебные подпункты** («Ответ:»,
  «Вопрос с открытым ответом:» без содержания) — баг импорта, ~150 задач.
- Флаг «длина >3000» для поиска склеек не годится — ловим `multiple_problems`.

**`fix_latex_junk` — ПРИМЕНЁН (ночь 2026-07-11): 834 задачи** (statement 677,
parts 320; паттерны pseudo_quotes 713 / junk_comment 179 / inline_bullet 149).
Идемпотентность подтверждена, контрольные #4853/#29073 проверены, эмбеддинги всех
834 пересчитаны (`night_embeddings`). Кэш похожих НЕ перезаливался — **освежить
кэш похожих (`cache_similar`) — на потом**. Детали — в архиве.

**Отложено из fix_latex_junk:** footnote-очередь **50 задач**
(`reports/fix_latex_junk/footnote_deferred_ids.txt` — перенос сносок в
скобки/Hint, НЕ удаление); junk_comment manual **60 задач**
(`junk_comment_manual_ids.txt` — граница `%`-комментария без `\n`); чистка
`title` (обрубки по 300 симв. + мусорные заголовки).

**Склейка жёсткой нарезки строк — v1 ПРИМЕНЕНА (2026-07-19, ветка
`feat/glue-lines`, в `main` НЕ мержилось).** Команда `glue_pdf_lines`
(dry-run по умолчанию; `--source-id`, `--limit`, `--examples N`, `--preview`,
`--include-flagged`, `--confirm` — только после «да» Макара). Устройство:
«тень» текста (математика $…$/$$…$$/\(..\)/\[..\]/\begin…\end заменена
U+E000, переносы на местах) → классификатор границ (правила в докстринге) →
склейка до неподвижной точки (глейка может воссоединить разорванную
inline-формулу, маска меняется — поэтому один проход не идемпотентен).
Ловушки/решения: подпункты при записи адресуются по pk (метки НЕ уникальны —
в выборке 34 пары-дубля «а»); дефисные разрывы автоматом НЕ клеятся
(`AUTO_HYPHEN_JOIN=False`, «денежно-\nкредитную» склеилось бы в порчу) — все
в `reports/glue_lines/hyphen_doubtful.txt` с пометкой «[склеил бы]»; строка-
элемент перечня величин («мс (6) =?», «VC(21)=40» без обрамления $...$) не
клеится ни как следующая, ни как предыдущая строка границы (находка ревью
Макара, Акимова #49998) — иначе соседние пункты перечня сливаются в фразу;
solution в v1 не обрабатывается (обоснование в докстринге). Тесты —
`problems/tests/test_glue_pdf_lines.py` (48).

**Применение (2026-07-19):** 17 источников — 16 «жёсткая нарезка» + МатЭк
целиком по варианту Б (см. `matek_options.md`). Изменено **4 159 задач**
(statement 3 680 + подпунктов 4 027), склеек **15 172**, предохранитель 0.
Бэкап старых значений — `reports/glue_lines/backup_apply_20260719.json`
(JSON id → старый текст) + автобэкап sqlite в `backups/`. Контроль: повторный
dry-run по тем же источникам → 0 (идемпотентность подтверждена); ILE не
тронут (тот же единственный кандидат #1531, что и до применения); пост-
контроль 20 случайных задач ДО/ПОСЛЕ — `reports/glue_lines/
post_apply_sample.html` (генератор `glue_post_apply_sample`, сверяет
glue_field(ДО) == ПОСЛЕ(база) автоматически). Свежий замер нарезки
(`full_defect_scan`): источников «жёсткая нарезка» 16 → 11, задач в них
13 603 → 11 335. Список изменённых id для пересчёта эмбеддингов —
`reports/glue_lines/affected_problem_ids.txt` (= `changed_ids.txt`).
Отчёты: `reports/glue_lines/` (dry_run_stats.md, matek_options.md,
narezka_remeasure.md, preview.html, post_apply_sample.html). Ссылка «открыть
в каталоге» в обоих генераторах превью — `/catalog/problem/<id>/` (реальный
путь `problem_detail`, раньше была битая `/catalog/<id>/`).

**Очередь дальнейшей чистки (по убыванию эффекта):**
1. Пересчёт эмбеддингов затронутых 4 159 задач (см. «Ловушка done-файла» —
   уже выполнен процедурой при применении, см. `embeddings_recompute.log`).
2. Склейка v2: пустые строки посреди предложения (карточка в Notion,
   найдено на ревью 2026-07-19: КСИГМА #49691/#49737, ОЭШ #47649) — свой
   dry-run + превью + стоп-гейт Макара, идёт поверх v1.
3. Глобальный `white-space: pre-line` — ТОЛЬКО после v2.
4. Повисшие служебные подпункты (~150) — отдельный скрипт.
5. Заголовки: обрубки + `apply_batch1_titles` (~7 098 кандидатов).

---

## Ручное ревью внешнего вида — офлайн-пакеты (ветка `feat/batch2-sweep`)

Единственный достоверный измеритель качества — человеческое ревью (решение
Notion 3abb11c92bc18133aea8c3ee9d8716bd: детекторы ловят 80% дефектов, но
зря флагают 60% идеальных задач). Инфраструктура приехала с сиблинг-ветки
`feat/review-snapshots` (общий предок 196bdaf) — брались ТОЛЬКО добавленные
там файлы; их копии `batch2_unblock.py`/`batch2_full_sweep.py` — устаревшие
снимки наших же файлов, утащенные чужим `checkout`.

- **Категории** — `problems/review_categories.py`, ЕДИНСТВЕННАЯ точка правды.
  10 штук: `perfect` (kind `ok`, клавиша 1), семь дефектных (2–8), `disputed`
  (9), `trash` «Гагно» (kind `trash`, клавиша **0**). Оболочка строит кнопки и
  горячие клавиши ИЗ манифеста и не хардкодит ничего — держится тестом.
  ⚠️ Клавишу `perfect` двигать нельзя: на ней моторика 2 401 задачи. `trash`
  намеренно на дальней «0», чтобы промах не превращал идеальную в выброшенную.
- **Правила выбора идут по `kind`, а не по списку ключей.** `EXCLUSIVE_KINDS =
  ('ok', 'trash')` — вердикт из одного нажатия (сбрасывает отмеченное и
  листает дальше); остальные переключаются, Enter подтверждает; пустой Enter
  не делает ничего. Комментарий — ко всей задаче, не к категории.
- **Модель `ReviewVerdict`** — строка на пару «задача × категория», ключ
  идемпотентности `(bundle, problem, category)` (миграция **0022**).
  ⚠️ Ревьюера в ключе НЕТ, поэтому импорт отдельно отказывается затирать
  вердикт другого ревьюера (иначе 2 401 ручная метка ушла бы молча).
  Миграция проставила существующим вердиктам `bundle='ile_20260721'` — без
  этого повторный импорт того же файла создал бы 2 401 дубль.
- **Форматы:** пакет `qls-review-bundle-v1`; вердикты — `qls-review-verdicts-v2`
  (список `categories`). Импорт понимает и **v1** (одна `category`) — 2 401
  вердикт по ILE лежат в v1 и переводу не подлежат. Импорт снимает категории,
  от которых ревьюер отказался.
- **Команды:** `export_review_bundle --source-id N --out <dir> --bundle-id <id>`
  (рендерит боевые страницы через Django test client, вендорит KaTeX, переписывает
  `/static/`-`/media/` на относительные — пакет открывается с `file://` без сети);
  `import_review_verdicts <файл.json> [--reviewer X] [--bundle Y]`.
- **Пакеты:** `reports/review_bundles/ile_20260721` (2 401, пройден Аничем),
  `ap_homeworks_20260728` (936, собран), **`aa_20260728`** (1 593 — Сборник
  тестов АА, 39 МБ / zip 11 МБ, текущая очередь).
- **Проверка оболочки:** `node scripts/review_shell_e2e.js <пакет>` — Playwright
  жмёт клавиши как ревьюер (одиночный/множественный выбор, Enter, «Гагно»,
  перезагрузка, скачивание JSON). 26 проверок. Питон-тесты этого класса не видят.
- Тесты — `problems/tests/test_review_bundle.py` (25).

## Шлюз «не навреди» по правкам текста

⚠️ **Текстовый предфильтр к ошибкам KaTeX СЛЕП — экономить им рендер нельзя.**
Замерено 2026-07-28 на 1 762 откаченных полях: контрольная выборка 100 полей,
не отмеченных шестью текстовыми признаками, дала 3 поля с выросшим числом
ошибок KaTeX. «Сломанная формула» — это то, что решил KaTeX, а не то, что мы
про него думаем. Рендерить надо ВСЁ.

- `problems/revert_damage.py` — шесть текстовых признаков ухудшения (голая
  табличная разметка вне математики, сырая LaTeX-команда без слеша, усыхание
  видимого текста >15%, обрубленное начало, схлопывание абзаца, потеря
  строки-заголовка перечня) + седьмой (KaTeX) считается рендером.
- `manage.py batch2_revert_damage_audit --render-all --with-render` — замер по
  бэкапу отката; `node scripts/katex_render_check.js` — счёт ошибок KaTeX
  парами ДО/ПОСЛЕ, конвейер дословно повторяет боевой из `catalog/base.html`
  (маскировка `\$`, те же разделители, `$$` раньше `$`, `throwOnError: false`).
- **Результат замера:** откат 21 июля ухудшил **233 поля в 195 задачах**
  (13,2%). Отчёт `reports/batch2_sweep/applied_revert_audit.md`, список id
  `applied_revert_damage.txt`. Починка — карточка в Notion, НЕ сделана.

## Платформа для репетиторов (ветка `feat/platform-foundation`, в `main` не влито)

Каркас продукта «репетитор ↔ ученик ↔ родитель». Ветка создана от вершины
`feat/batch2-sweep` (не от `main`: `main` не содержит ни одного коммита,
которого нет в линии, а в базе применены `problems` 0018–0022, файлов которых
на `main` нет — новая миграция от `main` столкнулась бы номером).

**Новые модели — все в `problems/models_platform.py`** (отдельный модуль внутри
приложения `problems`, импортируется одной строкой в конце `models.py`; так
Django видит их с `app_label='problems'`, а гигантский `models.py` не растёт):
`UserProfile` (роли tutor/student/parent + синхронизация со старым
`User.role`), `ProblemComment` (+ менеджер `visible_for`), `CustomProblem` /
`CustomProblemOption`, **`AssignmentItem`** (позиция задачи в домашке),
`SavedFolder` / `SavedProblem` / `SavedGraph`, `ExamAttempt`, `AnswerDraft`,
`LearningEvent`. Админка — `problems/admin_platform.py`.

⚠️ **`AssignmentItem` — ключевая модель.** У `Assignment` остался старый
простой M2M `problems` (обратная совместимость), но вся обвязка — порядок,
балл, комментарии, решалка, график — висит на позиции. Новые экраны ходят
через `assignment.items`.

**Изменения существующих моделей (только добавления):** `Assignment` —
`group`, `kind`, `exam_mode`, `starts_at`, `ends_at`, `duration_minutes`,
`due_at`, `show_results_immediately` + методы `is_open_for`/`open_state_for`,
`deadline_at`; `Submission` — `problem_item` (и `problem` стал nullable ради
своих задач репетитора); `StudentGroup` — `description`.
Миграции: **0023** (основная), **0024** (описание группы), **0025** (график
у позиции). ⚠️ В 0023 пришлось вручную переставить операции — автодетектор
поставил `AddConstraint uniq_submission_per_item` раньше `AddField
submission.problem_item`.

**Модули:** `problems/event_log.py` — единственная точка записи учебных
событий, ⚠️ НЕБЛОКИРУЮЩАЯ (try/except: падение лога не имеет права уронить
сдачу домашки); `problems/answer_check.py` — автопроверка (числа через
`fractions.Fraction`, как в «Классике»; множественный выбор — «всё или
ничего»); `teacher/access.py` — переиспользуемый `tutor_required`.

**Страницы:** `/teacher/` — дашборд входящих (только навигация, проверять
оттуда нельзя намеренно); `/teacher/groups/…` — вся работа с группой;
`/teacher/groups/<g>/assignments/<a>/` — задание целиком ДО решений;
`/teacher/problems/…` — редактор своих задач; `/profile/` — данные,
сохранённое, статистика-заглушка.
⚠️ Проверка решений ПЕРЕЕХАЛА под групповые URL; старые
`/teacher/assignment/<pk>/` и `/teacher/submission/<pk>/review/` оставлены
редиректом (`legacy_*`, помечены «устарело, удалить после сессии 5»).

**MathLive 0.110.0** с CDN (UMD `mathlive.min.js`), партиал
`problems/templates/platform/_mathfield.html` + `problems/static/platform/
mathfield.js`. Компонент — «вставщик формул»: основной ввод остаётся
textarea, формула ложится в текст как `$…$` — формат хранения тот же, что у
всего банка. ⚠️ **calc2 не тронут ни одной строкой** (параллельная ветка
`feat/calc2-shipu`); от графиков сделана только серверная половина —
`SavedGraph`, `AssignmentItem.graph`, `POST /api/graphs/save/`, вставка
плашки `[[График: имя]]`.

**Команды:** `backfill_profiles`, `link_anonymous_events`,
`seed_platform_demo` (идемпотентна; `tutor@test.local` и др., пароль
`demo12345`).

**Тесты:** `problems/tests/test_platform_models.py` (41) +
`problems/tests/test_answer_check.py` (16). Полный прогон — **546/546**.

## Статистика, геймификация и контрольные (та же ветка `feat/platform-foundation`)

**Единый список задач в работе** — `problems/assignment_rows.py`, ОДНА сборка
на страницу ученика и на страницу репетитора. Источник правды —
`AssignmentItem`; старый M2M `Assignment.problems` оставлен для совместимости,
списки по нему больше не строятся (бэкфилл — `backfill_assignment_items`,
идемпотентна). Набор полей ответа один на любую задачу: «Ответ» (краткий,
автопроверка) → «Моё решение» (textarea + MathLive) → «Прикрепить файл»;
меняется только элемент ввода первого поля (переключатели у теста).
Карточка — `student/templates/student/_problem_card.html`, стили и скрипты —
`_work_style.html` / `_work_js.html`.
⚠️ **Вложенные `<form>` браузер выбрасывает.** Блок вопроса к задаче стоит
внутри формы отправки работы, поэтому он НЕ форма, а `<div>` с кнопкой
`type="button"`. Раньше был `<form>` — из-за этого не навешивался обработчик
(кнопка отправляла всю домашку) и не применялись стили (крошечное поле).

⚠️ **Срок работы — ОДНО поле `Assignment.deadline`.** `due_at` УСТАРЕЛ, не
читается и не пишется нигде (данные перенесены миграцией 0026). Спрашивать
срок только через `deadline_at` / `models_platform.assignment_deadline()`.
Два поля уже давали видимый баг: список ученика писал «без срока» у работы
со сроком.

**Геймификация — `problems/models_gamification.py` + `problems/gamification.py`.**
Модели: `ParentLink`, `StudentProgressProfile`, `Achievement`/
`EarnedAchievement`, `PersonalRecord`, `DailySummary`. Владение темой —
ДОПОЛНЕНА существующая `StudentTopicProgress` (attempted / solved /
solved_hard / mastery_level / last_activity_at), второй модели нет.
- Все числа правил — константами в начале `gamification.py`. Опыт:
  **2/5/15/45/90** за сложности 1–5 (не 5/5/15/40/40 из задания — при них
  фарм лёгкого давал 200 опыта в час против 120 у сложности 4). Затухание
  за лёгкие (≤2): первые 5 в день полные, 6–15-я половина, с 16-й пятая
  часть; сложности 3+ затухание не грозит никогда. Порог уровня N =
  100 × (N−1)^1.5. Серия: день зачтён при сдаче работы ИЛИ ≥3 попытках,
  2 заморозки в месяц.
- Точка входа — `process_learning_event`, подключена к `event_log.log_event`,
  **НЕБЛОКИРУЮЩАЯ** (падение начисления не роняет сдачу).
- `recalculate_gamification` — полный пересчёт из журнала, идемпотентна,
  использует ТЕ ЖЕ чистые функции правил. `seed_achievements` — 29 штук.

**Слой агрегации — `problems/stats.py`.** (пользователь, период) → готовые
словари. Считаем В БАЗЕ (`values().annotate()`), не циклами по событиям;
кэш 5 минут по ключу (user, period). Игра считается ОТДЕЛЬНОЙ функцией
`game_stats()` и в учебную шкалу не входит. Координаты клеток теплокарты
считает питон, шаблон только рисует.
⚠️ Редкость достижений — `models_gamification.rarity_map()`, ОДИН запрос на
всю страницу: метод на каждой плитке давал под шестьдесят запросов.

**Экраны.** `/profile/stats/` — ученик, С геймификацией (Chart.js с CDN,
теплокарта SVG руками, переключатель периода через JSON без перезагрузки).
`/teacher/groups/<id>/stats/` и `/teacher/students/<id>/stats/` — репетитор,
БЕЗ геймификации, тепловая матрица «ученики × темы». `/parent/`,
`/parent/<id>/` — родитель, БЕЗ геймификации и **без единого показателя
работы репетитора** (обоснование в докстринге `problems/views_parent.py`).
⚠️ **Страница «Прогресс» ПОГЛОЩЕНА:** `/student/progress/` редиректит на
`/profile/stats/`, в навигации пункт называется «Статистика».

**Контрольные — `problems/exam_engine.py`.** ВРЕМЯ СЧИТАЕТ СЕРВЕР.
`expires_at` вычисляется в момент НАЖАТИЯ «Начать» (не при загрузке
страницы: бесплатный Render просыпается ~30 с). Клиент только рисует отсчёт
и пересинхронизируется по `seconds_remaining` из каждого ответа. Завершение
ЛЕНИВОЕ — истёкшая попытка закрывается при любом обращении, временем сдачи
ставится `expires_at`; команда `finalize_expired_attempts` — страховка.
Запас `GRACE_SECONDS = 10`.
- Прохождение: `/student/exam/<id>/` → `start` → `take` → `autosave` →
  `finish` → `result`. Автосохранение — `problems/static/platform/exam.js`.
- ⚠️ **Автосохранение НИКОГДА не отвечает 500** (503 + `retry`): пятисотка
  для клиента неотличима от «сохранилось», и он выбросил бы значение из
  очереди. Гонка двух автосейвов одной задачи (остановка ввода + потеря
  фокуса) ломала `update_or_create` об уникальность — **нашёл браузер,
  питон-тесты не видели**. Починено на трёх уровнях: очередь на клиенте,
  повтор-как-обновление на сервере, контракт 503. Плюс SQLite получил
  `OPTIONS: {'timeout': 20}`.
- Конструктор — `/teacher/groups/<id>/exams/new/`, валидация на форме.
  Результаты репетитору — `/teacher/groups/<g>/exams/<e>/results/`
  (ученик × задача + строка «верных по задаче»).
- ⚠️ **В прод контрольные нельзя до переезда с бесплатного Render** —
  пробуждение посреди работы съедает полминуты на каждое сохранение.

**Проверка браузером:** `node scripts/exam_manual_check.js` — 9 сценариев
(закрытие вкладки, обрыв сети, подмена часов устройства);
`node scripts/session_shots.js` — скриншоты ролей на 1280 и 380px.

**Тесты:** `test_assignment_rows.py` 20, `test_gamification.py` 36,
`test_stats.py` 24, `test_exams.py` 42. Полный прогон на тот момент — 668/668
(сейчас по проекту **973**, см. «Сессия 5»).

⚠️ **Вылет навигации на 380px не чинили.** `templates/_nav.html` тянет
страницу вбок (scrollWidth 936 при 380) на КАЖДОЙ странице сайта, включая
нетронутые — карточка в Notion. Содержимое новых страниц в 380px
укладывается, таблицы прокручиваются внутри `.table-wrap`/`.sub-table-wrap`.

## Сессия 3: подпункты, разбор работы, подбор домашки, экспорт (та же ветка)

**Ответы по пунктам — `problems/part_grading.py` + `PartAnswer` (миграция
0027).** У задачи с «а)» и «б)» СВОЁ поле ответа на каждый пункт и своя
автопроверка. `part = NULL` означает «вся задача» — задача БЕЗ пунктов это
частный случай «один пункт», второй ветки логики нет.
⚠️ **Автопроверка каталожной задачи — только по ЧИСЛОВОМУ эталону.** В банке
поле «ответ» сплошь и рядом фраза целиком («E ≈ −1,22 (по модулю больше 1) —
спрос эластичен»); сравнивать её со строкой ученика значит ставить ноль за
верный ответ. Словесный эталон разрешён только у СВОЕЙ задачи репетитора —
его писал человек именно как эталон.
⚠️ **Тесты идут своим путём** (`part_grading.applies(item) = not item.is_test`),
признак — ТИП задачи, а не «нарисованы ли переключатели»: тест без
размеченных вариантов рисуется строкой ввода, но сверять его надо по метке
(«б» против «Б»), а не как число.
Черновики контрольной тоже по пунктам (`AnswerDraft.part`), сохраняются
раздельными запросами. ⚠️ В `exam_engine.save_draft` `None` = «не трогать
поле»: пустая строка означала бы «очистить», и решение стиралось бы вместе
с сохранением ответа.

**Единый экран разбора работы — `/student/work/<pk>/`** (`problems/
work_review.py` + `student/work_review.html`). ОДИН на домашку и контрольную;
`student:exam_result` и `student:submission_detail` — редиректы сюда.
Репетитор открывает ТОТ ЖЕ шаблон по адресу `/teacher/groups/<g>/
assignments/<a>/students/<s>/`. Главное на экране — балл. Комментарий ко
всей работе — модель `WorkFeedback` (миграция 0028), пишется на экране
проверки. Запросов 13 при восьми задачах (было 42 — лечится предзагрузкой
решений в `build_rows`).

**Подбор домашки по описанию — `problems/hw_generator.py`,
`/teacher/assignment/generate/`.** ЗАДАЧИ НЕ ПРИДУМЫВАЮТСЯ: модель разбирает
ФРАЗУ в структуру «тема · сложность · сколько», каждая строка уходит в свой
поиск по банку. Обращение к модели РОВНО ОДНО на домашку; темы выбираются из
готового меню 21 канонической темы. Модель `claude-haiku-4-5` ($1/$5 за млн),
**≈ $0,002 за домашку**. Ключ ТОЛЬКО из `ANTHROPIC_API_KEY`; нет ключа или
библиотеки — функция сама выключается с понятным текстом. Лимит 30 обращений
в сутки, кэш 15 минут, расход построчно в `AiUsageLog` (миграция 0029).
Дедупликация и потолок «не больше трёх задач из одного источника» — в
`find_problems`.

**Экспорт задания — `problems/assignment_export.py`**, кнопки на странице
задания, адрес `/teacher/groups/<g>/assignments/<a>/export/?fmt=pdf&for=teacher`.
⚠️ **pdflatex, а НЕ xelatex** — отсюда преамбула `inputenc/fontenc T2A`, а не
`fontspec`. Старый экспорт подборок `catalog/latex_export.py` (xelatex) НЕ
ТРОНУТ: `fontspec` в pdflatex не работает, переписывать работающую функцию
ради соседней нельзя. Битая разметка условия не роняет листок — задача
пропускается с пометкой. Нет TeX Live → отдаётся `.tex` с объяснением.

**Время на экране — `problems/timefmt.py`, ЕДИНСТВЕННАЯ точка форматирования
в питоне.** ⚠️ Шаблонный фильтр `{{ value|date:"…" }}` переводит момент в пояс
проекта САМ (`expects_localtime=True`), а питоновская f-строка `f'{dt:%H:%M}'`
печатает UTC. Отсюда брались две строки на одной странице с разницей в три
часа.

**Форма работы — `problems/static/platform/work_form.js`.** ⚠️ Enter в
текстовом поле ОТПРАВЛЯЕТ ФОРМУ — это встроенная «неявная отправка» HTML, а
не наш баг; однажды Shift+Return сдал контрольную посреди работы. Запрет
неявной отправки + подтверждение с фактами подключены к домашке и к
контрольной.

**Часы контрольной — `problems/static/platform/exam_timer.js`.** ⚠️ Только
МОНОТОННЫЕ часы (`performance.now()`), `Date.now` запрещён тестом по тексту
файла; серверная сверка умеет только УМЕНЬШИТЬ остаток. Отдельный
`GET /student/exam/<pk>/time/` раз в 15 секунд — независимо от ввода.
Серверный потолок «остаток не больше выданного при старте».

⚠️ **Многострочный `{# … #}` печатается на страницу как текст** — лексер
Django ищет комментарий без флага DOTALL. Проверка на весь проект —
`problems/tests/test_template_hygiene.py`.

**Тесты сессии 3:** `test_exam_timer_js.py` 5, `test_work_form_js.py` 6,
`test_template_hygiene.py` 2, `test_part_answers.py` 14,
`test_work_review.py` 15, `test_hw_generator.py` 17,
`test_assignment_export.py` 13.

**Тесты сессии 4:** `test_option_grading.py` 27, `test_answer_approval.py` 17,
`test_exam_autosave_js.py` 4, `test_text_clean.py` 24, `test_search_first.py` 15,
`test_print_sheet.py` 14. Полный прогон по проекту — **864/864**.
Браузерные проверки — `node scripts/session3_check.js` (10) и
`node scripts/session3_cycle.js` (6).

## Сессия 4: дефекты приёмки, слой ИИ, подбор поиском, печать (та же ветка)

**Балл за задачу — `assignment_rows.item_max_score(item)`, ЕДИНСТВЕННАЯ точка.**
⚠️ Из-за её отсутствия жила самая дорогая ошибка проекта: `student/views.py::
auto_check_submission` (автопроверка КАТАЛОЖНОГО теста, эпоха «В2») ставила
жёстко зашитую единицу, а максимум читался из `AssignmentItem.points` —
полностью верный ответ на тест ценой 3 балла давал «1 / 3 б.» и вердикт
«ЧАСТИЧНО» рядом с комментарием «Верно ✓». Соседняя `auto_check_custom` брала
балл правильно: один вопрос был решён в двух местах, и устарело то, которое
не трогали. Обе теперь пишут балл через `_write_auto_feedback`.

**Правило множественного выбора — «всё или ничего»**, объяснено в докстринге
`problems/answer_check.py`. Сравнение — КАК МНОЖЕСТВО меток
(`check_catalog_test`), а не строкой: порядок отметок значения не имеет.
⚠️ **Галочки рисует ТОЛЬКО «тест: все верные».** Тип «тест: верно/неверно»
раньше тоже был галочками, и ученик мог отметить «Верно» И «Неверно»
одновременно. Замер: 454 из 455 таких задач банка — одно утверждение с двумя
вариантами, то есть одиночный выбор.

**Разбор вариантов — `assignment_rows.option_review(item, submission)`:** четыре
исхода (`hit` / `wrong` / `missed` / `skip`), партиал
`student/_option_review.html`. Собирается ТОЛЬКО после сдачи — до неё это
выдача ответов браузеру.

**Утверждение ответов — `AssignmentItem.answer_override`** (JSON
`{"": "…", "<pk пункта>": "…"}`, миграция **0030**). Машина проверяет
каталожную задачу ТОЛЬКО после утверждения человеком; каталог не трогается.
Утверждение идёт ПО ПУНКТАМ и отката к каталожному ответу НЕ имеет.
⚠️ Тесту и своей задаче репетитора утверждение не требуется
(`AssignmentItem.answers_approved`) — иначе шлюз отменил бы автопроверку
тестов. Эндпоинт `teacher:api_item_answers`, блок на странице задания.
⚠️ Бэкфилла нет намеренно: у собранных ранее работ каталожные задачи уйдут
на ручную проверку, пока их не утвердят.

**Автосохранение контрольной останавливается после сдачи** — флаг `stopped`
в САМОЙ очереди `exam.js` (источников сохранения пять, плюс интервалы
гасятся). Проверка ИСПОЛНЕНИЕМ в node: `problems/tests/test_exam_autosave_js.py`.

## Слой работы с моделью — `problems/ai/` (Сессия 4)

Единственная дверь наружу: `ai.run(profile, user_text, schema, user)`.
Причина не в экономии, а в 152-ФЗ: тексты несовершеннолетних нельзя вывозить,
переезд на российскую модель должен стоить настройку.
- Поставщик и модель — настройками `AI_PROVIDER` / `AI_MODEL`;
  `providers.py` знает `anthropic` и `fake`. Подставной поставщик делает
  сменяемость ПРОВЕРЯЕМОЙ, а не обещанной.
- Ключ ТОЛЬКО из окружения (`ANTHROPIC_API_KEY`); нет ключа — функция сама
  выключается человеческим текстом.
- ⚠️ **Системный промпт = НЕИЗМЕННОЕ ЯДРО + профиль, ядро первым и только
  на нём `cache_control`.** Скидка 90% даётся на ПРЕФИКС и проверяется
  ПОБАЙТНО: одна дата, одно имя, один счётчик в ядре — и попаданий нет
  вообще, а первая отправка ещё и на четверть дороже. В `CORE` не должно
  быть ничего изменяемого; держится тестом.
- ⚠️ Токены кэша — ОТДЕЛЬНЫЕ поля `AiUsageLog` (`cache_write_tokens`,
  `cache_read_tokens`, `provider`, `seconds`, миграция **0031**): по общей
  сумме входных токенов «работает ли кэш» не отличить от нуля попаданий.
- Правила заземления — в ядре (`prompts.CORE`), не в каждом профиле.

## Подбор домашки: поиск первый, модель вторая (Сессия 4)

⚠️ **Модель НЕ должна знать экономику.** На живом запросе она написала
«КТВ — альтернативное название КПВ» (неверно: КТВ это кривая ТОРГОВЫХ
возможностей), выдала всем строкам одну тему, тема работала жёстким фильтром
— вместо пяти задач нашлось три. Подтем сотни, перечислить их в промпте
нельзя; словарь предметной области — сам каталог.

- **`catalog/hybrid.py`** — два поиска: `dense_search` (эмбеддинги) +
  `lexical_search` (слова, ищет и в РЕШЕНИИ), слияние по рангам **RRF, k=60**.
  Косинус и «сколько слов совпало» несопоставимы — общая шкала была бы
  подгонкой на глаз. `confidence(hit)` → `exact` / `close` / `far`.
  ⚠️ Разреженный режим BGE-M3 недоступен: `sentence-transformers` отдаёт
  только плотный, остальные требуют `FlagEmbedding` и нового индекса.
- **`hw_generator.search_hints(text)`** — 12 реальных задач банка уходят
  модели СЛОВАРЁМ; строится сам, обновляется вместе с каталогом.
- **Тема — бонус к рангу, не фильтр.** Квота заполняется ВСЕГДА: потолок по
  источнику отступает, недобор берётся из общего поиска, порога-отсечки нет.
  Но каждая задача честно помечена уверенностью.
- **Стоп-гейт показывает ЗАДАЧИ, а не темы** (`preview_rows`), плюс
  «Переискать по моим формулировкам» — БЕЗ обращения к модели.
- Названия в списках — `text_clean.preview_title` (обрезка по границе слова).

## Санитайзер текстов и печать (Сессия 4)

**`problems/text_clean.py`** — чистка ПРИ ПОКАЗЕ И ЭКСПОРТЕ, в базу не пишем.
Чинит пробелы вокруг формул и операторы (`min` → `\min`); дефисный перенос,
сырой корень, непарные доллары и «($L)» только СЧИТАЕТ.
⚠️ Последний рубеж — `same_numbers_and_signs(до, после)`: правка, изменившая
хоть одно число, отменяется целиком. Текст с НЕЧЁТНЫМ числом долларов не
трогается совсем — разметке верить нельзя.
Команда **`text_defect_census`** (только чтение): 903 задачи из 18 865
видимых (4,8%) + 1 177 подпунктов.
⚠️ Прикидка регуляркой «`$…$` перед буквой» даёт 4 924 и ВРЁТ вдесятеро:
она спаривает закрывающий доллар одной формулы с открывающим следующей.
Доллары надо спаривать ПОСЛЕДОВАТЕЛЬНО, как это делает KaTeX.

**Версия для печати** — `/teacher/groups/<g>/assignments/<a>/print/?for=teacher`,
шаблон `teacher/assignment_print.html`, строки — `assignment_export.print_rows`
(ОДНА сборка с `.tex`). Отдельная страница без базового шаблона; KaTeX тем же
конвейером, что в `catalog/base.html`.
⚠️ **`<!doctype html>` обязателен и стоит ПЕРВЫМ.** Без него браузер уходит в
quirks mode, и KaTeX ОТКАЗЫВАЕТСЯ рисовать вообще — на листке остаётся сырой
`$120-2P$`. Страница при этом отдаёт 200, питон-тесты слепы. Найдено
скриншот-проверкой, держится `test_doctype_is_first`.

**PDF за флагом** `ASSIGNMENT_PDF_ENABLED` (по умолчанию False):
`scripts/print_to_pdf.js` печатает ТУ ЖЕ страницу через Playwright. Выключен
арифметикой: 512 МБ на всё приложение против 200–400 МБ на один Chromium.

**`.tex` собирается ЛЮБЫМ движком** — преамбула ветвится через `iftex`:
`\ifPDFTeX` → `inputenc`/`fontenc T2A`, иначе `fontspec` + Latin Modern Roman.
⚠️ Причина: файл, собранный XeTeX-ом, терял ВСЮ кириллицу без единой ошибки.
TeX Live на машине нет — сборку проверяет человек в Overleaf.

**Браузерная проверка сессии:** `node scripts/session4_check.js 8124` (15
сценариев). ⚠️ Проверка обязана сверять КОД ОТВЕТА: первая версия открывала
листок репетитора под учеником, получала 403 и «успешно» мерила ширину
страницы ошибки.

## Сессия 5: кабинет преподавателя по 25 замечаниям владельца (та же ветка)

**Набор деталей интерфейса — `templates/_kit.html`.** Кнопки трёх весов
(`.k-btn--main/--plain/--quiet`), поля (`.k-input/.k-area/.k-select` +
`.k-select-wrap`, `.k-check`), карточка `.k-card` (+`--calls` — тонкая рамка
акцентом без заливки), пометка состояния `.k-state` (полоса слева, фон
нейтральный), крупный балл `.k-score`, плитки `.k-tile`, разделитель `.k-sep`.
Подключён в `teacher/base.html` и `platform/base.html`. Витрина —
**`/teacher/styleguide/`**.
⚠️ `display` из правил набора СИЛЬНЕЕ браузерного `[hidden]{display:none}` —
поэтому в наборе есть явное `.k-btn[hidden]{display:none}`. Без него
спрятанная кнопка продолжает рисоваться (поймано глазами).

**Максимальный балл за позицию.** `AssignmentItem.points` заполняется при
СОЗДАНИИ: `models_platform.default_points(is_test)` — **10 задаче, 3 тесту**
(`DEFAULT_POINTS_PROBLEM/DEFAULT_POINTS_TEST`). Миграция **0032** проставила
пустым позициям `LEGACY_POINTS = 1` — то число, по которому они и так
считались. Правится на странице задания, эндпоинт `teacher:api_item_points`.

**Автоматический ноль за пустоту.** `part_grading.save_part_answers`: пусто и
в ответе, и в решении, и файла нет (`part_grading.wrote_anything`) → ноль
ставит машина, в очередь ручной проверки не идёт. Ответ пуст, а решение
написано → к человеку. Правило ПО КАЖДОМУ ПУНКТУ. Признак —
`PartAnswer.auto_zero` (миграция **0033**); он отличает ноль за пустоту от
нуля за ошибку. Разово применить к уже сданным — `manage.py apply_auto_zero
[--confirm]`.

**Порядок задач в работе — `assignment_rows.ordered_items` + `section_marks`.**
Тесты первыми, открытые задачи следом; подписи «Тестовая часть» / «Задачи» с
пунктиром (`templates/_section_sep.html`). ОДНА сборка на экран ученика,
экран репетитора, страницу печати и `.tex`.
⚠️ Подписи ставятся, только если список ДЕЙСТВИТЕЛЬНО поделён надвое (ровно
одна смена признака) — проверяем сам список, а не флаг: при чередовании
подпись обещала бы часть, которой нет.
⚠️ `Assignment.manual_order` (миграция **0034**) отменяет перестановку. Ставится,
когда репетитор задал порядок словами в описании (`hw_generator.describes_order`).

**Печатный листок.** Подпункты БУКВАМИ (`<ul class="parts">`, не `<ol>`); место
под решение по весу задачи — `assignment_export.solution_lines` (у теста 0, у
задачи `балл+1`, границы 3…12); шапка ученика в одну строку без сплошной черты;
свой подвал с адресом ГЛАВНОЙ (`site_home` из `build_absolute_uri('/')`);
плашка графика только ученику; верный вариант помечается галочкой в своей
колонке (`.options li.is-right::before`), а не словом в строке.
⚠️ В `print_rows` индекс строки называется `index`: имя `position` затирал
ВЛОЖЕННЫЙ цикл по вариантам ответа, и подпись части бралась по номеру
последнего варианта. Поймано сквозным сценарием, не тестами.

**Экраны группы.** Вкладок ТРИ: `overview` (по умолчанию, бывшая статистика),
`assignments`, `materials`. Вкладка «Ученики» удалена как обеднённая копия
таблицы со статистики. `?tab=students` и `/teacher/groups/<pk>/stats/` —
редиректы на обзор. Задания сгруппированы по состоянию:
`views_groups.assignment_state` / `group_assignments_by_state` →
«Требуют вас» / «Идут сейчас» / «Завершены».

**Сводка решений — два вида.** `/teacher/groups/<g>/assignments/<a>/submissions/`
`?view=students` (карточки по ученикам, `views_groups.student_cards`) и
`?view=problems` (прежняя таблица). Выбор запоминается в сессии
(`SUBMISSIONS_VIEW_KEY`). «Машина уже насчитала» считает ТОЛЬКО машинные
проверки (`feedback.reviewed_by is None`).

**Проверка работы — поток.** `teacher/views.review_queue` / `review_position`
дают «задача N из M», стрелки и следующую НЕПРОВЕРЕННУЮ задачу того же
ученика. `go=next` ведёт дальше, после последней — экран завершения
**`/teacher/groups/<g>/assignments/<a>/students/<s>/done/`** (`teacher:work_done`),
где живёт «Комментарий ко всей работе». Балл ставится пресетами
(`_score_presets`: 0 / половина / максимум). «Отметить ошибки» убрано совсем.
⚠️ `update_student_progress` пропускает работу без каталожной задачи — у своей
задачи репетитора тем нет, и функция падала прямо при сохранении оценки.
⚠️ `part_rows` у задачи БЕЗ пунктов берёт ответ из `Submission.submitted_answer`,
если записи `PartAnswer` нет: без этого экран писал «ответа нет» ответившему.

**Подбор по описанию.** Два раздельных числа — открытых задач и тестов;
`hw_generator.split_by_kind` делит квоту по темам **методом наибольшего
остатка** (`allocate`), тип при поиске — ЖЁСТКИЙ отбор (`is_test_problem`).
⚠️ `_any_problems(rows, limit, kind)` — последний рубеж добора. Раньше он
БЕЗУСЛОВНО исключал тесты, и просьба «три теста» молча выполнялась тремя
открытыми задачами. Добор идёт по каждой недобравшей строке отдельно и
уважает её тип.
Переключатель «домашка / контрольная» (`?kind=exam`); найденное для
контрольной уезжает в её ОБЫЧНЫЙ конструктор (`exam_create`) с корзиной
`exam_cart` — второй формы настроек времени нет.

**Три способа набора** — `teacher/_build_modes.html` (плитки «Описать словами»
/ «Искать самому» / «Написать свою»), настройки переживают переход через
`teacher/_build_keep.html` (sessionStorage `hw_settings`, `hw_manual_order`).
⚠️ У контрольной конструктор живёт ВНУТРИ группы: без номера группы адрес не
собирается, и партиал падал ошибкой. Без группы «искать самому» ведёт в
конструктор домашки.

**Время по-человечески — `problems/timefmt.human_deadline` / `deadline_pair`**:
«завтра до 20:00», «через 3 дня», «прошёл 2 дня назад»; точная дата уходит в
подсказку. Считает по КАЛЕНДАРНЫМ дням, не по «24 часа».

**Четвёртое условие «Требуют внимания»** (`stats.needs_attention`,
`STALE_REVIEW_DAYS = 3`): работа сдана и ждёт проверки дольше трёх дней.
Формулировка про репетитора, а не про ученика.

**Проверка браузером:** `node scripts/night_item_card.js` (17 проверок карточки
позиции), `night_review_flow.js` (поток проверки), `night_exam_generate.js`
(контрольная по описанию), `night_end_to_end.js` (сквозной сценарий),
`night_shots.js` (съёмка, СВЕРЯЕТ КОД ОТВЕТА), `night_generate_step2.js`.
Контактный лист — `./venv/bin/python scripts/night_index.py` →
`reports/night/index.html`.
⚠️ Экраны подбора требуют слоя модели. Для ОСМОТРА боевой ключ не нужен:
поднимается второй сервер с подставным поставщиком (`AI_PROVIDER='fake'`,
`AI_FAKE_REPLY`) — он в проекте заведён ровно для этого.

**Тесты сессии 5:** `test_auto_zero.py` 11, `test_section_order.py` 11,
`test_night_bugs.py` 72 (+ дописано в `test_print_sheet.py`,
`test_option_grading.py`, `test_answer_approval.py`). Полный прогон —
**973/973**.

## Сессия 6: фиксы после приёмки ключевого экрана (та же ветка)

**Балл обрезается по максимуму задачи, и это делает СЕРВЕР.**
`teacher/views.py::_max_score_for` — одна точка, максимум нужен и при показе
формы, и при приёме POST. В базе нашлась оценка «9 из 5»: поле «своё»
принимало любое число, ограничение стояло только в разметке (`max=`), а форму
можно отправить в обход браузера.

⚠️ **ПУСТАЯ ПОЗИЦИЯ ОТПРАВЛЕННОЙ РАБОТЫ ПОЛУЧАЕТ НОЛЬ** —
`part_grading.close_blank_position`, вызывается из
`student/views.py::accept_answers`. Раньше приём работы делал `continue` по
позиции, где не написано ничего, ДО всякой проверки: она оставалась
`not_started`, на экране стоял прочерк, в очередь не попадала, в сумму не
входила. Правило автонуля из сессии 5 до неё просто не доходило.
Пустую работу ЦЕЛИКОМ не трогаем (`saved == 0`) — она не отправлена.
⚠️ Признак «ноль за пустоту» — `part_grading.is_auto_zero`, ОДИН на разбор
работы и экран проверки; строка комментария машины — константа
`part_grading.BLANK_COMMENT`. Состояние строки в разборе — `blank`, не
`wrong`: балл тот же, но «неверно» тому, кто не отвечал, — неправда.
⚠️ Проверка автонуля стояла на `score is None`, то есть срабатывала ТОЛЬКО
там, где машина судить не могла. Стоило утвердить эталон — и не написавший
ни строчки получал «неверно, правильный ответ: 5000». Теперь условие —
«не написано ничего», независимо от эталона.
Бэкфилл — `manage.py apply_auto_zero [--confirm]` (два класса: пустые в
очереди и не начатые в отправленных работах; идемпотентна).

⚠️ **БАЛЛ ПОЗИЦИИ — ЕДИНСТВЕННАЯ ПРАВДА, `ProblemPart.points` — ТОЛЬКО ВЕС.**
`assignment_rows.part_max_score`. Раньше балл пункта из каталога брался КАК
ЕСТЬ и молча перебивал поставленный репетитором: задача стоила на карточке
3 балла, а в сумме работы 2. Веса уважаются (1 и 3 из пяти баллов дадут 1,25
и 3,75), остаток округления отдаётся последнему пункту — иначе «10 баллов»
складывались в «9,99». ⚠️ У УЖЕ ПРОВЕРЕННЫХ работ сохранённые `max_score`
пунктов держат прежнее округление: переписывать их нельзя, это знаменатель
выставленных оценок.

**`Assignment.points_locked`** — баллы правятся ДО первой сдачи. После —
заперты у ВСЕЙ работы (итог это сумма). Защита в `api_item_points` (409),
не только в разметке. Цифра остаётся крупной (`.k-score__value`), рядом
подпись «заперто», наверху страницы одна строка о причине.

**Видимость комментария — ТРИ значения** (`ProblemComment.Visibility`,
миграция **0035**): `group` / `private` / `self`. Названия для интерфейса —
`models_platform.visibility_choices_for(role)`, ОТ ЛИЦА ПИШУЩЕГО; пометка
для читающего — `ProblemComment.note_for(viewer)`, ставится ТОЛЬКО у
исключений. ⚠️ Прежнее «только я и автор» написано от лица УЧЕНИКА: у
репетитора это означало «только я и я», ученик комментария не видел.
`private` от репетитора ТРЕБУЕТ адресата (сервер отвергает без него) —
иначе то самое несуществующее состояние сохранялось бы снова. `self` не
видна никому, кроме автора, даже репетитору домашки. Миграция круг
читателей не меняет. `ProblemComment.by_tutor` — роль В ЭТОЙ РАБОТЕ
(требует `select_related('assignment', 'assignment__group')`).

**Токены `--focus` / `--focus-ring`** в `_tokens.html` — кольцо фокуса
НЕЙТРАЛЬНОЕ. Малиновый акцент оставлен навигации и мелочам: поле,
обведённое акцентом, — крупное акцентное пятно на каждом клике.
⚠️ Тёмная тема: главная кнопка читалась блёклой НЕ из-за текста (белый на
прежнем `#334155` давал 10,35:1) — сама кнопка не отличалась от панели
(1,52:1). `--btn-bg` осветлён до `#46566d`: 2,11:1 с панелью, 7,47:1 с
текстом. Держится тестом на ОБА числа.

⚠️ **`display` из набора деталей СИЛЬНЕЕ браузерного `[hidden]`** — второй
раз за две сессии. Список правил `.k-btn[hidden], …` в `_kit.html`
дополнен на `.k-select-wrap`. Ловится ТОЛЬКО глазами на скриншоте.

⚠️ **`/student/work/<pk>/` принимает pk ЗАДАНИЯ, а не сдачи.** Ошибка в
адресе не даёт ошибки на экране: открывается ДРУГАЯ работа, и проверка
молча меряет не то.

⚠️ **Браузерные сценарии работают по боевой `db.sqlite3`** и оставляют в
ней следы: оценка «2 из 2 за неверный ответ», из-за которой началась эта
сессия, поставлена `night_review_flow.js`. После прогона сценариев —
`seed_platform_demo`. Карточка в Notion на отдельную базу.

**Проверка браузером:** `node scripts/fix_end_to_end.js 8199` (сквозной
сценарий приёмки, 18 проверок), `node scripts/night_item_card.js 8199`
(карточка позиции, 30). Контактный лист —
`./venv/bin/python scripts/fix_index.py` → `reports/fix/index.html`.

**Тесты сессии 6:** `test_score_integrity.py` 12, `test_blank_zero.py` 17,
`test_check_block.py` 12, `test_points_lock.py` 10,
`test_comment_visibility.py` 18, `test_talk_block.py` 11,
`test_small_fixes.py` 14. Полный прогон по проекту — **1067/1067**.

## Сессия 7: ревью владельца на 55 пунктов (та же ветка)

Закрыты фазы 0–10, 11.1–11.4, 12.0, 14.1–14.3. Тесты — **1166/1166**
(было 1085; новый файл `problems/tests/test_review55.py`, 80 проверок).
Миграции **0036** (`WorkDifficulty`) и **0037** (`UserProfile.city/goal`,
`TutorNote`). Контактный лист снимков — `reports/review/index.html`
(`scripts/review_index.py`).

**Набор деталей пополнен** (`templates/_kit.html`): `.k-score__of` /
`.k-score__max` (крупный балл «N из M»), `.k-score--pair`, `.k-mark--*` и
`.k-flag--*` (пять состояний строки), `.k-hintmark` (+ партиал
`templates/_hint.html`), `.k-pair`, `.k-level--*`.
⚠️ **Модификаторы `.k-mark--*` и `.k-flag--*` записаны ДВУМЯ классами**
(`.k-mark.k-mark--correct`): экраны задают у той же карточки `border`
сокращённой записью, а она сбрасывает `border-left-color`. При равной силе
полоса состояния молча теряла бы цвет на всех экранах сразу.
⚠️ Набор подключён и в `student/base.html` — там уже завёлся ручной дубль
`.k-sep`, а разбор работы глазами ученика владелец называет эталоном вида.

**Статистика — `problems/stats.py` (фаза 8).** Новое: `level_of`,
`canonical_topics`, `accuracy_pair`, `topic_progress`, `group_accuracy`,
`_graded_ratios`, `_catalog_ratios`, `_topic_events_for`.

| Показатель | Входит | НЕ входит |
|---|---|---|
| «Решено» | домашки, контрольные, каталог | **игра** |
| «Доля верных» (оба числа) | домашки, контрольные, каталог | **игра** |
| «Последняя активность» | всё, **включая игру** | — |
| Теплокарта «ученики × темы» | всё, **включая игру** | — |

⚠️ **Неполный балл идёт ВЕСОМ**: 8 из 10 — вклад 0,8 (`_graded_ratios`).
⚠️ **Строка «Всего» и строка «по группе» — средневзвешенные по числу
решённых**, а не средние из процентов: иначе тема с одной задачей весила бы
столько же, сколько тема с сорока.
⚠️ **Показываем все 21 каноническую тему, плюс неканонические С ДАННЫМИ в
конец.** Тем в базе сильно больше двадцати одной; показать только канон
значило бы спрятать настоящие попытки, а «все 21» просили ради обратного.
⚠️ Теплокарта считается ЗА ВСЁ ВРЕМЯ и переключателю периода не подчиняется
(`group_topic_matrix(group, 'all')` прибито во вьюхе) — отсюда пометка «за
всё время» у заголовка блока.

**Субъективная сложность работы** — `WorkDifficulty` + `average_difficulty`
/ `difficulty_for_work` / `difficulty_for_student` в `models_platform.py`.
Спрашиваем у ученика на разборе работы, шкала 1–10, необязательно, одна
оценка на пару (работа, ученик). Вычислить её из баллов НЕЛЬЗЯ: низкий балл
говорит, что ученик не справился, а не что задача трудная.
⚠️ Нет оценок → `None` и «нет оценок» словами: ноль по шкале 1–10 был бы
оценкой. ⚠️ Блок — `<div>` с `type="button"` и СВОИМ `csrf_token`: у
контрольной форм на экране может не быть вовсе.

**Карточка ученика — ЕДИНСТВЕННЫЙ экран об ученике.**
`/teacher/students/<id>/stats/` → редирект на `/teacher/student/<id>/
progress/`. Обе ссылки стояли в ОДНОЙ таблице обзора группы.

⚠️ **ПОЛЕ ФОРМЫ С ИМЕНЕМ `action` ЗАТЕНЯЕТ `form.action`.** Обращение к
свойству отдаёт сам `input`, а не адрес, и форма уходит на
«…/generate/[object HTMLInputElement]» → 404. В подборе поле переименовано в
`step_action`, у всех форм проставлен явный `action="…"`. Правило общее: не
называть поля `action`, `submit`, `method`, `elements`, `length`.

⚠️ **Суточный лимит обращений к модели считает только УДАЧНЫЕ** (`ok=True`
в `ai.used_today`). Отказ поставщика — не расход. У отказа появился вид
(`AiUnavailable.kind` / `ProviderError.kind`: `no_key` / `limit` / `other`),
по нему экран выбирает человеческий текст; код ошибки остаётся в журнале.

⚠️ **Пресеты балла — `[0, максимум/2, максимум]` без округления.** У значения
и подписи РАЗНЫЕ формы записи: подпись через запятую («1,5»), значение с
точкой («1.5») — его кладут в `input[type=number]` и разбирают `float()`, и
запятая дала бы пустое поле и ноль на сервере. `step` поля «своё» — `any`.

⚠️ **Браузерные стрелки числовых полей** гасит одно правило в `_kit.html` на
классах кабинета (`.k-input`, `.k-score input`, `.form-control`), НЕ на
голом `input[type=number]`: голый селектор задел бы каталог и calc2.

⚠️ **`timefmt.human_ago`** — «сегодня / вчера / 2 дня назад» для активности.
Отдельная функция от `human_deadline`: срок смотрит вперёд, активность назад,
и слова для одного дня у них разные.

**Не сделано в сессии 7** (закрыто в сессии 8, см. ниже): фазы 11.5,
12.1–12.3, 13, 14.4, 15.

## Сессия 8: остаток ревью на 55 пунктов (та же ветка)

Ревью закрыто ЦЕЛИКОМ. Тесты — **1235/1235** (было 1166). Миграция **0038**.

⚠️ **ЛИТЕРАЛЬНЫЙ ДОЛЛАР И KaTeX — `templates/_katex_dollars.html`,
ЕДИНСТВЕННАЯ ТОЧКА.** Конвейер был скопирован в четыре шаблона; теперь один
партиал, подключение держится тестом. Правило: маскируем приватным символом
`\uE000` только те «\$», что стоят **вне** формул. Внутри формулы «\$» —
законная запись доллара, а вот `\uE000` KaTeX не принимает вовсе
(«Unexpected character») и при `throwOnError: false` печатает ВСЮ формулу
красным исходником. Так экран проверки показывал таблицу КПВ сырым кодом.
Границы формул ищутся теми же парами и в том же порядке, что у авторендера
(`$$` раньше `$`), а закрывающий разделитель — В ОБХОД «\$».
Замер: `node scripts/dollar_render_compare.js <файл.json>` — 157 → 3 ошибки
на 84 полях, 46 → 0 на 400 задачах, хуже нигде.

⚠️ **ПЕРЕНОСЫ СТРОК В УСЛОВИИ — `white-space: pre-line`, А НЕ
`|linebreaksbr`.** Фильтр резал текст тегами `<br>`, и авторендер переставал
видеть формулу целиком: «\[» в одном текстовом узле, «\]» в другом (235
видимых задач). Переносы при этом РОВНО ТЕ ЖЕ. Общего `pre-line` на весь
сайт это не вводит — правило стоит только там, где переносы и так были
принудительными (`.rv-statement`, `.problem-statement`).
⚠️ Содержимое такого блока пишется БЕЗ отступов внутри тега: отступы шаблона
при `pre-line` превращаются в пустые строки на экране.
⚠️ Широкая формула обязана прокручиваться внутри себя
(`:is(table, .katex-display) { overflow-x: auto }`), иначе таблица КПВ
выносит весь экран за край окна на 380px.

**Подбор по описанию (12.1–12.3).**
- `hw_generator.preview_rows` дедуплицирует **на всю подборку** (общий
  `chosen`), как это всегда делал `find_problems`. Раньше эти два места
  расходились, и одна задача честно попадала в две строки.
- Кандидатов под строку — `candidates_wanted(count)` = `max(5, 2×count)`.
- «Показать ещё 5» — `teacher:api_more_candidates`, поиск по банку,
  суточный счётчик обращений НЕ трогает.
- ⚠️ Квота делится по типу (`split_by_kind`) ТЕПЕРЬ НА ШАГЕ «Что нашлось».
  Тип едет через форму (`row_kind`); повторное деление закрыто
  `views_generate._split()` — второй проход дал бы по четверти квоты.
- Новый экран `/teacher/assignment/build/` (`teacher:assignment_build`):
  слева превью работы, справа настройки. ⚠️ Работу создаёт НЕ он — форма
  уходит в прежние `assignment_create` / `exam_create`. Позиции для превью
  собираются В ПАМЯТИ (`picker.cart_rows`) и проходят через ТУ ЖЕ
  `assignment_rows.ordered_items` + `section_marks`.
- Балл позиции из конструктора: `picker.parse_points` +
  `create_items(points=…)`.
- ⚠️ После создания работы открывается САМА РАБОТА
  (`teacher:group_assignment`), а не дашборд и не экран результатов.

**История работ — `problems/stats.py`:** `work_history` (ученик) и
`group_work_history` (группа) поверх общих `_work_scores` / `_work_percents`;
разметка — `teacher/_work_history.html`. ⚠️ Работа без срока в столбец
«невовремя» не попадает вообще — прочерк, а не ноль.
⚠️ Стили `.wk-table` переехали в общий `platform/_stats_style.html`:
таблица стоит на ДВУХ страницах, а правила лежали в шаблоне одной.

**Пункты в своей задаче (15.1) — модель `CustomProblemPart`.**
⚠️ Отдельная таблица, а не `ProblemPart`: последняя ссылается на `Problem`,
то есть на общий каталог, куда задача репетитора не попадает никогда. Поля
названы КАК У `ProblemPart` — конвейер обращается к ним по именам.
⚠️ ДВЕ ТАБЛИЦЫ — ДВА ПРОСТРАНСТВА НОМЕРОВ: у `PartAnswer` и `AnswerDraft`
вторая ссылка `custom_part`, ключ пункта — `assignment_rows.part_key`
(номер у каталожного, «c<номер>» у своего), запись — `part_link`, чтение —
`answer_row_key`. Ограничение «задача целиком» переписано на «пусты ОБЕ
ссылки»: иначе три пункта своей задачи ломались об уникальность.
⚠️ В миграции 0038 операции переставлены ВРУЧНУЮ (`AddField` раньше
`AddConstraint`) — та же ловушка, что в 0023.
⚠️ Допуск сравнения теперь СВОЙ у каждого пункта
(`assignment_rows.part_tolerance`).
`part_grading.applies()` НЕ ТРОНУТ.

**Оценка в разборе глазами ученика (14.4):** `teacher:api_grade_submission`,
партиал `teacher/_grade_inline.html`. ⚠️ Балл обрезает СЕРВЕР той же
`_max_score_for`. ⚠️ Пресеты считает ОДНА `work_review.score_presets`
(`teacher/views._score_presets` делегирует ей). ⚠️ Свой `csrf_token` в ветке
репетитора: форм на его экране разбора не было вовсе.

**Набор деталей пополнен:** `.k-check--box` (крупная галочка и кружок,
недоступное состояние пунктиром), `.page-header` / `.page-title` /
`.back-link` (переехали из `teacher/_picker_style.html`: их классы стоят и
на экранах, которые тот файл не подключают).

⚠️ **БРАУЗЕРНЫЕ СЦЕНАРИИ БОЛЬШЕ НЕ ПОРТЯТ ВИТРИНУ** —
`config/settings_check.py`: своя `db_check.sqlite3` (снимок боевой) плюс
подставной поставщик модели (`QLS_FAKE_REPLY`). Запуск:
```
cp db.sqlite3 db_check.sqlite3
QLS_FAKE_REPLY='{"rows": [...], "note": ""}' \
  ./venv/bin/python manage.py runserver 8199 --settings=config.settings_check
```
⚠️ Замерять рендер KaTeX в своей странице можно только с `<!doctype html>`:
без него браузер уходит в quirks mode и KaTeX ОТКАЗЫВАЕТСЯ рисовать вообще —
первая версия замера мерила пустоту.

**Сценарии сессии 8:** `build_path.js` (путь создания работы целиком, 19
проверок; `exam` вторым аргументом), `custom_parts.js` (9), `picker_look.js`
(6), `eyes_grade.js` (8), `session8_cycle.js` (сквозной, 19),
`review8_shots.js` (все тронутые экраны × 2 темы × 1440/380),
`raw_tex_probe.js`, `dollar_render_compare.js`, `demo_clean.js`,
`picker_inventory.py` (опись кнопок ручного поиска: 24 = 24).

**Тесты сессии 8:** `test_katex_dollars.py` 9, `test_build_flow.py` 22,
`test_group_history.py` 9, `test_custom_parts.py` 14,
`test_eyes_grading.py` 15.

## Прочие команды-замеры (только чтение)

- `ile_forum_solutions` — ILE это форум, и в `solution` местами лежит реплика
  ученика, а не разбор (#476). Замер: 27 задач из 1 769 решений (1,5%) —
  класс ТОЧЕЧНЫЙ. ⚠️ Из 90 слабых кандидатов детектора реальными оказались 18:
  автоматический ярус пришлось разбирать глазами, иначе цифра завысилась бы
  вдесятеро (ручной список — константа `HAND_CONFIRMED_WEAK` в команде).
  Отчёт `reports/ile_forum/forum_solutions.md`.
- `tikz_census` — tikz-код по ВСЕМУ банку (прошлый замер шёл только по ILE).
  40 задач в видимых полях, синтаксически целых всего 2; **25 из 40 уже стоят
  в очереди пере-импорта** `reports/batch2/skipped_flagged.txt` — классы
  пересекаются сильнее, чем предполагалось. Отчёт `reports/tikz/tikz_census.md`.

⚠️ **Расхождение миграций `game`.** В базе применены `game` 0007–0012, файлов
на этой ветке и на `main` нет — `showmigrations game` показывает 0001–0006 и
молчит. Файлы целиком лежат на `feat/econ-rush-figure` (есть и на origin) и
`feat/bank-report`; на `feat/econ-rush-arena` — без 0012. Ловушка деплоя с
чистой загрузкой базы. **Не запускать `makemigrations game`** на ветке без
файлов — сочинит свою 0007 под тем же номером. Карточка в Notion.

## Задачи, планы, баги
Живут в Notion: https://app.notion.com/p/39bb11c92bc18105a448e3a2271fe964
Актуальная выжимка — в разделе «Текущий фокус» выше.

---

## Протокол сессии (обязательно)

### В начале каждой сессии
1. Прочитать этот файл.
2. Прочитать в Notion базу «Задачи»: всё со статусом «В работе» и «Надо».
3. Прочитать 5 последних записей в «Решения».
4. ЕСЛИ Notion недоступен — сказать об этом ВСЛУХ, работать по блоку «Текущий
   фокус», предупредить, что данные могут быть устаревшими (указать дату блока).
   Не молчать и не делать вид, что всё в порядке.

### В конце каждой сессии
1. Изменилась техника (новая команда, поле модели, ловушка) → правка CLAUDE.md.
2. Приняли решение → карточка в Notion «Решения»: что решили, почему,
   какие варианты отвергли.
3. Задача сделана → статус «Готово». ЕСЛИ нужна визуальная проверка владельцем →
   статус «На проверке».
4. Нашлась новая задача или баг → карточка в «Задачи», статус «Надо».
5. Что-то выкатили или посчитали → запись в «Результаты».
6. Перегенерировать блок «Текущий фокус».
7. В итоговом отчёте показать ОТДЕЛЬНЫМ СПИСКОМ, что записано в Notion.

### Правила работы с Notion
- НИКОГДА ничего не удалять. Только менять статусы и добавлять.
- Перед созданием карточки — поиск по названию. Не плодить дубли.
- Не заводить новые базы, разделы и варианты select без спроса владельца.
- Не переписывать чужие карточки — добавлять комментарий.
- Массовая запись (больше 5 карточек) — только после показа списка владельцу
  и его подтверждения.

### Как генерировать блок «Текущий фокус»
Между маркерами NOTION-SYNC:START и NOTION-SYNC:END:
- строка «Обновлено: ГГГГ-ММ-ДД»
- ссылка на штаб
- «В работе:» — все задачи со статусом «В работе» (название + ответственный)
- «На проверке:» — все со статусом «На проверке»
- «Надо (ближайшее):» — до 7 задач со статусом «Надо»
- «Последние решения:» — 3 последних из базы «Решения»
Блок только читается из Notion и никогда не правится руками. Notion — истина.

---

*Полная история завершённых сессий — в `CLAUDE_ARCHIVE.md`. Стратегия, бизнес-план,
конкуренты, экономика ИИ, задачи и планы — в Notion-штабе:
https://app.notion.com/p/39ab11c92bc181f7bbe1efa8a89e38a9*
