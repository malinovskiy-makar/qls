# Зависимости сервиса кодирования (С4). Отдельный файл, а не общий с Django:
# сюда едет весь ML-стек, а в боевой образ Django он не едет вовсе (ADR 0002).
#
# ⚠️ ВЕРСИИ ML-СТЕКА ЗАКРЕПЛЕНЫ, И ЭТО САМОЕ ВАЖНОЕ В ФАЙЛЕ.
# Они обязаны совпадать с requirements/local.in — тем билдом, которым
# посчитаны все ~31 500 векторов корпуса. Модель (BAAI/bge-m3) одна и та же,
# но токенизация и пулинг живут в библиотеке: мажорный подъём сдвинет вектор
# ЗАПРОСА относительно векторов КОРПУСА. Косинус поедет молча — ни одна
# проверка не покраснеет, поиск просто станет хуже без видимой причины.
# Поднимать — только вместе с полным пересчётом корпуса и замером
# benchmark_search против reports/bge_benchmark/after_bge_m3.txt.
sentence-transformers==5.1.2
transformers==4.57.6
tokenizers==0.22.2

# CPU-сборка torch: GPU на сервере нет, а обычное колесо тянет CUDA на
# несколько гигабайт впустую.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

fastapi==0.141.1
uvicorn[standard]==0.52.4
numpy==2.5.2
