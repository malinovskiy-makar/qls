# -*- coding: utf-8 -*-
u"""Настройки разработки с ВКЛЮЧЁННЫМ режимом «График».

Зачем отдельный модуль. Флаг `GAME_FIGURE_ENABLED` читается из окружения
(`GAME_FIGURE_ENABLED=1 manage.py runserver`), а конфигурации предпросмотра
в `.claude/launch.json` переменные окружения задавать не умеют. Без этого
модуля посмотреть режим глазами можно было только из терминала, и приёмка
упиралась в способ запуска.

⚠️ ТОЛЬКО ДЛЯ РАЗРАБОТКИ. На проде флаг выключен (`settings_production.py`),
и включение режима — отдельное решение владельца, а не следствие того, что
рядом лежит этот файл.

    manage.py runserver 8011 --settings=config.settings_figure
"""
from .settings import *          # noqa: F401,F403

GAME_FIGURE_ENABLED = True
