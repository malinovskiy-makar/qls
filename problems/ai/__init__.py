"""
Слой обращения к языковой модели — ЕДИНСТВЕННАЯ дверь наружу.

⚠️ ЗАЧЕМ ОН НУЖЕН, И ПРИЧИНА НЕ В ЭКОНОМИИ. Мы будем отправлять тексты,
написанные несовершеннолетними. По 152-ФЗ (ч. 5 ст. 18, с 2025 года) базы с
персональными данными граждан РФ обязаны находиться в России, а за данные
детей предусмотрена в том числе уголовная ответственность. Переезд на
российскую модель — вопрос времени, и он должен стоить НАСТРОЙКУ, а не
квартал работы. Поэтому весь код продукта знает только `ai.run(...)`, а
какой поставщик за этим стоит — знает `providers.py`.

Что здесь есть и чего намеренно нет:
  * есть — выбор поставщика настройкой, ключ только из окружения, единый
    учёт токенов и денег, кэширование неизменного начала запроса, разбор
    структурированного ответа, человеческие сообщения на все отказы;
  * НЕТ — «на будущее»: ни очередей, ни ретраев с экспонентой, ни
    абстрактных фабрик. Задача сейчас одна (разбор запроса на домашку), и
    слой сделан ровно под то, чтобы вторая задача села на готовое.
"""
from .core import (  # noqa: F401
    AiUnavailable, available_profiles, is_available, run, unavailable_reason,
)
