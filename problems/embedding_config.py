"""
Конфигурация модели эмбеддингов — единый источник истины.

Импортировать отсюда, НЕ дублировать строку модели в разных файлах.
build_embeddings.py и catalog/semantic.py обязаны использовать одно и то же имя.
"""

# Модель для семантического поиска (sentence-transformers).
# Вектор длины EMBEDDING_DIM; должен совпадать во всех местах.
EMBEDDING_MODEL_NAME = 'BAAI/bge-m3'

# Размерность вектора для данной модели.
EMBEDDING_DIM = 1024
