# -*- coding: utf-8 -*-
"""Решалки Олмат (source_id=16): 3 активные (не-дубли) задачи с подтверждённым
архитектурным разрывом `$$` между полями `statement`/`solution` — см.
диагностику отдельной параллельной сессии (коммит `cdb1fa5`,
`reports/corpus_converter_scaleup/reshalki_dollar_diagnosis.md`, не в этой
ветке) и её независимое воспроизведение в этой сессии (тот же алгоритм
посимвольного счётчика `$$`-токенов, что и `_protect_math_and_currency`,
даёт те же числа: 151 задача, 50 с нечётным `$$`, 49 с разрывом между
полями, из них 46 — дубли, 3 — активные).

`may_render_as_markdown()` эти три НЕ ловит: разрыв происходит на уровне
БД (импортёр `import_olmat_reshalki.py:240` режет исходную пару `$$…$$`
пополам между `statement` и `solution`), а конвертер видит каждое поле
по отдельности и непарный `$$` просто не считает математикой — ни
warning, ни complex_table здесь не срабатывают. Исключение отсюда, а не
из шлюза: сам шлюз не тронут (единственная точка решения по-прежнему
`may_render_as_markdown`), это отдельная, явно поименованная страховка
поверх него, пока причина (импортёр) не починена или задачи не слиты
с дублирующими из ЛШ Олмат 2025."""

#: id из Problem (source_id=16), активные (status != 'duplicate'),
#: human_review не 'approved' на момент диагностики (2026-08-27).
FORCED_EXCLUDE_RESHALKI_DOLLAR_SPLIT = frozenset({47083, 47204, 47206})
