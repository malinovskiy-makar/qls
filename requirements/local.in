# Локальная разработка: всё боевое плюс ML-стек для эмбеддингов.
# Ставится ТОЛЬКО на машину разработчика — на прод не едет (ADR 0002).
-r base.in

# ⚠️ ВЕРСИИ ML-СТЕКА ЗАКРЕПЛЕНЫ НАМЕРЕННО.
# Все ~31 500 векторов корпуса посчитаны ИМЕННО этим билдом. Модель (BAAI/bge-m3)
# одна и та же, но токенизация и пулинг живут в библиотеке, и мажорный подъём
# сдвигает вектор запроса относительно корпуса. Косинус поедет молча: ни одна
# проверка не покраснеет, а поиск станет хуже.
# Поднимать — только вместе с полным пересчётом корпуса и замером
# benchmark_search против reports/bge_benchmark/after_bge_m3.txt.
sentence-transformers==5.1.2
transformers==4.57.6
tokenizers==0.22.2
torch
safetensors
huggingface_hub
scikit-learn
scipy

# Подбор домашки по описанию. Без ключа ANTHROPIC_API_KEY функция сама
# выключается — на проде не нужен.
anthropic

# ⚠️ openai ПЕРЕЕХАЛ В base.in 13.09.2026 — это общий сетевой клиент для
# нескольких ИИ-провайдеров (problems/ai/providers.py), и умный поиск через
# core.run зовёт его на бою. Здесь его больше нет, но local.in по-прежнему
# его ставит — строкой `-r base.in` выше.

PyYAML
cryptography
