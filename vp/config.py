"""Сезонное в разделе «Высшая проба»: то, что меняется раз в год, а не с вёрсткой.

⚠️ ОБНОВЛЯТЬ КАЖДЫЙ СЕЗОН. Даты тура живут ТОЛЬКО здесь: посадочная страница `/vp/`
читает их отсюда, в шаблоне их нет. Новый сезон — правка `TOUR_DATES`; не забыть про
пункт меню «Тренажёр ВП» (`config/context_processors.py::_menu`) — он сезонный и нужен
только пока идёт тур, потом решается его судьба (ADR 0127). Метка NEW у него гаснет по
отдельной дате `NEW_BADGE_UNTIL`.
"""
from datetime import date

#: Даты 1 тура отборочного этапа (сезон 2026/27). Пустой кортеж — фразы про даты на
#: странице просто нет.
TOUR_DATES = (date(2026, 9, 26), date(2026, 9, 30))

# Метка NEW у пункта шапки видна, пока localdate() < этой даты.
# ⚠️ ДАТА ОТДЕЛЬНАЯ ОТ СЕЗОННОСТИ САМОГО ПУНКТА И ЭТО НАМЕРЕННО:
# если после тура пункт решат оставить в шапке, метка «новое» не
# должна остаться вместе с ним (решение владельца 22.09.2026).
NEW_BADGE_UNTIL = date(2026, 10, 1)

#: Раздел «Классы» на странице списка: (значение `VPVariant.grade_band`, подпись).
BANDS = (('9-10', '9–10 классы'), ('11', '11 класс'))
