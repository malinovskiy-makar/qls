"""Сезонное в разделе «Высшая проба»: то, что меняется раз в год, а не с вёрсткой.

⚠️ ОБНОВЛЯТЬ КАЖДЫЙ СЕЗОН. Даты тура живут ТОЛЬКО здесь: посадочная страница `/vp/`
читает их отсюда, в шаблоне их нет. Новый сезон — правка `TOUR_DATES`; не забыть про
пункт меню «Высшая проба» (`config/context_processors.py::_menu`) — он сезонный и нужен
только пока идёт тур, потом решается его судьба (ADR 0127).
"""
from datetime import date

#: Даты 1 тура отборочного этапа (сезон 2026/27). Пустой кортеж — фразы про даты на
#: странице просто нет.
TOUR_DATES = (date(2026, 9, 26), date(2026, 9, 30))

#: Раздел «Классы» на странице списка: (значение `VPVariant.grade_band`, подпись).
BANDS = (('9-10', '9–10 классы'), ('11', '11 класс'))
