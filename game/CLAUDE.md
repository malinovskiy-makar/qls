> **Владелец:** Claude Code
> **Обновлён:** 2026-09-02 (сессия «Wecon Rush — закрытие»)
> **Статус:** актуален

# Правила приложения `game` (Wecon Rush)

Локальные правила для `game/`. Общие запреты — в корневом
[CLAUDE.md](../CLAUDE.md); **как устроена сама игра** (механика, экономика v2,
зачётность, таблица лидеров, фильтры, анти-чит, тревога, дуэли, команды пула)
— [docs/GAME.md](../docs/GAME.md). Здесь только то, что нужно знать, правя
код этого каталога.

---

## ⚠️ `makemigrations game`: смотря на какой ветке

**Это главное правило этого каталога.**

**На этой ветке (`feat/wecon-rush`) цепочка `game` непрерывна: 0001–0015.**
Ветка `feat/econ-rush-figure` влита сюда, файлы миграций 0007–0012 на месте,
`makemigrations --check` молчит. Новые миграции здесь **законны и нумеруются
с 0016**.

**На `main` — по-прежнему запрещено, пока эта ветка не влита.** Там лежат
только 0001–0006. Django не знает про существующие номера и **сочинит свою
0007 под занятым**. При слиянии получится конфликт номеров, который не
разрешается: две разные миграции претендуют на одну позицию в цепочке, и ни
откатить, ни применить их вместе нельзя.

**Что делать вместо (на `main`):**
- нужно изменить модели игры → работать на `feat/wecon-rush`, там цепочка полная;
- нужно проверить состояние → `manage.py makemigrations --check --dry-run`
  (только проверяет, ничего не создаёт);
- кажется, что миграция всё-таки нужна на `main` → сначала прочитать
  [ADR 0004](../docs/adr/0004-game-migrations-split.md), потом спросить владельца.

Подробности расхождения и почему миграции нельзя перенести отдельно от кода —
[ADR 0004](../docs/adr/0004-game-migrations-split.md).

---

## Игровой пул отделён от банка задач

`GameQuestion` — **своя** сущность, а не ссылка на `Problem`. Так сделано
намеренно: удаление или скрытие задачи в банке не должно ломать журнал уже
сыгранного забега.

Темы и сложность в журнале **денормализованы** (`state['log']` хранит их прямо
в записи о вопросе): сводке не нужно ходить в базу за вопросом, которого может
уже не быть.

---

## Состояние забега лежит в КЭШЕ, а не в сессии

Забег живёт в кэше `default` под ключом `rush:run:<run_id>`; в сессии остаётся
только `run_id`. Читает и пишет **одно место** — `game/state.py`
(`load_run` / `save_run`, а для чужого забега `load_by_id` / `save_by_id`).

⚠️ **`request.session[...]` для состояния забега не трогать.** Имя
`SESSION_KEY` в `views.py` осталось ради одного релиза совместимости: забег,
начатый до выкатки, доигрывается. После следующей выкатки его и ветку
`state._legacy` можно убрать.

Долгая память человека (`econ_rush_seen`, `econ_rush_counted`,
`econ_rush_last`) осталась в сессии намеренно: в кэше с TTL она терялась бы
при каждом истечении. Обоснование — [ADR 0061](../docs/adr/0061-game-run-state-in-redis.md).

---

## Кто что считает

**Очки, комбо и жизни считает сервер** и отдаёт готовыми числами — клиент их
только рисует. У клиента остался **только таймер**.

Раньше считал клиент, и серверный журнал расходился с экраном. Урок «Штриха» v1:
две записи одного и того же всегда разъезжаются.

Конец по времени (`reason: 'time'`) сообщает клиент, но перебить сервер не может:
забег, закрытый по жизням, остаётся `'lives'`.

⚠️ **То же правило действует и в дуэли.** Табло соперника рассылает
`api_answer` после записи состояния; сообщение `score` от клиента консьюмер
игнорирует молча. Второго счёта у нас нет, сверить нечем.

⚠️ **Числа механики в клиенте не выдумывать.** Запас жизней, размер целевого
забега, ступени комбо приходят из `config.py` через `CFG`. Это держит тест
`test_page_js.py::test_mechanics_numbers_come_from_config`, и он уже ловил
захардкоженную тройку жизней в клиенте дуэли.

---

## Наказание — жизнь, а не время

У всех режимов `time_wrong = 0` и `time_skip = 0`:

- **ошибка** → минус жизнь и комбо в ноль, время не трогаем (одно наказание, не два);
- **пропуск** → бесплатен полностью: жизнь цела, комбо цело, время цело.

Если сделать пропуск платным, угадывать станет выгоднее, чем честно пропускать —
и игра начнёт учить угадыванию. Третья ошибка завершает забег.

Скип комбо **не рвёт**. Неверный ответ — рвёт.

---

## Длину условия режет ОТБОР ПРИ ВЫДАЧЕ, а не сборщик пула

В пул попадает всё до 700 знаков; кто из режимов какую длину выдержит, решает
`config.MODE_MAX_CHARS` и `views._candidate_rows`.

⚠️ Правя порог, правьте **константу**, а не сборщик: порог в сборщике требует
пересборки пула, а константа действует сразу.

---

## Числа игры — предмет замера, а не вкуса

Стартовое время режимов, прибавка за верный ответ, число жизней и пороги комбо
лежат в `game/config.py::MODES`. Они выбраны рассуждением, а не измерением, и
это записано как долг: тюнить их надо по замеру живых забегов.

Мерить есть чем: `manage.py game_economy_report` и вкладка «Экономика v2» на
`/game/stats/`. Считает их **одна** функция `game/economy_report.py::report`.

Тот же урок уже получен на порогах «Штриха». Не подкручивать на глаз.

---

## Точность ответа

Классика сравнивает значения **точно**, через `fractions.Fraction`
(`views.py::parse_exact_number`): целые, десятичные с запятой или точкой, дроби
`a/b`. `0,1 = 0.1 = 1/10` — верно; `1/3 ≠ 0.33`. Мусорный ввод — неверный ответ,
а не пятисотая.

Рапид засчитывается только при **полном** совпадении множества выбранных
вариантов с правильным.

⚠️ Той же функцией сверяется ответ модели в `answer_second_opinion`: иначе
«0,5» и «1/2» разошлись бы на ровном месте и создали ложное расхождение.

---

## Генераторы: длинное тире живёт в БАЗЕ, а не в файлах

Сканер `scripts/check_em_dash.py` читает каталог `game` и показывает ноль, а
игрок при этом может видеть тысячи вопросов с запрещённым знаком: тире
приезжает из строк генератора **в базу**. Файловый сканер такой дефект не
увидит по устройству.

Правило: правя тексты генераторов, проверяйте по базе —
`test_generators.py::GeneratedPoolHasNoEmDashTests` наполняет пул и читает
его, а не файлы.
