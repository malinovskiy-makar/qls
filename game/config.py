"""
Константы механики Econ Rush.

⚠️ Все значения — предмет тюнинга после живых прогонов; менять здесь,
фронтенд и API читают только отсюда.

Четыре режима (как контроли времени в шахматах): Пуля / Блиц / Рапид /
Классика. У каждого — свой тип вопросов, стартовое время, бонус времени
за верный ответ и запас жизней. Очки и комбо общие для всех режимов.

Наказание за ошибку — ЖИЗНЬ, а не время (механика Puzzle Rush): штраф
временем слабо сдерживал угадывание, особенно в «Классике» с её десятью
минутами. Отсюда time_wrong = 0 и time_skip = 0: за ошибку и за пропуск
время не трогаем, чтобы не наказывать дважды и чтобы честный пропуск был
выгоднее угадывания.
"""

# Режимы игры. duration — стартовый запас времени (секунд),
# time_* — дельты времени (секунд), lives — запас жизней,
# question_type — тип GameQuestion.
# ⚠️ Все числа — предмет тюнинга после живых прогонов.
MODES = {
    'bullet': {
        'title': 'Пуля',
        'question_type': 'boolean',   # данетка: Верно/Неверно
        'duration': 60,
        'time_correct': +2,
        'time_wrong': 0,              # за ошибку время не трогаем — минус жизнь
        'time_skip': 0,               # пропуск полностью бесплатный
        'lives': 3,
    },
    'blitz': {
        'title': 'Блиц',
        'question_type': 'single',    # один из вариантов
        'duration': 120,
        'time_correct': +5,
        'time_wrong': 0,
        'time_skip': 0,
        'lives': 3,
    },
    'rapid': {
        'title': 'Рапид',
        'question_type': 'multi',     # несколько из вариантов
        'duration': 300,
        'time_correct': +15,
        'time_wrong': 0,
        'time_skip': 0,
        'lives': 3,
    },
    'classic': {
        'title': 'Классика',
        'question_type': 'numeric',   # числовой ответ вводом
        'duration': 600,
        'time_correct': +30,
        'time_wrong': 0,
        'time_skip': 0,
        'lives': 3,
    },
    # ⚠️ Режим «График» — АУДИТ ЧУЖОГО РЕШЕНИЯ, а не выбор картинки из
    # четырёх. Формат «выбери правильный график» решался узнаванием, без
    # счёта; здесь игроку показывают ОДНО размеченное решение и просят найти
    # первый неверный шаг (решение Notion от 2026-07-26).
    #
    # ⚠️ Прежний тип `figure_choice` НЕ удалён: генератор, вариант типа в
    # модели и клиентская ветка плиток остались на месте, просто ни один
    # режим их больше не выдаёт. Откат решения = правка этой одной строки.
    #
    # Тайминги поставлены РАССУЖДЕНИЕМ: на один вопрос нужно решить систему,
    # опознать область и проверить площадь — это 40–60 секунд, а не 18. По
    # действующему решению «пороги игры тюнить только замером» они подлежат
    # замеру (карточка заведена в Notion), на глаз их не крутить.
    #
    # Вопросы этого режима бывают только сгенерированные, поэтому режим живёт
    # за СВОИМ флагом GAME_FIGURE_ENABLED (он не связан с
    # GAME_GENERATED_ENABLED: это отдельное семейство, оно не зависит от
    # семнадцати архетипов).
    'figure': {
        'title': 'График',
        'question_type': 'figure_audit',    # найти первый неверный шаг
        'duration': 600,
        'time_correct': +25,
        'time_wrong': 0,
        'time_skip': 0,
        'lives': 3,
    },
}

# Режим по умолчанию (обратная совместимость: старые клиенты без ?mode=).
DEFAULT_MODE = 'blitz'

# ---------------------------------------------------------------------------
# ЭКОНОМИКА ОЧКОВ, ВЕРСИЯ 2
# ---------------------------------------------------------------------------
#
# ⚠️ ЧТО БЫЛО НЕ ТАК В ВЕРСИИ 1. Очки считались как «100 × комбо»: сложность
# вопроса не влияла ни на что. Симуляция (scripts/game_economy_sim.py)
# показала два перекоса:
#   • стратегия «играю только 1★» давала в 2–4 раза больше честного забега —
#     лёгкие вопросы отвечаются быстрее, серия рвётся реже, а очки те же;
#   • в Классике +30 с за верный ответ при запасе 600 с делали забег
#     бесконечным: хороший игрок набирал время быстрее, чем тратил.
#
# Версия 2 привязывает очки к сложности, к скорости ОТНОСИТЕЛЬНО честного
# времени на прочтение и размышление, и снижает итог за низкую точность.
# Сами формулы — в game/scoring.py (чистые функции без Django), здесь
# только числа: их тюнят замером, а не переписыванием кода.
#
# Обоснование целиком — docs/adr/0055-game-economy-v2.md.

ECONOMY_VERSION = 2

# Очки за верный ответ по эффективной сложности, ДО множителей.
BASE_BY_DIFFICULTY = {1: 20, 2: 50, 3: 100, 4: 170, 5: 250}

# Бонус за скорость: мгновенный ответ (но не быстрее прочтения) = ×1,5.
SPEED_BONUS_MAX = 0.5
READ_CHARS_PER_SEC = 20        # время прочтения = длина текста / 20
READ_MIN_S = 2.0               # пол: короткая данетка не даёт нулевого опорного

# База времени на размышление по режимам. Итог = THINK_S × (0.6 + 0.2 × d):
# 1★ — 0,8 базы, 5★ — 1,6.
THINK_S = {'bullet': 6, 'blitz': 12, 'rapid': 30, 'classic': 60, 'figure': 60}

# Сколько вопросов в целевом забеге «работа над ошибками».
MISTAKES_RUN_SIZE = 10

# Комбо: длина серии верных ответов → множитель очков.
# ⚠️ Ступени ПОЛОЖЕ, чем в версии 1 (там было ×2/×3/×4). При ×4 одна удачная
# серия перевешивала весь остальной забег, и набирать её выгоднее было на
# лёгких вопросах, чем честно играть.
# Неверный ответ рвёт серию; пропуск — НЕ рвёт.
COMBO_STEPS = [(12, 2.0), (9, 1.75), (6, 1.5), (3, 1.25)]

# Забег БЕЗ ЕДИНОГО фильтра. Плата за то, что игрок не подбирал себе пул.
SCOPE_MULTIPLIER = 1.3

# Точность забега: ≥ 85 % → ×1, ≤ 50 % → ×0,3, между — линейно.
# Это главный тормоз угадывания: угадывающий набирает очки, но теряет итог.
ACCURACY_FULL_AT = 0.85
ACCURACY_FLOOR_AT = 0.5
ACCURACY_MIN_MULT = 0.3

# Прибавка времени за верный ответ, доля от time_correct режима. Лёгкие
# вопросы дают меньше — иначе выгодно фильтровать пул до одних лёгких.
TIME_BONUS_SCALE_BY_DIFFICULTY = {1: 0.2, 2: 0.5, 3: 1.0, 4: 1.0, 5: 1.0}
# Суммарная прибавка за забег не больше запаса режима × этого числа.
TIME_BONUS_CAP_FACTOR = 1.0

# Зачётность забега (лидерборд, фаза 5).
RANKED_MIN_CORRECT = 5         # меньше верных — забег не идёт в таблицу
RANKED_RUNS_PER_DAY = 10       # зачётных забегов на режим за московские сутки
SPEED_BOARD_MIN_CORRECT = 10   # доска «среднее время» — только при ≥ 10 верных


def combo_multiplier(streak):
    """Множитель очков для текущей серии верных ответов."""
    for threshold, mult in COMBO_STEPS:
        if streak >= threshold:
            return float(mult)
    return 1.0


# ---------------------------------------------------------------------------
# Режим «График» — аудит чужого решения (game/figures/)
# ---------------------------------------------------------------------------

# Доля вопросов, в которых показанное решение ВЕРНО и правильный ответ —
# «Ошибки нет». Без неё игрок за пять вопросов понимает, что ошибка есть
# всегда, и перестаёт проверять последний шаг: ему остаётся выбрать из трёх.
# Остальные 85 % делятся поровну между тремя видами ошибок — сначала
# решается, будет ли ошибка вообще, и только потом какая (см.
# figures/base.py::pick_injector).
FIGURE_CLEAN_SHARE = 0.15


# ---------------------------------------------------------------------------
# Статистика по вопросам (game/stats.py)
# ---------------------------------------------------------------------------

# Ниже этого числа ПОПЫТОК (верных + неверных) процент не показываем нигде:
# ни игроку на карточке, ни в каталоге, ни в измеренной сложности. На пяти
# ответах «40 % верных» — это не сложность вопроса, а шум.
STATS_MIN_ATTEMPTS = 20

# Измеренная доля верных → сложность 1–5. Словарём, а не в коде: пороги —
# предмет тюнинга, как и всё остальное в config.py. Читается сверху вниз:
# первая полоса, в которую попала доля (p >= lo), даёт сложность.
# p ≥ 0,85 → 1 (почти все решают) … p < 0,30 → 5 (почти никто).
MEASURED_DIFFICULTY_SCALE = [
    (0.85, 1),
    (0.70, 2),
    (0.50, 3),
    (0.30, 4),
    (0.00, 5),
]

# Пороги служебной страницы /game/stats/: ниже первого вопрос почти наверняка
# сломан (не тот ключ ответа, потерялась формула), выше второго — тривиален
# и только разбавляет пул.
STATS_BROKEN_BELOW = 0.10
STATS_TRIVIAL_ABOVE = 0.95


# ---------------------------------------------------------------------------
# Фильтры стартового экрана
# ---------------------------------------------------------------------------

# Раскладка тем и группы фильтров живут в `game/filters.py` — там же,
# где «Особенности» и «Характер задачи». Держать половину раскладки
# здесь, а половину там значило бы завести два источника правды.
from .filters import TOPIC_GROUPS      # noqa: F401

# ⚠️ Ниже этого числа вопросов под фильтром режим ВЫКЛЮЧАЕТСЯ с честной
# строкой. Забег из трёх вопросов — не забег: он кончится раньше, чем игрок
# успеет разогнаться, и выглядит как поломка, а не как узкий фильтр.
MIN_PLAYABLE = 10

# Диапазон сложности.
DIFFICULTY_MIN = 1
DIFFICULTY_MAX = 5


# ---------------------------------------------------------------------------
# Вызов дня и дуэль (наборы)
# ---------------------------------------------------------------------------

# Часовой пояс отсечки суток — ЯВНОЙ константой. Игроки российские, «новый
# вызов» обязан наступать по Москве, а не по UTC сервера.
# Москва с 2014 года без перехода на летнее время, поэтому запасной вариант
# «фиксированный UTC+3» точен, если базы часовых поясов на машине нет.
DAILY_TZ = 'Europe/Moscow'
DAILY_TZ_FALLBACK_HOURS = 3

# Сколько вопросов в вызове дня — отдельно на режим: в «Пуле» вопрос
# читается за секунды, в «Классике» на него уходит минута.
DAILY_SIZE = {
    'bullet': 15,
    'blitz': 12,
    'rapid': 10,
    'classic': 8,
    'figure': 10,
}

# Длина набора дуэли.
DUEL_SIZE = 10


# ---------------------------------------------------------------------------
# Ощущения забега
# ---------------------------------------------------------------------------

# МЯГКАЯ эскалация: текущее комбо → целевая полоса сложности (включительно).
# Читается сверху вниз, первая подходящая полоса выигрывает.
# ⚠️ Полоса МЯГКАЯ: если под фильтрами игрока в ней пусто, она расширяется,
# а не роняет забег. Пользовательский фильтр сложности — жёсткая рамка,
# эскалация ходит только ВНУТРИ неё.
# ⚠️ Эскалации НЕТ в курированных забегах (набор, вызов дня, дуэль, работа
# над ошибками): там список задан заранее, двигать нечего.
ESCALATION_BANDS = [
    (9, (4, 5)),
    (6, (3, 4)),
    (3, (2, 3)),
    (0, (1, 2)),
]


def escalation_band(streak):
    """Целевая полоса сложности для текущей серии верных ответов."""
    for threshold, band in ESCALATION_BANDS:
        if streak >= threshold:
            return band
    return (DIFFICULTY_MIN, DIFFICULTY_MAX)


# Множитель очков на последней жизни. Считает СЕРВЕР (очки вообще считает
# только он), клиент лишь рисует. 1 — выключено.
# ⚠️ Было 2 — снижено до 1,25. Двукратный множитель НАГРАЖДАЛ УГАДЫВАНИЕ:
# выгоднее было довести себя до одной жизни и там рисковать, чем играть
# аккуратно с тремя.
LAST_LIFE_MULTIPLIER = 1.25
