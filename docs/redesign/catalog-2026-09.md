# Редизайн каталога и страницы задачи — журнал фаз

> **Ветка:** `feat/catalog-redesign` от `origin/main` (`6b84061`).
> **Промпт:** «Редизайн каталога и страницы задачи — единый промпт на 7 этапов»
> (владелец, 04.09.2026). Мокапы — `data/*_mockup.html` (не отслеживаются git;
> владелец положил их в `data/`, а не в `~/weconomics-data/`).
> **Как возобновить:** новая сессия с тем же промптом читает эту таблицу и
> продолжает с первой незакрытой фазы.

## Стартовые числа (сессия 1, 2026-09-04)

| Что | Число | Откуда |
|---|---|---|
| Задач на `/catalog/` без фильтров | **5 090** | `filters.base_queryset('catalog').count()` на локальной `db.sqlite3` |
| Всего задач в базе | 31 694 | `Problem.objects.count()` |
| Канонических тем в базе | 23 (из 849 записей `Topic`) | `apply_topic_mapping.CANONICAL` |
| Тегов / источников | 551 / 23 | `Tag`, `Source` |
| Видимых по сложности | 2 → 79, 3 → 13, 4 → 32, 5 → 1 089, без сложности 3 877; **«★» (1) — 0** | группировка по `difficulty` |
| Видимых по формату | без формата 3 625; «тест: один ответ» 846; «верно/неверно» 348; «все верные» 271; **«числовой ответ» — 0** | группировка по `problem_type` |
| С решением / с ответом | 1 704 / 1 560 | `exclude(solution='')`, `exclude(answer='')` |
| Без тем / без тегов | 2 944 / 487 | `topics__isnull`, `tags__isnull` |
| Заголовок есть у | всех 5 090 | `exclude(title='')` |
| Тестов в наборе, время полного прогона | **3 875** (шаг A, параллельно, ≈75 с) + **8** серийных (≈145 с); всего ≈3,7 мин, а не 25 | `scripts/run_tests.py`, 2026-09-04 |

## Расхождения с промптом и окружением (сессия 1)

- **PostgreSQL 55432 недоступен, `docker` в PATH нет** — тесты идут на SQLite
  (`scripts/run_tests.py` по умолчанию). Джоб CI «migrations» и прогон на
  PostgreSQL локально не выполнить; это делает CI после пуша. Перед
  стоп-гейтом слияния — зафиксировать отдельно.
- **Мокапы лежат в `data/`**, а не в `~/weconomics-data/mockups/…` — так
  сказал владелец в промпте («мокапы лежат в папке data»). Они не
  отслеживаются git и в коммиты не входят.
- **Ветка `feat/olympiads-screens` несёт 27 коммитов, которых нет в
  `origin/main`** (сессия 4 олимпиад). Ветка редизайна режется от
  `origin/main` по промпту, поэтому этих коммитов в ней нет. В рабочем
  дереве после переключения остался неотслеживаемый `data/olympiads/raw` —
  не трогается.
- **Notion «Решения» 04.09 (чат «Полировка к бете», ветка `feat/beta-polish-0904`)**
  содержит два решения, задевающих эту работу: (а) карта тем `/catalog/map/`
  сводится к ПЯТИ блокам и пяти цветам через единый модуль
  `problems/sections.py`; (б) ширина полосы — 960 везде, 860 на страницах
  чтения. В `origin/main` модуля `problems/sections.py` ещё нет, ветки
  `beta-polish` на remote нет. Этап 1 красит чип темы через
  `topic_blocks.section_of()` (семь разделов карты, как в промпте); переход
  на пять цветов — одна строка в `topic_blocks.py`, когда решение (а)
  доедет до кода. Ширины этап 1 не трогает. **Вопрос владельцу — в отчёте.**

- **Правило сайта «ноль длинных тире»** (`problems/tests/test_typography.py`,
  сканер `scripts/check_em_dash.py`): мокапы владельца тире содержат. В этапе 1
  заменено: «Ищем по смыслу: точные слова не нужны», «Enter – найти».
- **`calc2.tests.test_calc2_math.Calc2MathRegressionTest.test_calc2_control_numbers`
  красный и на чистом `origin/main`** (проверено в отдельном worktree 04.09) —
  не дефект этой ветки, в отчёт владельцу.
- **Ловушка мутационных проверок:** мутация и откат файла одного размера в
  одну секунду оставляют устаревший `.pyc`; полный прогон падал на верном
  коде (`section_of` отвечал из мутированного байткода). Драйвер теперь
  ставит `PYTHONDONTWRITEBYTECODE=1`, после прогона чистится `__pycache__`;
  откат — записью исходника, а не `git checkout` (иначе теряются
  незакоммиченные правки того же файла).
- **Найдено глазами, не тестами:** авторазмер поля мерил `scrollHeight`
  пустого textarea, а Chrome считает в нём перенесённый placeholder — на
  узком окне поле раздувалось на весь экран. Поле растёт только под текст.
  Чип активного источника пропадал, когда под остальными фильтрами у него
  ноль задач (бейдж врал числом) — теперь строится по id источника.

## Фазы

| фаза | статус | коммит | числа-инварианты | заметки/вопросы |
|---|---|---|---|---|
| −1 Сверка с реальностью (сессия 1) | ✅ | — | 5 090 видимых; Python 3.13.15; `check` 0 ошибок | PG недоступен (см. выше) |
| 1.1 Поле поиска + общий партиал бегущей подсказки | ✅ | `406f951` | HTML каталога содержит все 11 фраз и «Смотри, мне нужен кач про Канье Веста»; главная — прежние 6; `SLIPS` — ровно в 1 файле (`templates/_typing_placeholder.html`) | фразы — `catalog/placeholder_phrases.py`; JSON через фильтр `script_json` (штатный `json_script` пишет кириллицу как `\uXXXX`) |
| 1.2 Карта в высоту поля и полоса выбранного | ✅ | `406f951` | подпись карты «29 тем, 343 тега» считается из `topic_map.json` (тест подменяет данные → «2 темы, 7 тегов»); при 5 фильтрах — 5 чипов и бейдж 5, при 0 — ни чипа, ни бейджа, ни «Сбросить» | `catalog/topic_blocks.py` заведён целиком (пять блоков + `section_of`); чипы — ключ `filters.chips`, партиал `_catalog_chips.html`; крестик — ссылка (без JS работает) |
| 1.3 Карточки списка: без номера, без таблицы, превью без сирот | ✅ | `406f951` | 5 090 без фильтров (не изменилось); `ct-card-id` — 0 вхождений; `?view=table` → `rows`; превью фикстуры содержит `P=20`, без « , »; «☆» только при заданной сложности; `tchip` только при теме | `catalog/preview.py`: `preview_text`, `looks_like_statement_cut`, `cut_words`; `_card` отдаёт `show_title`, `kind_label`, темы с разделом |
| 1.4 Тесты, зубастость, прогон, журнал | ✅ | следующий за `406f951` | `test_no_slop.py`: пустая база → числа на `/catalog/` ⊆ {0} ∪ {29, 343 из данных карты}; шаблоны `catalog/` без слов-обещаний; подпись карты меняется с данными. Зубастость: 15 + 3 дефекта, пойманы все 18. Полный прогон: 3 875 OK (13 пропусков) + 8 серийных, 1 красный — calc2, красный и на `origin/main` | обновлён чужой тест `test_r15_align.CatalogDifficultyTests` (таблицы нет — сторожит её отсутствие); ruff, bandit `-ll`, pip-audit чисты; джоб CI «migrations» и PG-прогон локально невозможны (нет PostgreSQL) |
| 2.1 Множественный выбор в разборе, адресе и запросе | ✅ | `6fb1e68` | `parse` → списки `topics/difficulties/sources/features`; `query(parse(query()))` замыкается; ∣A ∪ B∣ = 4 на фикстуре из 5 задач; сложности 4 и 5 = 3; «★» (1) в корпусе 0 → не в `options`; ноль под фильтрами — `zero`; тесты `teacher` и `test_work_flow` зелёные без правок разметки | читатели одиночных ключей: `_relief`, `scope` (→ `filters.scope_label`), `test_tails` |
| 2.2 Пять блоков тем, форматы под «Тест», поля «характер»/«особенности» + команда загрузки | ✅ | `6fb1e68`, `b62932f` | миграция `problems/0050_problem_character_features` (AddField ×2): `--check` чист до/после, с нуля на SQLite OK, накат на копию 6 с, откат/повтор OK, рабочая база накачена за 4 с; `_topic_options` → `topic_blocks.BLOCKS`, 23 живые темы легли в блоки micro/macro/fin/math/other; форматы теста — только из корпуса (3 из 4) | PostgreSQL для миграций локально нет — CI. Команда `import_problem_attributes`: сухой прогон по умолчанию, `--apply` пишет только `character`/`features` через `bulk_update`, снимок старых значений `reports/problem_attributes/undo_*.jsonl` (тот же формат — им же откат), второй сухой прогон даёт 0; 5 тестов |
| 2.3 Эндпоинт живого состояния и вынос отрисовки | ✅ | `174520c` | `GET /catalog/api/filter-state/` (`catalog:api_filter_state`): `total` = числу страницы при тех же параметрах; `counts.topic` — все темы корпуса; `?topic=A&topic=B` → 3 = ∣A ∪ B∣; `results_html` побайтно равен секции страницы; `api_tags?topic=` — теги темы без нулей и без скрытых | `_catalog_context(request)` общий для страницы и эндпоинта; партиал `_catalog_results.html`; страница отличается только редиректом по номеру |
| 2.4 Прогон, журнал, Notion | ✅ | следующий за `174520c` | полный прогон: шаг A **3 904** OK (13 пропусков), шаг B 8 — 1 красный calc2 (известный, красный и на `origin/main`); задач без фильтров по-прежнему **5 090**; `teacher` и `test_work_flow` зелёные; зубастость этапа 2: 16 дефектов, после усиления двух тестов пойманы все 16; ruff, bandit `-ll`, pip-audit, `check`, `makemigrations --check`, сканер тире — чисты | `test_no_slop`: эндпоинт на пустой базе — нули и пустые группы. Старое окно «Все фильтры» (до этапа 3) показывает четыре блока: у «Поведенческой экономики» и «Введения» 0 видимых задач — правило нуля |
| 3.1 Окно «Все фильтры»: разметка и стили | ⬜ | | | |
| 3.2 Скрипт живого обновления | ⬜ | | | |
| 3.3 Клавиатура, приборка, прогон | ⬜ | | | |
| СТОП-ГЕЙТ 1 — приёмка и слияние | ⬜ | | | |
| 4.1 Страница задачи: сетка, шапка, действия, облачка | ⬜ | | | |
| 4.2 Условие, подпункты, решение и ответ | ⬜ | | | |
| 4.3 Похожие и карточка чата | ⬜ | | | |
| 4.4 Прогон, документы, журнал | ⬜ | | | |
| 5.1 Модель попытки `CatalogAttempt` | ⬜ | | | |
| 5.2 Профиль проверки в слое ИИ | ⬜ | | | |
| 5.3 Эндпоинт и экран проверки | ⬜ | | | |
| 5.4 Минимальный живой чат | ⬜ | | | |
| 5.5 Прогон, документы, журнал | ⬜ | | | |
| 6.1 Подсказки с командой загрузки | ⬜ | | | |
| 6.2 Фото и файл | ⬜ | | | |
| 6.3 Прогон, журнал | ⬜ | | | |
| 7.1 Разбор слитных ответов | ⬜ | | | |
| 7.2 Эндпоинты и попытки теста | ⬜ | | | |
| 7.3 Экран теста | ⬜ | | | |
| 7.4 Прогон, CI, документы, зачистка, журнал | ⬜ | | | |
| СТОП-ГЕЙТ 2 — приёмка и второе слияние | ⬜ | | | |
| Финал | ⬜ | | | |
