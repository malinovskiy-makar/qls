# Передача базы задач: что внутри и что мы поняли

Собрано Аничем 20 августа 2026 для Макара. Цель передачи — посчитать эмбеддинги
на всём массиве сразу, а чинить, отсматривать и выкладывать постепенно.

---

## 1. Что в архиве

| Файл | Что это |
|---|---|
| `ile.json` | все задачи источника ILE (iloveeconomics) с разметкой |
| `aa.json` | все задачи Сборника тестов АА с разметкой |
| `matek.json` | все задачи МатЭка с разметкой |
| `manifest.json` | числа: сколько задач в каждом файле, сколько с вердиктом, MD5 |
| `ПЕРЕДАЧА_ИДЕИ.md` | этот файл |
| `materials/` | вердикты ревью, починки волны 1, план нулевого шага МатЭка |

**Разметка каждой задачи** (поля в JSON):

- `human_review` — `approved` (человек сказал «идеально»), `defect` (брак),
  `null` (человек не смотрел);
- `verdicts[]` — по одной записи на категорию: ключ категории, человеческое
  название, комментарий ревьюера, цитаты, пакет, дата;
- `visibility` — `status`, `needs_quality_review`, `solution_needs_review`,
  `hidden_pending_review`, `visible_in_catalog`;
- дальше сама задача: `statement`, `parts[]`, `answer`, `solution`, `topics`,
  `tags`, `source_refs`, `attachments`.

Тексты отдаются дословно из базы, без обрезки.

---

## 2. Состояние базы на 20 августа

- задач всего **31 694**, подпунктов ~63 100
- одобрено человеком **5 090**, брак **1 536**, не смотрели **25 068**
- видно в каталоге **5 090** — скрытие применено 19.08
- сайт не работает: у Render истёк пробный период. Поэтому любые изменения
  каталога сейчас безопасны — промежуточное состояние никто не увидит.

**Три механизма скрытия, их нельзя смешивать:**

| Признак | Отвечает на вопрос | Задач |
|---|---|---|
| `status='hidden'` | убрано руками | 1 882 |
| `needs_quality_review` | скрыто, потому что ПЛОХОЕ | 3 518 |
| `hidden_pending_review` | скрыто, потому что человек НЕ СМОТРЕЛ | 26 604 |

Третий — новый, наложен поверх остальных на всё неодобренное. `--revert`
снимает только свой признак. Обратимость проверена на боевом состоянии:
`apply → revert → apply` даёт побайтно тот же набор 26 604 id.

---

## 3. Ручное ревью — три пакета

| Пакет | Задач | Идеально | С браком | Комментариев | Медиана темпа |
|---|---|---|---|---|---|
| `ile_20260721` | 2 401 | 1 919 (79,9 %) | 482 | 42 | 1,86 с |
| `aa_20260728` | 1 593 | 1 462 (91,8 %) | 131 | 43 | 0,74 с |
| `matek_20260808` | 2 632 | 1 709 (64,9 %) | **923 (35,1 %)** | **746** | 3,29 с |

Итого 6 626 отсмотренных. «Гагно» (trash) — 93 задачи (2 у АА, 91 у МатЭка).
Собран, но не пройден пакет `ap_homeworks_20260728` (936 задач).

**Категории брака АА:** broken_formula 61, other 43, merged_structure 24,
junk 11, trash 2, broken_table 1.
**Категории брака МатЭк:** other 435, merged_structure 299, broken_formula 118,
trash 91, leaked_solution 44, junk 17, broken_table 5.

**Темп ревью вопросов не вызывает.** Разрыв 0,74 с у АА против 3,29 с у МатЭка
даёт состав, а не строгость: при одинаковом типе вердикта темп совпадает
(дефект с комментарием — 7,56 с против 8,25 с). В МатЭке 746 прокомментированных
дефектов против 43 у АА. Вердикты трёх пакетов сравнимы, пересматривать
июльские не нужно.

---

## 4. Главный вывод: ИИ-чистка правила не то, что было сломано

Сверка вердиктов человека с историей чисток по ILE:

| Что делала чистка с забракованной задачей | Задач из 482 |
|---|---|
| **не трогала вообще** | **332 (69 %)** |
| трогала другое поле | 4 |
| трогала нужное поле, дефект остался | 4 |
| поле в вердикте не названо | 135 |

Доля брака там, где чистка работала — 17,3 %, где не работала — 21,2 %.
Разница четыре пункта при том, что модель просматривала всё подряд.

**Три причины, все наши:**

1. В промпте не было перечня дефектов. Просили «почини битый LaTeX, OCR-порчу,
   задвоения, вынеси решения». Два самых массовых типа — голая математика без
   разметки и слипшаяся построчная нарезка — в промпт не входили. Из 18 077
   отправленных задач модель вернула 11 125 нетронутыми.
2. Решение принималось за один проход, без отдельного шага «сначала найди дефекты».
3. Результат никто не смотрел: 4 930 задач Батча 2 применены полной заменой
   поля без человеческой проверки.

**Обратная сторона важна:** из 1 919 идеальных задач чистка касалась 684 и не
испортила ни одной. Механика безвредна — провален охват, а не аккуратность.

**Прицельно по категориям:** утёкшие решения (67) — ни одна не упёрлась в
предохранители, виноват детектор `find_leaks` (ищет маркер только в начале
строки и только в условии, а в реальности 23 утечки внутри поля решения, у 22
маркер в середине строки, 21 вообще без маркера). Сломанные формулы (191) — не
тронуто 126. Слипшаяся структура (137) — разбито 6. Развалившиеся таблицы (20) —
модель бралась за две, обе человек всё равно забраковал.

---

## 5. Утверждённая схема работы

Смысл: **на сайте показываем только проверенное человеком.** Прячем не потому,
что плохое, а потому что мы про него ничего не знаем.

1. «Идеально» → на сайт сразу (5 090). ✅ сделано
2. Выложенное, но не проверенное → скрыть отдельным признаком. ✅ сделано
3. Дефектные → **волна 1**: модель чинит по вердикту человека, обязательно
   объясняя свою правку.
4. Человек смотрит результат пятью кнопками: починил идеально / починил не до
   конца / не починил / сломал / скрыть на потом. Цитированием указывает, что
   модель не поняла.
5. **Волна 2**: модель чинит **оригинал** (не свою же неудачную правку) с
   учётом цитат.
6. Остаток — вручную или выбросить.

Прогноз по 6 626 задачам: 76,8 % уезжают сразу; после двух волн на сайте от
90 % (худший случай) до 97,6 % (лучший); время Анича 15–23 часа.

⚠️ Реальная доля успешных починок пока **не измерена** — это главное
недостающее число, и первым его даст разбор волны 1.

**Правило SUPERSEDES:** вердикт пакета разбора отменяет прежний брак. Без него
починенная задача осталась бы скрытой навсегда. Это единственное исключение из
правила «брак сильнее идеального».

---

## 6. Нулевой шаг МатЭка — что это и почему стоит

**Суть проблемы.** Главная беда МатЭка — не формулы, а решения не на своём
месте. Из 923 дефектных задач **436 (47 %)** прокомментированы словами про
решение или ответ: «решение в ответ» 94, «убрать решение» 40, «решение
слиплось» 34, **«г д е это решение к абв» 21**. Формулы упомянуты всего в 9
комментариях из 746.

Последнее — системная ошибка парсера: подпункты «г», «д», «е» на самом деле
решения к пунктам «а», «б», «в». Импорт разрезал `.tex` не там.

**Почему это чинится дёшево.** У МатЭка есть авторские `.tex` — 569 путей из
569 резолвятся. Правка это **перенос** текста между полями, буква в букву:
без модели, без трат, без риска переписать содержание.

**Почему стоит — три причины:**

1. ⚠️ **Исходников нет на диске.** Они в частях 2 и 3 переездного пакета
   (`qls_part2_materials.zip`, `qls_part3_materials.zip`), распакована только
   часть 1. Код ждёт их по пути
   `materials/Archive 6/Archive 5/Archive 3/Archive 2/Overleaf Projects`.
   Пока их нет, прототип честно опирается только на базу.
2. ⚠️ **Детектор по виду текста не годится, замерено.** На 14 задачах, про
   которые владелец прямо написал «г д е это решение к абв»: «вторая половина
   длиннее первой» бесполезен (отношение гуляет 0,24 … 6,8), «есть формула»
   бесполезен (пропускает гуманитарные разборы). Детектор со всеми признаками
   нашёл **2 случая из 14**. Форма меток («подряд, чётное число») ловит 12 из
   14 — но она только **необходима**, не достаточна. Кто из половинок решение,
   знает лишь оригинал или человек.
3. ⚠️ **Сверка затронет и одобренные.** Форму держат 216 из 923 дефектных —
   и **191 из 1 709 «идеальных»**. Эти 191 сейчас опубликованы. Трогать их
   вслепую нельзя: у задачи законно бывает шесть вопросов.

**Охват честный:** уверенно ~94 задачи (там и жалоба человека, и разрезаемая
форма), с проверкой оригиналом — до 216 из 923. Не половина, а четверть.
Остальные ~700 дефектов не про структуру, сверка их не чинит.

**Состояние:** команда `matek_zero_step` только читает. Прототип на 30 задачах,
шлюз пройден 28, провален 0, у двух формы нет. **В базу не записано ничего.**
План — `materials/matek_zero_step_plan.md` в этом архиве.

**Порядок разблокировки:** распаковать части 2 и 3 → прогнать сверку на 12
прямых случаях и глазами сверить с `.tex` → завести профиль разбора на каждый
из 12 корневых проектов → прогнать по 216 с формой, включая 191 «идеальную» →
применять только после стоп-гейта, с бэкапом.

⚠️ **Шлюз здесь сверяет запись целиком, а не поле с полем.** Перенос законно
меняет содержимое каждого отдельного поля, и полевой шлюз покраснел бы ровно
на том, ради чего починку затевали (в прошлой сессии 37 ложных провалов из 64).
`record_tokens()` собирает числа и знаки со всей записи одним списком.

---

## 7. Волна 1 починки по Сборнику АА — что сделано

Боевой прогон схемы «человек пометил дефект → модель чинит → человек смотрит
результат». 131 задача пакета `aa_20260728`.

| | |
|---|---:|
| задач в пакете | 131 |
| с правкой (`action=fixed`) | **87** |
| отказов | 44 (14 «чинить нечего», 30 «дефект непонятен») |
| медиана правки | 2 символа |
| среднее | 10 символов |
| не прошли шлюз «не навреди» | **18 из 87** |
| прибавилось ошибок KaTeX | **ни у одной** |

Признаки у непрошедших: изменилась последовательность чисел и знаков — 14;
цифры в отрисованном тексте другие, то есть тихая потеря числа — 10; числа
иначе разбиты при тех же цифрах — 3. Ничего не отбраковано намеренно: на
экране разбора видна пометка шлюза, и оценка человека заодно измеряет сам шлюз.

**В базу не записано ничего** — починки лежат файлами в
`reports/repair_wave1/` (`aa_repairs.json` в этом архиве). Оболочка разбора —
`reviewer.html`, пять исходов, клавиши 1–5.

⚠️ **Исходы починки — отдельный словарь** (`problems/repair_outcomes.py`), не
тот же, что категории дефектов. Первый отвечает «что вышло у ПОЧИНКИ», второй —
«что не так с ЗАДАЧЕЙ». Смешав их, потеряли бы счёт. `fixed_partial` и
`not_fixed` в базе неразличимы — правда об исходах живёт в файле оценок.

---

## 8. ⚠️ Чего в базе НЕТ — читать перед выгрузкой

Тексты задач в этом архиве **не содержат**:

- починок волны 1 по АА (87 правок — только файлами);
- нулевого шага МатЭка (только план и предпросмотр на 30 задачах).

Что в базе **есть** из прошлых ИИ-проходов: Батч 1 (поля, теги, флаги),
Батч 2 (чистка текста, 4 930 задач), `fix_latex_junk` (834),
`glue_pdf_lines` (4 159), чистка ILE патчами `apply_ile_cleanup`.

Для эмбеддингов это не помеха: пересчитывать всё равно придётся после каждой
волны починки по изменённым id.

---

## 9. Открытые решения — ждут Макара

1. **93 задачи trash** — скрыть навсегда или удалить. Рекомендация: скрывать
   (правило проекта — ничего не удалять безвозвратно).
2. **Доразметка тем.** В каталоге осталось 5 090 задач, тему имеет около 37 % —
   примерно 1 880 против прежних ~8 500. **Поиск по темам ослаб вчетверо.**
   Механизм есть: автотемы, точность 92,5 % при пороге 0,70 и покрытии 31 %.
3. **Перепроверить июльский свип.** `sweep_field()` в
   `problems/batch2_unblock.py` устроен как полевая сверка, и им 21 июля
   откачено 1 762 поля. Часть откатов могла быть ложной — висит карточка
   «Починить 195 задач, испорченных нашим же откатом».
4. **Node.js не установлен** — два признака проверки одобренных на дыры
   (ошибки KaTeX и слипшиеся числа) не измерены вовсе.
5. **Распаковать части 2 и 3** ради исходников МатЭка (см. раздел 6).
6. **Пакеты `reports/defect_review/*` собраны до правки `selectionText()`** и
   несут старый код цитирования — пересборка ждёт решения.

**Закрыто и пересматривать не надо:** снимать `approved` по признаку из отчёта
о дырах нельзя. Проверено поштучно на 60 задачах: «форумная реплика в решении»
(51) — 94 % ложных, «пустое условие» (9) — 100 % ложных, это листки, у которых
общего условия нет по устройству. Из 60 кандидатов снятия заслуживают 3,
максимум 5.

---

## 10. Грабли — повторить очень легко

⚠️ **Полевая сверка против сверки записи целиком.** Шлюз, сравнивающий поле с
полем, краснеет на законных переносах текста между полями — то есть ровно на
том, ради чего починку затевали. 37 ложных провалов из 64 в эксперименте.

⚠️ **Детекторы качества негодны для отбора.** Ловят 80 % дефектов, но зря
флагают 60 % идеальных. Полнота по сломанным формулам 1 %, по утёкшим решениям
0 %. `merged_list` срабатывает на 55 % идеальных — это шум, а не детектор.
Единственный достоверный измеритель — человеческое ревью.

⚠️ **Текстовый предфильтр к ошибкам KaTeX слеп** — замерено на 1 762
откаченных полях. Рендерить надо всё.

⚠️ **`innerText` и `getSelection()` включают скрытый MathML.** Формула
читается трижды: HTML-рендер, `annotation` с исходным TeX, MathML. Мерить и
цитировать надо на клоне узла без `.katex-mathml` и `annotation`.

⚠️ **Несуществующая команда (`\myarray`) не даёт видимой ошибки.** Движок молча
её выбрасывает, соседние числа слипаются («31» + «8p» → «318p»). Проверять не
только `.katex-error`, но и фактический отрендеренный текст.

⚠️ **CSS-классы, совпадающие с внутренними классами KaTeX** (`text`, `mord`,
`base`, `strut`, `mfrac`, `sqrt`). CSS матчит по токену — рамка протаскивается
прямо в формулу. Всегда префикс `qls-`.

⚠️ **Сырой текст в превью читается как порча.** Правильная разметка формул
выглядит мусором. Показывать отрендеренный вид по умолчанию, исходник — по
кнопке. На этом уже спотыкались с #859: чистка отработала правильно, а
выглядело как поломка.

⚠️ **Слово «тест» значит разное в разных пакетах.** У ILE семь таких вердиктов
оказались достоверными: «тест» — это тип задачи (выбор варианта ответа), ровно
как 166 таких комментариев у МатЭка. Фильтр должен быть пакет-специфичным и по
списку id, а не по тексту комментария. Поиск по слову уничтожил бы 166 честных
вердиктов.

⚠️ **`$…$` не признак математической задачи.** Такие задачи ломаются реже,
потому что доллары означают уже сделанную нормальную разметку, а не математику.
Правильный признак — числа в тексте: в ILE задачи с числами дают 22,6 % брака,
чисто текстовые 8,5 %.

⚠️ **Без сверки с сырым исходником суждение «кто испортил поле» — догадка.**
На МатЭке две из трёх версий «испортил наш откат» оказались неверны: в одном
случае число придумала модель ещё раньше, а пробел вырезал импортёр; в другом
сломанную формулу написал сам автор.

⚠️ **`PYTHONUTF8` на Windows.** Без неё вывод с кириллицей в файл обрывается на
середине молча. `setx PYTHONUTF8 1` не доходит до уже запущенных процессов —
кодировку вывода чинит `manage.py::force_utf8_output()`, но чтение файлов не
чинит никто: в скриптах всегда писать `encoding='utf-8'` явно.

⚠️ **Ревьюер под двумя никами** — `анич` и `макарчмо2008`. Это один человек.

---

## 11. Карточки в Notion

Штаб: https://app.notion.com/p/39ab11c92bc181f7bbe1efa8a89e38a9

| Тема | ID страницы |
|---|---|
| На сайте только проверенное человеком | `3c1b11c92bc181ce8ecdf4a05c95df19` |
| Детекторы качества негодны | `3abb11c92bc18133aea8c3ee9d8716bd` |
| Чистка дефектов ILE по вердиктам Анича | `3abb11c92bc181f4941ed7dd0305f921` |
| Разбор вердиктов Сборника АА | `3b6b11c92bc181f9b1f8d469a7affff8` |
| Разбор вердиктов МатЭк (35,1 % брака) | `3c0b11c92bc181d0840deae2bcfb1232` |
| МатЭк отдаём на ревью целиком | `3b6b11c92bc1815a97c8d06e56995f20` |
| Дыры в одобренных | `3c1b11c92bc181fa8cc4ee1700298673` |
| Перепроверить июльский свип | `3c0b11c92bc181a2a939fcc5b654d4d6` |
| Эксперимент: чинит ли модель по указке | `3c0b11c92bc181b2b03de0d2f9a8ded9` |
| Исходники покрывают 61 % каталога | `3b6b11c92bc1814c8d6aecd682c13cb6` |
| Ручное ревью внешнего вида (дорожная карта) | `3a1b11c92bc181628e2bcc91bb2446a8` |
| Передать рабочую копию Аничу (Windows) | `3bfb11c92bc18159bca6ceb97680345d` |

---

## 12. Что делать дальше, по порядку

1. Посчитать эмбеддинги на всём массиве — ради этого архив и собран.
2. Аничу разобрать 131 задачу волны 1 → получаем **первое настоящее число**:
   какую долю модель чинит с первого раза и совпадает ли мнение шлюза с
   человеком.
3. С честным коэффициентом запускать волну по ILE (482) и МатЭку (923).
4. Параллельно: распаковать части 2 и 3, поставить Node.js, доразметить темы.
5. Переподнять прод и залить локальное состояние.
