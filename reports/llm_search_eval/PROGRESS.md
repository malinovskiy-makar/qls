# LLM-слой над поиском — офлайн-замер

Ветка `feat/llm-search-eval`, начата 09.09.2026.
Состояние машинно-читаемо — `STATE.json`. Здесь — человеческий журнал по фазам.

---

## Фаза −1. Сверка с реальностью — СДЕЛАНА, есть расхождения

База не менялась: все запросы только на чтение. Отпечатки для сверки в конце
сессии записаны в `STATE.json` (`fingerprint_problem_md5`, `fingerprint_part_md5`).

### Что сошлось

| Что | Ожидалось | В базе |
|---|---|---|
| `Problem` | 41 307 ± | 41 307 |
| `EconConcept` | 1 886 | 1 886 |
| Канонические темы | 29 | 29 |
| Канонические теги | 344 | 344 |
| `title_candidate` непустой | ~37 тыс. | 40 425 |
| Дедуп-группы | 4 571 | 4 571 |
| Видимо поиску (`base_queryset('catalog')`) | ~14 тыс. либо ~26,5 тыс. | **14 082** |

`hidden_pending_review` НЕ снят. Все метрики сессии считаются относительно
14 082 задач. У всех 14 082 есть вектор.

Файл базы, на который смотрит Django: `C:/Users/shipu/qls/db.sqlite3` (SQLite).

### Расхождение 1. C58 — подмножество C65, «119 запросов» не существует

`problems/data/eval_set_c.json` — 58 случаев, `problems/data/eval_set_c_v3.json` —
61 случай. Сверка по нормализованному тексту (нижний регистр, «ё»→«е», без
пунктуации, схлопнутые пробелы): **все 58 формулировок C58 есть в C65**.
Различных формулировок в объединении — **61**, а не 119.

У каждого случая ровно один `relevant_id`; случаев с двумя якорями нет.

Это задевает не только эту сессию. Карточки решений от 08.09 («Формула
отпечатка: ввозим v2_meta_first») и 09.09 («Порог снижен с 0,55 до 0,40»)
называют «119 запросов (C58 + C65)». Там 58 формулировок посчитаны дважды, и
средние по «119» перевешены в их сторону. Порядок вариантов формул это вряд ли
переворачивает, но цифру «119» в прошлых карточках стоит читать как «61
различная формулировка, 58 из них учтены дважды».

### Расхождение 2. Десять размеченных запросов — внутри тех же 61, а не рядом

`reports/formula_choice/razmetka_336.json` — 336 строк, 10 запросов, шкала
`good` 183 / `unsure` 111 / `bad` 42. Все десять взяты из `eval_set_c_v3`
(случаи #44, #53, #42, #61, #2, #7, #46, #22, #62, #55) — то есть входят в те
же 61. Значит «129 = 119 + 10» из дополнения к заданию — тоже двойной счёт.

**Честный эталон сессии: 61 различная формулировка, у 10 из них есть ручная
разметка владельца.**

### Расхождение 3. В банке v2_focus_repeat, а не v2_meta_first

`ACTIVE_SPEC_NAME = 'v2_focus_repeat'` (версия 95); все 41 307 векторов версии
95, сборка `bge-m3/st-fp32`, смеси формул нет. Переключение сделано сегодня
коммитом 51ca189, `build_check` от 09.09 18:06 даёт `min_cosine` 0,9999999 на
200 строках — файл и банк сходятся.

Причина переключения видна в `reports/formula_choice/phase3_result.json`: на
ручной разметке пула `v2_focus_repeat` обходит `v2_meta_first`
(precision@10 0,65 против 0,60; nDCG@10 0,852 против 0,811). То есть реальность
ушла вперёд задания, а не сломалась.

Побочное наблюдение, важное для ожиданий от сессии: на ручной разметке базовый
плотный поиск уже даёт nDCG@10 ≈ 0,85 и precision@5 ≈ 0,66. Прежние «nDCG ≈
0,12» — это метрика по одному якорю, она меряет другое. Запас у LLM-слоя
меньше, чем кажется по 0,12.

### Расхождение 4. Ветка взята не от `main`

Задание просило ветку от `main`. На `main` НЕТ `problems/data/eval_set_c_v3.json`
(набор C65 — половина эталона) и НЕТ `problems/embedding_formula.py` со
спецификацией активной формулы. От `main` эксперимент не собирается.
Ветка `feat/llm-search-eval` создана от `51ca189` (`feat/embedding-formula-v2`).
Переделать — одна команда, если владелец решит иначе.

### Расхождение 5. `EconConcept` не имеет алиасов

У модели два поля: `canonical` и `section`. Поля алиасов нет. Сопоставление
`concepts_free` со словарём будет по `canonical`, точным совпадением после
нормализации регистра и «ё» — как и просило задание, но без алиасной ветки,
которой не существует.

### Ключа OpenRouter нет

`OPENROUTER_API_KEY` не задан ни в окружении процесса, ни в пользовательских
переменных Windows, ни в `.env` (файла `.env` в проекте нет). Без ключа фазы 1
(`S_concept`), 2 и 3 не запускаются.

### Лексическая нога не собрана

`catalog/lexical_bm25.py` нет, `bm25s` не установлен, `pymorphy3` 2.0.6 стоит.
`catalog/hybrid.py` на месте — старый `contains` + RRF k=60, код-сирота.
`Claude outputs/PROMPT_HYBRID_EVAL_20260909.md` — задание на сборку ноги,
которое НЕ выполнялось. Ногу собираю в фазе 1.

### Модели на OpenRouter

`GET /api/v1/models` → 200, 430 моделей. Все семь нашлись точными слагами,
замены не требуются. Цены — в `STATE.json` и в смете ниже.

---

## Фаза 0. Смета — посчитана, стоп-гейт 1 НЕ пройден

Длина карточек замерена на 200 случайных видимых задачах (`tiktoken`,
`o200k_base`), не на глаз:

| Карточка | среднее | медиана | p90 | максимум |
|---|---|---|---|---|
| судьи (фаза 2) | 349 | 350 | 468 | 541 |
| реранкера (фаза 3) | 163 | 159 | 214 | 253 |
| префикс карты (29 тем + 344 тега) | 4 213 | — | — | — |

Карточка реранкера в задании оценена в ~100 токенов; реально 163.

Смета — `estimate.py` / `estimate.json`, при 61 запросе и пуле 110 кандидатов
на запрос. В выход заложен запас на рассуждение: ×4 для GPT-5.6 Sol и Terra,
×2 для DeepSeek V4 Pro и Claude Sonnet 5.

Итого **$18,39**, с 15 % на повторы и отказы — **$21,15** при бюджете $35.

Размер пула станет известен только после фазы 1. Основные деньги (фаза 2,
$12,0 из $18,4) тратятся уже после этого, так что смета будет пересчитана по
фактическому пулу до первого платного вызова судей.

---

## Дальше

Ждём ответа владельца на стоп-гейт: эталон 61 запрос, формула
`v2_focus_repeat`, ветка от `feat/embedding-formula-v2`, ключ OpenRouter.

---

## Ответы владельца на стоп-гейт (09.09.2026)

1. Эталон — **61 различная формулировка**, десять с ручной разметкой внутри как
   контроль. Все «119» и «129» в задании читаются как «61». Дополнительно
   записать деление на короткие и описательные.
2. S0 — то, что в банке: **v2_focus_repeat, версия 95**. Спека и версия
   зафиксированы в `STATE.json` и во всех отчётах. Карточку решения о
   переключении владелец завёл сам.
3. Смета **$21,15 при бюджете $35 утверждена**, судья GPT-5.6 Sol остаётся.
4. Ключ владелец кладёт в `.env` сам. До его появления — всё, что не требует API.

---

## Фаза 0. Провайдер, счётчик, разбор ответа — СДЕЛАНА

`OpenRouterProvider` в `problems/ai/providers.py`: один ключ на семь моделей,
модель — параметр вызова, стоимость запрашивается явно (`usage: {include: true}`)
и читается из ответа. `Reply.cost_usd = None`, когда посредник промолчал: ноль
читался бы как «бесплатно» и занижал бы жёсткий счётчик.

Повторов в провайдере нет намеренно — ни один из четырёх соседей по файлу не
повторяет запрос сам, и `core.run()` тоже. Повтор с паузой (2 с, 4 с), журнал
отказов и счётчик бюджета живут в `reports/llm_search_eval/orclient.py`.

Журнал отказов пишется **до** паузы и с `fsync`: обрыв процесса во время
ожидания иначе стирает причину отказа. Это прямой урок run2.

Разбор ответа терпит три вещи, которые run2 видел живьём: массив вместо
объекта, обрезанный хвост и обрамление ```` ``` ````. Неразобранный ответ даёт
исключение, а не пустой объект: пустой объект читался бы как «модель всё сочла
негодным».

⚠️ **Django `.env` не читает вовсе** — `config/settings.py` берёт всё из
`os.environ`. Ключ, положенный в `.env`, сам собой в процесс не попадёт, поэтому
загрузку файла делает код замера (`orclient.load_env_file`), а провайдер
по-прежнему знает только окружение. Уже заданная переменная сильнее файла.

## Фаза 1. Пул — СДЕЛАНА без ноги S_concept

| Нога | Что это | Время |
|---|---|---|
| S0 dense | плотный поиск по v2_focus_repeat, топ-30 | индекс 3,1 с; 61 запрос закодирован за 25,4 с |
| S1 bm25 | pymorphy3 + bm25s, топ-30 | сборка индекса 31,6 с; поиск 1,3 мс на запрос |
| S2 rrf | слияние S0 и S1 по рангам, k = 60 | — |
| S_concept | разбор запроса моделью | ждёт ключ |

Корпус — 14 082 видимых поиску задачи. Пул: от 31 до 58 кандидатов на запрос,
среднее 48,3. Инварианты проверены на каждом запросе: размер в границах 30…200,
только видимые задачи, без повторов, ни одной пары из дедуп-группы (убрано 228
дублей, у каждого записано, кто остался вместо него).

**Ноги находят разное.** Пересечение топ-30 плотной и лексической ног — в
среднем 8 кандидатов из 30. Это и есть причина пулинга.

**Якорь старого эталона в пуле только у 26 запросов из 61.** В сыром
объединении топ-30 обеих ног он есть у 30; ещё у 4 запросов якорь убран как
дубль в пользу фаворита группы, то есть представлен почти такой же задачей. У
31 запроса из 61 якорь не попадает в топ-30 ни одной ноги. Старую метрику по
одному якорю на пуле восстановить нельзя — она остаётся справочной.

**Коротких запросов по эвристике 2 из 61** (`q51`, `q57`). Живые формулировки
владельца почти все описательные, и деление по типу запроса в таком виде
метрику не разделит. Файл `query_types.csv` лежит рядом и правится руками.

## Предварительные числа: три ноги на ручной разметке владельца

Десять запросов, размеченных владельцем (336 пар). Ни одного вызова модели.

| система | P@5 | P@10 нижняя | P@10 верхняя | nDCG@10 | ≥3 годных в топ-5 | размечено в топ-10 |
|---|---|---|---|---|---|---|
| S0 dense | 0,580 | 0,440 | 0,593 | 0,623 | 70 % | 73 % |
| S1 bm25 | 0,200 | 0,210 | **0,654** | 0,268 | 10 % | **30 %** |
| S2 rrf | 0,480 | 0,290 | 0,582 | 0,434 | 50 % | 47 % |

⚠️ **Читать эту таблицу как «плотный поиск победил» нельзя.** Ручная разметка
снималась с другого пула — топ-10 шестнадцати вариантов формулы отпечатка, то
есть с кандидатов, которых находит плотный поиск. Поэтому у S0 размечено 73 %
топ-10, а у S1 — только 30 %, и строгая метрика наказывает лексику не за плохую
выдачу, а за то, что её кандидатов никто не смотрел.

Среди тех кандидатов, которые владелец видел, лексическая нога точнее плотной
(0,654 против 0,593). Истинное значение S1 лежит где-то между 0,21 и 0,65.
Сузить этот разброс — ровно то, ради чего в фазе 2 размечается весь пул.

## Тесты и зубастость

| Что | Тестов |
|---|---|
| `OpenRouterProvider` | 15 |
| `orclient` (бюджет, повторы, журнал, разбор JSON, `.env`) | 22 |
| `poolbuild` (RRF, дедуп, тип запроса, инварианты) | 20 |
| `metrics` (precision, nDCG, покрытие, якорь) | 21 |
| `concepts` (словарь, «ё», регистр, несловарное) | 12 |
| `lexical_bm25` (леммы, отпечаток, поиск, диск) | 14 |

Все тесты писались до кода и краснели по правильной причине. Прежние 44 теста
`test_ai_providers` зелёные — `Reply` изменился совместимо.

**Зубастость: 26 мутаций из 26 покраснели.** Первый прогон дал три выживших, и
каждая оказалась находкой, а не формальностью:

- «представителем группы становится первый попавшийся» — тест не отличал
  фаворита от первого, потому что в примере фаворит и стоял первым. Тест
  переписан: фаворит теперь второй;
- «неразобранный ответ отдаёт пустой объект» — мутация правила не ту ветку и
  дефект не возвращала. Исправлена сама мутация;
- «массив вместо объекта роняет всю пачку» — дефект **стенда**, а не кода.
  Отметка времени в `.pyc` хранится с точностью до секунды, годность кэша
  решается парой «время + размер». Две мутации подряд в одном файле, укоротившие
  его на одинаковое число байт в пределах секунды, неотличимы, и Python брал
  `.pyc` от предыдущей. Лечится `PYTHONDONTWRITEBYTECODE=1` в стенде.

`manage.py check` — 0 ошибок.

## Не сделано и почему

- **S_concept, фазы 2, 3, 4** — нужен `OPENROUTER_API_KEY`. На 09.09 22:20 файла
  `.env` в проекте нет.
- **`bm25s` не добавлен в `requirements/`** — модуль к каталогу не подключён, а
  правка боевых зависимостей ради офлайн-замера меняла бы сборку прода. Импорт
  ленивый, файл на проде безвреден. Добавлять — вместе с подключением ноги к
  каталогу, отдельным решением.
