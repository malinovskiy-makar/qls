# A0. Диагноз на данных (18.09.2026, ночь; только чтение)

База: локальная рабочая SQLite `db.sqlite3` (не бой). Каталог видит 14 082 задачи
(`filters.base_queryset('catalog')`).

## 1. Снимки обратной связи

| Что | Число |
|---|---|
| `Feedback` всего | 5 |
| со снимком | 4 |
| `page_key` у записей со снимком | textbook 2, olympiads 1, home 1 |
| `screenshot_note` у записей без снимка | `''` (1 запись) |

Записей меньше пяти со снимком — диагноз разведки на данных ни подтверждается,
ни опровергается (локальные записи — ручные пробы разработки, страницы короткие).
Подтверждение — браузерная проба A1.1 и тесты A1.3.

## 2. Профили (инвариант миграции A3)

| Что | Число |
|---|---|
| `UserProfile` | 24 |
| с заполненным `grade` | 5 |
| распределение `grade` | NULL 19, 11 → 2, 9 → 2, 10 → 1 |
| непустой `city` | 6 |

## 3. Пул игры (инвариант A7)

| Что | Число |
|---|---|
| вопросов в пуле | 8 610 |
| тегов в `pool_tags()` | 305 |
| из них `kind='canonical'` | 305 (неканонических 0) |
| уникальных имён тем в `GameQuestion.topics` | 17 |
| из них вне канона (нестрого: регистр, «ё», тире, кавычки) | 4 |

Имена тем вне канона (вопросов с этим именем):
- «Инфляция и безработица» — 300
- «Рынок труда» — 300
- «Спрос и предложение» — 900
- «Теория фирмы: производство и издержки» — 300

Числа по 300 — похоже на вопросы из архетипов-генераторов. В окне игры эти
темы не видны вовсе (окно рисует темы из канонической раскладки), и
отфильтровать такие вопросы по теме нельзя. Данные пула не чиним (решение
владельца 18.09) — карточка в Notion.

Локально неканонических тегов в пуле нет; правка `pool_tags()` (A7.2) всё равно
нужна — на бою пул мог собираться до чистки тегов 17.09.

Числа `pool_counts_for` ДО правки A7 (инвариант):

| Фильтр | bullet | blitz | rapid | classic | figure |
|---|---|---|---|---|---|
| все | 2 293 | 3 376 | 611 | 2 188 | 0 |
| только микро | 1 762 | 2 207 | 277 | 1 744 | 0 |
| «Эластичность» + тег 652 «Оптимум монополиста: MR = MC» | 4 | 17 | 12 | 5 | 0 |

## 4. Чат

| Что | Число |
|---|---|
| `ChatTurn` | 0 |
| ответ ровно 900 / 1 200 знаков (обрезанные) | 0 / 0 |
| с вложением | 0 |

Локально чатом не пользовались; число обрезанных на бою этой базой не
измерить — посчитать на бою той же выборкой (`Length('reply') in (900, 1200)`).
