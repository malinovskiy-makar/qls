# Полировка сайта к бета-тесту — журнал сессии

**Ветка:** `feat/beta-polish-0904` от `main` (`6b84061`)
**Начата:** 2026-09-04
**Источник задания:** 26 правок владельца от 03–04.09.2026, промпт «Полировка сайта к бета-тесту»

Журнал читается ПЕРВЫМ и обновляется ПОСЛЕДНИМ в каждой фазе.
При обрыве контекста: фазы со статусом ✅ не повторять — перечитать их коммиты
(`git log --oneline`), сверить чистоту дерева, продолжить со следующей.
Фаза 🔄 — посмотреть `git status` и `git diff`, доделать начатое.

---

## Таблица фаз

| Фаза | Что | Статус | Коммит | Осталось / вопросы |
|---|---|---|---|---|
| −1 | Сверка с реальностью | ✅ | `e98e7d0` | два расхождения, см. ниже — оба не блокируют |
| 0 | Библиотеки в репозиторий (п. 16) | ✅ | `568a979` | закрыты три молчаливых 404; карточка Notion про FullCalendar закрывается |
| 1 | Пять блоков тем (п. 2, 6) | ⬜ | — | |
| 2 | Тексты и точечные починки (п. 1, 3, 7, 8, 9, 13, 14, 15, 20, 21ч) | ⬜ | — | |
| 3 | Шапка и ширины (п. 4, 5, 10, 11, 12ч, 17ш, 19ч, 21ч) | ⬜ | — | |
| 4 | Аккаунты: регистрация, вход, профиль (п. 17, 19) | ⬜ | — | |
| 5 | Группы по коду приглашения (п. 22, 21ч) | ⬜ | — | |
| 6 | Одна клавиатура формул (п. 18, 23) | ⬜ | — | |
| 7 | Обратная связь и связь с админами (п. 26) | ⬜ | — | |
| 8 | Инспекция, кросс-аудит, стресс, безопасность (п. 12, 24, 25) | ⬜ | — | |
| 9 | Полный прогон, документы, Notion, сдача | ⬜ | — | |

---

## Фаза −1 — сверка с реальностью (✅)

### 1. Состояние репозитория — сошлось

- `git branch --show-current` → `main`
- `git rev-parse HEAD` → `6b8406110d9d7e24afb8e796e6e0dc10197eb143`
- `git rev-parse origin/main` → тот же `6b84061`, ровно как ожидал промпт
  («Длинное тире: три места…», 03.09)
- Несохранённых правок в отслеживаемых файлах — **нет**. В дереве лежат три
  НЕотслеживаемых объекта, к работе отношения не имеющих и в ветку не берущихся:
  `.txt`, `claude/`, `session_c15_transcript_20260830.md`.

### 2. Параллельные ветки — редизайна каталога впереди `main` НЕТ

`git worktree list` — двенадцать рабочих копий. Проверено прямым счётом
коммитов поверх `main` по файлам каталога
(`catalog/filters.py`, `catalog/templates/catalog/problem_list.html`,
`catalog/taxonomy_map.py`):

| Ветка | Коммитов по файлам каталога поверх `main` |
|---|---|
| `feat/smart-catalog` (`qls_palette`) | **0** — ветка стоит на том же `6b84061`, своих коммитов нет вовсе |
| `feat/topic-map` | 0 |
| `feat/topic-map-v2` | 0 |
| `feat/taxonomy-v2-openai-provider` | 1 |

Вывод: редизайн каталога (решения от 04.09 в Notion) **ещё не начат в коде**,
конфликтовать не с чем. Правило сессии остаётся прежним и соблюдается:
вёрстку каталога (поле поиска, окно фильтров, карточки) не трогаем,
группировку тем делаем общим модулем `problems/sections.py` (Фаза 1),
который редизайн потом переиспользует.

### 3. Версии — сошлось

- `venv313/Scripts/python.exe --version` → **Python 3.13.12**
- `manage.py --version` → **Django 5.2.17**
- `docker compose -f docker-compose.dev.yml ps` — контейнеры **были потушены**,
  подняты штатной командой `up -d`: `qls_postgres_dev`, `qls_redis_dev`,
  `qls_search_dev` запущены.

### 4. Проверки Django — сошлось

- `manage.py check` → `System check identified no issues (0 silenced)`
- `manage.py makemigrations --check --dry-run` → `No changes detected`

### 5. ⚠️ РАСХОЖДЕНИЕ ПЕРВОЕ: известный тест calc2 не красный, а ПРОПУЩЕН

Промпт ждал красный `calc2.tests.test_calc2_math.Calc2MathRegressionTest.test_calc2_control_numbers`.
Факт прогона на чистом `main` (180,7 с):

```
Ran 1 test in 180.695s
OK (skipped=1)
ПРОПУЩЕНА браузерная регрессия calc2
Причина: node-раннер calc2 не уложился в 180с (браузер или CDN)
```

Тест **не падает — он не добегает**. В логе прогона видны ровно те CDN, которые
вкладывает Фаза 0:

```
CSP: script-src-elem заблокировал бы https://cdnjs.cloudflare.com/.../d3.min.js
CSP: script-src-elem заблокировал бы https://cdnjs.cloudflare.com/.../math.min.js
```

**Последствие, которое надо держать в голове всю сессию.** Раннер упирается в
таймаут, ожидая загрузку библиотек из чужой сети. Фаза 0 эту сеть убирает —
значит, после Фазы 0 тест может ПЕРЕСТАТЬ пропускаться, реально добежать и
покраснеть на настоящем дефекте, который уже описан отдельной карточкой Notion
(«calc2_math красный: „Регулятор сцены · правка значения" — поле 40,375 px при
потолке 40», найдено 01.09, воспроизведено на `main`).

Это будет **не поломка от этой сессии, а снятие маскировки**: дефект был, его
прятал таймаут. Тест не чиню и не ослабляю (промпт запрещает, и подгонять число
без разбора запрещено правилами проекта). В Фазе 9 статус будет назван отдельной
строкой с этим объяснением.

Расхождение зафиксировано, но работу не блокирует: оно в пользу плана
(пропуск безопаснее падения) и ни одного шага не меняет.

### 6. ⚠️ РАСХОЖДЕНИЕ ВТОРОЕ: первый свободный номер ADR — 0070, не 0069

Промпт ожидал 0069. Прогон по ВСЕМ локальным веткам
(`for b in $(git branch --format='%(refname:short)'); do git ls-tree --name-only $b docs/adr/; done | sort -u | tail`):

```
docs/adr/0065-olympiads-own-models.md      (main)
docs/adr/0067-content-status-separate-from-hidden.md
docs/adr/0068-rerun-call1-only-carry-call2.md
docs/adr/0069-solution-in-call1-and-glm-effort-high.md   ← занят
```

`0069` уже занят на чужой ветке. По правилу промпта («возьми max + 1 по ВСЕМ
локальным веткам») ADR этой сессии нумеруются **с 0070 подряд**.

Заодно подтверждается уже стоящая карточка Notion «Два ADR с номерами 0050 и
0051 на разных ветках — перенумеровать до слияния»: номера ADR в проекте
раздаются несогласованно, и это ещё аукнется при слиянии веток.

### 7. Notion прочитан

Доступен, читается. Прочитано: база «Задачи» (6 карточек «В работе»,
50+ «Надо»), база «Решения» (25 последних), карточка решения про пять блоков тем
(ссылка из шапки промпта).

**Важно для Фазы 9:** все одиннадцать решений владельца от 04.09 **уже записаны**
в базу «Решения» из Cowork — вендор библиотек, роль при регистрации, общий модуль
клавиатуры, «раунд», ширины 960/860, карта к пяти блокам, обратная связь в базу,
«Учебник» после «Каталога», поля входа по теме, смена пароля без старого, и сама
карточка про эту сессию. **Дублировать их нельзя** — ADR ссылается на карточку,
карточка получает комментарий с номером ADR.

Уточнение по карточке пяти блоков: в теле карточки блок 27–28 назван
«Математика и данные», подпись на экране по решению владельца — короткая,
**«Математика»**. Расхождения нет, взята короткая подпись.

### Числа фазы −1

| Что | Число |
|---|---|
| Рабочих копий git | 12 |
| Локальных веток | 35 |
| Веток с коммитами по файлам каталога поверх `main` | 1 (`feat/taxonomy-v2-openai-provider`) |
| Первый свободный номер ADR | 0070 |
| Прогон известного теста calc2 | 180,7 с, skipped=1, не красный |
| Карточек Notion «В работе» / «Надо» | 6 / 50+ |


---

## Фаза 0 — библиотеки в репозиторий (✅ `568a979`)

Пункт 16. Семь библиотек вложены в `static/vendor/` теми же версиями, что
стояли на CDN.

### Числа фазы

| Что | Число |
|---|---|
| Файлов в `static/vendor/` | 98 |
| Вес папки | 4,5 МБ |
| Точных замен в шаблонах и скриптах | 22 |
| Ссылок на CDN в шаблонах сайта после | **0** |
| Ссылок на CDN в статике приложений после | **0** |
| `collectstatic` боевыми настройками | 278 скопировано, 698 обработано, **код 0** |
| Страниц проверено в браузере | 7 |
| Чужих запросов / ошибок консоли / упавших запросов | **0 / 0 / 0** |
| KaTeX на странице задачи | 26 формул |
| KaTeX в калькуляторе | 307 формул |
| `math-field` с теневым деревом в calc2 | 2 из 2 |
| Ячеек сетки календаря | 35 |
| Новых тестов | 4 (все с проверенной зубастостью) |

### Три молчаливых 404, найденных по спискам файлов пакетов

1. `mathlive@0.110.0/mathlive-static.min.css` — **файла в пакете нет вовсе**
   (есть `mathlive-static.css`).
2. `fullcalendar@6.1.11/index.global.min.css` — в шестой версии отдельных
   стилей нет, они встроены в JS. Мёртвый `<link>` удалён.
3. `fullcalendar@6.1.11/locales/ru.global.min.js` — локали живут в пакете
   `@fullcalendar/core`. **Это было видно на экране:** календарь показывал
   английские дни и месяцы. После правки замер даёт
   `["пн","вт","ср","чт","пт","сб","вс"]` и заголовок «сентябрь 2026 г.».

Закрывает карточку Notion «Календарь целиком зависит от чужого CDN — вложить
FullCalendar в репозиторий или нет»: вложен, и заодно починена локаль.

### Зубастость новых тестов

| Тест | Чем ломал | Покраснел? |
|---|---|---|
| `test_templates_have_no_cdn` | вернул CDN-ссылку KaTeX в `catalog/base.html` | ✅ FAILED |
| `test_app_static_js_has_no_cdn` | вписал домен CDN в `82-input.js` | ✅ FAILED |
| `test_font_folders` | убрал один шрифт из `katex-0.16.9/fonts/` | ✅ FAILED |
| `test_no_source_map_references` | вернул `//# sourceMappingURL=` в бандл | ✅ FAILED |

После восстановления — 4/4 зелёные, `git diff --stat` без следов поломки.

### Два дефекта, которые я внёс сам и поймал на живой странице

1. **`{% static %}` внутри JS-комментария** в `calc2.html` уронил страницу с
   500: Django разбирает шаблон раньше, чем браузер видит `/* */`, и тег без
   аргумента — синтаксическая ошибка шаблона. Комментарий переписан без тега.
2. **Комментарий, закрывший сам себя.** Во второй редакции того же места я
   написал внутри блочного комментария литерал `/* */` — символы `*/`
   закрыли комментарий раньше времени, остаток строки стал невалидным JS, и
   встроенный скрипт не выполнился ВЕСЬ: `window.MATHLIVE_DIR` оставался
   `undefined`, а `fontsDirectory` получал `/fonts` (корень сайта).

⚠️ **Проба этого не увидела и показала «0 ошибок».** Ошибка РАЗБОРА скрипта
приходит в `pageerror`, а не в `console`, а слушателя `pageerror` у пробы не
было. Слушатель добавлен — это была настоящая слепая зона, из-за которой
сломанная страница выглядела здоровой. После правки замер:
`MATHLIVE_DIR = "/static/vendor/mathlive-0.110.0"`,
`fontsDirectory = "/static/vendor/mathlive-0.110.0/fonts"`.

### Ещё одна ловушка: проба меряла страницу входа вместо страницы статистики

Демо-учётки (`student1`, `teacher1`, `admin`) в этой копии базы **погашены**
командой `lockdown_dev_accounts` — вход не удавался, и `/profile/stats/`
отдавала форму входа. Проба честно докладывала «0 ошибок», потому что на
странице входа их и правда нет. Заведены боты
(`scripts/ensure_probe_users.py`: `shot_bot`, `shot_bot_teacher`,
`shot_bot_parent`, пароль локальный), а в пробу добавлен ОСТАНОВ, если после
входа адрес всё ещё `/login`. После этого статистика показывает 5 полотен
графиков вместо нуля.

### Правки вендорных файлов — ровно одна

У `chart.umd.min.js` и `math.min.js` убрана хвостовая строка
`//# sourceMappingURL=…` (38 и 33 байта). Причина: `ManifestStaticFilesStorage`
роняет `collectstatic`, если карты нет, а сами карты весят **4,4 МБ** —
вдвое больше всей папки — и нужны только для отладки чужого кода. Записано в
`static/vendor/README.md`, сторожится тестом.

### Лицензии — не все MIT, вопреки заданию

Задание называло все семь MIT. Проверка по `package.json`:
**D3 — ISC, Math.js — Apache-2.0**, остальные пять MIT. Apache-2.0 требует
сохранять уведомления, поэтому рядом с `math.min.js` положен
`math.js.LICENSE.txt`, на который ссылается первая строка бандла.

### Замер на будущее (для Фазы 3)

Снимки на 380 px показывают **горизонтальное переполнение `scrollWidth 806 >
380` на всех шести страницах с общей шапкой**; калькулятор со своей шапкой
чист. Это в точности карточка Notion «Навигация сайта вылетает по
горизонтали на узком экране (380px)», и теперь у неё есть число: **806 px**.
Чинится в Фазе 3 (пункт 12).

### Снимки

`reports/site_polish_20260904/shots/phase0/` — 7 экранов × 2 темы × 3 ширины
= 42 снимка. Просмотрены: календарь (русский, сетка на месте), статистика в
тёмной теме (5 полотен, радар рисуется).

⚠️ **Замечено для Фазы 1:** на радаре «Разделы экономики» ДВЕ оси подписаны
одинаково — «Микроэкономика». Это и есть жалоба владельца про «рандомные
числа»: секции в `problems/stats.py` собраны по ключевым словам названий.
