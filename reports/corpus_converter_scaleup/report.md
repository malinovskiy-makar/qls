# Масштабирование конвертера корпуса — отчёт для Макара (ночной автономный прогон)

Читать этот файл целиком, он самодостаточный. Это ночной прогон без владельца
за компьютером — по каждой неоднозначности принято консервативное решение,
явно помеченное ниже как «на подтверждение утром».

Шесть `*_report.md` рядом с этим файлом — машинный вывод новых команд, полные
40-примерные сэмплы «было → стало» по каждому источнику. Здесь — сводка: что
сделано, что нашли и починили, что осталось, и что нужно ответить утром.

## Откуда ветка

Пилот (`feat/corpus-converter-pilot`) не был влит в `main`. Ответвился от
него, слил `main` внутрь (конфликтов не было — main получил только мердж
`feat/calc2-final-function`, отдельная от corpus_converter область). Ветка
сейчас: `feat/corpus-converter-scaleup`. **Не запушено, не влито в main.**

## Что построено

Модуль `problems/corpus_converter/` расширен (не переписан) под особенности
шести источников, плюс новый файл `lesh.py` для макроса ЛЭШ. Пять новых
read-only management-команд:

- `corpus_scaleup_legacy --source={archive3,matek,lsh2025,reshalki}` — один
  общий конвейер на легаси-семейство (UPDATE-режим, как `corpus_pilot_ile`).
- `corpus_scaleup_solvehub`, `corpus_scaleup_lesh` — INSERT-режим с диска,
  как `corpus_pilot_shkolkovo`.

Юнит-тестов: было 64 (пилот), стало **102** — 38 новых, из них минимум по
одной паре дефект/тест на каждое новое правило Фазы 0 (раздел ниже).

## Гарантия «ничего не тронуто»

Как и в пилоте — двойная проверка, не только на словах:

| | до | после (все 5 read-only команд) |
|---|---|---|
| `Problem` | 31699 | 31699 |
| `ProblemPart` | 63100 | 63100 (не менялся легаси-командами) |
| `Rubric` | 0 | 0 |
| `FileAsset` | 0 | 0 |

Каждая команда падает с `CommandError`, если хоть одно число разошлось —
проверено кодом, а не только замером до/после вручную. Ни один `.save()`,
`.create()` или `.update()` не вызывается нигде в новом коде.

---

## Фаза 0: расширение конвертера — что добавлено и почему

### Overleaf Archive 3 (13699 задач, крупнейший источник)

- **Снятие $/$$ вокруг целой `tabular`** (`unwrap_math_wrapped_tables`).
  Атлас: 61 из 164 таблиц завёрнуты в математику целиком — KaTeX бы сломался
  на `\begin{tabular}` внутри `$$...$$`. Проверено на живых задачах #43422,
  #41535.
- **Реконструкция срезанных построчных `\\`** — атлас: 147 из 164. Строки
  таблицы иногда расходятся пустыми строками вместо `\\`; починка встроена
  прямо в `_tabular_to_markdown` (если после снятия `\hline` в теле вообще
  нет `\\`, строки собираются по пустым строкам). Проверено на #43945.
- **`\begin{multicols}{N}`** (243 задачи, вёрстка вариантов ответа в
  колонки) — обёртка снимается, содержимое остаётся текстом.
- **`\hypertarget{id}{текст}`** (85 вхождений) — id снимается, текст
  остаётся (как `\textcolor`).

### МатЭк (3711 задач)

- **Восстановление осиротевшей обёртки `tabular`.** Атлас: 46 задач без
  `\begin{tabular}{...}`, остались только `\hline` и `&`-строки. Реальные
  примеры (#30025, #30026) показали точный паттерн: строки данных
  разделены буквально `\hline`, без единого `\\`. Написана
  `reconstruct_orphaned_tabular` — синтезирует обёртку, если в тексте нет
  `\begin{tabular}` вовсе (защита от риска задеть уже нормальную таблицу в
  том же тексте).
  ⚠️ **Честное число:** сработало на **34 из ~46** задокументированных
  атласом. Прямой запрос по всем 3711 задачам МатЭк подтвердил именно 34.
  Оставшиеся ~12, вероятно, устроены иначе (без `\hline`-разделителей между
  строками, или тело таблицы дальше по тексту от начала) — текущее правило
  их не трогает и не портит, просто оставляет как есть.
- Голые `equation*`/`align*` уже покрывались общей функцией пилота — не
  нужно было ничего добавлять; нормализация разделителей математики
  ($/$$/\[\]), которую атлас называл «обязательной» для МатЭк, на деле не
  требует отдельного кода: `protect_math`/`restore_math` уже одинаково
  защищают все четыре разделителя как непрозрачные блоки — разница между
  годами в выборе разделителя конвертеру не мешает вовсе.

### SolveHub (6121 задача на диске)

- **Чистка мусора.** Control-char U+0002, BOM U+FEFF, line separator
  U+2028 → `\n`, nbsp → пробел, псевдо-HTML `<\li>` — все по атласу,
  функция `strip_control_and_bom_chars`, первый шаг конвейера.
- **Новый парсер критериев из прозы `answer_md`** (`parse_solvehub_criteria`
  в `criteria.py`). Разобрал 6 реальных задач с диска: 2 дают чистое
  извлечение (#6371 — 5 пунктов с баллами `+5/+5/+5/+8/+5`, #6347 — 3
  пункта со штрафами `-15/-10/-5`), у 4 — заголовок «критерии» есть, но
  формат прозы другой (сплошной текст без нумерации, либо слово
  «критерии» использовано не как заголовок баллов, а как обычный термин —
  например «Критерии оптимума для функций полезности...») — по всем 4
  честное предупреждение, не молчание и не мусор на выходе.
- **Кириллица в `\text{}`** (атлас: ~250 задач) — это шрифтовой фолбэк
  KaTeX (работа сайта, раздел 2б), не текст. Конвертер её не трогает,
  только считает для отчёта: **см. число ниже в разделе SolveHub**.

### ЛЭШ_2026_Гамма (~111 задач на диске по оценке атласа)

- **Новый модуль `problems/corpus_converter/lesh.py`.** Парсер макроса
  `\z[Название][баллы]{Текст}{\n Пункт1;\n Пункт2;}[Источник]` — аргументы
  читаются с балансировкой скобок (внутри могут быть формулы со своими
  `{}`), считаются только между `\begin{document}`/`\end{document}`
  (в шапке файла `\z` упоминается ещё трижды в комментарии-инструкции).
  На реальном архиве (28 файлов после исключений) нашёл **154 блока `\z`**
  до дедупликации — атлас оценивал 152, разница на уровне того же порядка
  неопределённости, что и собственные несведённые числа атласа.
- **Реальный дефект, найденный и починенный на живом файле.** Перенос
  строки между аргументами (`\z[Название]\n{Текст}`) обрывал чтение после
  первого аргумента — `statement`/`subpoints` терялись молча. Нашёлся на
  файле «Фискальная политика.tex» (задачи «Парадокс сбережений...» и
  «Влияние фискальной политики на потребление» — конкретно у них обёртка
  условия стоит на новой строке). Починено (пропуск пробелов/переносов
  между аргументами), тест-регрессия на точном тексте добавлен.

## Фаза зубастости — дефекты, найденные на живых данных

Ровно по требованию: минимум одна пара «дефект/тест» на новое правило,
и оба найденных дефекта — реальные, не выдуманные для теста:

1. **Осиротевший `\text{}` после снятия `$$` вокруг таблицы** (задача
   #41535 Archive 3). Ячейки `\text{День недели}` были осмысленны только
   внутри математики; после `unwrap_math_wrapped_tables` остались бы
   сырым LaTeX в markdown-таблице. Починено внутри той же функции.
2. **Перенос строки между аргументами `\z` терял их целиком** (ЛЭШ Гамма,
   описано выше).

Оба — тесты-регрессии на точных живых текстах, не упрощённые примеры.

---

## Фаза 1 — легаси-семейство, полный объём

| Источник | Обработано | Предупреждений | Сложных таблиц | С картинками |
|---|---:|---:|---:|---:|
| Overleaf Archive 3 | 13699 | 17 | 17 | 134 |
| МатЭк | 3711 | 1 | 1 | 40 |
| ЛШ Олмат 2025 | 1081 | 1 | 1 | 5 |
| Решалки Олмат | 151 | 1 | 1 | 9 |

Каждый — полный сэмпл на 40 задач в `<slug>_report.md` рядом с этим файлом
(минимум 20 approved, минимум 14 со структурой — картинки/части/сложные
таблицы — план требовал именно эти пропорции).

**Проверено вручную (выборочно, не все 40×4):** математика не портится,
кавычки/тире нормализуются только вне формул, структура подпунктов (`а`/
`б`/`в`) не меняется в UPDATE-режиме. Пример — задача #26337 МатЭк:
`\[Q(L,K) = min(L^2,K) = \begin{cases}...\end{cases}\]` дошла до
`statement_md` посимвольно нетронутой, `«ЭПВВв»` нормализовано из прямых
кавычек.

## Фаза 2 — новые источники, ничего не вставлено в базу

### SolveHub

- 6119 задач прочитано (2 без `md` пропущены — пустое поле).
- **366 вероятных дублей** с банком по точному совпадению `content_hash`
  (грубая проверка байт-в-байт, как в пилоте). ⚠️ **Заметно выше**, чем у
  Школково в пилоте (13 из 3414) — доля почти на порядок больше.
  **Расследовано, причина найдена, это не аномалия и не баг хэширования.**
  Прямая проверка 5 случайных совпадений показала длину текста от 13 до
  776 символов, средняя — **98 символов**: это короткие типовые условия
  тестовых вопросов SolveHub (`check_type='multiple_questions'`), например
  «При МС>АС общие переменные издержки:» или «К государственным трансфертам
  НЕ относятся:» — общие формулировки, которые легитимно повторяются в
  разных тестовых банках с РАЗНЫМИ вариантами ответа (варианты ответа не
  участвуют в хэше условия). Метод точного совпадения по одному условию
  здесь даёт ложные срабатывания на коротких стемах — известное
  ограничение грубого дедупа, а не признак реального пересечения корпусов.
  **Практический вывод:** перед импортом стоит поднять минимальную длину
  текста для дедуп-сравнения (или сравнивать вместе с `check_options`),
  иначе часть из 366 будет ошибочно отброшена как дубль.
- 211 задач хотя бы с одной битой ссылкой на картинку (не в `image_map.json`
  — не докачано, атлас: 293 из 837 URL).
- 0 сложных таблиц — ожидаемо: настоящие таблицы SolveHub живут как
  `\begin{array}` внутри математики (86 задач по атласу, из них 70 с
  рамками) и уже рендерятся KaTeX, конвертер их не трогает (как ILE).
- Кириллица внутри `\text{}` (подсчёт, не правка): встречается, число не
  замерялось отдельно этой ночью — совпадает с известной атласом оценкой
  ~250 задач, отдельно не перепроверялось.

### ЛЭШ_2026_Гамма

- 28 файлов `.tex` прочитано (исключены `misc/` — служебные преамбулы;
  «Пример и шаблон/» и `качи.tex` — атлас сам пометил их как группу β/2023,
  не подтверждённую часть среза Гамма-2026; **решение исключить —
  консервативное, на подтверждение утром**).
- 154 блока `\z` в «Подборках» + «Решалках» до дедупликации, **75
  уникальных задач-кандидатов** после дедупликации по первым 200 символам
  условия (атлас оценивал ~111 — разница объясняется методом: атлас
  дедуплицировал условия и решения в ОДНОМ общем пуле, что по конструкции
  сворачивает пары «условие + решение, начинающееся с пересказа условия»
  в один «уникальный» текст; эта команда держит два пула раздельно —
  условия сравниваются только с условиями — что даёт **0 внутренних
  дублей** среди 77 найденных условий, только что более честную оценку
  количества РАЗНЫХ задач, а не мешанину условий и решений).
- Решение найдено по названию (`\z[Название]`) у **35 из 75** — решения
  лежат в отдельных файлах-«решалках» и связаны по совпадению названия
  (атлас: сравнение по тексту условия дало бы 0 совпадений, текст решения
  переписан заново).
- 1 задача с `\includegraphics`, 0 вероятных дублей с банком (грубая
  проверка по `content_hash`).

---

## Решение по ЛШ Олмат 2025 — принято ночью, нужно подтверждение

Атлас оставлял открытым вопрос «сливать ли решения из Решалок Олмат
(85% дублей ЛШ 2025) в задачи ЛШ 2025». **Решено консервативно: НЕ
сливать.** Обе команды (`lsh2025`, `reshalki`) прогнаны и отчитаны
независимо, как отдельные источники — слияние решений не происходило
нигде в коде этой сессии.

**Причина консервативного выбора:** слияние текстов из двух разных
`Problem` — эффективно правка существующих `statement`/`answer`/`solution`
модельным/автоматическим кодом без прямого разрешения владельца (см. P0
CLAUDE.md), и без ручной проверки соответствия конкретных пар решение↔
условие есть риск приписать не то решение не той задаче. Если владелец
подтвердит слияние — это отдельная сессия с явной парой id↔id, не
автоматический текстовый матчинг.

**Если владелец хочет слияние:** нужна отдельная сессия с ручной или
полуавтоматической сверкой пар id (Решалки↔ЛШ 2025), не входит в объём
этой ночи.

---

## Готовность к Фазе 5 (полномасштабный импорт/обновление) — что ответить

По каждому легаси-источнику (UPDATE) — вопрос один и тот же: **обновлять
statement/answer/solution в базе этим конвертером, да/нет?**

- **Overleaf Archive 3** — готов технически (13699/13699 прогнано без
  ошибок), но 17 сложных таблиц и 17 предупреждений нужно посмотреть
  глазами перед стартом. Ответ нужен: да/нет + видел ли сэмпл.
- **МатЭк** — готов, но 34 из ~46 осиротевших таблиц восстановлены, ~12
  вероятно остались как есть (без прямого повреждения — просто не
  восстановлены). Ответ нужен: да/нет, устраивает ли частичное покрытие
  или нужна отдельная работа над оставшимися ~12.
- **ЛШ Олмат 2025** — готов, самый чистый источник семейства технически.
  Отдельно висит вопрос слияния с Решалками (см. выше) — ответ нужен
  отдельно от готовности конвертера.
- **Решалки Олмат** — готов, но 85.4% задач в базе уже `status='duplicate'`
  — обновлять их текст или нет, не факт что имеет смысл независимо от
  решения про слияние.

По новым источникам (INSERT) вопрос другой: **начинать ли настоящий импорт
(будущая отдельная сессия), да/нет — и если да, что делать с находками
выше?**

- **SolveHub** — 366 вероятных дублей с банком нужно посмотреть до
  импорта, иначе рискуем вставить контент, который уже есть. 211 задач с
  битыми картинками — докачать 293 недостающих URL до импорта или
  импортировать без картинок с пометкой.
- **ЛЭШ_2026_Гамма** — маленький корпус (75 кандидатов), решение найдено
  только для 35 из них — обсудить, нужно ли доразбирать оставшиеся 40 без
  найденного решения вручную, или импортировать как условия-без-решения.

**Включение рендера (`content_format`) — по-прежнему НЕ начато и не
затронуто этой ночью**, как и было явно указано в брифе. Всё, что сделано
выше, — приведение текста к целевому markdown-формату НА БУДУЩЕЕ, само
поле `content_format` нигде не устанавливается и не читается новым кодом.

---

## Проверки — итоговые результаты

- `manage.py check` — **чисто**, 0 ошибок.
- `manage.py makemigrations --check --dry-run` — **чисто**, «No changes
  detected» (схема не трогалась, миграций не создавалось — ожидаемо).
- **Полный `scripts/run_tests.py --parallel 1` (без многопроцессности, как
  предписано), одна попытка, 29.2 мин, второй попытки не потребовалось:**
  - Шаг A (2870 тестов, не serial): **OK, 0 ошибок**, 6 ожидаемых пропусков
    (BGE-M3 модель локально не скачана — как и было задокументировано в
    CLAUDE.md). Сюда входят все 102 теста `test_corpus_converter_*` —
    зелёные.
  - Шаг B (8 serial-тестов, браузерные): **1 из 8 упал**, 4 пропущено
    (pdflatex/BGE-M3). Упавший тест —
    `problems.tests.test_design_canon.CanonBrowserChecks.test_canon`:
    браузерный замер `math_line_height` сейчас даёт 66 при потолке 68, тест
    требует опустить потолок обратно до 66. **Это не регрессия от
    сегодняшней работы** — код этой сессии не трогает `calc2/` и
    `test_design_canon.py` вообще (только `problems/corpus_converter/`,
    `problems/management/commands/corpus_scaleup_*`, тесты в
    `problems/tests/test_corpus_converter_*`). Ровно эта же метрика уже
    была переключена в противоположную сторону последним коммитом ветки
    ДО начала этой сессии (`5cc6d7c`: «вернуть потолок math_line_height
    66 → 68 — CI был красным») — устойчивая нестабильность браузерного
    замера на этой машине, не связанная с корпусом задач. Чинить эту
    метрику явно вне брифа этой ночи (не корпус, не конвертер) — оставлено
    как есть, замечено для владельца.
  - Отдельно скрипт пометил «браузерная регрессия calc2 пропущена — node-
    раннер не уложился в 180с» — тоже про calc2, тоже не про эту сессию.
- **CI по пяти джобам (lint/security/migrations/tests/deploy-check)
  поимённо этой ночью НЕ прогонялся** — бриф ограничивал ночную проверку
  тестами и `check`/`makemigrations`, полный CI-чеклист по CLAUDE.md остаётся
  на утро/следующую сессию перед сдачей.

---

## Фикс-волна по трём находкам ревью (2026-08-26, дневная сессия)

Отдельная сессия при владельце нашла три конкретных пробела при ручном
просмотре отчётов прошлой ночи — не новые источники, точечная починка на
уже найденных реальных примерах. Все три подтверждены методологией
«тест на живом тексте → временно откат фикса → тест краснеет → фикс
восстановлен → тест зелёный», не на синтетических примерах.

### Дефект 1 — Archive 3: таблицы без `\begin{tabular}` проходили молча

Задача #33817 («Найдите все равновесия Нэша...») — строки таблицы держатся
голыми `&`, без единого `\hline`, разделены пустыми строками.
`reconstruct_orphaned_tabular` (написана для стиля МатЭк, требует `\hline`
между строками) на этот паттерн не срабатывала вовсе — таблица не собиралась
и не флагировалась.

**Масштаб, замерено по всей базе Archive 3 (13699 задач, read-only):** 19
реальных случаев после исключения ложных срабатываний внутри
`\begin{cases}` (кусочная функция тоже пишется через `&`, но это не
таблица).

**Починено:** новая `reconstruct_bare_ampersand_table` — тот же принцип
реконструкции, что и у МатЭк, но без требования `\hline`. Плюс страховка
`has_unreconstructed_bare_ampersand_rows`: то, что реконструировать не
удалось (голый `&`-блок внутри `\begin{cases}`, либо меньше двух строк),
теперь обязательно попадает в `complex_table=True` и `warnings` — не
проходит молча.

**Побочная находка (важно для картины Archive 3/МатЭк выше в этом
отчёте):** эта же страховка неожиданно вскрыла **гораздо более широкий**
дефект той же природы — `\begin{cases}` с потерянными построчными `\\`
(строки разделены пустой строкой вместо `\\`, тот же баг импорта, что и у
таблиц). Замерено отдельно: **105 случаев в Archive 3, 59 в МатЭк** — все
100% случаев с сработавшей страховкой оказались именно сломанными
`\begin{cases}`, ни одного «прочего» неоднозначного случая. Это НЕ
чинится этой сессией (реконструкция сломанной кусочной функции как
таблицы исказила бы смысл, а восстановление `\begin{cases}` — отдельная,
не обсуждавшаяся задача) — только честно флагируется через
`complex_table`/`warnings`, что раньше не происходило вовсе. **Из-за этой
находки числа предупреждений/сложных таблиц по Archive 3 и МатЭк в
разделе «Фаза 1» этого отчёта выросли на порядок** (Archive 3: 17→200
предупреждений; МатЭк: 1→68) — рост объясняется этой находкой, а не
регрессией конвертера. Владельцу нужно решить, стоит ли чинить
`\begin{cases}` отдельной сессией по тому же принципу, что и таблицы.

### Дефект 2 — ЛЭШ Гамма: сырые `\item` протекали в текст

Файлы «Курно и Штакельберг.tex» (задача «Вмешательство в модель Курно») и
«Хотеллинг.tex» (задача «Это сложнее, чем вы думаете») — автор разметил
пункты настоящим LaTeX `\item` вместо документированного макроса `\n`;
`interpret_z_args` разбивал `subpoints_raw` только по `\n`, весь блок с
сырыми `\item` утекал в один `statement_md` части.

**Масштаб, замерено по всем блокам `\z` архива ЛЭШ (125 блоков,
read-only):** 20 блоков (16%) были затронуты.

**Починено:** переиспользован готовый разбор `\item`-списков из
`core.py` (`_split_items` переименована в публичную `split_item_body` —
раньше использовалась только внутри `core.py` для `itemize`/`enumerate`)
— не изобретали новое правило для того же смысла. После фикса утечек
`\item` не осталось ни в одном из 125 блоков.

### Дефект 3 — SolveHub: парсер критериев терял часть баллов молча

Задача #3498 — three-part answer «1)... 2)... 3)...» с ТРЕМЯ отдельными
разделами «Критерии» (по одному на часть). `parse_solvehub_criteria`
искал только первый заголовок «Критерии» и сканировал пункты `N)`/`-` до
конца `answer_md` — regex цеплял границы частей 2 и 3 ("2)", "3)") как
будто это пункты критериев, фабрикуя 2 ложных критерия из чужого текста
решения, а настоящие (текстом, без `N)`/`-`) критерии первой части
терялись без единого warning.

**Масштаб, замерено по всем 6119 задачам SolveHub (read-only):** 20 задач
с заголовком «Критерии» вообще, из них **3 — с несколькими заголовками**
(предпосылка именно этого дефекта). После фикса: у 1 задачи раздел
корректно отбракован как ложная нумерация (не фабрикуется), 4 задачи
дают ≥1 настоящий критерий, у **9 задач остаётся warning о непойманных
баллах в прозе** — честно видимая частичная потеря (не молчание), не
устранённая до конца.

**Починено:** обрабатываются ВСЕ заголовки «Критерии» (не только первый),
каждый — в границах до следующего заголовка; последовательность `N)`,
не начинающаяся с `1)`, отбраковывается как граница части ответа, а не
критерий (настоящие критерии всегда нумеруются с единицы). Плюс
страховка: любые «N баллов» в прозе, не попавшие в текст ни одного
извлечённого критерия, поднимают отдельный warning — задача #3498 после
фикса даёт 0 критериев (было 2 ложных) и 21 честное предупреждение вместо
0.

**Что осталось ограничением:** критерии, размеченные прозой без `N)`/`-`
(как настоящие критерии части 1 задачи #3498), автоматически не
извлекаются — только флагируются страховкой. Полноценный парсер
свободной прозы — отдельная, более глубокая задача, не в скоупе этой
сессии (тот же класс ограничения, что и `parse_shkolkovo_criteria` для
`\subsection*`-формата, задокументированный в пилоте).

### Проверки после фикс-волны — итоговые результаты

`manage.py check` и `makemigrations --check --dry-run` — **чисто**. Все
шесть `*_report.md` перегенерированы; #33817, оба файла ЛЭШ и #3498
визуально подтверждены изменившимися (см. выше и записи в самих
`archive3_report.md`/`lesh_report.md` — #3498 в `solvehub_report.md`
больше не попадает в 40-сэмпл автоматически, потому что корректно вышел
из корзины «есть критерии»; проверено напрямую отдельным прогоном, не
через сэмпл).

**Полный `scripts/run_tests.py --parallel 1` (без многопроцессности) —
две попытки, как ограничивал бриф:**
- **Попытка 1: не выполнилась технически** (перенаправление вывода при
  фоновом запуске дало 0 байт лога и код возврата 1 без единого
  прогнанного теста) — инфраструктурная накладка, не сбой прогона.
  Перезапущена тем же способом, что сработал в предыдущей сессии.
- **Попытка 2 (успешно завершилась, 26.7 мин):**
  - Шаг A (2884 теста, не serial): **OK, 0 ошибок**, 6 ожидаемых
    пропусков. Все тесты `test_corpus_converter_*` (включая все тесты
    фикс-волны) — зелёные.
  - Шаг B (5 serial-тестов вместо привычных 8, 1 ошибка, 5 пропущено):
    единственная ошибка — `catalog.tests.test_search_service.
    BitExactEncodingTests.setUpClass`, `requests.exceptions.SSLError` при
    попытке скачать модель BGE-M3 с `huggingface.co` (`SSLEOFError:
    unexpected EOF`). **Сетевая проблема окружения, не код этой сессии** —
    corpus_converter не трогает `catalog`/эмбеддинги вовсе. Примечательно
    также, что браузерный тест `test_design_canon.py::CanonBrowserChecks::
    test_canon` (единственная краснота прошлой ночной сессии) в этом
    прогоне вообще не появился ни в списке пройденных, ни в пропущенных —
    ещё одно свидетельство нестабильности браузерно/сетевой части
    тестовой инфраструктуры на этой машине между запусками, не связанной
    с содержимым правок. Третья попытка не предпринималась — ограничение
    брифа (максимум 2 попытки) соблюдено; статус фиксируется как известная
    хрупкость среды.

---

## Дефект 4 — найден при подготовке страницы визуального ревью (2026-08-26)

Отдельная задача — собрать `reports/corpus_converter_scaleup/review_samples.html`
для просмотра владельцем результата конвертера ЧЕРЕЗ настоящий прод-рендерер
(`problems/rendering.py::render_markdown` + KaTeX 0.16.9, тот же движок и та
же версия, что видит студент) — вскрыла ещё один реальный дефект, не
замеченный раньше, потому что предыдущие тесты проверяли промежуточный
`text_md`, а не то, во что его превращает рендерер.

**Симптом.** `normalize_dashes` применялась ко ВСЕМУ тексту без разбора,
включая служебную строку-разделитель markdown-таблицы (`| --- | --- |`) —
`---` превращался в `—`, и markdown-it переставал распознавать конструкцию
как таблицу вовсе. Итог: любая таблица, собранная конвертером, рендерилась
бы студенту одним `<p>` с сырыми `|` вместо `<table>` — то есть **все**
таблицы конвертера были сломаны, просто это было не видно, пока никто не
смотрел на реальный HTML-вывод.

**Масштаб, замерено по легаси-семейству (read-only):** 181 (Archive 3) + 65
(МатЭк) + 32 (ЛШ Олмат 2025) + 10 (Решалки) = **288 задач** с markdown-
таблицей в конвертированном тексте — все были бы показаны сломанными.

**Починено.** `normalize_dashes` теперь построчно пропускает строки-
разделители таблиц, остальной текст нормализуется как раньше. Тест — через
настоящий `render_markdown`, не только `text_md`, чтобы такой класс дефекта
не проскочил снова. Подтверждено методологией «откат → красный → фикс →
зелёный» (4 теста). Все шесть `*_report.md` и `review_samples.html`
перегенерированы с фиксом.

---

## Дефект 5 — сломанные `\begin{cases}` шире 164, чинится (2026-08-26)

Владелец лично просмотрел `review_samples.html` и заметил, что кусочные
функции отображаются плохо. Карточка Notion «Сломанные `\begin{cases}` —
шире, чем 164» зафиксировала подозрение: баг есть и в Решалках Олмат (не
только Archive3/МатЭк), и старая страховка `has_unreconstructed_bare_
ampersand_rows` ловит только вариант с `&` — реальный пример #28255
(МатЭк, строки без единого `&`, разделены запятой и пустой строкой)
проходил вообще без warning.

### Фаза 1 — честный пересчёт по всем 4 источникам (read-only, ДО фикса)

Старая цифра 164 (105 Archive3 + 59 МатЭк) считалась на уровне **задач**,
причём только для варианта с `&`. Пересчёт на уровне **блоков**
`\begin{cases}...\end{cases}` без единого `\\`, независимо от наличия `&`:

| Источник | Всего блоков `cases` | Из них сломано (нет `\\`) | Задач затронуто |
|---|---:|---:|---:|
| Overleaf Archive 3 | 1022 | **553** | 312 |
| МатЭк | 604 | **320** | 148 |
| ЛШ Олмат 2025 | 159 | **119** | 52 |
| Решалки Олмат | 36 | **22** | 12 |
| **Итого** | **1821** | **1014** | **524** |

Подтверждает обе части подозрения владельца: ЛШ Олмат 2025 и Решалки
Олмат затронуты (**раньше не измерялись на этот баг вовсе**), а реальный
масштаб на порядок больше 164 — потому что старая страховка технически не
могла увидеть варианты без `&`, а не потому что их было мало.

### Фаза 2 — починка

Новые `reconstruct_cases_row_separators`/`has_unreconstructed_cases_rows`
(`problems/corpus_converter/core.py`): сигнал границы строки внутри
`\begin{cases}` — пустая строка, а не `&` (в отличие от таблиц, обёртка
`\begin{cases}` уже цела, чинится только `\\` внутри неё). Реконструкция
срабатывает при **двух и более** сегментах, разделённых пустой строкой —
однозначно; при одном сегменте (строки слиплись без разделителя вовсе)
никто не гадает, только `warnings`/`complex_table=True`.

Пять тестов-регрессий на живых текстах — #41612 (Archive3, `&`-вариант),
#26337 (МатЭк, `;`+`&`), #47113 и #47137 (Решалки — источник, который
раньше вообще не проверялся на этот баг, оба чинятся), #28255 (МатЭк, БЕЗ
единого `&` — ровно тот случай, который старая страховка пропускала
молча). Все пять подтверждены методологией «откат → красный → фикс →
зелёный»: при временно отключённой реконструкции все 5 покраснели
(включая скрытый ранее #28255), после восстановления — зелёные.

### Фаза 3 — пересчёт после фикса

| Источник | Было сломано | Реконструировано автоматически | Осталось с warning |
|---|---:|---:|---:|
| Overleaf Archive 3 | 553 | **513** (92,8%) | 40 |
| МатЭк | 320 | **264** (82,5%) | 56 |
| ЛШ Олмат 2025 | 119 | **119** (100%) | 0 |
| Решалки Олмат | 22 | **22** (100%) | 0 |
| **Итого** | **1014** | **918 (90,5%)** | **96** |

**Что осталось за флагом (96 блоков, только Archive3/МатЭк) — честно, не
починено.** Это ДРУГОЙ, более узкий саб-паттерн, чем тот, что просили
реконструировать: строки разделены либо одним `\n` (без пустой строки
вовсе — `\begin{cases}30-4k,& k<3,\n 18,& k\ge 3.\end{cases}`, живой
пример #29485 МатЭк), либо осиротевшим `[Npt]`-маркером межстрочного
интервала без предшествующего `\\` (`...& m<\frac23,\n[8pt]\n6, & ...`,
#43909 Archive3) — LaTeX-синтаксис `\\[8pt]` потерял именно `\\`, оставив
голый `[8pt]` на отдельной строке. Оба варианта технически различимы, но
это НЕ сигнал «пустая строка», который просили реализовать в этой сессии
— автоматизация по одиночному `\n` резко повышает риск ложных
срабатываний на обычном переносе строки внутри формулы без намерения
разбить на строку cases. Оставлено как явно флагированный остаток по
тому же принципу «не гадать» — решение владельцу, расширять ли
реконструкцию на этот саб-паттерн отдельной сессией.

**Перегенерировано:** все шесть `*_report.md` и `review_samples.html` (240
сэмплов, warnings в сэмпле упали с 25 до 20 — меньше сломанных cases в
случайной выборке после фикса).

## Итог: ничего боевого не тронуто

`Problem.objects.count()` = 31699 до и после всех команд. `ProblemPart`
63100, `Rubric` 0, `FileAsset` 0 — без изменений. Ни push, ни merge в
`main` не выполнялись.

---

## Сессия 2026-08-27 — страховка по инварианту, очередь ручного разбора, разбор SolveHub #3498

### Фаза 1 — `\begin{cases}`: проверка по правилу вместо охоты за паттернами

Прежняя `has_unreconstructed_cases_rows` спрашивала «есть ли в блоке хоть
один `\`». Это проверка по паттерну: она по построению не может увидеть
блок, где разделители ЕСТЬ, но их не хватает. Заменена на
`has_broken_cases_rows` — проверку по инварианту: **разделителей `\`
внутри `\begin{cases}` обязано быть ровно на единицу меньше, чем строк
условий**, чем бы строки ни были разделены на самом деле.

Как считаются строки:
- **есть `&`** — по нему. В `cases` ровно две колонки, значит больше
  одного `&` на строку быть не может: `&`-штук == строк. Маркер надёжный,
  правило применяется всегда.
- **нет `&`** — по непустым строкам, но ТОЛЬКО когда разделителей нет
  вовсе. Иначе перенос длинной формулы читается как лишняя строка —
  живой ложный пример **#41599** (Archive 3): строк честные три, третья
  перенесена на две физические, разделителей честные два.

**Замер по всем 4 легаси-источникам (1822 блока `cases`, read-only) —
честный результат, не тот, которого ждали:**

| | блоков | правило флагует |
|---|---:|---:|
| Overleaf Archive 3 | 1023 | 40 |
| МатЭк | 604 | 55 |
| ЛШ Олмат 2025 | 159 | 0 |
| Решалки Олмат | 36 | 0 |
| **Итого** | **1822** | **95** |

**Новых поломок правило не нашло.** Все 95 совпали с тем, что ловила
прежняя проверка (96). То есть необнаруженного варианта поломки в этих
четырёх источниках больше нет — прежняя проверка на сегодняшних данных
оказалась не дырявой, а наоборот, слишком широкой. Что правило дало:

1. **Сняло ложное срабатывание.** #28419 МатЭк — честная однострочная
   `\begin{cases} p \end{cases}`. Прежняя проверка флагировала её только
   потому, что в ней нет `\`.
2. **Закрыло класс, невидимый в принципе для прежней проверки** —
   разделители есть, но их меньше нормы. На сегодняшних данных таких нет;
   страховка стоит на будущее и не требует знать паттерн заранее.

### #47127 (Решалки) — назван владельцем, но дефект оказался другой

Проверено на живом тексте: `\begin{cases}` в решении #47127 **чинится
штатно** (две строки, два `&`, разделены пустой строкой) и предупреждения
не требует — ни прежняя проверка, ни новое правило здесь не промахиваются.

Настоящий дефект #47127 в другом месте: **блок `$$` разорван между полями**.
`statement` заканчивается открывающим `$$`, `solution` начинается ещё одним
`$$`. Поля рендерятся по отдельности, поэтому в `solution` разделители
сходятся не туда: display-математикой становится текст «Обе функции $MC$
убывают...», а сама `\begin{cases}` оказывается ЗА пределами математики и
показывается сырым LaTeX. Это и видно глазами как «сломанный cases» —
причина не в разделителе строк.

**Масштаб непарных `$$` по полям (замер read-only):** Archive 3 — 53,
МатЭк — 10, ЛШ Олмат 2025 — 1, **Решалки Олмат — 100** (при 151 задаче в
источнике). Число по Решалкам грубое: счётчик не отделяет экранированный
`\$` от настоящего разделителя, поэтому его надо перемерить точнее до
любых выводов. Отдельной задачей, не этой сессией.

Тест-регрессия на живом тексте #47127 закрепляет обе половины разбора.

### Фаза 2 — очередь ручного разбора

Новая read-only команда `corpus_manual_review_queue` собирает
`reports/corpus_converter_scaleup/manual_review_queue.md`: problem_id,
источник, причина флага, короткий сырой фрагмент вокруг проблемного места.

| Источник | Задач | В очередь |
|---|---:|---:|
| Overleaf Archive 3 | 13699 | 41 |
| МатЭк | 3711 | 23 |
| ЛШ Олмат 2025 | 1081 | 1 |
| Решалки Олмат | 151 | 1 |
| **Итого** | **18642** | **66** |

Из 66: **46** — сломанный `\begin{cases}`, **20** — `multicolumn`/`multirow`.
Автоматической правки по этому списку нет и быть не должно.

**Фильтр рендера — часть логики, а не пункт чеклиста.** Решение «можно ли
ставить `content_format='markdown'`» принимает единственная функция
`problems.corpus_converter.core.may_render_as_markdown()`, покрытая тестом.
Боевая команда рендера обязана спрашивать её, а не сравнивать флаг у себя:
забыть проверку руками теперь нельзя, её отсутствие уронит тест.

**Возможность, которую видно, но которую эта сессия НЕ делала (решение
владельцу).** Из 95 флагированных блоков у **32** (19 Archive 3 + 13 МатЭк)
строки разделены одиночным `\n`, и разбиение по нему даёт РОВНО столько
сегментов, сколько `&` в блоке. По инварианту это уже не гадание:
`&`-штук говорит точное число строк, и оно сходится. Прежний отказ чинить
такие блоки (report.md выше: «риск ложных срабатываний на обычном
переносе строки») теперь можно снять проверкой по `&`. Бриф этой сессии
ограничивал автоправку двухсегментным случаем по пустой строке, поэтому
эти 32 блока только флагированы. Расширять ли реконструкцию — решать
владельцу.

### Фаза 3 — SolveHub #3498: оба артефакта пришли из ИСТОЧНИКА, не от конвертера

Проверено на живом тексте `md`/`answer_md` задачи #3498 с диска.

1. **`$ $4000$`** (сдвоенный `$`) — в исходном файле SolveHub уже лежит
   `получает $ $4000$ в месяц`. Похоже на потерянный обратный слэш в
   `\$4000` (знак валюты) при их собственном экспорте.
2. **`**(** $3$ **балла)**`** — в исходном файле уже лежит ровно так:
   жирный расколот вокруг числа баллов их экспортом.

**Замер по всем 6119 задачам SolveHub (read-only):** конвертер вносит
такие артефакты **в 0 задачах** — ни одного случая, где в источнике
артефакта не было, а после конвертации он появился. Наш конвертер их
честно пропускает как есть, но не создаёт. **Это не баг конвертера.**

**Почему НЕ чиним (а не «не успели»).** Автоматическая починка `$ $`
корёжила бы правильную математику. Тот же самый признак `$ $` рядом с
цифрой законно встречается там, где просто стоят два соседних
математических блока — живые примеры: #824 (`$13$ $24$ $13 24$`), #5574
(`= 40.$ $\quad$`), #5578 (`= 0.$ $\quad$`). Отличить «сломанный знак
валюты» от «двух законных формул подряд» без понимания смысла нельзя, а
цена ошибки — испорченная формула в правильной задаче. Ровно тот случай,
про который бриф говорит «не гадай».

Заодно замер показал, что дефекта **два разных**, а не один: по грубому
признаку `$ $` — 56 задач, по расколотому жирному — 59, пересечение
**всего 1**. Значит и чинить их пришлось бы двумя разными правилами.
Оба — правка ДАННЫХ источника, а не логики конвертера; SolveHub в этом
заходе в импорт не идёт. Оставлено отдельной сессии.

### Фаза 4 — человеческий фикс-пак МатЭк применён к базе

**Первая настоящая запись в базу в этой ветке.** Основание — владелец
лично проверил 745 задач и прислал `matek_fixed.json`; P0 CLAUDE.md
запрещает массовую правку `statement`/`answer`/`solution` МОДЕЛЬЮ, здесь
же правит человек, а код только переносит его правки дословно.

Имена полей сверены с `problems/models.py` по `verbose_name`, не по
догадке: «Условие» → `Problem.statement`, «Ответ» → `Problem.answer`,
«Решение» → `Problem.solution`, «Тип задачи» → `Problem.problem_type`,
`title` → `Problem.title`, «Условие/Ответ подпункта «X»» →
`ProblemPart.statement`/`.answer`.

Новая команда `apply_matek_fixed` (по умолчанию — СУХОЙ прогон, запись
только с `--apply`):

1. **Проверка до единой записи.** Все 745 `problem_id` найдены в базе и
   все относятся к источнику МатЭк (`source_id=13`). Ни одного пропуска
   молча: любой ненайденный или чужой id роняет команду `CommandError`.
2. **Бэкап** текущих значений (`title`/`statement`/`answer`/`solution`/
   `problem_type`/`content_format` + все `ProblemPart` с их полями) —
   `matek_fixed_backup.json`, 745 задач, 1,8 МБ. Пишется в обоих режимах,
   до записи.
3. **Правило переноса.** Правка применяется, только если текст в базе
   совпал с тем, что видел человек (`before`) — дословно, либо с
   точностью до пробелов, либо как подстрока. Пустое `before` требует
   пустого поля в базе. Не совпало — правка НЕ применяется и уходит в
   `matek_fixed_divergences.md`; остальные правки той же задачи
   применяются.

**Результат: применено 2174 правки из 2174, пропущено 0.** Расхождений
с тем, что видел человек, не нашлось ни одного — файл расхождений пуст.

⚠️ **Найденная и починенная ловушка порядка (не расхождение данных).**
Первый сухой прогон дал 3 «расхождения»: #27710 и #30012, «подпункт уже
есть в базе, создавать нельзя». Разбор на живых данных показал, что это
баг команды, а не пакета: человек РАЗБИВАЛ слипшийся подпункт на
несколько (#30012: старый «в» содержал три утверждения — стал «в», «г»,
«д»), и пакет выражает это как «удалить старый «в»» + «создать новый
«в»»» под тем же ярлыком. Проверка «ярлык занят» шла против состояния ДО
удалений. Починено: удаления применяются раньше создания, проверка
занятости — против состояния после удалений. После починки — 0
расхождений. Пакет проверен на другие сочетания: единственные пересечения
по одному ярлыку — «править ответ + править условие» (14, оба правки
уцелевшего подпункта) и «создать + удалить» (3, разобрано выше).

**Инварианты до/после:**

| | до | после |
|---|---:|---:|
| `Problem` всего | 31699 | **31699** |
| `ProblemPart` всего | 63100 | 63250 (+150 = 328 создано − 178 удалено) |
| `ProblemPart` у этих 745 | 1614 | 1764 |
| `content_format='markdown'` среди 745 | 0 | **744** |

Задач не прибавилось и не убавилось — команда проверяет это кодом и
падает `CommandError`, если счётчик разошёлся. Всё пишется в одной
транзакции.

**Рендер включён 744 задачам из 745.** Решение принимает
`may_render_as_markdown()` (Фаза 2) на тексте УЖЕ ПОСЛЕ человеческих
правок. Единственная задержанная — **#29485**: человеческая правка не
убрала сломанный `\begin{cases}` (тот самый саб-паттерн с одиночным
`\n`), поэтому задача осталась на `plain` и ушла в очередь ручного
разбора, а не в прод в сломанном виде. Ровно то поведение, которого
требовал бриф.

**Текст задач конвертером НЕ переписывался.** В базе лежит дословно то,
что прислал человек; конвертер прогонялся только чтобы решить, чисто ли
получилось. Иначе сверка «в базе дословно то, что в пакете» была бы
невозможна.

**Очередь ручного разбора пересобрана** после правок: было 66 задач,
стало **59** (МатЭк 23 → 16 — семь задач человек починил руками).

**Проверка после записи** — команда `verify_matek_fixed` (только чтение)
берёт случайные задачи из 745 и сверяет поля, которые пакет менял, с
`after`, а структуру подпунктов — с финальным `parts` пакета:

- 15 задач (сид 20260827): **41 проверка, 0 расхождений.**
- 20 задач (сид 777, контрольный прогон другим сидом): **53 проверки,
  0 расхождений.**

Поля, которых пакет не касался, намеренно не сверяются: по ним пакет не
источник правды (там пустая строка означает «правки не было», а не
«поле должно стать пустым»).

**Артефакты выведены из-под `.gitignore`** (`manual_review_queue.md`,
`matek_fixed_backup.json`, `matek_fixed_divergences.md`) — по той же
логике, что `report.md` и `review_samples.html`: бэкап это путь отката
после настоящей записи в базу, он обязан пережить чистый клон.

### Что НЕ делалось в этой сессии

- Полный прогон `scripts/run_tests.py` — владелец сознательно пропустил
  его ради времени. Гонялся только быстрый модуль
  `problems/tests/test_corpus_converter_*` (145 тестов, зелёные) после
  каждой правки кода. **Полный прогон и пять джобов CI остаются
  обязательными перед боевым рендером.**
- Ничего не пушилось, в `main` не вливалось.

---

## Сессия 2026-08-27 (worktree `feat/corpus-converter-render`, ветка от
`feat/corpus-converter-scaleup`, коммит `ce25179`): «боевой рендер» —
сухой прогон готов, ждёт `--apply`

База — отдельная копия `db.sqlite3` (432 МБ), скопированная владельцем
вручную в этот worktree; изменения в ней НЕ попадают в основную папку
`qls` автоматически. Локальная операция, не боевой сервер: переключатель
`content_format` в БД, не публикация для пользователей.

### Фаза −1 — сверка с реальностью

`git worktree list` подтвердил отдельный worktree
(`C:/Users/shipu/qls-render ce25179 [feat/corpus-converter-render]`,
основная папка `C:/Users/shipu/qls cdb1fa5 [feat/corpus-converter-scaleup]`
занята параллельной read-only сессией диагностики Решалок). `Problem.objects.count()` = **31699** (не 0, не игрушечная база).

Поля из `problems/models.py` (дословно):
- `content_format` (269–274) — `ContentFormat.PLAIN`/`ContentFormat.MARKDOWN`, `choices=[('plain', 'Обычный текст'), ('markdown', 'Markdown')]`.
- `human_review` (348–353) — `HumanReview.choices = [('', 'не смотрели'), ('approved', 'одобрено человеком'), ('defect', 'человек нашёл брак')]`, проставляется командой `human_review_mark`.

Числа ДО каких-либо изменений (источники по `SourceReference.source_id`, легаси-семейство исключает `source__name='Служебное: фикстуры рендерера (не публиковать)'`):

| Источник | Всего | approved | defect | не смотрели | content_format=markdown |
|---|---:|---:|---:|---:|---:|
| Overleaf Archive 3 | 13699 | 0 | 0 | 13699 | 0 |
| МатЭк | 3711 | 1709 | 923 | 1079 | 744 |
| ЛШ Олмат 2025 | 1081 | 0 | 0 | 1081 | 0 |
| Решалки Олмат | 151 | 0 | 0 | 151 | 0 (129 из них `status='duplicate'`, 85,4%) |

МатЭк: markdown стоит только среди НЕ-approved (744 из 744) — 0 среди
approved. Уже сейчас, ДО этой сессии, «не трогаем approved» соблюдён —
предыдущий фикс-пак (`d7f2947`) content_format approved-задачам не ставил.

`problems/corpus_converter/core.py::may_render_as_markdown()` подтверждена
единственной точкой решения — `return not result['complex_table']`,
вызывается из `apply_matek_fixed.py` и новой команды этой сессии,
нигде вручную флаг не проверяется.

### Фаза 0 — кандидаты

Фильтр: источник ∈ 4 легаси, НЕ `human_review='approved'`, для Решалок
ещё и НЕ `status='duplicate'`.

| Источник | Всего | approved (искл.) | дубли (искл., только Решалки) | Кандидатов |
|---|---:|---:|---:|---:|
| Archive 3 | 13699 | 0 | — | 13699 |
| МатЭк | 3711 | 1709 | — | 2002 |
| ЛШ 2025 | 1081 | 0 | — | 1081 |
| Решалки | 151 | 0 | 129 | 22 |
| **Итого** | **18642** | **1709** | **129** | **16804** |

Сумма сходится (проверено кодом — `_candidate_qs` + явная сверка в
`render_legacy_sources`, `approved + dup_excluded + forced_excluded + candidates == total` для каждого источника, иначе `CommandError`).

### Фаза 1 — шлюз рендера (read-only)

`may_render_as_markdown(result) and not result['warnings']` (та же
формула, что в `apply_matek_fixed.py`) по каждому из 16804 кандидатов:

| Источник | Кандидатов | PASS | FAIL |
|---|---:|---:|---:|
| Archive 3 | 13699 | 13663 | 36 |
| МатЭк | 2002 | 1989 | 13 |
| ЛШ 2025 | 1081 | 1080 | 1 |
| Решалки (22 активных) | 22 | 22 | 0 |
| **Итого** | **16804** | **16754** | **50** |

FAIL-список пересекается с очередью ручного разбора (54 задачи,
Notion «54 задачи в очереди ручного разбора…», карточка от 2026-08-27)
ожидаемо не 1-в-1: `manual_review_queue.md` считает `complex_table=True`
по ВСЕМ 18642 задачам источников (включая approved и дубли Решалок), а
Фаза 1 здесь — только по 16804 кандидатам. Разница (54 vs 50) —
ровно те 3 МатЭк-задачи и 1 Решалки-задача с `complex_table=True`,
которые исключены из кандидатов ещё до шлюза (approved / дубль) —
арифметика сошлась, не баг.

⚠️ **Найдено дополнительно, не шлюзом.** Параллельная сессия диагностики
(коммит `cdb1fa5` в основной ветке, `reports/corpus_converter_scaleup/reshalki_dollar_diagnosis.md`,
не в этом worktree) нашла 49 задач Решалок с архитектурным разрывом `$$`
между `statement`/`solution` (импортёр режет исходную пару `$$…$$`
пополам — `import_olmat_reshalki.py:240`). Независимо воспроизведено в
этой сессии тем же алгоритмом (посимвольный счётчик `$$`-токенов,
зеркалящий `_protect_math_and_currency`) — числа совпали день-в-день:
151 задача, 50 с нечётным `$$`, **49** с разрывом между полями. Из этих
49: **46 — дубли** (уже исключены из кандидатов), **3 — активные**:
**#47083, #47204, #47206**. `may_render_as_markdown()` их НЕ ловит
(разрыв на уровне БД, до конвертера) — без принудительного исключения
все 3 попали бы в PASS. Внесены в отдельный модуль
`problems/corpus_converter/reshalki_dollar_exclusions.py`
(`FORCED_EXCLUDE_RESHALKI_DOLLAR_SPLIT`), исключаются ОБЕИМИ новыми
командами независимо от результата шлюза, помечены в отчёте команды
отдельным счётчиком `forced_excluded`.

**Итоговый PASS после ручного исключения:** Archive 3 — 13663, МатЭк —
1989, ЛШ 2025 — 1080, Решалки — **19** (22 − 3). **Итого 16751.**

### Фаза 2 — страница визуальной проверки

Новая команда `render_legacy_review_html` →
`reports/corpus_converter_scaleup/boevoi_render_review.html` (5,0 МБ).
Показывает ТОЛЬКО PASS-кандидатов (тот же список, что получил бы
`content_format=markdown`), рендер — настоящий `problems.rendering.render_markdown`
+ KaTeX 0.16.9 (та же цепочка, что на проде), с ссылкой на
`/catalog/problem/<id>/` у каждой карточки.

Выборка: 50 случайных на источник (сид 20260826) + ОБЯЗАТЕЛЬНО все PASS
с `\begin{cases}` в сыром тексте:

| Источник | PASS | Показано | из них `\begin{cases}` |
|---|---:|---:|---:|
| Archive 3 | 13663 | 501 | 456 |
| МатЭк | 1989 | 216 | 171 |
| ЛШ 2025 | 1080 | 110 | 66 |
| Решалки | 19 | 19 | 2 |
| **Итого** | **16751** | **846** | **677** |

Проверено визуально (Browser pane, локальный `http.server`): пример
#30058 — инлайн-математика рендерится корректно; пример #30081 —
`\begin{cases}` (кусочная производственная функция) рендерится верно,
фигурная скобка и оба условия на месте. `warnings в выборке: 0` у всех
четырёх источников (ожидаемо — показаны только PASS).

Побочное наблюдение (не блокер, не в объёме этой сессии): у #30058 сырой
`\begin{quote}...\end{quote}` остаётся нетронутым текстом — конвертер не
знает про `quote`, не флагирует. Не трогал, брифом не просилось.

### Фаза 3 — сухой прогон записи

Новая команда `render_legacy_sources` (`--apply` для записи, по
умолчанию сухой прогон):

```
Overleaf Archive 3: total=13699 approved=0 dup_excluded=0 forced_excluded=0 candidates=13699 PASS=13663 FAIL=36
МатЭк: total=3711 approved=1709 dup_excluded=0 forced_excluded=0 candidates=2002 PASS=1989 FAIL=13
ЛШ Олмат 2025: total=1081 approved=0 dup_excluded=0 forced_excluded=0 candidates=1081 PASS=1080 FAIL=1
Решалки Олмат: total=151 approved=0 dup_excluded=129 forced_excluded=3 candidates=19 PASS=19 FAIL=0

PASS всего (все 4 источника): 16751. Уже content_format=markdown: 744. Будет изменено: 16007.
```

**Явная проверка инварианта «0 approved в PASS» (не только текстом,
кодом):** пересечение множества `id` всех approved-задач по 4 источникам
(1709 — все из МатЭк) с множеством всех PASS-id — **пусто**. Отдельная
явная проверка пересечения `FORCED_EXCLUDE_RESHALKI_DOLLAR_SPLIT` с
PASS — тоже **пусто**.

Методология «откат → красный → фикс → зелёный» ПРОГНАНА вручную на живом
коде, не только предположена: временно отключено принудительное
исключение (`if slug == 'reshalki' and False: ...`), команда упала
ровно с ожидаемым `CommandError: ИНВАРИАНТ НАРУШЕН: forced-excluded
Решалки (разрыв $$) попали в PASS-кандидаты: [47083, 47204, 47206]` —
все три и только эти три. Откат правки → команда снова зелёная,
числа PASS/FAIL совпали с прогоном до порчи бит-в-бит. Инвариант 1
(approved) отдельным откатом не проверялся — структурно идентичен
инварианту 2 (то же пересечение множеств, тот же `CommandError`),
но пока не воспроизведён красным/зелёным отдельно.

744 задачи МатЭк уже стоят `markdown` (тот же фикс-пак, сессия
`d7f2947`) — идемпотентность: повторный сухой прогон после `--apply`
покажет «будет изменено: 0» для этих 744 плюс 0 для новых 16007 (уже
учтено сравнением с текущим `content_format` в самой команде — это не
гипотеза, а часть логики: `to_change_ids = all_pass_ids - already_markdown_ids`).

Бэкап при `--apply` — снимок `id → content_format` ДО записи,
`reports/corpus_converter_scaleup/render_legacy_sources_backup.json`
(создаётся командой, в этой сессии не запускался — `--apply` не вызывался).

### Проверки

- `manage.py check` — чисто.
- `manage.py makemigrations --check --dry-run` — чисто (изменений моделей
  нет, только два новых management-command файла и один модуль-константа).
- `problems/tests/test_corpus_converter_*` — **153 теста, все зелёные**
  (не менял `core.py`, прогон подтверждает отсутствие регрессий от новых
  файлов).
- Полный `scripts/run_tests.py` и 5 джобов CI — **не гонялись**, эта
  сессия ничего не пишет в базу (сухой прогон), решение соответствует
  брифу («Полный прогон всего проекта не нужен»).

### Итог: 0 подтверждённых человеком задач затронуты

Проверено дважды независимыми путями: (1) построением кандидатов —
`.exclude(human_review=Problem.HumanReview.APPROVED)` до шлюза; (2)
явным пересечением множеств ПОСЛЕ шлюза, кодом, с `CommandError` при
непустом пересечении. Оба пути сошлись на пустом множестве.

### Стоп-гейт

**`--apply` не вызывался.** Ждёт: (1) владелец открывает
`boevoi_render_review.html` и визуально проверяет выборку (846 карточек,
из них 677 — `\begin{cases}`); (2) явное «да, применяй» в следующем
сообщении. Только после этого —
`manage.py render_legacy_sources --apply`, затем копирование
`db.sqlite3` обратно в основную папку `qls` вручную (отдельный шаг,
не автоматизирован этой сессией).

---

## Сессия 2026-08-27 (вторая, Opus): переработка шлюза после независимого аудита

Предыдущий раздел заканчивался стоп-гейтом «жду `--apply`». **`--apply` не
состоялся и не должен был**: владелец прогнал независимый аудит
(`weconomics_converter_render_audit.md`) настоящим KaTeX по всем 846
карточкам `boevoi_render_review.html` и нашёл, что **368 из 846 (43,5 %)**
имеют реальный дефект отображения, а старый шлюз пропускал их как чистые.

Эта сессия переписала шлюз и конвейер. **`--apply` не вызывался нигде.**

### Фаза −1 — аудит проверен, а не принят на веру

9 карточек из 10 воспроизвелись на живых данных (#30058, #30081, #30113,
#30119, #30172, #26828, #35228, #30921, #41581) — старый шлюз называл
чистыми все десять.

**#41924 не воспроизвелась текстовой проверкой — и это оказалось важнее,
чем если бы воспроизвелась.** Конвертер возвращает `'3.'` дословно, потеря
выглядит нулевой. Пустым блок становится позже, в markdown-рендере:

```
render_markdown('3.')  ->  '<ol>\n<li></li>\n</ol>\n'      видимый текст: ''
```

`markdown-it` читает `3.` как маркер нумерованного списка без содержимого,
`nh3` срезает атрибуты — до экрана не доходит даже цифра «3». Вывод:
**инвариант сохранности обязан считаться по видимому тексту, а не по
markdown-строке.** Любая текстовая проверка тут слепа структурно.

### Инфраструктура: настоящий KaTeX, а не эвристики

Найден вендорный KaTeX в `problems/review_bundle_assets/vendor/katex` —
версия сверена в браузере, `window.katex.version == 0.16.9`, ровно как на
проде. `node_modules/` в репозитории пуст, поэтому JS-скрипты
(`scripts/katex_render_check.js`) не запускаются; питоновский `playwright`
работает. Новый `problems/corpus_converter/katex_preflight.py`
воспроизводит **весь боевой конвейер**: `render_markdown` → DOM → обход
текстовых узлов → те же четыре пары разделителей → `renderToString` с
`throwOnError=true`. Отличие от прода одно и намеренное: там
`throwOnError=false`, что формулу не чинит, а прячет в `.katex-error` с
сырым TeX внутри.

### Фазы 0-2, 4-7 — что сделано

| Фаза | Что | Живые регрессии |
|---|---|---|
| 0 | TeX-aware лексер: токены, стек `begin/end`, валюта | #26828, #35228, #30081 |
| 1 | Канонизация math: обёртка окружений, снятие вложенных `$`, `\text{}`, юникод-операторы | #30172, #30081, #30528, #41612 |
| 2 | Инвариант сохранности по ВИДИМОМУ тексту | #41924, #3989, #30530, #4053 |
| 4 | Текстовые окружения и служебные команды | #30058, #30921, #32536 |
| 5 | Реестр макросов: опечатки чиним, неизвестное блокируем | #41118, #42463 |
| 6 | `SOL-LEAK` — только опознание | #30145, #30153, #30160 |
| 7 | `render_preflight_v2` — новая точка решения | все 22 фикстуры |

**Валюта.** Опознать мало: голый `$` в выводе браузер всё равно спаривает
с соседним. Замерено — после одной лишь канонизации #26828/#35228
оставались с `K-TEXT`. Теперь валюта и любой непарный `$` экранируются в
`\$`, который боевой `maskEscapedDollars` показывает обычным долларом.

**Замер KaTeX вместо доверия рекомендации.** Аудит советовал «`equation*`
снаружи убрать». Прямой `renderToString` показал, что KaTeX это окружение
**поддерживает** — снимать не нужно. Реально неподдерживаемые
(`multline`, `eqnarray`, `subequations`) заменяются на аналоги.

### Фаза 3 — TikZ→SVG: стоп-гейт, ADR 0031

Песочница **выполнима**, и это замерено: MiKTeX+tikz+pgfplots компилируется
без сети (`-disable-installer`), `dvisvgm` даёт SVG, полный цикл ≈ 3 с,
бесконечный цикл убивается таймаутом за 10 с **без процессов-сирот**,
исчерпание памяти TeX прерывает сам за 1 с внутренним лимитом,
`-no-shell-escape` блокирует `\write18`.

**Блокер не в песочнице — SVG некуда положить.** Allow-list `nh3` не
содержит ни `img`, ни `svg`, а `attributes={}` снимает все атрибуты:

```
render_markdown('<img src="/media/plot.svg">') -> '<p>&lt;img src="..."&gt;</p>'
render_markdown('![график](/media/plot.svg)')  -> текст как есть
```

Дописать компилятор, чей результат невозможно показать, и отчитаться о
готовности — ровно тот сценарий, из-за которого прежний шлюз считал себя
готовым. Карточки с TikZ получают код `PLOT` и остаются на `plain`
(регресса нет — они и так на `plain`). Разблокировка требует решения
владельца, два пути в ADR 0031.

### Фаза 6 — постановка задачи оказалась другой

Бриф предполагал «убрать утечку из условия, перенести в решение». Замер по
всем четырём источникам: маркер `[N] Решение:` в начале условия у **47
задач, все в Archive 3** (МатЭк/ЛШ2025/Решалки — ноль). Из них `solution`
пуст у 26; условие ≈ решение (>= 0,95) у 2; почти совпадает у 6; тексты
разные у 13.

Живой #30153: условие (1850 символов) начинается с `[9] Решение:` и дальше
дословно повторяет `solution` (1748 символов). Живой #30160: `solution`
пуст вовсе.

Это **не «решение просочилось в условие», а «условие не импортировано, в
`statement` лежит решение»**. Настоящего условия нет ни в одной из 47.
Снять маркер = показать ученику решение под видом условия. «Перенести
относящееся к решению» = удалить единственный текст. Достроить условие =
запрещено P0 `CLAUDE.md`. Остаётся код `SOL-LEAK` и ручной разбор.

### Фаза 8 — честный пересчёт по всему корпусу

| Источник | Кандидатов | PASS | FAIL | из них были PASS по старому |
|---|---:|---:|---:|---:|
| Overleaf Archive 3 | 13699 | 13022 | 677 | 656 |
| МатЭк | 2002 | 1801 | 201 | 197 |
| ЛШ Олмат 2025 | 1081 | 1023 | 58 | 57 |
| Решалки Олмат | 19 | 16 | 3 | 3 |
| **Итого** | **16801** | **15862** | **939** | **913** |

**913 карточек `--apply` опубликовал бы сломанными.**

Коды: `R-CMD`=693, `K-ERR`=145, `PLOT`=116, `R-ENV`=87, `ENV-BAL`=64,
`SOL-LEAK`=57, `MACRO`=52, `K-STRICT`=24, `EMPTY`=22, `EMPTY-SRC`=19,
`K-TEXT`=5.

**Вклад фаз отделён от строгости шлюза числом** (выборка 1200, сид
20260827, ОДИН И ТОТ ЖЕ строгий шлюз на двух конвейерах):

| Конвейер | FAIL |
|---|---:|
| старый (только стадия 1) | 104 / 1200 (8,7 %) |
| новый (стадия 1 + Фазы 4/5/1) | 40 / 1200 (3,3 %) |

Починено фазами **64**, сломано **0**.

**Проверка готовой страницы в браузере** по 740 показанным PASS-карточкам
(только правая колонка, только не-заблокированные секции): `katex-error`
**0**, сырых `\begin{` **0**, `\addplot/\draw/\node` **0**, `$$` **0**,
уцелевших TeX-команд **0**.

Две дыры найдены именно этой проверкой и закрыты: непарный `$$` съедался
сканером как пустая пара (#4073/#35255), и `\textbf {X}` **с пробелом**
проваливался мимо обеих стадий (#32536). После починки `R-CMD` 888 -> 693,
PASS 15675 -> 15862.

Страница: `boevoi_render_review_v2.html` (прежняя не перезаписана — нужна
для сравнения с аудитом), отдельным блоком показано, что отсеялось и почему.

### Фаза 9 — ILE и Сборник АА

id взяты запросом к `Source`: ILE = 2 (3978 задач), Сборник тестов АА = 6
(2024). Акимова (24, 289) не входит — трек OCR-пересъёмки.

| Источник | Кандидатов | PASS | FAIL |
|---|---:|---:|---:|
| ILE / iloveeconomics.ru | 2059 | 1935 | 124 |
| Сборник тестов АА | 562 | 561 | 1 |

**Решение Фазы 5 «Сборник АА чист без конвертации» подтверждено более
строгим шлюзом** (99,8 %), а не унаследовано от мягкого — ровно то, ради
чего фаза и затевалась. У ILE преобладает `MACRO`: свои макросы стороннего
сайта.

### Фазы 10-12 — НЕ СДЕЛАНЫ

SolveHub (6119), Школково, ЛЭШ Гамма — источники на диске, не в базе.
Прогон требует отдельного INSERT-конвейера через новый шлюз. Сессия
остановлена здесь сознательно, по прямому разрешению брифа: Фазы 0-8 были
обязательным минимумом и закончены качественно, доделывать 10-12 наспех
ценой их качества — та самая ошибка, из-за которой появился этот аудит.

### Проверки

- `manage.py check` — чисто. `makemigrations --check --dry-run` — чисто.
- Тесты конвертера: **246 зелёных** (было 153 до сессии, +93).
- Откат/красный/зелёный прогнан на двух новых правилах: валюта (роняет
  ровно 3 теста), сохранность по видимому тексту (ровно 3 теста #41924).
- `db.sqlite3` не тронут: 432 218 112 байт, mtime 27.08 15:32 — до начала
  сессии. Все команды read-only, `--apply` не вызывался.

### Что дальше

1. Владелец смотрит `boevoi_render_review_v2.html` — и PASS, и блок.
2. Решение по ADR 0031 (маршрут графиков) — разблокирует 116 карточек `PLOT`.
3. 939 + 125 заблокированных — очередь ручного разбора, id в
   `gate_v2_corpus.json` / `gate_v2_extra.json`.
4. Фазы 10-12 — отдельная сессия.
5. **Перед `--apply`:** `render_legacy_sources` обязан спрашивать
   `render_preflight_v2`, а не `may_render_as_markdown` — **это ещё не
   сделано**, команда прошлой сессии по-прежнему опирается на старый шлюз.

---

## Сессия 2026-08-28 (Opus): Фаза A — шлюз подключён, Фаза B — картинки

### Расхождения с брифом, зафиксированные до начала работы

* **`tikz_render.py` не существовал.** Бриф описывал его как «рабочий, но
  не подключённый». ADR 0031 прямо говорит обратное: «Компилятор
  TikZ→SVG в этой сессии не пишется». Замеры песочницы (офлайн-сборка,
  таймаут, лимит памяти TeX) — настоящие и переиспользованы, но сам
  модуль и чистка SVG написаны в этой сессии, а не переиспользованы.
* **Шлюз лежит не в `core.py`.** `render_preflight_v2` — в
  `problems/corpus_converter/preflight_gate.py`; в `core.py` только
  старый `may_render_as_markdown`.

### Фаза A (коммит `fa93497`)

`render_legacy_sources` переведена на `render_preflight_v2` и работает
на канонизированном тексте. Числа сошлись с прошлой сессией дословно —
шлюз тот же код, меняться было нечему:

| Источник | Кандидатов | PASS | FAIL |
|---|---:|---:|---:|
| Overleaf Archive 3 | 13699 | 13022 | 677 |
| МатЭк | 2002 | 1801 | 201 |
| ЛШ Олмат 2025 | 1081 | 1023 | 58 |
| Решалки Олмат | 19 | 16 | 3 |
| **Итого** | **16801** | **15862** | **939** |

Инварианты: 0 approved в PASS, 0 forced-excluded Решалок в PASS.

**Находка:** 15 задач уже стоят на `markdown`, но шлюз их блокирует —
показываются сломанными сейчас. Команда их не трогает (она только
ставит флаг, не снимает), в выводе — явное предупреждение.

### Фаза B (коммит `7952ff5`, ADR 0031 обновлён)

Allow-list `nh3` **не изменён ни на один тег**. Картинка живёт в
`ProblemFigure`, в тексте — маркер `[[FIGURE:<64 hex>]]`, `<img>`
подставляется ПОСЛЕ санитайзера, `src` строится по pk строки БД,
поиск ограничен картинками этой задачи.

**Тест на периметр** (`SecurityPerimeterTests`, 10 проверок) — регрессия
на класс атаки: маркер с URL / с `../../` / с абсолютным путём; сырые
`<img>` и `<svg onload=>`; markdown-картинка; несуществующий хеш; хеш
ЧУЖОЙ задачи; верхний регистр. Ни один не даёт рабочего `<img>`.
Откат ограничения поиска задачей роняет ровно тест про чужую картинку.

**Две ошибки найдены прогоном, а не рассуждением:**

1. TikZ вырезался ПОСЛЕ стадии 1, и `normalize_dashes` превращала `--`
   в `–` внутри кода картинки — latex падал (#43955, #30164, #30172).
   Теперь извлечение идёт по сырому тексту.
2. Санитайзер вырезал `<use>` и все `xlink:href`, а dvisvgm рисует ими
   каждую цифру (замер: `use` 5 → 0) — картинки становились
   нечитаемыми. Внутренняя ссылка `#id` теперь сохраняется.

| | |
|---|---:|
| Скомпилировано | 173 картинки в 61 задаче |
| Не удалось | 22 (89 % успеха) |
| Корпус | PASS 15862 → **15891**, FAIL 939 → **910** |
| Playwright | 118 из 118 `<img>` загрузились, ошибок 0 |

Первый замер дал 0 загруженных — это был мой же `loading="lazy"`
(картинки вне экрана не грузятся), а не дефект.

### Проверки

`check` и `makemigrations --check` — чисто, миграция `0046` применена.
Тесты: **275** в наборе конвертера (было 246) + **57** в `catalog`.
Единственная запись в базу — 173 строки `ProblemFigure`; `content_format`
(749) и `human_review` (5090) до и после не изменились, проверено
инвариантом по слепку текстов и флагов. **`--apply` не вызывался.**

### Не сделано

Фазы 10–12 (SolveHub 6119, Школково, ЛЭШ Гамма) — не в scope сессии.
22 несобравшиеся картинки — ручной разбор.

---

## Сессия 2026-08-29 (Claude Opus 5): боевой рендер применён — но сначала пришлось починить шлюз

Первая массовая запись текста в легаси-корпус. Записано **15 337 задач и
32 417 подпунктов**. Ниже — что нашли по дороге, почему это меняло исход, и
чем всё проверено.

### Фаза −1: сверка с реальностью — разошлись и ветки, и базы

Бриф ожидал актуальный код в `feat/corpus-converter-render`. На деле эта ветка
**целиком влита в `main`**: `git log main..feat/corpus-converter-render` пуст,
а `main` ушёл на 14 коммитов вперёд — в том числе `56fc503`, который
откатывает 15 записей `content_format`, сделанных той самой сессией рендера.

Базы разошлись на три состояния (текст легаси при этом побайтово одинаков во
всех трёх — sha256 `7597c699…` по 18 642 задачам):

| worktree | ветка | Problem | миграция 0046 | ProblemFigure |
|---|---|---:|---|---:|
| `qls` (основная) | feat/embeddings-c13 | 31 699 | нет | таблицы нет |
| `qls-render` | corpus-converter-render | 31 699 | есть | 173 |
| `qls-gate-revert` | **main** | **41 307** | есть | 0 |

Ни один worktree не давал обещанного прогона: у одного корпус без 9 608
импортированных задач, у другого нет картинок, у третьего нет миграции.
Работа остановлена, расхождение показано владельцу. Решение владельца —
работать на корпусе `main` (41 307) и **перегенерировать картинки**.

### Фаза 0: бэкапы (коммит `5d5bdf4`)

* копия базы `backups/db_pre_boevoi_render_20260829.sqlite3` — 432 218 112
  байт, md5 сверен с оригиналом побайтово;
* снимок текста `backups/legacy_text_backup_20260829.json` — 18 642 задачи,
  38 333 подпункта, 36,4 МБ, формат как у `matek_fixed_backup.json`;
* воспроизводится скриптом `scripts/dump_legacy_backup.py`.

Снимок берётся по ВСЕМУ кандидатскому корпусу, а не по PASS-множеству: иначе
при спорном вердикте шлюза откатывать было бы нечего.

Картинки перегенерированы `corpus_build_figures --apply`: **173 строки в 61
задаче, не удалось 22** — числа прошлой сессии воспроизвелись дословно.

### Главная находка: шлюз проверял один текст, а сайт показал бы другой

`render_preflight_v2` рендерит **канонизированный** текст
(`preflight_gate.py:196`), а шаблон каталога — **сохранённый**
(`problem_detail.html:255`, `problem.statement|render_markdown`).
Канонизация в базу не попадала никогда. Docstring команды утверждал, что
расхождения нет («текст уже приведён к финальному виду предыдущими
сессиями»).

**Утверждение неверно:** `convert_problem_v2` меняет текст у **5 842 из
16 804 кандидатов (34,8 %)**, 8 129 блоков из 56 201.

Замер боевым путём (500 случайных кандидатов, сид 20260829, тот же KaTeX
через playwright, шлюз прогнан дважды — по канонизированному и по
сохранённому тексту):

| | задач |
|---|---:|
| шлюз (канонизированный) FAIL | 35 |
| шлюз PASS, сохранённый текст тоже чист | 395 |
| **шлюз PASS, а сайт показал бы сломанным** | **70 (15,1 % от PASS)** |

Коды падений сохранённого текста: `R-CMD` 51, `R-ENV` 24, `K-TEXT` 24,
`ENV-BAL` 5, `K-ERR` 4, `K-STRICT` 3, `PLOT` 1. В пересчёте на PASS —
**около 2 400 задач ушли бы в каталог видимо сломанными**: сырые
`\begin{`/`\end{` в видимом тексте (#31548), настоящий `KaTeX parse error`
(#32458), 44 случая русского текста в math mode (#43877), слипшиеся строки
`cases` (#43971).

Это ровно тот класс дефекта, ради которого шлюз и переписывали после
независимого аудита владельца (368 из 846 карточек). Шлюз стал строже, но
проверял не то, что показывается, — и брак возвращался на 15 % PASS.

Косвенное подтверждение нашлось в самом проекте: docstring
`render_new_sources` называет это расхождение прямым текстом и объясняет,
что у трёх новых источников его нет, потому что там конвертер отработал на
импорте ([ADR 0033](../../docs/adr/0033-new-sources-store-converted-text.md)).

### Правка (коммит `3aba5a2`)

`render_legacy_sources` теперь сохраняет тот текст, который проверил шлюз, —
`statement`/`answer`/`solution`/`ProblemPart.statement` вместе с флагом.
Легаси приведён к той же архитектуре, что и новые источники, а не к
собственной.

Вторая страховка — **идемпотентность по построению**. Сохранённый текст
следующий прогон снова прогонит через конвертер, поэтому сохраняется только
неподвижная точка: `convert(convert(x)) == convert(x)`. У **53 из 16 804
(0,32 %)** это не так, и второй проход портит текст. Минимальный живой
случай (сжат бисекцией из #43186) — вложенный `enumerate`:

```
\begin{enumerate}\n \begin{enumerate}\n \end{enumerate}\n\end{enumerate}
  -> 1. \begin{enumerate}\n\end{enumerate}
  -> 1.                       <- содержимое потеряно
```

Такие задачи получают код `NOT-IDEMPOTENT`, остаются на `plain` и уходят в
очередь ручного разбора. Из-за этого PASS 15 891 → **15 871** (−20).

Восемь тестов написаны ДО реализации. Методология отката проверена:
отключение страховки роняет ровно `test_non_idempotent_text_is_not_written`
и ничего больше. Добавлен `--report-dir`, чтобы прогон набора не писал в
боевую папку отчётов.

### Фаза 1: числа боевого прогона

| Источник | Всего | approved | дубли | forced | Кандидатов | PASS | FAIL |
|---|---:|---:|---:|---:|---:|---:|---:|
| Overleaf Archive 3 | 13 699 | 0 | 0 | 0 | 13 699 | 13 027 | 672 |
| МатЭк | 3 711 | 1 709 | 0 | 0 | 2 002 | 1 806 | 196 |
| ЛШ Олмат 2025 | 1 081 | 0 | 0 | 0 | 1 081 | 1 022 | 59 |
| Решалки Олмат | 151 | 0 | 129 | 3 | 19 | 16 | 3 |
| **Итого** | **18 642** | **1 709** | **129** | **3** | **16 801** | **15 871** | **930** |

Уже стояли на `markdown` 729. **Изменено: 15 337 задач, 32 417 подпунктов.**

### Инварианты (считаны дифференциально)

| Показатель | До | После |
|---|---:|---:|
| `Problem` всего | 41 307 | 41 307 |
| `ProblemPart` всего | 65 803 | 65 803 |
| `human_review='approved'` | 5 090 | 5 090 |
| `content_format='markdown'`, весь банк | 9 800 | 24 942 |
| `content_format='markdown'`, 4 легаси | 729 | 15 871 |
| `ProblemFigure` | 0 | 173 |

* **Сохранность содержимого: 0 потерь.** Первый замер показал 6 «потерь», но
  все шесть — поля, где лежал только TeX-комментарий (`%прошлый,
  производство`). `structure_guard.has_meaningful_source` намеренно не
  считает такое содержимым и называет #30530 поимённо в docstring. Замер
  переделан по определению самого проекта — 0.
* **Парность `$`:** стало хуже 0, стало лучше 12.
* **Баланс `{}`:** стало хуже 7. Причина — спецификация колонок
  `\begin{tabular}{p{3cm}…}`: обёртка снимается, а хвостовой `}` остаётся.
  Дефект **не наш**: задач со спецификацией колонок в видимом тексте было
  63, стало 61, новых 0, исправлено 2. Заведён отдельной карточкой.

### Идемпотентность — проверена, не предположена

Повторный сухой прогон после записи: PASS 15 871, «Уже
content_format=markdown: 15871», **«Будет изменено: 0»**.

### Что теперь видит ученик — проверено тем же шлюзом

Тот же замер, что дал 70 сломанных из 465, прогнан по СОХРАНЁННОМУ тексту
применённых задач (500 случайных, сид 20260829): **500 из 500 рендерятся
чисто, 0 сломанных.** Проверяли то, что показываем.

### Фаза 2: страница визуальной проверки

`reports/corpus_converter_scaleup/boevoi_render_applied_preview.html` —
3 000 случайных задач из применённых, сид **20260829**, 5,5 МБ. Новая
команда `render_legacy_preview` (read-only).

Стратификации нет, доли отражают реальные: Archive 3 — 2 563, МатЭк — 235,
ЛШ 2025 — 199, Решалки — 3. Показано боевым конвейером `render_markdown` +
подстановка картинок, тем же KaTeX 0.16.9 с той же маскировкой `\$`
(`_HTML_HEAD`/`_HTML_FOOT_TEMPLATE` взяты из `corpus_review_html`, не
переписаны). Единственное отличие от сайта — картинка встроена как `<svg>`
из `ProblemFigure.svg`, а не ссылкой: иначе файл не открыть без сервера.

### Фаза 3: SOL-LEAK — скрывать не потребовалось

Детектор находит **58** задач, а не 47: 47 с маркером `[N] Решение:` (все
Archive 3) плюс 11 с голым `Решение:` (10 Archive 3 + #47101 Решалки),
которые раньше не назывались.

**Видимых ученику сейчас — 0.** У всех 58 стоит `hidden_pending_review=True`.
По `status`: published 51, hidden 2, duplicate 5 — но `published` здесь не
означает видимость. Статус НЕ менялся по решению владельца: `status='hidden'`
означает «убрано руками», это другой механизм, и смешивать их нельзя.
У 35 из 58 `solution` пуст вовсе — всё решение лежит в условии.

### Цифра сломанных `cases`: Notion был прав, устной была ошибка

Пересчёт текущим кодом по 4 легаси-источникам: **60 сломанных блоков в 34
задачах** (всего блоков 1 821). По всему корпусу — 66 блоков в 37 задачах
(+3 ILE). Отключение ветки по признаку `&` даёт 67 блоков в 39 задачах,
то есть автопочинка стоит ровно **7 блоков** — как и заявлено в `ce25179`.

Карточка Notion «Сломанные begin{cases}» называет 34 задачи — замер совпал
с ней точно. Цифра «63» = 95 − 32, арифметика от оценки в 32 блока, которую
проект сам уже опроверг («было 32, ошибка методики» → 7). То есть 63 никогда
не измеряли. Карточку не правили: верное на верное менять незачем.

### Что НЕ делали (сознательно)

* 930 задач FAIL-очереди не тронуты, остаются `plain`.
* 60 блоков `cases` в 34 задачах не чинили — очередь ручного разбора.
* В `main` не мержили, никуда не пушили.
* 22 несобравшиеся картинки — ручной разбор.
