# Вычитка 2019 (29 вопросов)

Сверка `materials/vsosh_region/2019/parsed_2019.json` с PDF `test_answers_9.pdf`
(7 стр.) и `test_answers_10.pdf` (8 стр.; `_11` — копия `_10`). Все 29 вопросов
просмотрены по картинкам страниц; numeric и single перерешаны, boolean решены,
в multi оценён каждый вариант. Вопрос №20 (9 кл., «Нью-Йорк») — в `unparsed`
осознанно, не вычитывался.

**Специальная проверка года (влипшие колонтитулы):** ни в одном statement /
option / solution нет остатков «Всероссийская олимпиада школьников», «Региональный
этап…», «Первый тур. Тест.» (проверено и глазами, и текстовым сканом). Все стыки
страниц проверены отдельно — контент склеен без потерь:
- 9 кл.: №3 (решение, стр. 1→2 «…налог по | ставке t…»), №13 (вариант 4 на стр. 5),
  №19 (решение на стр. 7);
- 10–11: №3 (стр. 1→2), №12 (решение, стр. 4→5 «…вариант 4), а также означает…»),
  №15 (решение, стр. 6→7 «…К этому же ответу можно | прийти…»), №19 (стр. 7→8).

## mechanical (4)

1. **№5 [9,10,11], решение** — съеден дефис составного слова при склейке переноса:
   в parsed «отличия корреляции от **причинноследственной** связи», в PDF
   «причинно-следственной» (перенос «причинно-|следственной», стр. 2 обоих файлов).
2. **№8 [9,10,11], решение** — задвоенный знак умножения на переносе строки:
   в parsed `$0{,}8^{4} = 0{,}1^{4} \cdot \cdot 2^{12} = 0{,}4096$`. В PDF знак «·»
   типографски повторён в конце строки и в начале следующей (стр. 3 обоих файлов);
   parsed сохранил оба — при рендере видно «· ·». Условие, варианты и маркер
   ответа не задеты.
3. **№11 [9,10,11], решение (Способ 2)** — развалившиеся этажные дроби во второй
   выключной формуле (самозафлагировано плашкой notes, см. Примечания). PDF
   (10–11, стр. 4): `−1 + 0,5(1/(1+r) + 1/(1+r)² + …) + 0,5/(1+r)² + 0,5/(1+r)³ =
   −1 + 0,5/r + 0,5/(1+r)² + 0,5/(1+r)³`. В parsed: «`0{,}5 ( 1 1 + r+ \frac{1}{2}
   (1 + r) + …)`», «`\frac{0{,}5}{2} (1 + r)`», «`0{,}5 r`» — степени знаменателей
   ушли в числители/потерялись, `\frac{1}{1+r}` рассыпалась в «1 1 + r».
   Первая формула NPV в том же решении собрана верно. Условие, варианты и ответ
   [все четыре] не задеты.
4. **№15 [10,11], решение** — съеден дефис составного слова при склейке переноса:
   в parsed «суммарный излишек в **странеэкспортере** упадет», в PDF
   «стране-экспортере» (перенос «стране-|экспортере», стр. 7 файла 10–11). Рядом
   в том же решении «стране-экспортере»/«страны-импортера» с дефисом — целы.

## answer_mismatch (0)

Нет. Все помеченные ответы совпали с моими решениями (включая перерешанные
numeric: №16=1200, №17=1/2, №18=0, №19=100, №20 [10,11]=50; и все single/multi).

## ok (25)

- №1 [9,10,11] boolean, Нет — верно (экзогенный рост цены лыж ⇒ спрос на комплемент падает).
- №2 [9,10,11] boolean, Нет — верно (заморозка ⇒ возможен дефицит).
- №3 [9,10,11] boolean, Да — верно (кривая Лаффера); формула T = t·(100−t)/2 собрана верно.
- №4 [9,10,11] boolean, Да — верно (пример 810→1600 сходится).
- №6 [9,10,11] single, 3) макроэкономика — верно (Нордхаус и Ромер, 2018).
- №7 [9] single, 2) кредитором — верно.
- №8 — маркер 4) «снизилась на 59,04 %» верен, перерешано: (2·0,4)⁴ = 0,4096 (сам дефект решения учтён в mechanical №2, условие/варианты чистые… вопрос в mechanical).
- №9 [9] single, 4) богатство одинаково — верно (равенство сумм половин при сортировке ⇒ все равны).
- №10 [9] single, 4) может быть любой — верно (при Q=1 издержки на любом заводе равны 1).
- №12 [9] multi, [1,4] — верно.
- №13 [9] multi, [1,2,4] — верно.
- №14 [9] multi, [4] — верно.
- №15 [9] multi, [1,3] (БРИКС, ВТО) — верно.
- №16 [9,10,11] numeric, 1200 — перерешано: Q=60, 60·20=1200 ✓.
- №17 [9] numeric, 1/2 — перерешано: P* = 6 + t/2 ✓.
- №18 [9] numeric, 0 — перерешано ✓.
- №19 [9,10,11] numeric, 100 — перерешано: 600 Игреков / 6 = 100 ✓.
- №7 [10,11] single, 4) европейский колл = право купить в определённый момент — верно.
- №9 [10,11] single, 1) 0 — **известная ловушка, ответ ВЕРЕН**: кривая Лоренца выпукла, и если она проходит через точку (0,5; 0,5), лежащую на диагонали, то по выпуклости совпадает с линией абсолютного равенства целиком ⇒ Джини = 0 даже «максимально возможный».
- №10 [10,11] single, 1) только первый — верно (MC второго и третьего заводов → 0 при малых q, они всегда используются).
- №12 [10,11] multi, [3] — перерешано: индекс Лернера 0,2 ⇒ ε = −5 ✓.
- №13 [10,11] multi, [1,2,4] — верно.
- №14 [10,11] multi, [1,2,4] — перерешано: в 1) 2 = (2,5+1,5)/2 ✓; в 2) и 4) изоэластичный спрос, цены не зависят от сценария ✓; в 3) 2,5 ≠ 2,75 ✗ ✓.
- №15 [10,11] multi, [1,4] — верно (сам вопрос в mechanical №4 из-за дефиса в решении; маркеры и варианты чистые… вопрос в mechanical).
- №17 [10,11] numeric, 1/2 — перерешано (монополия, линейный спрос: переложение 1/2) ✓.
- №18 [10,11] numeric, 0 — перерешано ✓.
- №20 [10,11] numeric, 50 — перерешано: 500 − 450 = 50 ✓.

*(№5, №8, №11, №15 [10,11] по вердикту отнесены к mechanical и в счёт «ok» не
входят; их условия, варианты и маркеры ответов сверены и чисты — дефекты только
в текстах решений.)*

## Примечания

- **Плашка notes у №11** («математика линеаризована: этажную дробь из PDF не
  удалось собрать автоматически — сверить с оригиналом») — сверено с оригиналом
  (10–11, стр. 4): первая выключная формула NPV
  `−1 + 0,5/(1+r) + 1/(1+r)² + 1/(1+r)³ + 0,5/(1+r)⁴ + 0,5/(1+r)⁵ + …`
  в parsed собрана **верно**; вторая (преобразование к `−1 + 0,5/r + …`)
  действительно развалилась — детали в mechanical №3. Плашка честная.
- **Опечатка самого PDF, честно сохранённая в parsed (чинить не надо):**
  №11, решение, Способ 1 — «проект приносит как **разз** 0,5 млн р.» — так
  напечатано в обоих PDF (стр. 4), проверено зумом.
- №17 (оба варианта): в PDF «Ответ: 1/2 или 0,5», в parsed канонизировано до
  `1/2` — эквивалентно (движок игры считает 1/2 = 0,5), потери нет.
- №9 [10,11] (Джини): помеченный ответ 0 подтверждён перерешиванием — это не
  расхождение (см. ok).
- Выключная формула в решении №12 [10,11] `\frac{P-MC}{P} = \frac{1}{-\varepsilon}`
  собрана из этажной дроби PDF корректно — контраст с №11 показывает, что сбой
  сборки дробей точечный, а не системный.

**Итог: N=29, mechanical K=4, answer_mismatch M=0, ok L=25. K+M+L=29.**
