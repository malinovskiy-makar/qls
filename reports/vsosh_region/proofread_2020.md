# Вычитка 2020 (36 вопросов)

Сверка `materials/vsosh_region/2020/parsed_2020.json` с PDF `test_answers_9.pdf`
(9 класс, 7 стр.) и `test_answers_10.pdf` (10–11, 7 стр.; `_11` — копия `_10`).
Каждый вопрос: визуальная сверка с рендером страницы (глифы, формулы, маркеры
правильного ответа в рамках) + самостоятельное перерешивание. Мелкие формулы
(2.3 [9], AVC в 3.1 [9], корни в 3.1 [10–11], показатель Q⁵ в 3.3 [10–11],
«торвголи»/«субтитут») проверены точечными кропами 4–4.5x.

Итог: mechanical 1 / answer_mismatch 0 / ok 35 (1+0+35 = 36).

## mechanical (1)

### 3.1 [10, 11] — развалившиеся корни (потерян охват винкулума)
PDF: `test_answers_10.pdf`, стр. 4. Класс дефекта: винкулум корня обрезан до
первого символа, подкоренное выражение «выпало» наружу. Вопрос был помечен
плашкой notes («математика линеаризована»), и именно здесь она сработала по делу:

- **Вариант 4)**: в parsed `$q_{4} = \sqrt{K}^{2} + L^{2}$` (читается как
  (√K)² + L²), в PDF винкулум накрывает всё: `\sqrt{K^{2} + L^{2}}`.
  Это меняет математику: (√K)²+L² = K+L² не даёт постоянной отдачи от
  масштаба, и помеченный жюри ответ (варианты 2, 3, 4) с такой формулой
  перестал бы сходиться. С формулой из PDF ответ [1, 2, 3] верен.
- **Решение, строка q₃**: в parsed `\sqrt{tK} \cdot tL` — в PDF
  `\sqrt{tK \cdot tL}` (корень над всем произведением).
- **Решение, строка q₄**: в parsed `\sqrt (tK)^{2} + (tL)^{2}` и
  `t\sqrt{K}^{2} + L^{2}` — в PDF `\sqrt{(tK)^{2} + (tL)^{2}}` и
  `t\sqrt{K^{2} + L^{2}}`.

(Лишняя закрывающая скобка `t\sqrt{KL})` в той же строке решения — опечатка
самого PDF, parsed её честно сохранил; см. Примечания.)

## answer_mismatch (0)

Нет. Все ответы ключа сошлись с самостоятельным перерешиванием:

- Данетки: 1.1 Нет, 1.2 [9] Да, 1.3 Нет, 1.4 [9] Да, 1.5 Нет, 1.2 [10–11] Да,
  1.4 [10–11] Нет — всё подтверждается.
- Single: 2.2 [9] P\*=45 (a=90b); 2.3 [9] 25 % (80·1,25²=125); 2.4 [9] max
  альт. издержки k=5; 2.5 [9] децильный = 1 при y=x; 2.2 [10–11] P\*=34
  (a=68b); 2.3 [10–11] 32 д.е. (50/1,25²); 2.4 [10–11] вогнутая y=1−0,5x²;
  2.5 [10–11] 0,19/0,01 = 19; 2.1 (оба) — Нобель-2019 за полевые эксперименты
  по бедности (вариант 4).
- Multi: 3.1 [9] AVC константа у TC₁ и TC₃ → [0,2]; 3.2 [9] NPV≈11,91 →
  I∈{10,11}; 3.3 [9] [0,1]; 3.4 (оба) выигрыш при p≠альт. издержкам → [0,2,3];
  3.5 [9] фикс. издержки 2 неустранимы, MC₁=1<MC₂=2 → [2,3]; 3.1 [10–11]
  CRS у q₂,q₃,q₄ → [1,2,3] (с формулами из PDF); 3.2 [10–11] NPV≈13,82 →
  [0,1,2]; 3.3 [10–11] все четыре верны; 3.5 [10–11] [0,1,2] (при Q=1 второй
  завод даёт 2/3 < 1).
- Numeric: 4.1 = 30 (выручка 1200→840); 4.2 [9] = 4 (14→10); 4.3 [9] = 5
  (q\*=(p−1)/2: 5→10); 4.4 [9] = 0,25; 4.5 [9] = 200; 4.2 [10–11] = 56
  (81−25); 4.3 [10–11] = 0 (min AVC=3 > обеих цен); 4.4 [10–11] = 0,375;
  4.5 [10–11] = 35 (b=1/4, a=50, AP=50−15). Строки «Ответ: …» в PDF совпадают
  со значениями `correct` во всех 10 numeric.

## ok (35)

- **9 класс (21):** 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.4, 2.5,
  3.1, 3.2, 3.3, 3.4, 3.5, 4.1, 4.2, 4.3, 4.4, 4.5 — и общий с 10–11
  вопрос 4.1 засчитан один раз в этом списке.
- **10–11 класс (14 из 15):** 1.2, 1.4, 2.1, 2.2, 2.3, 2.4, 2.5, 3.2, 3.3,
  3.4, 3.5, 4.2, 4.3, 4.4, 4.5 (кроме 3.1 — см. mechanical).

Маркеры правильных ответов (рамки) сверены у всех 26 вопросов с вариантами и
у 7 данеток — ни одного съехавшего индекса. Влипших колонтитулов и обрывков
на стыках страниц не найдено (проверены переходы: 9 класс p1→2, p4→5, p6→7;
10–11 p1→2 в решении 1.4, p2→3 в решении 2.2, p4→5 в решении 3.3, p5→6 в
решении 3.5) — везде текст parsed непрерывен и чист.

## Примечания

**Сверка вопросов с плашками notes («математика линеаризована»):**

- **3.1 [9]** — дробь `\frac{2^{q}}{q}` в решении собрана ВЕРНО: в PDF это
  слэш-дробь `2^q/q` (стр. 3), содержание совпадает поглифово. Варианты
  (TC₁=2q, TC₂=q², TC₃=20q+4, TC₄=2^q+1) — точны.
- **3.5 [9]** — этажная дробь в решении `AC_{1}(q_{1}) = 1 + \frac{2}{q_{1}}`
  собрана верно (в PDF слэш-дробь `1 + 2/q₁`, стр. 5); формулы условия
  TC₁=q₁+2, TC₂=2q₂ точны. Ложная тревога плашки.
- **3.1 [10–11]** — плашка сработала по делу: корни развалились, см. mechanical.
- **4.3 [10–11]** — кусочная функция с фигурной скобкой (стр. 7) линеаризована в
  `\{2019, q= 0; q^{2} + q+ 2020, q> 0.` — все числа и условия на месте,
  содержание полное и верное (закрывающей скобки нет и не требуется по смыслу).
  Также верно собраны этажные дроби 3.5 [10–11] (`\frac{1}{q_{1}+1}`,
  `\frac{1}{q_{2}+2}`) и 2.3 [10–11] (`\frac{50}{(1+r)^{2}}`).

**Точки кривой Лоренца в 4.4 (особая проверка):** [9] — (0; 0), (0,5; 0,25),
(1; 1), G = 0,5 − 0,25 = 0,25 (25 %); [10–11] — (0; 0), (0,5; 0,125), (1; 1),
G = 0,5 − 0,125 = 0,375 (37,5 %). Все цифры совпадают с PDF поглифово и
подтверждаются пересчётом (1 − 2·площадь под ломаной).

**Опечатки самого PDF, честно сохранённые в parsed (чинить НЕ надо):**

- 2.3 [9], вариант 4: `50 %;.` — лишняя точка после точки с запятой (стр. 3).
- 3.1 [9], решение: «константой **во первом** и третьем случае» (стр. 3).
- 3.3 [9], условие: «товар-**субтитут**» вместо «субститут» (стр. 4).
- 3.4 [9], вариант 4: «выигрывает от **торвголи**» (стр. 4); в версии 10–11
  того же вопроса — «торговли» правильно, parsed различает их корректно.
- 4.2 [9], решение: `π₀(10 − Q)Q − 2Q − 2` — в PDF пропущен знак «=» после π₀
  (стр. 6); задвоенный на переносе строки минус в parsed корректно схлопнут.
- 3.1 [10–11], решение: лишняя закрывающая скобка `t√{KL})` есть в самом PDF
  (стр. 4).
- 3.3 [10–11], решение: «если бы выручка **возрастает** по цене» — грамматика
  оригинала (стр. 4–5).

**Прочее:** показатель степени в 3.3 [10–11] подтверждён кропом: в PDF именно
`P_d = 10 − Q^5`. Преамбулы секций (в т.ч. разные версии «Задания 4» для 9 и
10–11 с оговоркой про «не изменится») совпадают с PDF.
