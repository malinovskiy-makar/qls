# Вычитка 2021 (40 вопросов)

Сверка `materials/vsosh_region/2021/parsed_2021.json` с PDF `test_answers_9.pdf` /
`_10.pdf` / `_11.pdf` (по файлу младшего класса из `grades`; вопросы только [10]
или [11] — по своему файлу). Все 24 страницы отрендерены и просмотрены; мелкие
формулы (цепочки дробей в 3.4, мультипликатор в 4.5) сверены по кропам 4x —
каждая цифра. Ответы дополнительно сверены со сводными сетками «Правильные
ответы» (стр. 8 всех трёх файлов): расхождений нет. Каждый вопрос перерешан
(numeric/single — полное решение; boolean — решение; multi — оценка каждого
варианта); расхождений с ключом жюри не найдено.

## mechanical (3)

1. **2.1 [11]** — подменённые глифы курсива (файл 11, стр. 2). В parsed
   в условии: «(Опдлзз Шбдгрйнвн внртгапрсбдммнвн аамйа он ьйнмнлзцдрйзл
   матйал оалюсз А. Мнадкю)»; на картинке: «(Премии Шведского государственного
   банка по экономическим наукам памяти А. Нобеля)». Курсивный шрифт
   `LibertinusSerif-Italic` лечёного файла 11 извлекается со сдвигом −1
   кодпоинта на каждую кириллическую букву (П→О, р→п, е→д…). Это единственный
   битый кусок файла 11, попавший в parsed (см. Примечания, п. 4). Остальное
   в вопросе верно: варианты, маркер правильного (рамка на «4) усовершенствование
   теории аукционов» = correct 3), сетка ответов — всё сходится.

2. **3.5 [9]** — развалившаяся формула в решении (файл 9, стр. 5). Система
   двух случаев (фигурная скобка) склеена в «дроби»: в parsed
   `TC(Q) = \{ \frac{TC_{1}(Q) + TC_{2}(0) = Q^{2} + 49,}{TC_{1}(3) + TC_{2}(Q-3) = 6Q+ 40}, \frac{Q \le 3;}{Q> 3}.`;
   на картинке — кусочная запись: TC(Q) = { Q²+49 при Q ≤ 3; 6Q+40 при Q > 3 }.
   Все числа и оба случая сохранены, но рендер даёт «уравнение над уравнением»
   как дробь — читается как ложная математика. Условие, варианты и ключ [0, 3]
   верны.

3. **3.5 [10, 11]** — два дефекта в решении (файл 10, стр. 5; файл 11, стр. 5–6).
   (а) Развалившаяся кусочная формула, притом с битым LaTeX: в parsed
   `TC(Q) = \frac{Q^{2},}\{\{6Q+ 40\}, \frac{Q< 10;}{Q \ge 10}.` (у `\frac`
   второй аргумент — `\{`, KaTeX такое не соберёт); на картинке —
   TC(Q) = { Q² при Q < 10; 6Q+40 при Q ≥ 10 }.
   (б) Влипшие обрывки рисунка: хвост решения «…построив график $TC(Q)$: $Q$
   / 10 $TC$ / 100» — это подписи осей графика из PDF (сам график потерян),
   приклеенные к тексту. Условие (включая кусочную TC₂ с избегаемыми 49),
   варианты и ключ [0, 2, 3] верны.

## answer_mismatch (0)

Нет. Все 40 ключей жюри совпали с моими решениями:
- boolean (10): 1.1[9]/[10]/[11] — Верно; 1.2, 1.3[9,10], 1.5 — Неверно;
  1.4[9] — Верно; 1.4[10], 1.3[11], 1.4[11] — Неверно. Маркеры (рамка у
  «Да/Нет») и сетки совпадают с `correct`.
- single (11): Лоренц y=x³ → 1:7:19; инфляция 200 % → 3^t (в 9 классе индекс 1,
  в 10 — индекс 3, порядок вариантов разный — parsed это учёл); облигации →
  25 %; AVC(2020) = 2021; Нобель-2020 → аукционы; безработица →
  «недостаточно информации»; смешанная политика → сдерживающая монетарная +
  стимулирующая фискальная; приток капитала 7 млн.
- multi (11): дериватив [3]; налог 9 кл. [0,1] / 10–11 кл. [0,1,2]; КТВ [1,2];
  три товара [0,2,3]; облигации A/B [1,3] (порог r = 2/9 > 0,2 — ловушка жюри
  в варианте 3 учтена ключом верно); слияние фирм [0,3] (9 кл.) и [0,2,3]
  (10–11 кл., TC(5)=25≠70).
- numeric (8): 4.1 = 100; 4.2 = 64 (unit «%», в PDF «Ответ: на 64 %» ✓);
  4.3[9] = 12,5 (unit «%» ✓); 4.3[10,11] = −0,25 (unit пустой, в PDF
  «Ответ: −0,25» без единицы ✓); 4.4 = 20 (unit «%» ✓); 4.5[9,10] = 20;
  4.5[11] = 2000. Все строки «Ответ: …» и сетки совпадают с `correct`/`unit`.

## ok (37)

Все вопросы, кроме трёх из раздела mechanical:
1.1[9], 1.2, 1.3[9,10], 1.4[9], 1.5, 2.1[9,10], 2.2[9], 2.3[9], 2.4[9], 2.5,
3.1, 3.2[9], 3.3[9], 3.4[9], 4.1, 4.2[9,11], 4.3[9], 4.4, 4.5[9,10],
1.1[10], 1.4[10], 2.2[10,11], 2.3[10], 2.4[10], 3.2[10], 3.3[10,11], 3.4[10],
4.2[10], 4.3[10,11],
1.1[11], 1.3[11], 1.4[11], 2.1... — 2.1[11] в mechanical; далее: 2.3[11],
2.4[11], 3.2[11], 3.4[11], 4.5[11].
Годоспецифичные отличия дублей между классами (9/10/11 единиц товара X в 1.1;
запятая в 2.2[9] «трети, и богатейшей»; «Если»/«если» в 3.4[10]/[11]; «цены
этих облигаций»/«цены облигаций»; «т.е.»/«т. е.» в 3.3) — parsed передал точно
по своему файлу.

## Примечания

**Сверка формул вопросов с плашкой notes:**
- **3.4 [10] и 3.4 [11]** («математика линеаризована… сверить с оригиналом») —
  сверено по кропу 4x, каждая цифра: $P_A=\frac{N}{1+r}$;
  $P_B=\frac{0{,}1N}{1+r}+\frac{0{,}1N+N}{(1+r)^2}$;
  $(1+r)P_B=0{,}1N+\frac{1{,}1N}{1+r}$; неравенство
  $\frac{1}{1+r}>\frac{0{,}1}{1+r}+\frac{1{,}1}{(1+r)^2}$; порог $r>\frac29$;
  интервал $r\in(\frac{2}{10};\frac29)$. Все дроби собраны ВЕРНО, потерь цифр
  нет. Косметика (не дефект содержания): после выключных дробей точка/начало
  следующего предложения оторваны пустой строкой («…$\frac{1{,}1N}{1+r}$ ␤␤ .
  При больших…»).
- **4.5 [11]** (та же плашка) — дробь $\frac{1}{rr+er}$ собрана верно;
  $300/(0{,}1+0{,}05)=2000$ сходится с PDF (файл 11, стр. 7). Косметика: запятая
  после дроби оторвана пустой строкой.

**Опечатки самого PDF, честно сохранённые в parsed (чинить не надо):**
- Преамбула «Задание 4» файла 9 (стр. 5): «в вопросе 4.3 — ответы „−0,25“ и
  „−5/4“» — двойная опечатка жюри: −5/4 вместо −1/4, и сама ссылка не про
  вариант 9 класса (там ответ 4.3 — «на 12,5 %»; комментарий скопирован из
  вёрстки 10–11 классов, где 4.3 — эластичность −0,25). Parsed передал дословно.
- Преамбула «Задание 4» файла 11 (стр. 6): «ответы „0,25“ и „−1/4“» — минус у
  0,25 потерян в самом PDF (в файле 10 — «−0,25»). Parsed передал дословно.
- Титул файла 9 (стр. 1): «2021/2021 год» (в файлах 10 и 11 — «2020/2021 год»)
  — в parsed не попадает (не контент вопросов).

**Сетка «Правильные ответы» (стр. 8 всех трёх файлов):** в parsed НЕ попала —
ни в одном вопросе нет обрывков сетки («Конкурс», «закрасьте кружочек»,
«Образец заполнения» — 0 вхождений). При этом сами сетки использованы как
третий контур сверки ключей: все 20+16+... позиций всех трёх классов совпали
с parsed.

**Глифы файла 11 (лечёная кодировка):** прямой шрифт, полужирный и математика
извлекаются чисто (проверено на всех вопросах файла 11). Битым остался только
курсив `LibertinusSerif-Italic` — сдвиг −1 кодпоинта. Кроме попавшего в parsed
куска в 2.1[11] (см. mechanical), в PDF битым курсивом извлекаются
«поведенческой экономикой» (коммент 1.2) и «Корреляция» (коммент 1.5) — но эти
вопросы общие для 9–11 классов, их текст в parsed взят из чистого файла 9 и
корректен; плюс подписи на странице сетки (в parsed не попадают).

Итого: 40 = 3 mechanical + 0 answer_mismatch + 37 ok.
