# Журнал: синхронизация банка v4, заголовки, источники, каталог — 17.09.2026

Дневная сессия, владелец на связи. Ветка `feat/bank-sync-v4` от `main` (`e398611f`).
Повторный запуск читает этот файл и продолжает с первой незакрытой фазы.

Как обращаться к копии боя (решение владельца, Фаза 0):

```bash
DATABASE_URL=postgres://qls:qls_dev_password@127.0.0.1:55432/weco_prod_copy \
  venv313/Scripts/python.exe manage.py <команда> --settings=config.settings_test_pg
```

Без `DATABASE_URL` и `--settings` команды идут в рабочую `db.sqlite3` (дом).

---

## Фаза −1. Сверка с реальностью — ГОТОВО

- `git status`: чисто, кроме личных неотслеживаемых файлов владельца в корне
  (не трогались). `main` на `e398611f`. `manage.py check` — 0 ошибок.
- Все якоря найдены. Мелкие расхождения с описанием в промпте (план не меняют):
  1. `Tag.KIND_CHOICES` — четыре вида, а не два: `canonical`, `author`,
     `legacy`, `junk`; по умолчанию `legacy`.
  2. `beta 0.0` не написан в `templates/_nav.html` буквально: это настройка
     `SITE_VERSION` (`config/settings.py:450`), шаблон выводит `{{ site_version }}`
     дважды (широкая и узкая шапка); тест — `config/tests/test_nav.py:211`.
  3. «Отметьте, что случилось, или опишите своими словами» — это текст ошибки
     сервера в `problems/views_platform.py:455` (`api_feedback`), а не надпись в
     `_feedback.html`. Поле «Другое, своими словами» — в `_feedback.html:274` (JS).
  4. Файловая вьюха снимка — `FeedbackAdmin.screenshot_view`
     (`problems/admin_platform.py:170`); слова «удал» в ней нет, отказ пишет
     «Снимка нет». **Гипотеза для Фазы 4.5:** ссылка «открыть» собрана
     ОТНОСИТЕЛЬНОЙ (`'screenshot/'`) и со страницы записи
     `/admin/problems/feedback/<pk>/change/` уводит на
     `…/<pk>/change/screenshot/`. Этот адрес ловит общий шаблон админки
     `<path:object_id>/`, и Django пишет «…не существует. Возможно, он был
     удалён?» — ровно то, что видит владелец. Файл, скорее всего, ни при чём.

## Фаза 0. Ветка, журнал, копия боя — ГОТОВО

- Ветка `feat/bank-sync-v4` создана от `main`.
- Docker: `qls_postgres_dev` (55432) и `qls_redis_dev` (56379) подняты
  (после перезапуска Docker Desktop стояли в `Exited`).
- Копия боя: база `weco_prod_copy` в `qls_postgres_dev`, восстановлена из
  `weconomics-data\prod_dumps\weconomics-20260917-015942.dump.gz`
  (165 431 458 байт) командой
  `gunzip -c … | docker exec -i qls_postgres_dev pg_restore -U qls -d weco_prod_copy --no-owner --no-privileges`
  за 24 с, код 0, предупреждений 0.
- Сверка копии: `problems_problem` **14 087**, `problems_problempart` **28 887**
  (оба совпали с ожиданием), тем 865, тегов 896, источников 27, привязок к
  источникам 14 087. Миграции `problems` на копии и дома — обе до
  `0068_chat_turn_attachment`.
- **Предположение/решение:** `config/settings.py` `DATABASE_URL` не читает
  (SQLite прописан жёстко). Владелец выбрал `config.settings_test_pg` — файл
  отличается от обычных настроек только чтением базы из `DATABASE_URL`, код не
  меняется.
- Notion (решение владельца «как предложено»): основная карточка
  «Синхронизация банка…» → «В работе»; взяты в работу существующие
  «Заголовки: обрубки + apply_batch1_titles», «Таблица Tag загрязнена: 551
  запись…», «Каталог: превью условий вырезают формулы…»; заведены пять новых
  (источники, прогрев, фильтр «с решением», контраст, обратная связь и подвал).
