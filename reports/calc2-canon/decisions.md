# Решения по ходу работы «Графики под канон»

Формат карточки для базы «Решения» (39bb11c9-2bc1-81b9-9cfd-000b7f654c8f):
Решение · Контекст · Почему так · Источник · Дата · Статус «Действует».

---

## Р0.1 Файлы протокола кладём в git принудительно

**Решение.** `reports/calc2-canon/*` и `reports/calc2-audit.md` добавляются
через `git add -f`.
**Контекст.** `reports/` целиком в `.gitignore` (строка 34). Файл прогресса,
который живёт только на диске, теряется при `git clean` и не виден владельцу
в диффе.
**Почему так.** Работа объявлена возобновляемой: следующая сессия начинается
с чтения `PROGRESS.md`. Прецедент есть — под `reports/` уже принудительно
добавлен 21 файл (замеры BGE, превью генераторов, отчёты ВсОШ).
**Статус.** Действует.
**В Notion.** ✔ занесено

---

## Р0.2 Старый DESIGN.md заменён целиком, содержимое не переносилось

**Решение.** Прежний `DESIGN.md` (289 строк, формат Google Stitch с YAML-шапкой,
сгенерирован скиллом `impeccable`) заменён каноном целиком.
**Контекст.** Он описывает систему вокруг чернильно-синего `#4f7cff`; на живом
сайте этого цвета нет ни в одном месте.
**Почему так.** Решение владельца от 18.08.2026
(3c0b11c9-2bc1-814c-bfed-e68a441730b3), часть 5.3 канона.
**Побочное.** Формат Stitch с машиночитаемой YAML-шапкой потерян — если он
нужен скиллу `impeccable` или живой панели, шапку придётся отдать отдельным
файлом (`.impeccable/design.json`). Сейчас на неё в коде проекта никто
не ссылается — проверено `grep`.
**Статус.** Действует.
**В Notion.** ✔ занесено

---

## Р0.3 В `points.md` добавлена колонка «Фаза»

**Решение.** Таблица пунктов ведётся в семи колонках вместо шести из промпта:
между «Метка» и «Кратко» стоит номер фазы.
**Почему так.** Возобновляемость: по номеру фазы видно, какие пункты относятся
к прерванной работе, без сверки с промптом.
⚠️ **Уточнено в фазе 2-бис.** У файла появилась вторая таблица («Хвосты
приёмки»), и прежняя проверка `grep -c "^| [0-9]" points.md` = 82 стала
считать её строки тоже. Считать надо ДО заголовка хвостов:
`sed -n '/^## Хвосты/q;p' points.md | grep -c '^| [0-9]'` = 82.
**Статус.** Действует.
**В Notion.** ▢ не заносить (мелочь формата)

---

## Р2.1 Округление на экране — одно место; сумма считается из округлённых слагаемых

**Решение.** `roundShown` / `fmtSum` / `shownDiff` в `20-plane.js` рядом с `fmt`.
**Почему.** «SW = CS + PS» на экране не сходилось с двумя числами над ним:
сумма считалась по сырым float и округлялась отдельно.
**Границы.** Математика не тронута: `STATE.sw` остаётся точной суммой,
из округлённого собирается только ПОКАЗ.
**В Notion.** ✔ занесено

## Р2.2 Вход в любую модель обнуляет кривые

**Решение.** `pickScene` чистит `STATE.curves` перед маршрутом карточки.
**Почему.** 19 маршрутов из 41 зовут `loadScene` и чистят сами, остальные 22
только переключали режим. Замер: 22 сцены из 41 приходили с чужим содержимым.
**В Notion.** ✔ занесено

## Р2.3 Блок результатов молчит там, где его инструмент заперт

**Решение.** `updateTaxPanel` и `updateMonoInterventionPanel` спрашивают тот же
класс `.scoped-off`, которым сюжет прячет орган управления.
**Почему.** Второй список «сцен с налогом» разъехался бы с маршрутами.
**В Notion.** ▢ не заносить (частность решения Р2.2)

---

## Р2б.1 Чекпоинт фазы включает прогон тестов, а не только `manage.py check`

**Решение.** В чекпоинт после каждой фазы добавлен шаг «прогнать тесты»
(как минимум `problems.tests.test_template_hygiene` и наборы calc2).
**Контекст.** Сырой шаблонный комментарий на каждой странице `/calc2/`
(2-бис.1) внесён фазой 1 (коммит `c727933`). Проверка на этот класс дефектов
в проекте **уже была и работала** — `test_no_multiline_hash_comments` краснеет
на нём. Её просто не запускали: чекпоинт требовал только `manage.py check`,
а он тестов не гоняет.
**Почему так.** Дефект был виден пользователю на каждой странице и прожил
целую фазу и визуальную приёмку. Стоимость шага — минуты; стоимость пропуска
уже измерена.
**Границы.** Полный прогон проекта (2 377 тестов) после каждой подфазы
избыточен; обязателен на финальной проверке. После подфазы достаточно гигиены
шаблонов и наборов calc2.
**Статус.** Действует.
**В Notion.** ✔ занесено

## Р2б.2 Радиусы общей шапки — не дефект calc2; правка шапки не делается

**Решение.** Пункт «calc2 переопределяет радиусы общей шапки (8px вместо 6px)»
закрыт как **не воспроизводится**. Правка не делается, заводится карточка.
**Замер.** Логотип 7px, переключатель темы 8px, «Выйти» 8px — **одинаково**
на `/calc2/`, `/student/` и `/catalog/`. Источник — inline-стили в
`templates/_nav.html`, общем файле платформы; calc2 не переопределяет ничего
(в `calc2.css` нет ни одного правила на `nav`).
**Почему так.** Граница 2 промпта: правка требует изменить общий файл
платформы → не делаем, карточка в «Задачи», направление «Дизайн».
Это же объясняет три исключения из замера радиусов в фазе 1
(529 элементов из 532 — пять канонных значений): все три принадлежат шапке.
**Статус.** Действует.
**В Notion.** ✔ занесено (решение + карточка «Радиусы общей шапки мимо канона: 7/8/8px вместо 6px», статус «Надо», ответственный Макар)

## Р2б.3 `--shadow-pop`: светлая тема отдана канону, тёмная оставлена своей

**Решение.** В светлой теме calc2 больше не держит своей копии — значение
приходит из `_tokens.html` (`0 6px 20px rgba(0,0,0,.14)`). В тёмной остаётся
своё: **канонная геометрия плюс `0 0 0 1px rgba(255,255,255,.07)`**.
**Замер.** У канонного токена тёмного варианта нет вовсе. Чёрная тень
`rgba(0,0,0,.14)` поверх оболочки `#07090e` даёт 1,02:1, поверх холста
`#10141c` — 1,03:1: края у всплывающего окна в тёмной теме не видно.
Кольцом окно и отделяется от фона.
**Почему не «оставить как было».** Прежние значения расходились с каноном
в обеих темах и двумя слоями сразу. Фаза 1 записала в комментарий кода, что
«тени и слои остаются своими», но канон 5.1 такого исключения не содержал —
комментарий кода канону не указ. Расхождение сведено к одному, и оно измерено.
**Побочное.** `--shadow-panel` не тронут: канон 1.7 говорит, что в покое тени
нет вовсе, то есть это правка панелей, а не значения токена. Остаётся
канонным долгом фазы 6.
**Записано.** `DESIGN.md`, часть 5.1, список «что остаётся своим».
**Статус.** Действует.
**В Notion.** ✔ занесено

## Р2б.4 У суммы на экране та же глубина округления, что у слагаемых

**Решение.** `shownDecimals` / `sumDecimals` в `20-plane.js`; `fmt(v, minDecimals)`
получил второй необязательный аргумент; заведена `fmtDiff(after, before)`,
на неё переведены оба столбца «Δ».
**Контекст.** `290,65 + 290,65` показывалось как `581,3`: арифметически верно,
но в столбце два знака у слагаемых и один у итога, и глаз ищет пропавшую копейку.
**Границы.** Незначащий ноль печатается **только там, где рядом стоят числа
с этим разрядом**. Одиночное `fmt(50)` так и остаётся «50», ось не превращается
в «10,00 · 20,00 · 30,00». Математика не тронута.
**Статус.** Действует.
**В Notion.** ✔ занесено

## Р2б.5 Цель по контрасту подписей — 4,6, а не 4,5

**Решение.** `INK_MIN_TEXT = 4.6` в `30-curves.js`; тот же порог в
`scripts/calc2_canon_probe.js` и (будущий) в тесте канона фазы 9.
**Замер.** При цели 4,5 минимум по 1 796 подписям вышел **4,51**, и **116**
подписей сели в полосу 4,51…4,6. При цели 4,6 минимум стал **4,60**, в полосе
ниже — 0.
**Почему так.** Поиск идёт шагом 0,01 по светлоте и останавливается на первом
прошедшем значении, поэтому запас при цели «ровно норма» получается нулевым:
любая будущая правка цвета роняет обратно целый класс подписей.
**Границы.** Цвета самих кривых не тронуты — двигается только светлота, оттенок
сохраняется. Исключений «оставить на 4,5» не понадобилось ни одного:
до 4,6 дотянулись все.
**Статус.** Действует.
**В Notion.** ✔ занесено

---

## Р3a.1 Ширину подписи меряет браузер, а не «столько-то пикселей на знак»

**Решение.** `measureText(str, size, weight)` в `20-plane.js`; `fitMargins`
считает левое поле по измеренной ширине подписей делений, правое — по
измеренной ширине НАЗВАНИЯ оси X.
**Замер.** Оценка `знаков × 6.2` занижала ширину: в «Производственной функции»
деления «1 000 … 5 000» начинались с координаты −7 px, то есть за холстом, и
на экране читалось «000». Название оси «t (ставка)» — 76 px при правом поле 32.
**Почему так.** Занижение неустранимо константой: разряды разделены узким
неразрывным пробелом, а ширина цифры зависит от шрифта, которым страницу
в итоге нарисовали.
**Границы.** Мерим в ОТДЕЛЬНОМ невидимом `svg` внутри `#graph-wrap`, а не в
самом холсте: по `#chart` ходят `applyLabelInk`, `applyLabelSize`,
`spreadLabels` и сборка `.tex` — служебный узел подмешался бы во все четыре.
Внутри обёртки, чтобы наследовать шрифт подписей.
**Осталось.** Сцены со своими шкалами (`prod`, `fx`, `laffer`, `monoexport`)
рисуют оси мимо `drawAxes`/`fitMargins` — 28 подписей за краем. Подфаза 3b.
**Статус.** Действует.
**В Notion.** ✔ занесено

## Р3a.2 Полоса под осью — одна на все сцены, а не список «кто пишет под осью»

**Решение.** `BOTTOM_BAND = 44` в `20-plane.js`: строка чисел делений плюс
строка подписи, которую сцены печатают на `oy + 24`.
**Замер.** При поле 30 «Дефицит = 40» и «Безработица = 30» уходили на 5 px за
нижний край холста в четырёх сценах.
**Почему так.** Альтернатива — реестр «какие сцены пишут под осью» либо
подгонка поля после отрисовки. Реестр разъехался бы со сценами; подгонка после
отрисовки — обратная связь, а на ней проект уже обжигался (ResizeObserver).
Полоса стоит 14 px из ~790 по высоте.
**Побочное.** Подпись оси в «Неравенстве» опущена с `oy + 30` на `oy + 38`:
на прежнем месте она садилась ровно на строку чисел (11 наложений в трёх
ширинах окна). Число и `BOTTOM_BAND` меняются вместе.
**Статус.** Действует.
**В Notion.** ✔ занесено

## Р3a.3 `$…$` на холсте разбирает `renderLabelText`, а не каждая сцена

**Решение.** `texToCanvasText` в `40-scenes-market.js`: снимает разделители
формул, переводит известные команды LaTeX в юникод, `\$` оставляет настоящим
знаком доллара. Вызывается из `renderLabelText`, то есть из всех подписей холста.
**Замер.** 12 подписей в двух сценах печатали сырой код: «перегиб TP:
$\max MP$ при $L = 10$», «$f'(x)$, производная». Стало 0.
**Причина.** На холсте нет KaTeX (он не встаёт в SVG, а `foreignObject` выпал бы
из снимка холста, то есть из выгрузки в PNG). Разбор `hasMathMarkup` проверял
только `[*^_]`, поэтому подпись, написанная долларами, молча уходила как есть.
**Почему в одном месте.** Авторы сцен пишут подписи так, как привыкли в
остальном проекте. Правка четырёх строк закрыла бы сегодняшние четыре и
пропустила бы следующую. Заодно найдена подпись, шедшая прямым `.text()` мимо
разбора вовсе — переведена на общий путь.
**Статус.** Действует.
**В Notion.** ✔ занесено

## Р3a.4 Проверка слэшей LaTeX освобождает `\uXXXX` и `\xXX` — и только их

**Решение.** `calc2/tests/check_tex_escapes.mjs` больше не считает ошибкой
`\uXXXX` и `\xXX`; смотрит КАЖДОЕ вхождение в литерале, а не первое.
**Контекст.** Проверка ловила `''`: после слэша идут `u` и `E`, формально
две буквы, хотя это шестнадцатеричный код символа.
**Почему только две.** Однобуквенные escape НЕ освобождаются никогда — на них
класс и стоит: `\b` съедает «\beta» (забой плюс «eta» — ровно та находка, ради
которой проверка написана), `\f` — «\frac», `\t` — «\text» и «\times»,
`\n` — «\ne», `\r` — «\right», `\v` — «\vec». Среди команд LaTeX нет ни
`\uXXXX`, ни `\xXX`, спутать не с чем.
**Проверено зубастостью.** Внесён дефект `'$\beta$ и \frac{1}{2}'` — оба
пойманы; после возврата файла проверка снова чистая.
**Статус.** Действует.
**В Notion.** ✔ занесено

## Р3a.5 Пункт 48 — не на холсте; проверено, отложено к панелям

**Решение.** Пункт 48 («`MC` набирается как `M` с индексом `C`») в подфазе 3a
не правится: на холсте его нет.
**Проверено.** `qtyParts` держит аббревиатуры явным правилом: пробег заглавными
(`/^[A-Z]+$/`) не разбирается на имя и индекс, поэтому «MC», «ATC», «DWL»,
«LRAS» на холсте набираются целиком.
**Значит.** Дефект на стороне KaTeX в панелях, где `$MC$` — произведение
переменных. Лечится `\mathrm{MC}`, но это сторона панелей: чинить в 3c либо
вместе с фазой 6, замерив сначала, где именно.
**Статус.** Действует.
**В Notion.** ✔ занесено
