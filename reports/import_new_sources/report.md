# Импорт трёх новых источников — отчёт сессии 2026-08-28

Ветка `feat/import-new-sources`, worktree
`.claude/worktrees/import-new-sources-edbc26`. Не запушено, в `main` не влито.

Читать целиком, файл самодостаточный.

---

## Коротко

| | было | стало | прирост |
|---|---:|---:|---:|
| `Problem` | 31 699 | **41 307** | +9 608 |
| `ProblemPart` | 63 250 | 65 803 | +2 553 |
| `Rubric` | 0 | 49 | +49 |
| `Source` | 24 | 27 | +3 |
| `content_format='markdown'` | 734 | 9 800 | +9 066 |

Импортировано: **Школково 3 414**, **SolveHub 6 119**, **ЛЭШ Гамма 75**.
Сумма 9 608 сходится с приростом `Problem` до единицы.

Ни одна задача, существовавшая ДО сессии, не изменилась: **0 из 31 699**
задач и **0 из 63 250** подпунктов. Проверено свипом по отпечаткам, а не
на словах (`import_invariants_check`).

---

## Откуда бралась ветка (важно для понимания)

`problems/corpus_converter/` **нет на `main`** — модуль живёт только на
ветках `feat/corpus-converter-*`. Поэтому ветка сделана от актуального
`main` (56fc503), и в неё влита `feat/corpus-converter-render` — самая
свежая и полная версия конвертера (в ней `preflight_gate`,
`katex_preflight`, `tikz_render`, которых нет в остальных). Без этого
слияния Фаза 4 была бы невозможна физически.

**Файла `claude/HANDOFF_CORPUS_CONVERTER_20260827.md` не существует** — ни
в рабочей копии, ни в истории git, ни в одной из шести рабочих папок.
Вместо него читались `reports/corpus_converter_scaleup/report.md` и
`reports/corpus_converter_pilot/report.md`.

**База.** Работа шла на КОПИИ dev-базы, снятой через `sqlite3.backup`
(безопасно при живых читателях). Оригинал `C:\Users\shipu\qls\db.sqlite3`
не открывался на запись ни разу — он и есть путь отката целиком.

---

## Фаза −1 — сверка с реальностью

`git worktree list` — шесть папок, работа шла в своей
(`import-new-sources-edbc26`), поверх занятых не писали.

| Источник | На диске | Пригодно к импорту |
|---|---:|---:|
| SolveHub | 6 121 файл | **6 119** (у двух пустое `md`) |
| Школково | 3 414 файлов | **3 414** |
| ЛЭШ Гамма | 40 `.tex`, после исключений 28 | **75** уникальных задач |

`Problem.objects.count()` до начала — **31 699**.

**Найденная ловушка.** `corpus_pilot_shkolkovo` в worktree молча прочитал
**0 задач** и отчитался успехом: путь к данным считается от родителя
`BASE_DIR`, а в worktree родитель другой. Новые команды на такое падают
громко и называют папку (`ingest.require_dir`).

---

## Фаза 0 — Школково, 3 414 задач

`manage.py import_shkolkovo --apply`

```
Problem  31699 -> 35113   ProblemPart 63250 -> 64062   Rubric 0 -> 45
с подпунктами 269 · с ответом 2370 · с решением 3406 · с картинками 301
предупреждений конвертера 933
```

**Маппинг сложности — одна строка, и она честная.** `DifficultyId` равен
`0` у ВСЕХ 3 414 записей: шкалы у источника нет вовсе.

| источник | банк | задач |
|---|---|---:|
| `DifficultyId = 0` | `difficulty = NULL`, `difficulty_native = ''` | 3 414 |

Выводить сложность из тем или длины текста — это выдуманное, а не
импортированное знание. Поэтому поле пустое.

**Остальные поля:** `title` ← `Name`; `statement` ← `statement_tex`;
подпункты а)/б)/в) → `ProblemPart`; `answer` ← `answer_tex`, иначе
`Answer.text`; `solution` ← `solution_tex`; рубрика ← `criteria_tex`;
`SourceReference` ← `Id` + `_source_url` + `source_name`.

**Ограничение, названное числом:** `criteria_tex` непуст у 836 задач, а
разобрано в рубрику — **45**. Остальные 791 в другом формате прозы
(`parse_shkolkovo_criteria` его не берёт), каждая с предупреждением в
`shkolkovo_warnings.txt`. Текст критериев остался на диске, не потерян,
но в банк не попал.

---

## Фаза 1 — SolveHub, 6 119 задач

`manage.py import_solvehub --apply`

```
Problem  35113 -> 41232   ProblemPart 64062 -> 65583   Rubric 45 -> 49
с подпунктами 722 · с ответом 3814 · с решением 2248 · с картинками 610
варианты ответа дописаны в условие у 2241 · предупреждений 109
```

**Маппинг сложности — прямой, шкала источника уже пятибалльная.**

| источник | банк | задач |
|---|---|---:|
| `level1` | 1 | 860 |
| `level2` | 2 | 2 533 |
| `level3` | 3 | 1 182 |
| `level4` | 4 | 772 |
| `level5` | 5 | 28 |
| `none` | `NULL` | 744 |

`difficulty_native` хранит метку источника дословно (`level3`).

**Ответ собирается из машинных полей** `correct_answer` + `check_options`
— по одному правилу на каждый из семи `check_type`
(`problems/corpus_converter/solvehub.py`). Ключевое измерение, сделанное
ДО импорта: **индекс `single_choice` — с НУЛЯ**. Свидетельств два: среди
задач, где в `answer_md` дословно встречается ровно один вариант, 0-база
сходится у 71 задачи против 5 у 1-базы; и значение `0` встречается 363
раза, чего при 1-базе быть не может. Ошибка на единицу дала бы неверный
ответ у 1 765 задач.

У `uncheckable` (2 294 задачи) ответа в источнике нет — поле пустое,
выдумывать нечего.

**Варианты ответа дописаны в условие** у типов выбора (2 241 задача):
без них задача выбора не имеет смысла. У `multiple_questions` и
`matching_list` подписи полей не теряются — уходят в ответ парами
«подпись: значение».

### Картинки — 99,4 % докачано

Просили попробовать скачать каждый URL и посчитать долю успеха.

| | было | стало |
|---|---:|---:|
| адресов картинок в корпусе | 837 | 837 |
| из них `data:` (уже в тексте, качать нечего) | 3 | 3 |
| скачано на диск | **544 (65 %)** | **829 из 834 (99,4 %)** |

Причина разрыва оказалась не в сервере. `scripts/solvehub/fetch_images.py`
искал ссылки по regex `https://api\.solvehub\.app/uploads/...` и не видел
остальные адреса вовсе, отчитываясь «уже скачано 544, к выгрузке 0»:
249 ссылок на `solvehub.s3.regru.cloud` (второе хранилище самого
SolveHub), 39 на `iloveeconomics.ru`, 3 на `googleusercontent`. Regex
заменён на разбор markdown-ссылки; добавлены разбивка по хостам, защита
от коллизий имён у разных хостов и итоговая доля.

Не скачались 5 — все `iloveeconomics.ru`, ссылки мёртвые. Названы
поимённо в логе, импорт из-за них не останавливался.

⚠️ **Картинки в банк как `FileAsset` НЕ заводились.** `FileAsset` пуст во
всём банке, показ картинок не подключён вовсе — санитайзер
`problems/rendering.py` режет `<img>`. Ссылка осталась в тексте задачи как
есть: выкинуть её значило бы потерять материал. Это известное
ограничение, отдельная задача.

### Дедуп — не трогали, как и просили

366 задач совпадают с банком по точному `content_hash`. Импорт их **не
отбрасывает и не помечает**: прошлая сессия установила, что это короткие
типовые формулировки тестовых вопросов (средняя длина 98 символов), а не
реальное пересечение корпусов. `find_duplicates` разберётся позже своей
обычной логикой; пороги не менялись.

---

## Фаза 2 — ЛЭШ Гамма, 75 задач

`manage.py import_lesh --apply`

```
Problem 41232 -> 41307   ProblemPart 65583 -> 65803
файлов .tex 28 · блоков \z в подборках 77 · названий в решалках 41
дублей внутри корпуса 0 · уникальных кандидатов 75
решение найдено по названию 35 · БЕЗ РЕШЕНИЯ 40 · с подпунктами 63
```

Импортированы **все 75**, включая 40 без решения — поле `solution`
пустое. Подбор по смыслу не выполнялся: «похожее» решение не у той задачи
хуже, чем никакого. Каждая из 40 названа отдельной строкой в
`lesh_warnings.txt`, а не растворена в счётчике.

**Шкалы сложности у источника нет:** второй аргумент макроса `\z` — это
БАЛЛЫ за задачу, а не сложность. `difficulty = NULL`, баллы сохранены в
заметке привязки к источнику вместе с файлом и названием.

Пункты решения не теряются: у блока-решения свои пункты, и их число не
совпадает с числом пунктов условия (живой пример «Классика» — 4 против
2). По частям их не разложить, поэтому они дописаны в конец решения.

---

## Фаза 3 — числовые инварианты

`manage.py import_invariants_check --baseline reports/import_new_sources/baseline_before_import.json --expect-total 41307`

```
старых задач в снимке 31699, сверено 31699 — ИЗМЕНЕНО 0
старых подпунктов    63250, сверено 63250 — ИЗМЕНЕНО 0
новых задач 9608, из них БЕЗ Source — 0
задач в базе 41307 (было 31699)
новых по источникам: SolveHub 6119, ЛЭШ 75, Школково 3414
```

Отпечаток задачи — md5 от `statement`/`answer`/`solution`/`human_review`/
`content_format`/`status`/`title`; подпункта — от `statement`/`answer`/
`solution`. Свип идёт по ВСЕМ старым id, не по выборке.

Проверка умеет краснеть — это отдельно проверено тестами: подмена
условия старой задачи, её `human_review`, `content_format`, текста
подпункта, удаление старой задачи и новая задача без `Source` ловятся
каждая своим тестом.

Идемпотентность всех трёх импортов подтверждена: повторный прогон даёт
«импортировано: 0».

---

## Фаза 4 — шлюз рендера, `content_format='markdown'`

`manage.py render_new_sources --apply` (настоящий KaTeX, 9 608 задач, 1 мин 20 с)

| источник | всего | PASS | FAIL | доля PASS |
|---|---:|---:|---:|---:|
| Школково | 3 414 | 2 964 | 450 | 86,8 % |
| SolveHub | 6 119 | 6 038 | 81 | **98,7 %** |
| ЛЭШ Гамма | 75 | 64 | 11 | 85,3 % |
| **Итого** | **9 608** | **9 066** | **542** | **94,4 %** |

Коды отказов: `R-CMD` 492, `K-ERR` 37, `R-ENV` 25, `K-STRICT` 17,
`PLOT` 15, `MACRO` 9, `EMPTY` 7, `ENV-BAL` 6, `K-TEXT` 4.

Инварианты после записи: approved среди кандидатов **0**; старых задач
изменено **0**; `Problem.objects.count()` не изменился (41 307);
повторный прогон — «будет изменено: 0».

**Чем этот шлюз отличается от `render_legacy_sources` и почему это
важно.** У легаси в базе лежит ИСХОДНЫЙ текст, конвертер запускается на
лету, и шлюз судит конвертированный текст — а сайт показывает
сохранённый. У новых источников этого расхождения нет: конвертер
отработал НА ИМПОРТЕ, в базе уже лежит его результат, и шлюзу отдаётся
ровно то, что лежит в базе. Что проверили, то и покажем.

---

## Фаза 5 — пакет на ревью, 2 000 задач

`manage.py export_chatgpt_batch --size 2000 --seed 20260828`
→ `reports/chatgpt_batch_20260828/` (8 файлов, 0,38–0,77 МБ, всего 4,7 МБ)

**НИКУДА НЕ ОТПРАВЛЕНО** — просто файлы на диске.

Выборка случайная по ВСЕМУ банку, сид фиксирован и воспроизводим
(`manifest.json` → `ids`). Состав: легаси 1 515 (75,7 %), новые 485
(24,3 % — при их доле в банке 23,3 %, то есть выборка равномерная, а не
подкрученная под новые источники).

Показ дословно повторяет ветку боевого шаблона: `markdown` →
`render_markdown` + `render_figures`, `plain` → `linebreaksbr`,
математика — KaTeX 0.16.9 с конфигурацией сайта.

Разбивка: задачи идут источник за источником и складываются в файл до
одного из двух потолков — 5 МБ или 250 задач. Второй потолок обязателен:
2 000 задач весят всего 4,7 МБ и по одним байтам легли бы в один
нечитаемо длинный файл.

---

## Найдено попутно (дефекты, не выдуманные)

1. **`fetch_images.py` не видел 293 из 837 ссылок** и годами отчитывался
   «к выгрузке 0». Починено, доля скачанного 65 % → 99,4 %.
2. **Молчаливый ноль при неверном пути к данным.** `corpus_pilot_shkolkovo`
   в worktree прочитал 0 задач и вышел успешно. Новые команды падают
   громко и называют папку.
3. **Сухой прогон затирал отчёт предупреждений.** Боевой прогон записал
   933 предупреждения, следующий сухой (проверка идемпотентности —
   импортировать нечего) перезаписал файл пустотой.
4. **Тесты писали в боевую папку отчётов репозитория.** Прогон полного
   набора после импорта превратил 933 настоящих предупреждения Школково в
   четыре тестовых. Испорченный отчёт выглядит как настоящий. Починено:
   `--report-dir`, тесты пишут во временную папку, отдельный тест
   `ReportDirIsolationTests` сверяет СОДЕРЖИМОЕ боевой папки (первая
   версия сверяла имена файлов и на откате вела себя неустойчиво).
5. **`too many SQL variables`** у SQLite на 63 250 подпунктах — свип
   инвариантов идёт итератором, а не `filter(id__in=[...])`.

---

## Проверки — статус каждого из пяти джобов CI

| джоб | статус |
|---|---|
| 1. Стиль (`ruff check .`) | **зелёный** |
| 2. Безопасность (`bandit -ll`, `pip-audit`) | **зелёный** (0 issues; 0 уязвимостей) |
| 3. Схема с нуля на PostgreSQL | **НЕ ПРОГНАН** — см. ниже |
| 4. Тесты | **шаг A зелёный** (3 133 теста, 13 пропусков), шаг B — 1 падение, см. ниже |
| 5. Продакшен-настройки (`check --deploy --fail-level WARNING`) | **зелёный** |

Плюс `manage.py check` — 0 ошибок, `makemigrations --check --dry-run` —
«No changes detected».

**Джоб 3 не прогнан честно, а не пропущен молча.** Ему нужен пустой
PostgreSQL 17; на этой машине Docker CLI установлен, но демон не
поднимается (`dockerDesktopLinuxEngine` недоступен, `docker desktop start`
возвращает 0 и ничего не запускает), локального PostgreSQL на 5432/5433
тоже нет. Тот же PostgreSQL нужен и полному варианту джоба 4 — тесты
прогнаны на SQLite. **Зелёный прогон на SQLite не доказывает прод**, это
записано в CLAUDE.md отдельно.

**Единственное падение шага B — не от этой сессии.**
`test_design_canon.CanonBrowserChecks.test_canon`, метрика
`math_line_height`: потолок 68, замер 66, тест требует опустить потолок.
Ровно эта метрика уже переключалась в обе стороны последними двумя
коммитами ветки ДО начала сессии (`b86a501` опустил 68→66, `5cc6d7c`
вернул 66→68, потому что CI покраснел). Это неустойчивость браузерного
замера на этой машине. Код сессии не трогает ни `calc2/`, ни шаблоны, ни
CSS — только `problems/corpus_converter/`,
`problems/management/commands/import_*`, `render_new_sources`,
`export_chatgpt_batch`, `scripts/solvehub/fetch_images.py` и тесты.
Трогать потолок в третий раз не стал.

Тестов по теме сессии: **85 новых**, все зелёные. Каждый набор проверен
методом «откат поведения → красный → восстановление → зелёный».

---

## Что осталось (известные ограничения, не блокеры)

- **SolveHub, 366 совпадений `content_hash`** — не разобраны, импортированы
  как есть. Разбор за `find_duplicates`.
- **SolveHub, 5 картинок** не скачались (мёртвые ссылки `iloveeconomics.ru`).
- **Картинки нигде не показываются** — `FileAsset` пуст, санитайзер режет
  `<img>`. Отдельная задача.
- **ЛЭШ, 40 задач без решения** — сопоставление по смыслу не делалось.
- **Школково, 791 задача с `criteria_tex`**, из которого рубрика не
  разобрана.
- **542 задачи новых источников не прошли шлюз** и остались на `plain` —
  очередь ручного разбора, коды причин в `gate_new_sources.json`.
- **Эмбеддинги для 9 608 новых задач не считались** — отдельный шаг
  (`build_embeddings`), в бриф сессии не входил.
- **Пакет 2 000 задач не отправлен** — по прямому указанию брифа.

## Путь отката

1. **Оригинал dev-базы не тронут вовсе** — `C:\Users\shipu\qls\db.sqlite3`
   никогда не открывался на запись. Это откат целиком, одной заменой файла.
2. `reports/import_new_sources/gate_new_sources_backup.json` — 9 066 id с
   их прежним `content_format='plain'`.
3. `reports/import_new_sources/baseline_before_import.json` — отпечатки
   корпуса до импорта, для `import_invariants_check`.
