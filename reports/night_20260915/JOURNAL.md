# Журнал ночной сессии 15.09.2026 — «Баги, поиск, тесты, дуэль, чат»

Ветка `feat/night-20260915` от `main` @ `bae68c5` (совпадает с `origin/main`, 0/0).
Машина Windows, `C:\Users\shipu\qls`, окружение `venv313`.
Журнал пишется по ходу. При повторном запуске продолжать с первой фазы без «✅».

> ⚠️ **15.09 первый запуск остановлен на хэндофф посреди Фазы 6**
> (`claude/HANDOFF_NIGHT_20260915.md`). Второй запуск только сверил факты.
> Третий запуск (15.09, днём) продолжает с Фазы 6 — его старт записан в
> разделе Фазы 6.
> Четвёртый запуск (15.09, вечер) продолжает по «Дополнению» промпта: сначала
> Фаза 8½ (косметика подпунктов и свежие векторы), затем Фаза 10 с пункта 10.1.
> Docker Desktop поднят: PostgreSQL 17 на 55432 (Фаза 14 проверит миграции на нём).
> Notion прочитан: «Задачи» («В работе» 6, «Надо» 54), «Решения» (12 последних);
> новая карточка 15.09 «Сайт падает NoReverseMatch на legal_document» — вне
> этой сессии, не трогалась.

## Таблица фаз

| Фаза | Тема | Статус | Чекпоинт |
|---|---|---|---|
| −1 | Сверка с реальностью | ✅ | — (ничего не меняли) |
| 0 | Ветка, журнал, Notion | ✅ | `2d51fe58` |
| 1 | Игра: клавиши не пробивают модалы, пауза забега | ✅ | фикс `1dd031c5`, чекпоинт `7389610c` |
| 2 | Игра: фриз с горящей кнопкой | ✅ | фикс `34046831`, чекпоинт `5599f36e` |
| 3 | Огромная иконка в шапке, подпись на графике | ✅ | фикс `cbecc135`, чекпоинт `fa7fc646` |
| 4 | «Проблема или предложение»: варианты, снимок, события | ✅ | фикс `34a9728d`, чекпоинт `a3c4e488` |
| 5 | Кнопка «Плохая задача» | ✅ | фикс `f1b4329b`, журнал — в коммите хэндоффа |
| 6 | Умный поиск: прогрев, разбивка времени, плашка, документ | ✅ | `12229eda` (первый запуск), фикс `a0af52b1`, чекпоинт «фаза 6» |
| 6½ | Пауза засчитывается: серверная фиксация с потолком | ✅ | фикс `f264464e`, чекпоинт «фаза 6½» |
| 7 | Поиск: анимация ожидания с фразами | ✅ | фикс `77db0328`, чекпоинт «фаза 7» |
| 8 | Виджет тестов: подпункты из условия (локальная база) | ✅ | фикс `1f105277`, применено на `db.sqlite3`, чекпоинт «фаза 8» |
| 8½ | Косметика подпунктов и свежие векторы | ✅ подпункты / ⚠️ векторы | фикс `f2f22557`, применено на `db.sqlite3`; векторы 3 200 из 4 096, остаток 896 досчитает владелец |
| 9 | Аналитика беты: своя таблица событий | ✅ | фикс `ecc10256`, чекпоинт «фаза 9» |
| 10 | Чат на задаче: GLM-5.3, режимы, файлы, логи | ✅ | 10.0 в чекпоинте фазы 9; фикс `675c580e`, чекпоинт `feca1374` |
| 11 | Бесконечные тесты в Wecon Rush | ✅ | фикс `f1988630`, чекпоинт `5226816a` |
| 12 | Дуэль: аккаунт, лобби, синхронный старт | ✅ | фикс `b07c53d9`, чекпоинт `41d12943` |
| 13 | Экраны Wecon Rush: раскладка | ✅ | фикс `665f6a84`, починка проверки `62a4d55b`, чекпоинт `85edb46a` |
| 14 | Финал: прогон, документация, Notion, отчёт | в работе | джобы 1, 2, 3, 5 — зелёные; джоб 4 (тесты на PostgreSQL) идёт |

---

## Фаза −1. Сверка с реальностью — ✅

1. `git status`: изменений в отслеживаемых файлах нет. Есть 7 неотслеживаемых
   личных файлов владельца (`.txt`, `Claude outputs/`, `New Text Document.txt`,
   `claude/HANDOFF_OLYMPIAD_TEXT_DEDUP_20260902.md`,
   `claude/RENDER_AUDIT_LEGACY_3000_20260829.md`, `kachat_vektora.ps1`,
   `session_c15_transcript_20260830.md`).
   **Предположение:** дерево считаю чистым — это старые заметки вне сессии,
   `git add` идёт только по именам, в коммиты они не попадут и при смене
   ветки не теряются.
2. Ветка `main`, `bae68c5` → `0024c37` → `86a1faf`. Ветки
   `feat/night-20260915` не было — первый запуск. `git fetch`: `main` =
   `origin/main`.
3. Python 3.13.12, Django 5.2.17.
4. `showmigrations game`: непрерывно до `0015_ended_reason_length` → новые с
   0016. `showmigrations problems`: последний `0064_alter_aiusagelog_user_and_more`
   → новые с 0065.
5. `manage.py check` — 0 ошибок (предупреждение про отсутствие `REDIS_URL`
   штатное для разработки).
6. Якоря — скрипт сверки нашёл 4 «расхождения», все ложные:
   - `templates/_feedback.html`: правило лежит внутри `<style>` (строка 30) как
     `.fb-btn svg { width: 15px; height: 15px; flex-shrink: 0; }` — в промпте
     записано сокращённо, дефект (размер значка только в позднем стиле) тот же.
   - `templates/_nav.html` (×2): скрипт принял `stroke-width="1.75"` за атрибут
     `width`. У обоих SVG есть только `viewBox="0 0 24 24"`, размеров нет.
   - `dead_test_widget_ids.txt`: 4 512 строк = 2 строки-шапки `#` + **4 510**
     строк данных (3 956 «варианты текстом в условии» + 554 «ни подпунктов, ни
     разметки»).
   Остальные якоря совпали дословно (игра, дуэль, поиск, чат, провайдеры ИИ,
   `core.run` с `provider/model/system/parse/log/check_budget`, `ProblemPart`,
   `answer_check`, `problem_types`, `entrypoint.sh` с `--max-requests 1000`,
   маршрут `api/feedback/`).

---

## Фаза 0. Ветка, журнал, Notion — ✅

- Ветка `feat/night-20260915` создана от `bae68c5`. Журнал заведён.
- Notion прочитан по протоколу: «Задачи» («В работе» и «Надо», первые 100
  строк), «Решения» — 10 последних по дате. Карточек решений от 15.09 не было.
  AI-поиск Notion на тарифе недоступен — искал обычным поиском по словам
  внутри каждой базы.
- **«Задачи» — 12 новых карточек (статус «Надо», ответственный Claude Code):**
  - Ф1 Игра: клавиши пробивают открытые окна — https://app.notion.com/p/3dcb11c92bc1816f85e6d76b6ea5cb9d
  - Ф2 Игра: фриз с горящей кнопкой — https://app.notion.com/p/3dcb11c92bc181c5bc56d47c46613f87
  - Ф3 Огромный значок в шапке и числа на графике — https://app.notion.com/p/3dcb11c92bc18145a34ccaff1bcc8bc8
  - Ф4 «Проблема или предложение»: варианты и снимок — https://app.notion.com/p/3dcb11c92bc181bab2eef9dd9fdad79a
  - Ф5 Кнопка «Плохая задача» — https://app.notion.com/p/3dcb11c92bc1816ca0ddea248ef3486b
  - Ф6 Умный поиск: прогрев, разбивка, плашка — https://app.notion.com/p/3dcb11c92bc181ec8baecd3ab0e4c222
  - Ф7 Поиск: анимация ожидания — https://app.notion.com/p/3dcb11c92bc181f1a026cf98ac960ad4
  - Ф9 Аналитика беты — https://app.notion.com/p/3dcb11c92bc181e89cb7c7f1f29e65a4
  - Ф10 Чат на задаче — https://app.notion.com/p/3dcb11c92bc181f78103c5992ce3b102
  - Ф11 Бесконечные тесты — https://app.notion.com/p/3dcb11c92bc181cd8286c92bfbdf934f
  - Ф12 Дуэль: синхронный старт — https://app.notion.com/p/3dcb11c92bc18109bdfbc22890404092
  - Ф13 Экраны Wecon Rush — https://app.notion.com/p/3dcb11c92bc181e4a9eff3438096d8d4
- **Ф8 — существующая карточка**, не дублировал: «Виджет теста не соберётся ни у
  одной из 8 992 переносимых задач» https://app.notion.com/p/3d9b11c92bc1817da7ecc8b2f544bbb2
  — комментарий «делается в ночной сессии 15.09, фаза 8».
- Комментарии-связки на смежных карточках (не переписывал): «Визуальная
  приёмка: макет wecon_rush_mockup.html…» (→ решение 6, фаза 13); «Фото
  рукописной работы ученика уходит в Anthropic…» (→ фаза 10 шлёт фото в
  GLM/Z.AI, вопрос остаётся открытым там); «Умный поиск открыт всем…» (→ фаза 6).
- **«Решения» — 7 карточек от 15.09.2026** (статус «Действует», источник —
  владелец в чате с Claude):
  1. Аналитика беты — своя таблица — https://app.notion.com/p/3dcb11c92bc181dd86eaca02f167cb4b
  2. Виджет тестов: варианты в ProblemPart — https://app.notion.com/p/3dcb11c92bc1815c8012e2e819ebb202
  3. Дуэль — строго синхронный старт — https://app.notion.com/p/3dcb11c92bc181638568f212137d460a
  4. Чат на задаче: режимы, файлы, GLM-5.3 — https://app.notion.com/p/3dcb11c92bc18163bef8ee0ca03157e9
  5. Бесконечные тесты внутри Wecon Rush — https://app.notion.com/p/3dcb11c92bc1811a847ff19bbb16af84
  6. Экраны Wecon Rush по списку без макета — https://app.notion.com/p/3dcb11c92bc181d4b8bbc2a40846010a
  7. Кнопка «Плохая задача» — https://app.notion.com/p/3dcb11c92bc18171a3acf9861e086c4c
- «Результаты» (`39bb11c9-2bc1-81da-91c5-e890da24d3c5`) — это обычная страница
  с разделами по датам, а не база; запись туда — в Фазе 14.
- **Замечено для владельца:** в «Решениях» карточка «Перенос корпуса на прод —
  весь видимый каталог минус подтверждённый брак…» от 13.09 заведена дважды
  (…81eab96af7c3968ec8b3 и …81058c5fe744a6a4fbda). В Notion ничего не удаляю —
  решать владельцу.

---

## Фаза 1. Игра: клавиши не пробивают модалы, забег умеет паузу — ✅

**Сделано** (фикс `1dd031c5`):
- `game/templates/game/game.html`: в начале глобального `keydown` два ранних
  выхода — `isTypingTarget` (INPUT/TEXTAREA/SELECT/contenteditable, кроме
  `#num-input` Классики, где Enter и пробел работают как раньше) и
  `modalIsOpen` (`.fb-back`, `.rp-back`, `dialog[open]`, `#quit-modal`,
  `#fmodal`, `#dmodal`, если не `hidden`).
- Пауза `rushPause` / `rushResume` рядом с `tick`: `timeLeft` на паузе не
  меняется, rAF отменён, сердцебиение снято и возвращается через
  `paintHud → paintAlarm`. Если забег кончился под окном — цикл не поднимается.
- Единое событие `weco:modal {open}`: шлют `_feedback.html` (в `build()` и
  `close()`) и окно выхода (`openQuit`/`closeQuit`); игра слушает и ставит
  или снимает паузу (закрылось одно окно, а под ним другое — пауза остаётся).

**Тест:** `game/tests/test_browser_modals.py` + раннер `browser_modals.mjs`
(Playwright в node, как у `test_design_canon`). Метка `serial`, причина в
комментарии: живой сервер + Chromium, допуск таймера 0,1 с. Блиц на пуле из
12 вопросов: открыть `.fb-btn` → «Проблема» → в textarea пробел, «1», Enter →
2 с. Остаток времени берётся у полосы `#time-fill` (`scaleX`), а не у цифр
таймера: выше 10 с цифры целые.

**Зубастость** (`game.html` и `_feedback.html` возвращены из `2d51fe58`):
красный по трём проверкам — `no_answers_sent` (ушёл 1 ответ),
`question_same` (вопрос сменился), `timer_paused` (t0 119,68 → t1 117,58 за
2 с). `timer_resumed` с дефектом зелёный — ожидаемо, таймер и так идёт. С
фиксом — зелёный, два прогона по 7,3 с.

**Быстрый круг:** `game` + `problems.tests.test_feedback`, шаг A — 788 тестов
за 88 с, OK. Логи — `reports/night_20260915/logs/f1_*.log`.

**Предположения и решения по ходу:**
1. `--scope-from-git` на изменённом ОБЩЕМ шаблоне (`templates/_feedback.html`)
   сам расширяет прогон до полного шага A — это ~112 мин на SQLite (замер
   08.09), а общие шаблоны задевают ещё фазы 3, 4, 5, 9. Фоновый полный
   прогон остановлен; быстрый круг гоняю явными лейблами: приложения
   изменённых файлов + тесты, которые ссылаются на изменённый общий шаблон
   (нашёл grep'ом). Полный прогон всех пяти джобов — в Фазе 14, как в промпте.
2. Окно выхода теперь ставит паузу (так в промпте). Это отменяет прежнее
   НАМЕРЕННОЕ поведение: комментарий в коде говорил «таймер идёт, иначе окно
   выхода стало бы бесплатной паузой». Комментарий переписан с датой решения.
3. В список открытых окон добавлен `#dmodal` («Бросить вызов») — тоже окно
   поверх игры, тот же дефект клавиш. В промпте его не было.
4. Окно фильтров из самого забега недоступно (открывается со старта и из окна
   дуэли) — паузу на нём не ставлю, только блок клавиш.
5. Серверное время ответа паузу не знает — бонус за скорость на вопросе с
   паузой сгорает (принято владельцем, комментарий у `rushPause`).
   **Найдено сверх промпта:** потолок длительности `_rank_run`
   (`duration × (1 + TIME_BONUS_CAP_FACTOR) + 60 с`, в Блице 300 с) меряет
   стенные часы — долгая пауза снимет забег с зачёта (`time_overrun`). Не
   чинил: учитывать паузу со слов клиента — ровно та дыра анти-чита, от
   которой потолок стоит. Вопрос владельцу — в отчёт.

**Осталось:** визуальная приёмка владельцем.

---

## Фаза 2. Игра: фриз с горящей кнопкой — ✅

**Сделано** (фикс `34046831`, правка текстов — в чекпоинте Фазы 2):
- `api()`: не-OK ответ → объект `{error, status}` (JSON-тело, если было);
  200 с не-JSON телом (страница входа после редиректа) → тоже объект. Сетевой
  сбой по-прежнему отклоняет промис.
- `postAnswer` → `answerFailed(d)`: `no_run` → оверлей «Раунд потерян» с
  кнопкой «На старт» (`/game/`); 409 → `advance()`; прочее и сетевой сбой →
  `inputLocked = false`, у Классики разблокируется поле, тост `#net-toast`
  «Нет связи. Нажмите ответ ещё раз» на 3 с.
- `prefetchNext()`: три повтора 300 / 800 / 1 600 мс, потом `prefetchFailed`;
  поколение цепочки `prefetchGen` гасит поздние ответы старой цепочки; `no_run`
  из буфера → `runLost`. `advance()`: ждёт буфер дольше 6 с — новая цепочка;
  `prefetchFailed` без буфера — тост с кнопкой «Повторить»; `runLost` — оверлей.
- `startRun()`: сетевой сбой → строка на стартовом экране «Нет связи: игра не
  началась. Попробуйте ещё раз.»
- Остальные вызовы `api()` просмотрены (всего 11): доска, статистика, итоги
  (`session/finish` уже с запасной сводкой), история, создание дуэли, панель
  рекордов — обработчики есть; счётчики фильтров без ответа просто остаются
  прежними, экран не замерзает — не трогал.
- Тост прозрачен для кликов (кликабельна только «Повторить»): иначе он мог бы
  закрыть собой вариант, который просят нажать ещё раз.

**Тест:** `game/tests/test_browser_freeze.py` + `browser_freeze.mjs` (serial,
причина в комментарии). Случай 1: первый ответ на `/game/api/answer/` — 502
страницей HTML → тост виден, через 300 мс повторный клик уходит на сервер
(перехватчик насчитал 2 вызова). Случай 2: `{"error": "Забег не начат",
"reason": "no_run"}` со статусом 400 → оверлей, «На старт» ведёт на `/game/`.

**Зубастость** (`game.html` из чекпоинта Фазы 1 `7389610c`): красные все четыре
проверки — тоста нет, повторный клик не ушёл (`calls: 1`), оверлея и кнопки
нет. С фиксом — зелёный, 4,0 с.

**Быстрый круг:** `--scope-from-git 7389610c` → лейбл `game`: шаг A — 763 теста
за 108,6 с OK, шаг B — 2 браузерных OK. **Первый прогон круга был красный** —
3 падения в `game.tests.test_wecon_texts`, исправлено (пункты 1–2 ниже).

**Предположения и решения по ходу:**
1. Текст оверлея из промпта «Забег потерян…» противоречит действующему решению
   владельца от 04.09 (слово «забег» на экране заменено на «раунд»), его держит
   `RoundNotRaceTests`. На экране — «Раунд потерян» / «Соединение или время
   ожидания. Начните новый.»
2. Длинное тире в видимых строках игры запрещено (`scripts/check_em_dash.py`,
   `EmDashTests`) — строка старта через двоеточие.
3. Тест `QuitModalTests.test_card_behind_is_blurred_and_timer_keeps_running`
   проверял СТАРОЕ решение (окно выхода без паузы) и после Фазы 1 зеленел
   впустую: искал `cancelAnimationFrame`, а пауза идёт событием. Переписан в
   `test_card_behind_is_blurred_and_round_pauses`: `openQuit` обязан объявить
   `weco:modal`. Зубастость: на `game.html` из `2d51fe58` утверждение ложно
   (красный), на фиксе истинно (зелёный).
4. `api()` превращает в объект и 200 с не-JSON телом — в коде промпта этого
   случая не было, но «HTML страницы входа» названа в корне дефекта.
5. У сервера два 409: «Вопрос уже отвечен» и «Забег уже завершён». Оба ведут в
   `advance()`, как в промпте. Второй бывает только при игре тем же раундом в
   двух вкладках: раунд не замерзает, а доигрывается до конца времени. Сервер
   не трогал.

**Осталось:** визуальная приёмка — выключить сеть посреди раунда.

---

## Фаза 3. Огромная иконка в шапке и подпись на графике — ✅

**Сделано** (фикс `cbecc135`):
- **3а.** Правила кнопки «Проблема или предложение» (`.fb-btn`, `.fb-btn svg`,
  `.fb-btn.is-busy`, медиа-запрос 1420, `.nav-panel .fb-btn …`,
  `.fb-btn--onpage`) вместе с комментарием-причиной перенесены из
  `_feedback.html` в новый партиал `templates/_fb_btn_style.html` — только CSS,
  без тега `<style>`. Партиал включён внутрь `<style>` шапки `_nav.html` (он
  стоит до разметки `<nav>`) и внутрь `registration/_auth_style.html` (он в
  `<head>` входа и регистрации). В `_feedback.html` остались стили окна,
  кружка Telegram и печати. У SVG значка — `width="15" height="15"` в
  `_nav.html` (×2), `login.html`, `register.html`.
- **3б.** Шкала очков графика «Динамика раунда» — чистые функции
  `scoreY`/`labelY` в `game/static/game/chart_math.js` (подключён `defer`
  рядом с `sound.js`): полоса `LABEL_PAD = 22` под подпись, подпись на 6 px
  выше точки. `chartCurve` зовёт `window.rushChartMath`.

**Тесты:**
- `problems/tests/test_fb_button_style.py`: на отрисованных `/catalog/`,
  `/game/`, `/calc2/`, `/login/`, `/register/`, `/student/`, `/profile/`
  правило `.fb-btn svg {` стоит раньше первой кнопки; у каждого значка кнопки
  есть атрибуты 15×15; в `_feedback.html` нет правил кнопки.
- `game/tests/test_chart_math.py`: node исполняет `chart_math.js` для очков
  [0, 10, 20], H = 130, P = 8 → подпись ≥ 12, точки в [8, 122]; страница
  рисует через модуль, старой формулы в ней нет.

**Зубастость:**
- 3а (5 шаблонов из `5599f36e`): красные все три — на `/catalog/` правило на
  позиции 110 014, кнопка на 82 177; на `/student/` 150 542 против 129 856; в
  `_feedback.html` снова `.fb-btn {`. С фиксом — зелёные.
- 3б (`LABEL_PAD = 8`, подпись на 5 px выше, `game.html` из `5599f36e`):
  подпись y = 3 при точках [122, 65, 8]; на странице нет подключения модуля.
  С фиксом — зелёные.

**Прогоны:** до коммита — шаг A 118 тестов OK (`test_fb_button_style`,
`test_chart_math`, `test_feedback`, `test_page_js`, `test_wecon_texts`,
статические проверки `test_design_canon`), шаг B — браузерный канон дизайна
`/calc2/` OK (37 с, без пропуска). Быстрый круг после правки сообщений тестов:
шаг A — 225 тестов за 26 с OK (лейблы фазы + все тесты шапки и входа).

**Предположения и решения по ходу:**
1. Экраны входа и регистрации шапку не подключают, а кнопка на них есть.
   Поэтому правила не «переехали в шапку», а стали партиалом, который оба
   места включают до разметки: одна копия правил, два места включения.
2. Сообщения утверждений в двух новых тестах переделаны на
   `assertTrue`/`assertFalse` с короткой строкой: первая версия при падении
   печатала весь HTML страницы (вывод проверки зубастости раздулся до 335 КБ).
3. Быстрый круг — явными лейблами (общие шаблоны расширили бы
   `--scope-from-git` до полного прогона): к тестам фазы добавлены все, кто
   смотрит в шапку и экраны входа — `config.tests.test_nav`,
   `problems.tests.test_obzor_nav`, `problems.tests.test_r15_builder`,
   `scripts.tests.test_scope_from_git`.

**Осталось:** приёмка глазами — обновить страницу на медленной сети (значок
сразу маленький); итог раунда — число над последней точкой видно целиком.

---

## Фаза 4. «Проблема или предложение»: варианты по экранам, снимок, события — ✅

**Сделано** (фикс `34a9728d`):
- **4а.** `problems/feedback_options.py`: списки заменены формулировками
  владельца, порядок сохранён; у `home` и `textbook` свои списки, из
  `FEEDBACK_ALIASES` они убраны; экран задачи остаётся алиасом каталога
  (жалобы на саму задачу — кнопка Фазы 5).
- **4б.** `grabShot()` отдаёт `{blob, note}`, `note` ∈ ok | timeout | error |
  nolib; таймаут 4 000 → 6 000 мс; форма шлёт `screenshot_note`. Поле
  `Feedback.screenshot_note` (`CharField(16)`, миграция
  `problems/0065_feedback_screenshot_note`); вьюха режет до 16 знаков; в
  админке столбец после «Снимок» и поле на странице записи.
- **4в.** `weco:modal` окно шлёт с Фазы 1 (`build()`/`close()`). Escape:
  слушатель окна закрывает его; до игры клавиша не доходит — игровой
  `keydown` выходит раньше по `modalIsOpen()` (Фаза 1), да и в Блице у Escape
  нет действия. Проверка добавлена в браузерный тест Фазы 1
  (`escape_closes_modal`).

**Миграция (скилл `weco-migration-safety`):**
- `showmigrations problems`: цепочка до 0064, новая 0065 сразу за ней;
- `makemigrations --check --dry-run` → «No changes detected»;
- с нуля на пустой SQLite — 117 миграций OK, включая 0065;
- копия рабочей базы: накат 0065 OK → откат к 0064 OK → повторный накат OK;
- PostgreSQL НЕ проверен: Docker не запущен (демона нет, порт 55432 закрыт) —
  проверка «с нуля на PostgreSQL» в Фазе 14. Рабочая `db.sqlite3` не
  мигрирована (перед утренней приёмкой нужен `migrate` — будет в отчёте).

**Тесты:** в `problems/tests/test_feedback.py` — свои списки у главной и
учебника; экран задачи = каталог; точные формулировки каталога и игры;
`screenshot_note` сохраняется и режется до 16; скрипт окна шлёт причину и
ждёт 6 с. В `game/tests/browser_modals.mjs` — проверка Escape.

**Зубастость** (по одному дефекту за раз, после каждого фикс возвращён):
- а) вьюха без поля → красный `test_screenshot_note_is_saved_and_trimmed`;
- б) старые списки → красные `test_home_and_textbook_have_their_own_lists`,
  `test_catalog_and_game_lists_are_the_owners_wording`;
- в) удалён алиас `problem` → красный `test_problem_screen_stays_a_catalog_alias`;
- г) `_feedback.html` из чекпоинта Фазы 3 → красный
  `test_script_sends_the_note_and_waits_six_seconds`;
- д) удалён слушатель Escape окна → красный `escape_closes_modal`.
- После возврата: 30 тестов + браузерный — OK.

**Быстрый круг:** `test_feedback`, `test_fb_button_style`, `test_media_route`
(админка обратной связи), `test_wecon_texts`, `test_page_js`,
`config.tests.test_nav` — шаг A, 155 тестов за 13,9 с, OK.

**Предположения:**
1. Промпт просит столбец в `list_display` после `has_shot`; поле показано и на
   странице записи (readonly) — иначе в карточке жалобы причины не видно.
2. Значение `screenshot_note` на сервере не сверяется со списком — промпт
   требует только обрезку; это подсказка разборщику, а не данные для логики.

**Осталось:** приёмка глазами — окно на каждом экране показывает свой список.

---

## Фаза 5. Кнопка «Плохая задача» в каталоге и в игре — ✅

**Сделано** (фикс `f1b4329b`):
- Модель `ProblemReport` в `problems/models_platform.py` рядом с `Feedback`
  (миграция `problems/0066_problem_report`): пять причин `Kind`, источник
  `Source` (`catalog` | `game`), `problem` — FK с `SET_NULL`,
  `game_question_id` — число без FK (`game` зависит от `problems`, не
  наоборот), текст, адрес, браузер, `handled`, `note`.
- `POST /api/problem-report/` (`problems/views_platform.py::api_problem_report`):
  гостю можно; CSRF обязателен; лимит частоты — механизм обратной связи со
  своим ключом `problem_report` (11-я жалоба подряд → 429); автор — только
  `request.user`; `other` без текста → 400 «Напишите, что не так»; текст до
  2 000 знаков; номера — целые 1…2 147 483 647, нужен хотя бы один; задачу
  игрового вопроса находит сервер (`apps.get_model('game', 'GameQuestion')`),
  в ответе только `{ok, id}` — до ответа клиент игры `problem_id` знать не
  должен (анти-чит).
- Админка `ProblemReportAdmin` (`problems/admin_platform.py`): список
  `created_at, source, kind, задача (ссылка на /catalog/problem/<id>/),
  game_question_id, кто, short, handled`; фильтры по причине, источнику,
  разобранности, дате.
- Окно `templates/_problem_report.html` на классах окна обратной связи
  (`fb-*` плюс `rp-back`/`rp-box`); причины — из `ProblemReport.Kind` через
  контекст-процессор (`problem_report_kinds`, `json_script`); шлёт
  `weco:modal`; при «Другое» подпись поля становится обязательной. Подключено
  сразу после `_feedback.html` в семи базовых шаблонах.
- Кнопка `.btn-report` «Плохая задача?» (SVG-флажок 15 px): на странице
  задачи — в строке действий рядом со «Скопировать ссылку»; в игре — под
  блоком ответа справа, над подсказкой клавиш, `data-game-question-id` ставит
  `showQuestion`. В `_problem_modal.html` строки со «Скопировать ссылку» нет —
  туда кнопку не добавлял, как велит промпт.
- Попутно пойман собственный дефект: при вставке модели свойство `short`
  модели `Feedback` уехало в тело `ProblemReport` (список админки обратной
  связи упал бы на первой записи). Возвращено, закрыто регрессионным тестом.

**Границы доступа.** Скилл `weco-security-boundary-review` вызывался, но его
текст до сессии не дошёл (сбой инструмента) — границы проверены по его
смыслу: таблица «ресурс × роль × действие» в докстринге
`problems/tests/test_problem_report.py`, каждая клетка закрыта тестом (гость
и ученик создают; без CSRF 403; GET 405; автор из сессии; список жалоб видит
только staff; задача игрового вопроса наружу не уходит).

**Миграция (скилл `weco-migration-safety`):** `makemigrations --check
--dry-run` → «No changes detected»; `manage.py check` — 0; с нуля на пустой
SQLite — 118 миграций OK (с 0065 и 0066); копия рабочей базы: накат 0066 →
откат к 0065 → повторный накат — OK. PostgreSQL не проверен (Docker выключен).

**Тесты:** `problems/tests/test_problem_report.py` — 23 теста (приём 5, отказы
11, доступ к админке 3, экраны 4); в `game/tests/browser_modals.mjs` —
проверка `report_window_pauses` (окно жалобы ставит раунд на паузу).

**Прогоны:** шаг A — 190 тестов за 18,8 с, OK (`test_problem_report`,
`test_feedback`, `catalog.tests.test_no_slop`, `game.tests.test_page_js`,
`game.tests.test_wecon_texts`, `test_fb_button_style`, `config.tests.test_nav`,
`test_media_route`); шаг B — `test_browser_modals` и `test_browser_freeze`, OK
за 10,4 с.

**Зубастость:** скрипт вносит по одному дефекту, гоняет нужный тест и
возвращает файл в `finally`. 21 дефект — 21 красный, без зубов 0
(`logs/f5_teeth.log`, по логу на дефект в `logs/teeth_f5/`): гостю нельзя;
автор из запроса; задача уходит в ответ; вопрос игры не приводит к задаче;
«Другое» без текста; неизвестная причина; неизвестный источник; без номеров
(сняты оба слоя отказа); номер без проверки на число; ненайденная задача;
ненайденный вопрос; длинный текст; GET; CSRF снят; лимит снят;
`Feedback.short` снова уехал; нет кнопки на странице задачи; нет кнопки в
игре; партиал не подключён в каталоге; причины не из модели; окно жалобы не
шлёт `weco:modal` (браузерный, `report_window_pauses`). Прогон шёл в момент
остановки сессии — после него отдельно проверено: дерево чистое, следов
дефектов в файлах нет.

**Предположения:**
1. Первый запуск скрипта зубастости остановился на втором дефекте: якорь
   «автор» встречался дважды (так же пишет автора `api_feedback`), и скрипт
   отказался портить неоднозначное место. Якорь уточнён; текст с дефектом
   теперь готовится до записи в файл.
2. Номер задачи, присланный вместе с номером игрового вопроса, игнорируется:
   задачу определяет сервер, иначе жалобу можно было бы привязать к чужой
   задаче.

**Осталось:** приёмка глазами (сначала `migrate` рабочей базы — см. хэндофф).

---

## Фаза 6. Умный поиск: прогрев, разбивка времени, плашка, документ — ✅

Первый запуск сделал половину и остановился на хэндофф — ниже его запись как
была. Третий запуск доделал фазу — разделы «Старт третьего запуска» и
«Доделано в третьем запуске» в конце.

**Сделано в первом запуске** (чекпоинт `12229eda`):
- 6б: `catalog/warmup.py`, `config/gunicorn_conf.py` (`post_worker_init`),
  `-c /app/config/gunicorn_conf.py` в `deploy/entrypoint.sh`. Настройки
  `SMART_SEARCH_WARMUP` ещё нет — прогрев не включается.
- 6в (половина): `corpus_build_seconds` и `pool_seconds` в `RerankResult`,
  в консоли и в `reports/smart_search_log.jsonl`.
- Тесты: `catalog/tests/test_warmup.py` (8, зелёные),
  `catalog/tests/test_smart_search_timing.py` (7: 3 зелёных, 4 красных
  намеренно — заголовок и плашка ещё не написаны). Прогон вместе с
  `test_rerank`: 47 тестов, 1 падение и 3 ошибки ровно из этих четырёх
  (`logs/f6_wip_tests.log`).

**Факты для продолжения (проверены):** `psutil` в `venv313` нет; в локальном
`.env` есть `GLM_API_KEY` и `SMART_SEARCH_RERANK=1`, `SEMANTIC_SEARCH_ENABLED`
не задан (значит, включён); Docker выключен; `dump_for_deploy` вырезает
`embedding`; `sentence_transformers` и torch не входят в
`requirements/base.txt`.

**Осталось:** 6а (замер — первым), настройка прогрева, заголовок
`X-Smart-Search-Ms` (6в), текст плашки (6д), живая проверка (6г), документы
(6е), зубастость, быстрый круг, чекпоинт.

**Предположения:**
1. Прогрев — в `post_worker_init`: хук зовётся после загрузки приложения в
   воркере (Django уже настроен), а запросов воркер ещё не принимает.
2. Сборку корпуса меряет сам `get_corpus()`, поток-локально: подмена
   `build_pool` в тестах честно даёт 0, а параллельные запросы `runserver` не
   путают чужую сборку со своей.

### Старт третьего запуска (15.09, днём)

- Дерево чистое: в отслеживаемых файлах изменений нет, только личные
  неотслеживаемые файлы владельца — ни коммита остатков, ни stash.
- `manage.py check` — 0 ошибок; ветка `feat/night-20260915`, HEAD `c34bce28`.
- Порт 8000 свободен → `manage.py migrate`: применены
  `problems.0065_feedback_screenshot_note` и `problems.0066_problem_report`, OK
  (`logs/f6c_migrate.log`).
- Временные файлы первого запуска удалены из папки сессии `e5a27f43…`:
  `db_copy.sqlite3` (1,29 ГБ), `zero_a*.sqlite3`, настройки и скрипты.
- Notion прочитан: «Задачи» — «В работе» (7) и «Надо» (40 свежих заголовков),
  «Решения» — 10 последних. Карточка Фазы 6 уже «В работе». Дубль решения
  13.09 «Перенос корпуса на прод — весь видимый каталог…»: оставлена созданная
  раньше `…81058c5fe744a6a4fbda` (11:20:06); вторая `…81eab96af7c3968ec8b3`
  (11:20:16) получила комментарий «дубль» и перенесена в страницу «На
  удаление». Ничего не удалено.
- Владелец посреди запуска обновил правило контекста: работать до 3–5 %
  остатка.

### Доделано в третьем запуске

**6а. Замер** (`logs/f6a_measure.log`, только чтение рабочей базы): корпус
bm25 — 14 082 задачи, холодный 22,32 с и 26,25 с, тёплый 0,0002 с; смысловой
индекс — 14 082 вектора, холодный 3,19 с и 3,03 с, тёплый 0,0002 с. RSS не
мерил: `psutil` в `venv313` нет. Числа — в `docs/EMBEDDINGS.md`.

**6б/6б′. Прогрев в фоновом потоке** (фикс `a0af52b1`):
- `SMART_SEARCH_WARMUP` в `config/settings.py` (из env, по умолчанию `1`) и
  строка в `deploy/env.example`.
- `post_worker_init` запускает daemon-поток `smart-search-warmup` и сразу
  возвращается; `warm_worker` без `notify`, в `finally` закрывает соединение
  своего потока.
- **Найдено сверх промпта:** замка у `get_corpus()` не было (в промпте — «под
  своим замком»), у `semantic.get_index()` тоже. С прогревом в потоке поиск,
  пришедший во время сборки, начал бы вторую сборку рядом — ещё 20+ с CPU и
  базы в том же воркере. Добавлен `threading.Lock` в обе функции; ожидание
  чужой сборки попадает в `corpus_build_seconds` ждущего поиска.

**6в. Заголовок** `X-Smart-Search-Ms` у `problem_list` и `api_filter_state`:
время `_search_ids` + `rerank.apply`, целое мс, 0 без запроса; ключ
`smart_search_ms` в контексте; комментарий «Не для шаблона» исправлен.

**6г. Живая проверка** (`rerank_trace`, платно; Docker выключен — плотная нога
недоступна, в логе traceback соединения с `search:8001`, это ожидаемо):

| Запрос | Ноги до дедупа | Пул | Пачек | Модель | Балл ≥ 50 / = 0 / не оценено |
|---|---|---|---|---|---|
| «налог на монополиста» | dense 0, bm25 50 | 46 (−4 дубля) | 1 | 6,492 с | 46 / 0 / 0 |
| «эластичность спроса» | dense 0, bm25 50 | 48 (−2) | 1 | 6,962 с | 47 / 0 / 0 |
| «фирма выбирает объём выпуска, а государство вводит потолок цены: как изменится излишек потребителей» | dense 0, bm25 50 | 46 (−4) | 1 | 5,986 с | 15 / 0 / 0 |

`rerank_trace` не печатает итоговый статус и общее время; все кандидаты
оценены, пачка не упала — на странице это был бы `rerank`. Логи —
`logs/f6g_trace_{1,2,3}.log`.

**6д. Плашка:** при `smart_search_status == 'rerank'` — «Ищем по словам с умной
сортировкой. Поиск по смыслу пока выключен, результаты могут быть беднее
обычного.»; иначе прежний текст.

**6е/6е′. Документы и векторы:**
- `problems/management/commands/embeddings_export_vectors.py`. ⚠️ Формат не
  `.npz`, как в промпте: `embeddings_import_vectors` читает пару
  `<префикс>.f32` + `<префикс>.meta.json` (`rows`, `ids`, `hashes`, `spec`,
  `version`), а промпт велит в таком случае писать в его формат. Третий файл —
  `.state.json`, копия домашней отметки сверки билда.
- Экспорт видимого каталога локально (`logs/f6e_export.log`): 14 082 вектора из
  14 082 задач среза, битых 0, `catalog.f32` — 55,0 МБ
  (`reports/night_20260915/vectors/`, в git не входит).
- ⚠️ **Порядок промпта «ввоз → `embeddings_check_build`» на бою невозможен:**
  ввоз без отметки о пройденной сверке не пишет вовсе, а сверка кодирует
  выборку моделью (`search_service.app.get_model`) — модели нет в `web`, у
  контейнера `search` нет ни базы, ни кода проекта (в образе только `app.py`).
  Сверка проведена дома 09.09.2026 на этих векторах (`STATE.json`:
  `v2_focus_repeat`, минимум 0,9999999, медиана 1,0) — экспорт везёт копию
  отметки, ввоз на бою идёт с `--state`. Так и записано в `docs/SERVER.md`.
- Хеш в файле — сохранённый `embedding_source_hash` (`text_hash` и
  `embedding_source_hash` — одна и та же MD5): ввоз пересчитает хеш по боевым
  полям и пропустит разошедшиеся.
- `docs/SERVER.md` — раздел «Включение смысловой ноги на бою»: прогрев,
  экспорт → `scp` → `docker compose cp` в `web` → план ввоза → `--apply` →
  контейнер `search` → флаги → `curl` → откат. У `web` папки `/srv/weconomics`
  нет (тома только `static`, `media`), поэтому `docker compose cp`.
- `docs/EMBEDDINGS.md` — раздел «Прогрев воркера, разбивка времени и векторы
  на бой (15.09.2026)».

**Тесты:**
- `catalog/tests/test_warmup.py`: убран тест сердцебиения; новые —
  `test_closes_its_db_connection`, `…_when_warmup_fails`,
  `GunicornHookTests.test_hook_warms_in_a_background_thread_and_returns_at_once`
  (хук < 1 с при долгом прогреве, поток daemon с именем),
  `EarlySearchWaitsForWarmupTests` ×2 (корпус и индекс строятся один раз).
- `catalog/tests/test_smart_search_timing.py` — четыре намеренно красных теста
  первого запуска позеленели.
- `problems/tests/test_embeddings_export_vectors.py` — 4: круговой обмен через
  настоящий `embeddings_import_vectors --apply` (векторы побайтно те же, хеш
  равен пересчитанному), видимый каталог по умолчанию, чужая формула остаётся
  дома, отказ без пройденной сверки.

**Зубастость** (`logs/teeth_f6.log`, лог на дефект — `logs/teeth_f6/`; якорь
проверяется на точное число вхождений, файл возвращается в `finally`):
10 дефектов — 10 красных, без зубов 0: хук зовёт прогрев напрямую; прогрев не
закрывает соединение; корпус без замка; индекс без замка; нет заголовка мс;
старая плашка при `rerank`; неверный хеш в экспорте; экспорт всего банка по
умолчанию; экспорт чужой формулы; экспорт без сверки. После — `git status`:
код на месте.

**Прогоны:** до зубастости — 54 теста OK (`f6c_tests_1.log`); после —
`test_warmup`, `test_smart_search_timing`, `test_rerank`, `test_smart_catalog`,
`test_search_service`, `test_no_slop`, `test_embeddings_export_vectors`: шаг A
106 OK, шаг B 3 OK, код 0 (`f6c_tests_2.log`). `ruff` по изменённым файлам —
чисто.

**Предположения:**
3. Предположение 1 первого запуска («прогрев прямо в `post_worker_init`»)
   отменено решением владельца — поток.
4. Отдельного теста на умолчание `SMART_SEARCH_WARMUP=1` нет: значение
   читается из окружения при загрузке настроек, тест зависел бы от `.env`
   разработчика. Умолчание записано в `env.example` и в документах.
5. Замок у `get_index()` берётся и на тёплом пути (один `acquire` на поиск):
   проще двойной проверки, цена ничтожна.

**Для боя:** `docs/SERVER.md`, «Включение смысловой ноги на бою». Прогрев
включится сам после выкатки ветки (entrypoint уже с `-c`).

---

## Фаза 6½. Пауза засчитывается: серверная фиксация с потолком — ✅

**Сделано** (фикс `f264464e`):
- `POST /game/api/pause/` и `POST /game/api/resume/` (`game/views.py`,
  `require_POST`, обычный CSRF): начало и конец паузы по часам сервера
  (`_now_ms`) в `state['pauses']` = `[[начало_мс, конец_мс|None], …]`; тело
  запроса не читается. Без раунда — 400 `no_run`, после конца раунда — 409.
- `api_answer` и `api_question`, заставшие паузу открытой, закрывают её
  моментом запроса и отвечают 409 `{'error': 'paused', 'reason': 'paused'}`;
  `api_session_finish` тоже закрывает открытую паузу (без 409).
- Потолок в `game/config.py`: `PAUSE_CAP_SECONDS = 120`, `PAUSE_MAX_COUNT = 5`;
  `credited_pause_ms(state)` суммирует закрытые паузы из первых пяти, не
  больше 120 с. Хранится не больше шести записей: последняя незачётная
  перезаписывается.
- `_rank_run`: `wall_ms - credited_pause_ms(state) > limit_ms` →
  `time_overrun`; `GameResult.wall_ms` остаётся стенным.
- Клиент (`game.html`): очередь `pauseSync`; `rushPause`/`rushResume` шлют
  pause/resume через `tellServer` (ошибки поглощаются); ответ и prefetch
  вопроса уходят после очереди; `answerFailed` на `reason === 'paused'` молча
  возвращает кнопки — без этой ветки 409 вёл бы в `advance()`, и вопрос
  пропадал бы. Комментарий «сервер паузы не знает» переписан под решение.
- `docs/GAME.md` — раздел «Пауза раунда» и пункт в «Анти-чит».

**Тесты:** `game/tests/test_pause.py` — 15: API (серверные часы при подмене
`_now_ms`, 409 на ответ и вопрос с закрытием паузы, финиш закрывает, хранение
не растёт, без раунда 400, CSRF 403 и GET 405), зачёт (потолок 120 с, первые
пять, открытая не в зачёт), `_rank_run` (пауза 30 с при превышении на 20 с —
в таблице; сверх потолка не помогает), клиент (шлёт pause/resume, ответ и
вопрос ждут очередь, ветка `paused` раньше ветки 409).

**Зубастость** (`logs/teeth_f6h.log`): 12 дефектов — 12 красных, без зубов 0:
пауза по сырым часам; ответ принят на паузе; вопрос выдан на паузе; финиш не
закрыл паузу; хранение растёт; `csrf_exempt` на pause; `_rank_run` без
вычета; без потолка секунд; без потолка числа пауз; клиент не шлёт паузу;
ответ мимо очереди; `paused` как «уже отвечен». После — игровые файлы на
месте (`git status`).

**Прогоны:** `test_pause`, `test_ranked`, `test_page_js`, `test_wecon_texts` и
браузерные `test_browser_modals`, `test_browser_freeze` (окна поверх раунда
теперь шлют pause/resume живому серверу): шаг A 121 OK, шаг B 2 OK, код 0
(`logs/f6h_tests_1.log`). `ruff` — чисто.

**Предположения:**
1. Промпт требует и «открытая пауза закрывается следующим запросом», и «на
   паузе ответ и вопрос — 409». Свёл так: запрос, заставший паузу открытой,
   закрывает её своим моментом И получает 409; повтор проходит. Без закрытия
   потерянный в сети resume заморозил бы раунд навсегда.
2. Пауза после конца раунда — 409 «уже завершён»; resume без открытой паузы —
   200, ничего не пишет.
3. Ответ, ушедший ДО открытия окна, в очередь не встаёт (иначе быстрые ответы
   Пули ждали бы prefetch). Если pause обгонит его на сервере, ответ получит
   409 и закроет паузу раньше: зачтённая пауза в этом редком случае короче
   настоящей, раунд не ломается.

**Осталось:** приёмка глазами — войти, `/game/` → Блиц → открыть «Проблема
или предложение» на 30 с → закрыть → доиграть: раунд в таблице при прочих
условиях зачёта; в админке у `GameResult` `wall_ms` больше длительности
раунда, `unranked_reason` пуст.

**Notion:** «Решения» — новая карточка «Пауза раунда засчитывается…»
(https://app.notion.com/p/3dcb11c92bc181f1bb87e6c18933d8f2); «Задачи» — карточка
фазы «На проверке» (https://app.notion.com/p/3dcb11c92bc1818f85a9c3cb0501138a).
Поиском проверено: карточек про паузу раньше не было.

---

## Фаза 7. Поиск: анимация ожидания с фразами — ✅

**Сделано** (фикс `77db0328`):
- `catalog/placeholder_phrases.py::SEARCH_BUSY_PHRASES` — 15 фраз, «думается
  думается…» первой.
- `problem_list.html`: `<span class="ask-busy"><i></i><span id="ask-busy-text">`
  с первой фразой из константы; `json_script` `search-busy-phrases`; угасание
  160 мс. Кольцо `.ask-busy i` уже крутилось (`askSpin`) — не трогал.
- `_catalog_js.html`: при отправке — смена фразы раз в 1,6 с через угасание;
  первая всегда первая, остальные перемешиваются на каждый запрос; при
  `prefers-reduced-motion` — первая фраза без смены.
- `_catalog_results.html`: после поиска первые 12 карточек получают
  `ct-appear` (220 мс, прозрачность и сдвиг 4 px, лесенка по 40 мс через
  `:nth-of-type`, без инлайновых стилей); при «уменьшить движение» — без
  анимации (своё правило плюс общее из `catalog/base.html`).

**Тесты:** `catalog/tests/test_search_busy.py` — 6: первая фраза в разметке
вместо старого текста; json_script отдаёт весь список; после поиска при 14
найденных `ct-appear` ровно у 12; без поиска — ни у одной; константа начинается
с «думается думается…», фраз 15; скрипт читает фразы, меняет раз в 1,6 с,
уважает reduced-motion, закрепляет первую. В `test_catalog_redesign.py`
помощник `_card_html` искал `'<a class="ct-card"'` с закрывающей кавычкой и
падал на карточках со вторым классом — искать без кавычки (с комментарием).

**Зубастость** (`logs/teeth_f7.log`): 7 дефектов — 7 красных: старый текст;
нет json_script; анимация у всех карточек; анимация без поиска; первая фраза
не закреплена; reduced-motion игнорируется; «думается» не первая.

**Прогоны:** первый круг красный — ошибка в `test_catalog_redesign` (см.
выше). После правки: `test_search_busy`, `test_catalog_redesign`,
`test_no_slop`, `test_smart_search_timing`, `test_smart_catalog`,
`test_design_canon` — шаг A 80 OK, шаг B 1 OK (браузерный канон), код 0
(`logs/f7_tests_2.log`). `ruff` — чисто.

**Живая проверка в браузере** (панель Browser, `runserver` 8421 на рабочей
базе, `/catalog/`, синтетическое событие `submit` — без перехода и без
платного поиска): до отправки `.ask-busy` скрыт, после — `flex`; «думается
думается…» → через 1,9 с «проверяю условие первого порядка…» → через 3,55 с
«заглядываю в ящик Эджворта…»; кольцо `askSpin` крутится; старого текста нет;
ошибок в консоли 0.

**Предположения:**
1. «Порядок случайный при каждом запросе» — перемешивает скрипт на каждую
   отправку формы, а не сервер при отрисовке: страница одинакова для кэша и
   тестов.
2. Первая фраза в разметке берётся из константы, а не литералом в шаблоне —
   иначе два места разошлись бы.

**Осталось:** приёмка глазами — `/catalog/` → ввести «налог» → Enter: пока
грузится, под полем сменяются фразы; выдача появляется лесенкой.

---

## Фаза 8. Виджет тестов: подпункты из текста условия — ✅

**Скилл** `weco-content-integrity` загружен до написания команды; всё из его
последовательности сделано (сухой прогон по умолчанию, счётчики до/после,
предпросмотр с краями, снимок и копия базы, инварианты, идемпотентность).

**Разведка базы** (`logs/f8_recon.log`, `logs/f8_recon_b.log`, только чтение):
- у живых виджетов подпункты — метки «а», «б», «в», `statement` = текст
  варианта, `answer` пусто, `points` пусто; верные — из `Problem.answer`
  («в», «б»). «Верно/неверно» — подпункты «а» Верно, «б» Неверно (задачи 5038,
  5475);
- у кандидатов ответ записан по-разному: 1 715 одиночных — строка варианта
  целиком («3. дохода»), 1 070 — метка («2»), 423 множественных — несколько
  строк, 389 — метки («124»), 492 «верно/неверно» — слово, 366 — номер;
- стиль блока: нумерованный подавляюще (1 606 + 804 + 410 + 354 + 360),
  «1. (a)» — 243, «а)» — 159; файл id — строки «id<TAB>причина».

**Сделано** (фикс `1f105277`):
- `problems/test_options_parse.py` — чистый разбор: хвост условия, шапка
  «Варианты ответа», три стиля, один стиль на блок, порядок 1…n / а… / a… без
  пропусков, ≥ 2 строк, отказ на глаголах-заданиях (нумерованный стиль),
  непустое условие; `correct_labels` читает ответ двумя путями (строка/текст
  варианта и метки через `label_set`) и при расхождении отказывает;
  `boolean_correct_label` — слово, строка «2. нет» или номер с поиском слова в
  условии (столбиком или одной строкой «1) Верно  2) Неверно»);
  `formula_worse` — нечётное число неэкранированных `$` или сильнее разбаланс
  `{}` (дифференциально).
- `test_options_from_statement` — сухой прогон по умолчанию (`REPORT.md` и
  `preview.html`: 20 случайных примеров и по 5 самых коротких и длинных
  вырезанных блоков), `--apply` (одна транзакция; снимок на диске ДО
  фиксации; инварианты: задач столько же, подпунктов ровно на созданное
  больше, `game_of` не пуст у каждой применённой; нарушение — откат всей
  транзакции и переименование снимка в `_rolled_back`), `--revert` (условие
  возвращается, если его не правили после записи; подпункты удаляются, если
  не правили; правленое называется), `--ids-file`, `--limit`, `--report`.

**Тесты:** `problems/tests/test_test_options_parse.py` — 22 (12 фикстур разбора:
оба буквенных стиля, с шапкой и без, `;` и `.` на концах, кириллица и
латиница, пропуск номера, подвопросы с глаголами, одна строка, текст после
блока, смешанные стили, пустое условие, пустой вариант; верные метки — строка
целиком, текст, метки, номер «1. (a)» → буква, несколько строк, неоднозначный
и чужой ответ; «верно/неверно» — слово, номер, строка; формулы).
`problems/tests/test_test_options_command.py` — 6: сухой прогон не пишет и
пишет отчёт; запись создаёт подпункты, режет условие, `answer` не трогает,
виджеты живы, подвопросы не тронуты; откат побайтно и кандидаты возвращаются;
второй `--apply` — 0; откат не удаляет правленый подпункт; мёртвый виджет —
откат всей транзакции. Первый прогон — 28 OK, код 0 (`logs/f8_tests_1.log`);
`ruff` — чисто.

**Зубастость** (`logs/teeth_f8.log`): 16 дефектов — 16 красных, без зубов 0:
без проверки порядка; без отказа на глаголах; блок из одной строки; шапка
остаётся в условии; текст варианта не чистится; неоднозначность не ловится;
номер «1. (a)» не переводится в букву; «верно/неверно» перепутаны; `$` не
проверяются; сухой прогон пишет; инвариант выключен; откат удаляет правленый
подпункт; откат не возвращает условие; кандидаты с подпунктами (не
идемпотентно); верные не отмечены; условие «верно/неверно» вырезается.

**8г. Применение на рабочей базе:**
- порт 8000 и 8421 свободны; копия базы до записи —
  `C:\Users\shipu\weconomics-data\backups\db_before_test_options_20260915.sqlite3`
  (backup API SQLite, 4,2 с; в обеих 41 307 задач и 65 803 подпункта);
- сухой прогон на полном объёме (`logs/f8_dryrun.log`): кандидатов 4 510
  (single 2 813, multi 837, boolean 860) — `parsed` 3 251,
  `boolean_synthetic` 825, `no_block` 127, `mixed_style` 91, `answer_mismatch`
  184, `single_multi_correct` 9, `formula_balance` 23, `verb_like_subquestion`
  0, `empty_stem` 0;
- 20 случайных примеров и 10 краёв прочитаны глазами до записи: вырез точный,
  условия с таблицей, картинкой `[[FIGURE:…]]` и «( $3$ балла)» целы, верные
  варианты сходятся с ответом во всех формах записи;
- `--apply` (`logs/f8_apply.log`): записано задач 4 076, подпунктов 16 282,
  условий переписано 3 251; задач в базе 41 307 → 41 307, подпунктов 65 803 →
  82 085 (ровно +16 282); снимок
  `reports/night_20260915/test_options/snapshot_20260915_112753.json` (7 МБ);
- повторный сухой прогон (`logs/f8_dryrun_after.log`, отчёт в
  `test_options_idempotency/`): кандидатов 434 — только отклонённые, к записи 0;
- **живые виджеты на видимом каталоге** (`game_of`, `logs/f8_live_*.log`):
  **1 409 → 5 485** (single 826 → 3 266, multi 238 → 1 049, boolean 345 →
  1 170) из 7 515 тестов, из них 1 377 `numeric` вариантов не имеют вовсе;
  прирост 4 076 — все применённые задачи в видимом каталоге.

**Замечания для владельца:**
1. У 3 251 переписанного условия устарел хеш эмбеддинга — их подхватит
   `build_embeddings --stale` (не запускал).
2. `content_hash` не пересчитан намеренно: это отпечаток условия источника для
   дедупликации при повторном импорте.
3. Косметика: у 15 задач одного источника варианты вида «1. (1) текст» —
   виджет покажет «1) (1) текст» (например 59576, 59593, 59730, 58683, 58144).
4. У «верно/неверно» условие по решению не менялось: где в нём была строка
   «1) Верно  2) Неверно», она остаётся над плитками.
5. 434 задачи не тронуты — id по причинам в конце `REPORT.md` (ручной разбор).

**Предположения:**
1. Верные варианты отмечаются в подпунктах (`answer` = «верно»), а не так, как у
   живых (пустой `answer` и метка в `Problem.answer`): `Problem.answer` трогать
   нельзя, а у большинства одиночных он хранит строку варианта целиком, которую
   `label_set` не разберёт. Разметку подпунктов `catalog_test_correct_labels`
   читает первой — это поддержанный формат, а не новый.
2. Метки подпунктов — как у автора: «1», «2» у нумерованных, буква у «а)» и у
   «1. (a)» (номер там только нумерация строк, ответ «2» переводится в «b»).
3. Откат возвращает условие, только если его не правили после записи, —
   иначе затёр бы чужую правку; такие id называются.
4. Сверх причин промпта добавлены `empty_stem` и `formula_balance` (скилл
   требует инвариантов по `$` и `{}`); пустой текст варианта и дубли меток
   идут в `mixed_style`.

**8д. Для боя** (сделать владельцу, прод не трогался):
```bash
cd /srv/weconomics/app/deploy
docker compose exec web python manage.py test_options_from_statement --dry-run --report /app/reports/test_options_prod
docker compose cp web:/app/reports/test_options_prod ./test_options_prod   # прочитать REPORT.md и preview.html
docker compose exec web python manage.py test_options_from_statement --apply --report /app/reports/test_options_prod
```
Перед `--apply` — свежий дамп базы (бэкап в 03:20 может быть старым). Файл id
по умолчанию (`reports/corpus_transfer_20260913/dead_test_widget_ids.txt`)
в образ не едет: `reports/` в `.gitignore` — на бою его надо положить
`docker compose cp` или передать `--ids-file`. Снимок отката остаётся в
`/app/reports/test_options_prod/` внутри контейнера — скопировать его наружу
сразу после записи, иначе пересборка контейнера его унесёт.

---

## Фаза 9. Аналитика беты: своя таблица событий — ✅

**Скиллы** `weco-migration-safety` и `weco-security-boundary-review` загружены до
модели и вьюхи; оба чек-листа пройдены (ниже).

**Сделано** (фикс `ecc10256`):
- Модель `Event` в `problems/models_platform.py` ровно по полям промпта,
  индексы `(name, ts)` и `(visitor, ts)`; миграция `problems/0067_event`.
- `POST /api/track/` (`problems/views_platform.py::api_track`, маршрут в
  `config/urls.py`): гостю можно; `csrf_exempt` осознанно, вместо токена —
  `Origin` (или `Referer`) обязан быть нашим хостом (`validate_host` с
  запасным списком разработки, как у Django), cookie `weco_vid` обязательна
  (иначе 400), ≤ 50 событий (иначе 400), свой счётчик в кэше
  `track:<visitor>:<окно>` — 600 событий за 10 минут (сверх — 429, события
  отброшены), `props` режутся до 2 КБ (строки до 300, лишние ключи
  отбрасываются, запрос не отвергается); `user` — только из сессии;
  `page_key` и путь без строки запроса считает сервер.
- `static/track.js` (134 строки): cookie `weco_vid` на год; `page_view`
  (`referrer`, `view`), `page_leave` на скрытии вкладки и `pagehide` с
  активным временем (счёт стоит, пока вкладка скрыта); клики по
  `a, button, [role=button], input[type=submit]` в фазе перехвата (окна
  гасят всплытие); `problem_open` на странице задачи — номер из адреса;
  `search {q_len, status, total, degraded}` по `data-search-*` секции выдачи;
  `search_submit {q_len}`; очередь со сбросом раз в 5 с, при 20 и на уходе;
  `sendBeacon` с запасным `fetch(keepalive)`; `window.weco.track`.
- `templates/_track.html` подключён сразу после `_feedback.html` во всех девяти
  шаблонах (семь базовых, вход, регистрация).
- Явные события одной строкой в существующих точках: `game_start`,
  `game_answer`, `game_end`, `duel_create` (`game.html`), `problem_open` из
  окна (`_catalog_js.html`), `copy_link` (`problem_detail.html`),
  `test_answer` (`catalog/static/catalog/js/problem_page.js`),
  `feedback_send`, `report_send`.
- `EventAdmin` (только чтение, без «добавить»): список, фильтры `name`,
  `page_key`, `user`, иерархия по дате, поиск по пути и посетителю.
- `analytics_export --since [--until] --out` → CSV (`utf-8-sig`, `props`
  JSON-строкой).
- `docs/SECURITY.md` — раздел «Аналитика беты: `/api/track/` без CSRF —
  осознанно».

**Миграция** (`logs/f9_makemigrations.log`, `logs/f9_migrations.log`):
`makemigrations problems --name event` → `0067_event`; `--check --dry-run` —
«No changes detected»; цепочка 0064 → 0065 → 0066 → 0067 без разрыва; с нуля
на пустой SQLite — 0065, 0066, 0067 OK; копия рабочей базы: накат OK → откат к
0066 OK → повторный накат OK; рабочая `db.sqlite3` мигрирована (порт 8000
свободен). PostgreSQL не проверен — Docker выключен (Фаза 14).

**Границы доступа:** таблица «ресурс × роль × действие» — в докстринге
`problems/tests/test_track.py`, каждая клетка закрыта тестом (гость пишет;
вошедший — `user` из сессии, не из тела; без CSRF-токена работает; чужой
`Origin` — 403; ни `Origin`, ни `Referer` — 403; `Referer` без `Origin` — 200;
без cookie — 400; > 50 и мусор — 400; сверх лимита — 429; GET — 405; список в
админке — 302 не сотруднику, 200 сотруднику). Наружу — только `{ok, saved}`.

**Тесты:** `problems/tests/test_track.py` — 20: API (10), админка (1), скрипт
(4: во всех девяти шаблонах и только в них; `/login/` отдаёт скрипт;
`sendBeacon`, `keepalive`, cookie, фаза перехвата, ≤ 200 строк; явные события
на местах), состояние поиска в разметке (2), выгрузка (2 + дата `--until`).
`node --check` для `track.js` и `problem_page.js` — чисто; `ruff` — чисто.

**Прогоны:** `test_track`, `test_feedback`, `test_problem_report`,
`test_fb_button_style`, `test_catalog_redesign`, `test_no_slop`,
`test_problem_test_page`, `test_search_busy`, `game.test_page_js`,
`game.test_wecon_texts`, `config.test_nav` + браузерные `test_browser_modals`,
`test_browser_freeze`, `test_design_canon` (страницы теперь грузят
`track.js`): шаг A 247 OK, шаг B 3 OK, код 0 (`logs/f9_tests_1.log`).

**Живая проверка в браузере** (панель Browser, `runserver` 8421 на рабочей базе,
после зубастости): на `/catalog/` есть `window.weco.track`, cookie `weco_vid`
поставлена, через 7 с два `POST /api/track/ → 200 OK` — настоящий браузер
проходит проверку `Origin`. В консоли одна ошибка загрузки ресурса
(`net::ERR_CONNECTION_REFUSED`) — не от аналитики (оба её запроса 200), не
разбиралась из-за правила контекста. В рабочую базу записались несколько
событий этой проверки — служебные строки аналитики, данных задач не касаются.

**Зубастость** (`logs/teeth_f9.log`): 16 дефектов — 16 красных, без зубов 0:
не проверяется `Origin`; `Referer` не принимается; CSRF снова требуется;
нет лимита окна; нет лимита пачки; `props` не режутся; `page_key` от клиента;
`user` не из сессии; строка запроса остаётся в пути; cookie не обязательна;
вход без скрипта; клики во всплытии; нет `game_answer`; нет `data-search-*`;
`props` в CSV не JSON; `--until` не работает. После — `git status`: код на
месте.

**После чекпоинта — находка автоматического ревью безопасности** (фикс
`68c6a3db`): `analytics_export` писал в CSV поля, которые присылает кто угодно
через `/api/track/` (имя события, путь, браузер, посетитель), а файл открывают в
Excel — ячейка `=HYPERLINK(…)` сработала бы у разборщика (CSV-инъекция формул).
Теперь ячейка, начинающаяся с `=`, `+`, `-`, `@`, табуляции или перевода строки,
получает апостроф в начале; строка в `docs/SECURITY.md`. Тест
`AnalyticsExportTests.test_formula_like_cells_are_neutralised`; зубастость —
без экранирования красный (`logs/teeth_f9csv.log`). `test_track` — 20 тестов,
OK; `ruff` — чисто.

**Предположения:**
1. Без cookie `weco_vid` запрос отвергается (400): без неё нечем считать
   лимит на посетителя, а скрипт ставит её до первой отправки.
2. Путь пишется без строки запроса — текст поиска не должен попасть в
   аналитику в обход `q_len`.
3. `page_leave` уходит на каждом скрытии вкладки с накопленным активным
   временем, группировка — по `props.view`; последняя запись просмотра и есть
   его длительность.
4. `duel_start` не добавлен: старт дуэли переписывается в Фазе 12 (синхронный
   отсчёт) — событие ставится там. `practice_*` — Фаза 11, `chat_send` —
   Фаза 10.

**Осталось:** приёмка глазами — открыть `/catalog/`, найти что-нибудь, открыть
задачу, вернуться; `/admin/problems/event/` — видны `page_view`, `click`,
`search`, `problem_open`, `page_leave` с длительностью.

---

## Фаза 8½. Косметика подпунктов и свежие векторы — ✅ подпункты; ⚠️ векторы: 896 из 4 096 досчитает владелец

**Скилл** `weco-content-integrity` загружен до правок. Последовательность
соблюдена: сухой прогон по умолчанию, счётчики до и после, выборка глазами до
записи, копия базы и снимки, инварианты, идемпотентность. Стоп-гейт владельца —
его решение в «Дополнении» промпта (15.09, в чате).

**Разведка** (только чтение, `logs/f8h_recon.log`, `logs/f8h_diff_a.log`):
- **(а) повтор номера — не 15 задач, а 253.** Форма «(N)» — 60 подпунктов, то
  есть ровно те 15 задач по 4 варианта, что названы в журнале Фазы 8; форма
  «1. 1) …» — 1 053 подпункта, «1. 1. …» — 7. Промпт называет обе формы, число
  15 — недосчёт старого регэкспа (он искал только скобки). Сверка по снимку
  Фазы 8: у 3 251 задачи с вырезанным блоком новый разбор даёт те же подпункты у
  2 998 и другие у 253; условие и метки не сдвинулись ни у одной, подпункты в
  базе совпадают со снимком.
- **55388 исключена из переприменения.** Варианты «1. 1) 4; … 4. 4) 1;
  5. 5) нет верного ответа», ответ «4». После снятия повтора текст варианта 1
  («4») совпал с меткой ответа «4» — сработал защитный отказ на неоднозначность
  (так и задумано). Старые подпункты верны (мультипликатор трансфертов при
  MPS 0,5 равен 1 — это вариант 4), виджет живой: косметика «4) 1» остаётся у
  одной задачи. К переприменению — 252.
- **(б) хвост «Верно/Неверно» — у 114 задач, а не у 554.** 554 — это класс «ни
  подпунктов, ни разметки» в `dead_test_widget_ids.txt` (Фаза −1), перепутанный
  с хвостами. «Верно/неверно» с подпунктами до записи — 1 470 (825 из Фазы 8),
  формы подпунктов: 1 342 «верно/неверно», 99 «да/нет», остальное единично.
  Формы хвоста: «1. Верно.», «1) верно», «1. 1) Верно» (с повтором номера),
  «Верно  Неверно» одной строкой. «Верно ли, что…» и «(верно/неверно) …» — не
  хвосты. 35590 («а) Верно» приклеено к концу условия, виджет мёртвый) не тронута.

**Сделано** (фикс `f2f22557`):
- `problems/test_options_parse.py`: в `_label_and_text` номер строки, повторённый
  в начале варианта («1. (1) …», «1. 1) …», «1. 1. …»), убирается; другой номер
  («1. (2) и (3)»), «1.5 млн» и «1 000 рублей» остаются. Путь один и для
  вариантов, и для сверки с ответом — ответ «2. (2) текст» по-прежнему сходится.
  `cut_boolean_tail`: хвост целиком из пары «Верно»/«Неверно» (номера 1, 2 /
  а, б / a, b или без них, одна строка или две, шапка «Варианты ответа»).
- Команда: проход `--boolean-tail` по всем «верно/неверно» с подпунктами
  (подпункты не трогает; задачу без живого виджета пропускает — там строка
  единственные варианты); `--ids` списком или файлом для сухого прогона, записи
  и отката; отчёт и откат понимают снимок без подпунктов.

**Тесты:** `test_test_options_parse` — 29 (новые: повтор номера 4, хвост 3 с
ложными фикстурами «Верно ли, что…», «Неверно, что…», «1) Верно 2) Неверно, если
…», «1) Верно / 2) Верно», «2) / 3)», «Да/Нет»), `test_test_options_command` — 10
(новые: `--ids` в сухом прогоне, записи и откате; проход хвоста: счётчики, вырез
только хвоста, идемпотентность и откат). 39 OK, код 0 (`logs/f8h_tests_1.log`),
`ruff` чисто.

**Зубастость** (`logs/teeth_f8half.log`): 13 дефектов — 13 красных, файлы
вернулись: повтор номера не снимается; снимается любой номер; «N.» без пробела;
хвост без проверки слов; без проверки меток; шапка остаётся; принимается строка
с хвостом текста; `--ids` не сужает запись; не сужает откат; `--ids` не читает
файл; мёртвый виджет не пропускается (откат всей транзакции); кандидаты без
подпунктов; флаг прохода хвоста не действует. Все красные — FAIL/ERROR проверок,
синтаксических поломок нет.

**Запись на рабочей базе** (порт 8000 свободен):
- копия до записи — `C:\Users\shipu\weconomics-data\backups\db_before_test_options_8half_20260915.sqlite3`
  (4,4 с; 41 307 задач, 82 085 подпунктов, как в рабочей);
- живые виджеты до — 5 485 (`logs/f8h_live_before.log`);
- откат (а) по снимку Фазы 8 с `--ids` на 252: условий возвращено 252, подпунктов
  удалено 1 118, правленых и пропавших 0 (`logs/f8h_revert_a.log`);
- `--apply` основного прохода (`logs/f8h_apply_a.log`): 272 задачи — 252 `parsed`
  и 20 `boolean_synthetic` (новый разбор оживил «верно/неверно» с ответом-номером
  и вариантами «1. 1) Да. / 2. 2) Нет.», задачи 54824–55045); подпунктов
  80 967 → 82 125 (+1 158 = 1 118 возвращённых + 40 плиток); условий 252; задач
  41 307 → 41 307; снимок `reports/night_20260915/test_options_8half/apply_a/snapshot_20260915_122351.json`;
- `--boolean-tail --apply` (`logs/f8h_apply_b.log`): кандидатов 1 490, вырезано 114
  хвостов, подпунктов 82 125 → 82 125; снимок
  `reports/night_20260915/test_options_8half/apply_b/snapshot_20260915_122413.json`;
- идемпотентность: оба повторных сухих прогона — «к записи: задач 0»
  (`logs/f8h_idem_a.log`, `logs/f8h_idem_b.log`);
- **живые виджеты после — 5 505** (boolean 1 170 → 1 190), инвариант «не
  меньше 5 485» держится (`logs/f8h_live_after.log`).

**Откат 8½ — в обратном порядке снимков:** `--revert …/apply_b/snapshot_20260915_122413.json`,
затем `--revert …/apply_a/snapshot_20260915_122351.json`. После этого 252 задачи
и 20 новых «верно/неверно» — без подпунктов, условия как до Фазы 8; полный откат
Фазы 8 — её снимком последним (у 252 он сообщит «подпунктов уже нет» и условие
не тронет — оно уже исходное).

**Замечания владельцу:**
1. У 16 из 114 вырезанных хвостов над плитками остаётся инструкция «Ответьте
   «1», если высказывание верно или «2», если высказывание неверно» — номеров на
   экране больше нет, плитки называются словами. Резать её спецификация не
   велела.
2. Хвосты «Да/Нет» не режутся (спецификация — только «Верно/Неверно»): у 20
   новых задач и у других «верно/неверно» строка «1) Да 2) Нет» стоит над
   плитками «Верно/Неверно».
3. 55388 и 35590 — см. разведку.

**Предположения:**
1. Правило (а) применено ко всем задачам, где новый разбор даёт другие
   подпункты (252), а не к 15 из промпта: правило владельца одно и называет обе
   формы.
2. Запись (а) — полным `--apply`, а не `--apply --ids`: он взял те же 252 и ещё
   20 задач, которые новый разбор сделал разбираемыми, — это решение Фазы 8
   («всё, что разбирается»), и иначе повторный сухой прогон не дал бы 0.

**Векторы** (`logs/f8h_build_embeddings.log`, `logs/f8h_stale_count.log`):
- `build_embeddings --stale --device cpu --batch-size 16` запущен 15.09 в 15:26 и
  нашёл **4 096** устаревших: формула `v2_focus_repeat` берёт и условие, и
  подпункты, поэтому устарели все задачи с новыми подпунктами, а не только
  3 251 переписанное условие. На CPU рядом с прогонами тестов — 0,2–0,4 задачи в
  секунду (пятьсот задач — от 21 до 43 минут).
- **Остановлен по решению владельца 15.09 в 19:18** («если долго — сделай всё
  кроме этого, досчитаю на арендованной машине»). Прогресс не потерян: команда
  пишет в базу каждую пачку из 100 задач сразу (`bulk_update` полей `embedding`,
  `embedding_version`, `embedding_model_build`, `embedding_source_hash`,
  `embedding_built_at` в `db.sqlite3`).
- Сверка после остановки (тот же `is_stale`, полный проход, 192 с, только
  чтение): пересчитано **3 200**, осталось **896** — список id
  `reports/night_20260915/vectors_stale_remaining_ids.txt`.
- Вывоз не делался. Старый вывоз `reports/night_20260915/vectors/catalog.f32`,
  `.meta.json`, `.state.json` (14 082 вектора, 55 МБ, 13:42 — до переписывания
  условий) **не удалён**, пока нового нет. Строка вывоза в `docs/SERVER.md` не
  менялась, над ней — предупреждение «сначала досчитать».
- **Как досчитать** (машина с копией этой `db.sqlite3`, моделью BGE-M3 и копией
  `reports/formula_v2/STATE.json` — без неё вывоз не пишет):
  ```bash
  venv313\Scripts\python.exe -u manage.py build_embeddings --stale
  venv313\Scripts\python.exe manage.py embeddings_export_vectors --out reports/vectors/catalog
  ```
  затем удалить старый `reports/night_20260915/vectors/catalog.*` и везти новый
  вывоз по `docs/SERVER.md`, «Векторы — файлом», — на бою ПОСЛЕ
  `test_options_from_statement --apply`.

## Фаза 10. Чат на задаче — ✅ (10.0 — третий запуск, 10.1–10.5 — четвёртый)

**10.0. Диагностика зрения** (`scripts/glm_vision_probe.py`, живой вызов 15.09
14:44, `logs/f10_vision_probe.log`): PNG 600×200 «Ответ: 42» с графиком, по два
вызова на модель (с картинкой и без).
- `glm-5.3` с картинкой — 400, код 1210 «messages.content.type is invalid,
  allowed values: ['text']»; без картинки — ответ, вход 197 токенов.
- `glm-5.3-flash` с картинкой — «Ответ: 42», вход 375 (+178 токенов
  изображения); без картинки — честное «изображение не передано».
- **Итог:** `CATALOG_CHAT_VISION_MODEL = 'glm-5.3-flash'`; текстовая модель чата
  остаётся `glm-5.3` (решение владельца). Фото и PDF надо сначала читать
  Flash-моделью в текст (приём ADR 0081), разговор вести GLM-5.3.
- Попутно: `PyMuPDF>=1.26.7,<1.27` уже в `requirements/base.in` — зависимость
  добавлять не нужно, `pip-audit` не задет.

**Остановлено по правилу контекста** (счётчик сессии ~845 тыс. токенов): 10.1–10.5
в этот запуск не начинались, чтобы не оставить фазу наполовину. Продолжать с
10.1 — `claude/HANDOFF_NIGHT_20260915.md`, раздел «Состояние после третьего
запуска».

### 10.1–10.5 — четвёртый запуск

**Скиллы** `weco-migration-safety` и `weco-security-boundary-review` загружены до
моделей и эндпоинтов; оба чек-листа пройдены (ниже).

**Сделано** (фикс `675c580e`):
- **10.1 Настройки.** `CATALOG_CHAT_PROVIDER` (`glm`), `CATALOG_CHAT_MODEL`
  (`glm-5.3`), `CATALOG_CHAT_VISION_MODEL` (`glm-5.3-flash`) и
  `AI_DAILY_COST_CAPS['catalog_chat']` (переменная `CATALOG_CHAT_DAILY_CAP_USD`,
  $1) в `config/settings.py`; строки с комментарием в `deploy/env.example`.
  `chat.answer` зовёт `core.run(provider=…, model=…, system=…)`. Потолок выбран —
  «Помощник на сегодня выбрал дневной бюджет. Завтра снова ответит.» (200,
  `error: budget`), а не 500 и не текст суточного лимита.
- **10.2 Режимы.** `theory`/`method`/`check`/`free`; блоки режимов, эталон и шаг
  зрения — в `problems/ai/prompts.py` (`CATALOG_CHAT_MODES`,
  `CATALOG_CHAT_REFERENCE`, `CATALOG_CHAT_VISION`). Эталонные `answer` и
  `solution` — только в `check` и только системным блоком (решение режется до
  8 000 знаков); ответ проверки до 1 200 знаков. Домашний режим поверх всех.
- **10.3 Файлы.** `ChatAttachment` в `problems/models_platform.py`;
  `POST /catalog/api/chat/upload/` — вход, CSRF, та же проверка файла, что у
  попытки (`attachments.validate_upload`), 10 файлов в сутки (429); PDF — первые 5
  страниц в PNG шириной 1 400 px (PyMuPDF, уже в `requirements/base.in`);
  картинка больше 1 600 px — JPEG q85; имя в хранилище даёт сервер. В модель
  зрения уходят пары `(mime, байты)`, не больше пяти; в GLM-5.3 — только текст
  расшифровки с пометкой `[расшифровка фото]`. Файл отдаёт только третья файловая
  вьюха в админке (`ChatTurnAdmin.file_view`): staff и право на просмотр, PDF
  скачиванием. `docs/SECURITY.md`: раздел «Пользовательские файлы» (три вьюхи,
  семь полей загрузки) и новый подраздел про вложения и журнал чата.
- **10.4 Интерфейс.** Три кнопки режимов с классом `.ai-sug` — над полем и не
  исчезают; скрепка (SVG) с `<input type="file" hidden accept="image/*,application/pdf">`;
  чип файла с «×»; «Проверь моё решение» без текста и файла — подсказка в чате
  (текст с сервера, `chatCheckEmpty`); ответы через `renderMath`;
  `weco.track('chat_send', {problem_id, mode, has_file})`; номер разговора —
  `crypto.randomUUID()` при загрузке страницы. Карточка чата и кнопки, пишущие в
  чат («Спросить ИИ, почему так», «Спросить про ошибку»), зависят теперь от
  поставщика ЧАТА (`chat_available`), а не от модели проверки.
- **10.5 Журнал.** `ChatTurn` — все поля промпта; пишется на каждую реплику, в
  том числе с ошибкой поставщика и отказом по лимиту или бюджету. Админка: список
  по времени, фильтры `mode`/`model`/`user`, поиск по реплике, ответу и
  расшифровке, страница записи с ответом и ссылкой на файл. `chat_export --since
  [--until] --out` → JSONL по разговорам, из профиля только `user_id`.

**Миграция** `problems/0068_chat_turn_attachment` (`logs/f10_migrations.log`,
`logs/f10_mig_zero.log`, `logs/f10_mig_copy.log`): `showmigrations` — хвост 0065,
0066, 0067; `makemigrations problems --name chat_turn_attachment` — две модели;
`--check --dry-run` — «No changes detected»; `check` — 0 ошибок; с нуля на чистой
SQLite — 120 миграций OK; копия рабочей базы (backup API, 13 с) — накат OK, откат
к 0067 OK, повторный накат OK. PostgreSQL — Фаза 14. Рабочая `db.sqlite3` пока
НЕ мигрирована: там идёт пересчёт векторов, мигрирую после него.

**Границы доступа** (таблица — в докстринге `problems/tests/test_chat_journal.py`):
загрузка — гость 403, чужая задача (скрытая) 404, лимит на человека 429; реплика
с чужим вложением или вложением к другой задаче — 400 без вызова модели;
админка и файл — гость и ученик на вход, сотрудник без права 403,
суперпользователь 200; наружу из профиля ничего (P0 по всем четырём режимам).

**Тесты:** `catalog/tests/test_chat_api.py` переписан — 20 (`ChatApiTests` 9,
`ChatModeTests` 3, `ChatJournalTests` 3, `ChatVisionTests` 3 — с учётом прежних,
приведённых к режимам); `catalog/tests/test_chat_upload.py` — 9;
`problems/tests/test_chat_journal.py` — 6; правки двух тестов страницы (провайдер
чата вместо `AI_PROVIDER`), причина в `test_media_route.ALLOWED`. Круг явными
лейблами: 119 OK (`logs/f10_tests_1.log`) и 84 OK (`logs/f10_tests_2.log`),
`ruff` чисто, `node --check` чисто.

**Зубастость** (`logs/teeth_f10.log`): 25 дефектов — 25 красных, файлы вернулись:
эталон во всех режимах; нет блока режима; эталон в тексте запроса; шаг зрения
пропущен; расшифровка без пометки; реплика с ошибкой не пишется; ответ кэшируется
поперёк режимов; ответ проверки не длиннее; PDF не на 1 400 px; PDF все
страницы; фото не уменьшается; имя файла ученика в хранилище; битый PDF — 500;
чужое вложение принято; бюджет текстом лимита; «Проверь» без файла — общий
текст; мусорный номер разговора; загрузка без входа; нет суточного лимита
файлов; карточка чата по модели проверки; файловая вьюха без права; PDF на нашем
домене; выгрузка с именем; `--until` не включительно; нет кнопки «Проверь моё
решение».

**Предположения:**
1. Шаг разговора идёт без кэша ответов (`cache_seconds=0`): режим и эталон живут в
   системных блоках, а ключ кэша ядра считается только по тексту — та же реплика
   в другом режиме получила бы чужой ответ. Шаг зрения кэшируется (в тексте хеш
   картинок).
2. Оба шага пишутся в `AiUsageLog` под одним именем работы `catalog_chat`, чтобы
   суточный потолок считал и зрение; различаются моделью.
3. Практика и чат не пишут статистику вопроса/учебные события — только журнал.
4. Реплика с фото тратит два обращения из суточного лимита пользователя (два
   вызова модели), как попытка с фото (ADR 0081).
5. «Помощник сейчас выключен» — отдельный короткий текст, если поставщика чата
   нет (карточки при этом на странице нет вовсе).

---

## Фаза 11. Бесконечные тесты внутри Wecon Rush — ✅

**Сделано** (фикс `f1988630`):
- **Сервер.** `config.PRACTICE` (`key`, `title`, `question_types`) — отдельная
  константа, НЕ запись в `MODES` (все 30+ мест, перебирающих `MODES`, её не
  видят); `config.practice_modes()` = Пуля, Блиц, Рапид; `PRACTICE_TTL` = 2 часа
  (`state.ttl_for`). Кандидаты практики — кандидаты трёх режимов вместе, с их
  порогами длины (`_candidate_rows`), поэтому `api_pool_counts['practice']` —
  ровно сумма трёх, и выдача с ним сходится; `total` не удваивается.
  `_new_state('practice')`: `practice=True`, `duration=None`, `lives=None`.
  `api_answer` в практике — `_practice_answer`: исход без очков, времени и
  жизней, `correct_choices` (только здесь), статистика вопроса не пишется.
  `api_session_finish` — `practice_summary` (`answered` без пропусков, `correct`,
  `skipped`, `accuracy`), без `GameResult`, рекорда и `LAST_KEY`. Попутно
  `_cap_generated` читает `MODES` через `.get` (падал бы на ключе вне `MODES`).
- **Без эскалации.** В практике комбо нет, а полоса эскалации при серии 0 —
  1–2 звезды: без правки практика выдавала бы только лёгкие, пока они не
  кончатся (`_pick_next`).
- **Клиент** (`game.html`): полоса `.practice-band` между режимами и нижним рядом
  входов («Бесконечные тесты», «без времени, жизней и очков: просто решай»,
  «N вопросов под фильтром», «Начать»; число обновляет `refreshCounts`; пул тестов
  пуст — полосы нет); `startRun({practice: true})` → `startPractice`; `body.practice`
  прячет `.hud-score`, `.timer-box`, `.time-track`, `.hud-lives`, `.hud-combo` и
  показывает «Вопрос N · верных M»; после ответа — подсветка по `correct_choices`
  без дельты времени и сердец, звуки верно/неверно; история `practiceLog`,
  «← Назад» / «Вперёд →» и стрелки, пробел — дальше (с неотвеченного — пропуск);
  ✕ и Esc — окно «Закончить?» (`#pr-end`, шлёт `weco:modal`, входит в
  `modalIsOpen()`) → сводка «Решено N, верных M, пропущено K, точность P %» с
  кнопками «Ещё раз» и «На старт»; `weco.track('practice_start' / 'practice_end')`.
- **Ловушка, закрытая попутно:** `rushResume` после закрытия окна поднимает цикл
  таймера, и в практике `tick` при нулевом времени закончил бы её обычным экраном
  итогов — у практики `rushPause` молчит.

**Тесты:** `game/tests/test_practice.py` — 12: константа и TTL; состояние без
длительности и жизней, без рекорда; выдаются все три типа и только они; ответ без
очков с `correct_choices`; в обычном раунде поля нет; финиш — сводка без
`GameResult` и `LAST_KEY`; лидерборд и счётчики; полоса на старте с числом; без
эскалации (выбор подменён на «последний номер»); клиентские инварианты. Круг
игры явными лейблами — 453 OK (`logs/f11_tests_1.log`), `test_practice` — 12 OK
(`logs/f11_tests_2.log`); `ruff` чисто; `scripts/check_em_dash.py` — 0.

**Зубастость** (`logs/teeth_f11.log`): 16 дефектов — 16 красных (14 FAIL, 2 ERROR:
без ветки практики в `api_answer` и `api_session_finish` тест падает на
отсутствующем поле, а не на синтаксисе), файлы вернулись (`git status` — только
незакоммиченные правки Фаз 12–13). Дефекты: практика берёт все режимы; TTL как у
раунда; у практики жизни; старт не принимает практику; ответ идёт путём раунда;
`correct_choices` в обычном раунде; финиш путём раунда; пропуски считаются
решёнными; счётчик практики 0; эскалация в практике; нет полосы на старте; окно
«Закончить?» не модальное; пауза в практике; ✕ не знает практику; комбо видно;
нет `practice_start`.

**Предположения:**
1. «Вперёд» на неотвеченном живом вопросе — пропуск (так же, как пробел): уйти
   дальше без ответа и есть «дальше без ответа».
2. После ответа практика НЕ уходит к следующему вопросу сама: без времени
   спешить некуда, человек смотрит подсветку и жмёт «Вперёд».
3. Практика не пишет статистику вопроса: ответ без часов сделал бы измеренную
   сложность легче, чем в раунде.
4. Счётчик практики на старте без фильтра — весь пул трёх типов (как у карточек
   режимов), под фильтром — число сервера с порогами длины режимов.

---

## Фаза 12. Дуэль: только с аккаунтом, лобби, синхронный старт, табло, итог — ✅

**Сделано** (фикс `b07c53d9`):
- **12а. Гость.** `duel_new` без `@login_required`: fetch гостя (`_wants_json`) —
  403 `{'ok': False, 'error': 'login'}`, адресная строка — на вход
  (`redirect_to_login`). Окно `#dm-auth` «Дуэль только с аккаунтом: соперника
  нужно как-то называть» со ссылками «Создать аккаунт» (`/register/?next=/game/`)
  и «Войти» (`/login/?next=/game/`): открывается по карточке дуэли без входа и на
  ответ `error: 'login'`, шлёт `weco:modal`, входит в `modalIsOpen()`,
  закрывается Esc и кликом мимо карточки.
- **12б. Лобби.** Окно вызова после успеха пишет «Создаём…» и уводит на
  `play_url`; второй шаг окна (ссылка, копирование, сокет и отсчёт в модалке)
  удалён. У набора `kind='duel'` нет карточки набора и кнопки «Играть» (шаблон не
  рисует `#set-play`, `initAutoSet` выходит раньше). Лобби: «Дуэль», название,
  «Режим «…» · без лимита вопросов · у обоих одни и те же вопросы»; код комнаты
  моноширинным 44 px и «Скопировать код»; главная «Скопировать ссылку» (48 px) и
  поле ссылки под ней; строка с пульсирующей точкой — автору «Ждём соперника…»,
  сопернику «Соперник: <автор>» (`auto_set.author`, `auto_set.is_author`), играть
  нельзя — причина; «Отменить» → `/game/`. На `/game/d/<код>/` — «Принять вызов»
  (автору «Вернуться в лобби»). «Играть по коду» с кодом дуэли и так вёл на
  `/game/s/<код>/`, то есть теперь в лобби. Сокет открывается при загрузке
  страницы набора — уже в лобби; гостю не открывается.
- **12в. Синхронный старт.** `COUNTDOWN_S = 5`. Когда в `present` двое разных,
  консьюмер назначает момент `state.duel_start_at(code, now + 5000)`: он пишется
  в словарь комнаты ОДИН раз (`started_at`), повторный вход получает тот же — и
  шлёт `start` `{at, now, in}`; `hello` после назначенного старта получает то же.
  Клиент считает смещение часов `now − Date.now()`, ведёт отсчёт к `at`
  пересчётом каждые 250 мс: 5…1 крупно (96 px), «Поехали!», на нуле сам зовёт
  `startRun`. Момент прошёл, а раунд режима ещё идёт — старт сразу; прошёл и
  раунд — «Раунд этой дуэли уже прошёл». `weco.track('duel_start', {mode,
  late_ms})`. Нет связи с комнатой — «Нет связи с комнатой, пробуем снова…».
- **12г. Звук.** `rushSound.countdown(n)`: 5…1 — 660 Гц 120 мс, «Поехали!» —
  990 Гц 450 мс; выключенный звук глушит отсчёт (`tone()` проверяет
  переключатель).
- **12д. Табло.** `#vs` без точности и комбо с обеих сторон (`accText` удалена);
  сторона — одна строка «имя · счёт · верных N · время» без переноса цифр,
  лента, жизни и подписи строкой ниже; до 720 px стороны друг под другом.
  Попутно `.vs-row[hidden]` и `.vs-lives[hidden]` действительно прячутся: у
  `display: flex` атрибут `hidden` не срабатывал.
- **12е. Итог.** `_duel_compare` отдаёт строки: Очки, Верных, Ошибок, Пропусков,
  Точность, Лучшее комбо, Среднее время верного ответа, Время раунда; столбец
  победителя по очкам подсвечен, при ничьей подсветки нет; «Реванш» на месте. В
  итоге раунда дуэли кнопка «Доска набора» называется «Итог дуэли» и ведёт на эту
  таблицу (соперник в момент финиша мог ещё играть, поэтому таблица там).
- **Попутно:** полоса «Бесконечные тесты» больше не видна на странице набора, в
  том числе в лобби (после Фазы 11 она оставалась на `/game/s/<код>/`).

**Каких метрик нет в `GameResult`** (опущены, не выдуманы): среднего времени
ВСЕХ ответов — хранится только среднее время верного (`avg_correct_ms`), строка
названа так же. У результатов до 08.09 нет `wall_ms` и `avg_correct_ms` — в
таблице «–».

**Тесты:** `game/tests/test_duel_sync.py` — 15: гость (XHR 403, адресная строка на
вход, окно на стартовой и его вызовы); лобби (нет «Играть», код, копирование,
«Ждём соперника», «Отменить»; соперник видит автора; окно вызова уводит в лобби и
не слушает сокет; «Принять вызов»); сокет (`COUNTDOWN_S`, `start` с `at`, `now`,
`in: 5` и записанным моментом; `hello` с тем же `at`); момент назначается один
раз; таблица итога числами; звук и вызовы `rush('countdown', …)`. Правки старых:
`test_duel.test_the_left_side_is_always_you` (нет точности и комбо), комментарий
`test_the_queue_reserve_never_shows_up_on_screen`. Браузерные:
- `test_browser_lobby` (380 px: без горизонтальной прокрутки, «Скопировать
  ссылку» ≥ 44 px, код ≥ 40 px, нет «Играть», нет полосы практики, табло раунда
  без прокрутки) — OK (`logs/f12_browser_2.log`);
- **`test_browser_duel_sync`** — два Chromium на живом daphne
  (`ChannelsLiveServerTestCase`): соперник видит автора, отсчёт с 5 до
  «Поехали!», экран раунда открылся у обоих, разница ≤ 1 с и не раньше 3,5 с после
  входа соперника — **на PostgreSQL OK за 45 с** (`logs/f12_e2e_1.log`). На SQLite
  в памяти пропускается с причиной: daphne отдельным процессом базу в памяти не
  видит.

Круг явными лейблами (`test_duel_sync`, `test_duel`, `test_duel_ws`,
`test_page_js`, `test_wecon_texts`, `test_sets`, `test_practice`) — 192 OK
(`logs/f12_tests_3.log`). Первый прогон поймал трёх старых сторожей
(`logs/f12_tests_1.log`): `id="duel-state"` запрещён тестом удалённой полосы
`.duel-bar` (строка лобби переименована в `duel-lobby-state`); тире в строке с
`'error':` в докстринге `duel_new`; литерал `'#pr-end:not([hidden])'` в
`modalIsOpen`. `ruff` чисто, `node --check` раннеров чисто. Другие браузерные
игры после правок (`test_browser_modals`, `test_browser_freeze`,
`test_browser_layout`) — 3 OK (`logs/f12_browser_3.log`).

**Зубастость** (`logs/teeth_f12.log`; раннер `teeth2.py` — тот же, но лейбл
принимает ключи прогона): 35 дефектов — 35 красных, файлы вернулись (`git status`
— только незакоммиченные правки Фаз 8½ и 13). Гость: XHR получает редирект;
гость создаёт дуэль; нет окна входа; окно не в `modalIsOpen`; отказ `login` не
ловится; гостю открывается окно вызова. Лобби: у дуэли «Играть»; нет кода;
сопернику «Ждём соперника»; нет имени автора; все — авторы; окно не уводит в
лобби; на приглашении «Играть»; «Скопировать ссылку» 30 px, код 20 px, полоса
практики в лобби, строка табло шире экрана (эти четыре — браузером на 380 px).
Старт: отсчёт 3; `start` без `now`; `hello` без старта; консьюмер не записывает
момент; момент не хранится; момент сдвигается при повторном входе; страница не
слушает `start`; отсчёт своими часами +1 с (последние два — двумя Chromium на
daphne и PostgreSQL). Звук: нет `countdown`; цифры немые. Табло: точность
осталась. Итог: нет строк; нет победителя в данных и в ячейке; комбо с точкой;
время ответа с точкой; время раунда в секундах; точность без «%».

**Предположения:**
1. Закрыть окно входа — Esc и клик мимо карточки; третьей кнопки нет (в промпте
   их две).
2. Строки лобби «Нет связи с комнатой…» и «Раунд этой дуэли уже прошёл» — честные
   состояния; другого способа начать дуэль, кроме общего отсчёта, не добавлено.
3. Сервер не запрещает начать раунд дуэли без соперника (`api_session_start_set`
   проверяет вход и попытку): запрет стоит в интерфейсе. Серверный замок —
   карточка «Надо».
4. Звук отсчёта у соперника, пришедшего по ссылке, может молчать: браузер не
   включает звук страницы без касания; у автора касание даёт «Скопировать
   ссылку».
5. Гонка `present` осталась как была: два входа в одну миллисекунду могут
   перезаписать словарь комнаты, и старт придёт при следующем входе или
   перезагрузке.

---

## Хвост: «connection refused» в консоли каталога — ✅ разобрано, не наш код

Замечание Фазы 9: при живой проверке на `runserver 8421` в консоли `/catalog/`
была одна ошибка загрузки ресурса `net::ERR_CONNECTION_REFUSED`, адрес не
записан.

**Проверка:**
- разовый Playwright-зонд (вне репозитория: живой тестовый сервер, пустая база)
  на `/`, `/catalog/`, `/catalog/?q=спрос`, `/game/` — ни одного упавшего запроса
  браузера, ни одной ошибки консоли, ни одного ответа ≥ 400;
- шаблоны (`templates/`, `catalog/templates/`), скрипты (`catalog/static/`,
  `static/`) и настройки — ни одного адреса `localhost`, `127.0.0.1` или чужого
  порта, который грузил бы браузер; `SEARCH_SERVICE_URL` зовёт только сервер;
- копия рабочей базы до Фазы 8½ (92 таблицы `problems_*`/`catalog_*`) — ни одной
  ссылки на `localhost`/`127.0.0.1`; в видимых задачах ссылки на внешние сайты
  есть в 44 условиях и 52 решениях, но все — текстом: встроенных внешних
  картинок 0.

**Что отказывает на самом деле:** только СЕРВЕРНЫЙ вызов сервиса поиска. Без
контейнера `search` локально каталог пишет в журнал `search service unavailable:
[WinError 10061] … actively refused — идём по словам` и отвечает лексикой;
браузер к этому адресу не ходит.

**Вывод:** ошибка в консоли той проверки — локальная среда (вероятнее всего
перезапуск `runserver` при правке файлов или собственный запрос панели
браузера), не код сайта. По правилу «Дополнения» — записано, не трогаю.

---

## Фаза 13. Экраны Wecon Rush: правки раскладки по списку — ✅

**Замер до и после** — разовый зонд вне репозитория (живой тестовый сервер,
четыре режима, три строки доски; «после» — с рекордом у одного режима в
`localStorage`), `/game/` на 1280, 1000, 700, 460 и 380 px.

**Страница → что изменил → почему** (для утреннего сравнения по скринам):
1. `/game/`, доска → вкладки режимов в одну строку (`flex-wrap: nowrap`,
   прокрутка вбок со скрытой полосой, `flex: none` у вкладки) → в колонке 380 px
   вкладки переносились на вторую строку: высота 64 px вместо 30.
2. `/game/`, доска → сегменты «Неделя / Всё время» и «Очки / Верных / Скорость»
   в обёртке `.lb-segs` (зазор 8 px), у кнопок `min-height: 32px` → второй
   сегмент при переносе под первый съезжал вправо на 8 px (`margin-left` у
   второго); кнопки были 28 px, стали 32 (сегмент 30 → 34 px).
3. `/game/`, доска → строка — сетка «место | имя и дата | значение»: значение
   справа по центру двух строчек, дата мельче под именем → дата тянулась и под
   значение (`grid-column: 2 / -1`), значение стояло по верхней строчке; высота
   строки 53 → 45 px.
4. `/game/`, доска → подвал гостя — одна ссылка-кнопка «Войти»
   (`/login/?next=/game/`, 36 px) → было «Войдите, чтобы попасть в таблицу: вход».
5. `/game/`, под режимами → убрана строка ссылок «Вызов дня · Дуэль с другом» →
   дублировала карточки нижнего ряда; привязка `a[href="/game/duel/new/"]`
   осталась с проверкой на null.
6. `/game/`, нижний ряд → 4 колонки, до 720 px — 2, до 480 px — 1,
   `align-items: stretch` → `auto-fit` раскладывал 3 + 1 на 700 px (76, 76, 76 и
   57 px отдельным рядом). После: 700 px — 2 + 2, 460 и 380 px — по одной.
7. `/game/`, нижний ряд и окно вызова → «Бросить вызов» → «Создать дуэль»,
   подпись «соперник по ссылке или коду»; заголовок окна и кнопка «Создать
   вызов» → «Создать дуэль», ошибки окна — «Не удалось создать дуэль» → одно
   действие одним глаголом на карточке, в окне и на кнопке.
8. `/game/`, «Добавить фильтры» → бейдж с числом выбранных значений, как
   `.btn-all .n` каталога; кнопка и так стояла по центру.
9. `/game/`, карточки режимов → строка «рекорд» всегда 20 px, кубок 16 px →
   кубок приходил SVG 24×24 без размера в CSS; после — у всех четырёх карточек
   строка 20 px с рекордом и без (карточка 271 → 277 px, ряды ровные).
10. `/game/r/<код>/` (сделано раньше в этой фазе) → ссылка «Weconomics · Wecon
    Rush» `min-height: 32px` → была 18 px, остальные пять страниц на 380 и
    1280 px красного не дали.
11. Попутно, найдено браузерным тестом: на 380 px `/game/` после п. 1 стал шире
    экрана на 22 px (вкладки в одну строку раздували колонку сетки) →
    `.start-right { min-width: 0 }`, как у левой колонки.

**`data-soon`** у карточек «Вызов дня» и дуэли даёт только `position: relative`
для пометки «скоро»; саму пометку `markSoon` вешает при выключенных `HAS_DAILY`
/ `HAS_DUEL`, а оба флага включены — «скоро» не показывается, атрибут оставлен
(промпт велел убрать, только если он помечает работающие карточки).

**Не трогал** (сомнение или не раскладка): знаки «✕» у выхода из раунда и «→» у
кода набора — текстовые символы, не эмодзи, замена на SVG — отдельная правка;
подписи карточек нижнего ряда на 721–900 px могут занять две строки — обрезать
текст хуже переноса.

**Тесты:**
- `game/tests/test_start_layout.py` — 5: строки ссылок под режимами нет, а
  привязка с проверкой на null; «Создать дуэль» на карточке, в окне и на кнопке;
  нижний ряд 4 → 2 → 1; вкладки, обёртка сегментов, сетка строки и подвал
  «Войти»; бейдж фильтров.
- `game/tests/test_browser_layout.py` — к `GamePagesLayoutBrowserTest` (шесть
  страниц на 380 и 1280 px) добавлен `StartScreenBrowserTest`: `/game/` на
  1280, 700, 460 и 380 px, с рекордом у одного режима в `localStorage` — ряды
  нижних карточек и карточек режимов ровные, строка «рекорд» одной высоты у
  всех, вкладки в одну строку (≤ 40 px), сегменты одной высоты и с одним краем,
  дата под именем и значение справа по центру, нет горизонтальной прокрутки;
  на 1280 — «Войти» ≥ 32 px с `next`, нет дубля ссылок. Общий запуск раннера
  вынесен в `run_node`.
- Круг явными лейблами (`test_start_layout`, `test_page_js`, `test_wecon_texts`,
  `test_filter_window`, `test_leaderboard`, `test_duel_sync`, `test_duel`,
  `test_practice`) — 222 OK (`logs/f13_tests_1.log`).
- Браузерные: первый прогон красный — `start@380:no_hscroll` (402 при 380,
  `logs/f13_browser_1.log`), это и есть п. 11; после починки `test_browser_layout`
  (2 класса) и `test_browser_lobby` — 3 OK (`logs/f13_browser_2.log`).
- `ruff` чисто, `node --check` раннера чисто, `scripts/check_em_dash.py game` — 0.

**Коммиты:** фикс `665f6a84`, починка проверки `62a4d55b`.

**Зубастость** (`logs/teeth_f13.log`): 17 дефектов — 16 красных, **один без
зубов**: `entry_row_no_two_columns` (убрано правило двух колонок до 720 px)
остался зелёным. Причина — в самой проверке: браузерный раннер разрешал
«последний ряд короче» и пропустил ОДИН ряд из четырёх карточек на 700 px.
Проверка починена (`62a4d55b`: в ряду не больше колонок, чем положено ширине,
все ряды, кроме последнего, полные); `StartScreenBrowserTest` зелёный
(`logs/f13_browser_3.log`), повтор того же дефекта — красный
(`logs/teeth_f13b.log`). Красные с первого раза: вернулась строка ссылок;
привязка без проверки на null; старое имя карточки; старый заголовок окна;
`auto-fit` в нижнем ряду; вкладки с переносом (в стилях и в браузере); сегменты
без обёртки; дата под значением; значение не по центру; «Войти» без `next`;
старая строка подвала; бейдж без класса; бейдж без стиля; строка «рекорд» без
резерва; ссылка на странице итога 18 px. Файлы вернулись: `git status` без
`game/`.

---

## Фаза 14. Финал: полный прогон, документация, Notion, отчёт — в работе

**Старт пятого запуска (15.09, вечер).** Прошлый процесс Claude Code завершился,
не дождавшись четырёх фоновых задач: круг Фазы 13 (`logs/f13_tests_1.log` —
222 OK), браузерные (`logs/f13_browser_2.log`, `logs/f13_browser_3.log` — OK),
повтор `pip-audit` (OK) и повтор зубастости одного дефекта
(`logs/teeth_f13b.log` — красный). Все четыре успели дописать логи; порченых
файлов нет (`git status` без `game/`). Пересчёт векторов жил отдельным процессом
и шёл дальше — остановлен по решению владельца (Фаза 8½).

**Рабочая база.** Порт 8000 свободен → `migrate problems`:
`0068_chat_turn_attachment` OK (`logs/f14_migrate_workdb.log`). Откат —
`migrate problems 0067`: миграция добавляет две таблицы и данных задач не трогает.

**1. `check` и `makemigrations --check`** (`ci_step1_check.log`): «System check
identified no issues», «No changes detected», коды 0.

**2. Пять джобов CI** (джобы 1, 2, 3 и 5 — на `665f6a84`, после него код не
менялся, кроме раннера `game/tests/browser_start.mjs` в `62a4d55b`; тесты — на
`85edb46a`):

| Джоб | Команда | Статус |
|---|---|---|
| lint | `ruff check .` | ✅ «All checks passed!», код 0 (`ci_lint.log`) |
| security | `bandit -r problems catalog teacher student game calc2 config olympiads -ll` | ✅ «No issues identified», код 0 (`ci_security_bandit.log`) |
| security | `pip-audit -r requirements/base.txt` (PyMuPDF уже в локе) | ✅ «No known vulnerabilities found», код 0 (`ci_security_pip_audit.log`); первая попытка упала без интернета — не разрешался `pypi.org`, повтор после возврата сети |
| migrations | `migrate --noinput` и `makemigrations --check --dry-run` на пустом PostgreSQL 17 (`qls_ci_night`, `config.settings_test_pg`) | ✅ 120 миграций OK, «No changes detected», коды 0 (`ci_migrations.log`) |
| tests | `scripts/run_tests.py problems catalog teacher student calc2 game calendar_stub config olympiads --settings=config.settings_test_pg --verbosity 2` | идёт (`ci_tests.log`) |
| deploy-check | `check --deploy --fail-level WARNING` с окружением джоба из `ci.yml` | ✅ «no issues (1 silenced)», код 0 (`ci_deploy_check.log`, перепроверено с переменными `ci.yml`) |

**3. Документация.** `docs/GAME.md` — сбои сети посреди раунда, «Плохая задача?»,
бесконечные тесты, синхронная дуэль (пути автора и соперника, `started_at`,
отсчёт со звуком, окно входа, табло в строку, итог дуэли); пауза раунда — раздел
Фазы 6½. `docs/SECURITY.md` — `/api/problem-report/`; `/api/track/` и вложения
чата — разделы Фаз 9 и 10. `docs/SERVER.md` — `test_options_from_statement` на
бою и «сначала досчитать векторы»; прогрев gunicorn и включение смысловой ноги —
разделы Фазы 6. `docs/EMBEDDINGS.md` — прогрев, поля лога, `X-Smart-Search-Ms`,
вывоз (Фаза 6) и «вывоз устарел, осталось 896». `docs/DATA.md` — повтор номера,
хвост «Верно/Неверно», откат в обратном порядке, эмбеддинги, порядок на бою.
`docs/TESTING.md` — список меток `serial`: четыре → десять. `CLAUDE.md` — пять
команд и «Текущий фокус» по Notion на 15.09. ADR 0103 (своя аналитика), 0104
(подпункты тестов из условия), 0105 (синхронная дуэль), 0106 (практика).

**4. Notion (записано).** Карточки фаз 1–13 и 6½ — «На проверке» (Ф13
переведена в пятом запуске, остальные — по ходу фаз); комментарии к Ф8 (8½),
Ф11, Ф12; ссылки на ADR в четырёх карточках «Решений» и пометка в карточке чата;
«Надо» — дуэль без серверного замка, «Да/Нет» и инструкция над плитками, «✕» и
«→» на SVG, векторы 896 из 4 096.
