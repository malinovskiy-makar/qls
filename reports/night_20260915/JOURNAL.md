# Журнал ночной сессии 15.09.2026 — «Баги, поиск, тесты, дуэль, чат»

Ветка `feat/night-20260915` от `main` @ `bae68c5` (совпадает с `origin/main`, 0/0).
Машина Windows, `C:\Users\shipu\qls`, окружение `venv313`.
Журнал пишется по ходу. При повторном запуске продолжать с первой фазы без «✅».

## Таблица фаз

| Фаза | Тема | Статус | Чекпоинт |
|---|---|---|---|
| −1 | Сверка с реальностью | ✅ | — (ничего не меняли) |
| 0 | Ветка, журнал, Notion | ✅ | `2d51fe58` |
| 1 | Игра: клавиши не пробивают модалы, пауза забега | ✅ | фикс `1dd031c5`, чекпоинт следом |
| 2 | Игра: фриз с горящей кнопкой | ✅ | фикс `34046831`, чекпоинт следом |
| 3 | Огромная иконка в шапке, подпись на графике | ✅ | фикс `cbecc135`, чекпоинт следом |
| 4 | «Проблема или предложение»: варианты, снимок, события | ✅ | фикс `34a9728d`, чекпоинт следом |
| 5 | Кнопка «Плохая задача» | в работе | |
| 6 | Умный поиск: прогрев, разбивка времени, плашка, документ | | |
| 7 | Поиск: анимация ожидания с фразами | | |
| 8 | Виджет тестов: подпункты из условия (локальная база) | | |
| 9 | Аналитика беты: своя таблица событий | | |
| 10 | Чат на задаче: GLM-5.3, режимы, файлы, логи | | |
| 11 | Бесконечные тесты в Wecon Rush | | |
| 12 | Дуэль: аккаунт, лобби, синхронный старт | | |
| 13 | Экраны Wecon Rush: раскладка | | |
| 14 | Финал: прогон, документация, Notion, отчёт | | |

---

## Фаза −1. Сверка с реальностью — ✅

1. `git status`: изменений в отслеживаемых файлах нет. Есть 7 неотслеживаемых
   личных файлов владельца (`.txt`, `Claude outputs/`, `New Text Document.txt`,
   `claude/HANDOFF_OLYMPIAD_TEXT_DEDUP_20260902.md`,
   `claude/RENDER_AUDIT_LEGACY_3000_20260829.md`, `kachat_vektora.ps1`,
   `session_c15_transcript_20260830.md`).
   **Предположение:** дерево считаю чистым — это старые заметки вне сессии,
   `git add` идёт только по именам, в коммиты они не попадут и при смене
   ветки не теряются.
2. Ветка `main`, `bae68c5` → `0024c37` → `86a1faf`. Ветки
   `feat/night-20260915` не было — первый запуск. `git fetch`: `main` =
   `origin/main`.
3. Python 3.13.12, Django 5.2.17.
4. `showmigrations game`: непрерывно до `0015_ended_reason_length` → новые с
   0016. `showmigrations problems`: последний `0064_alter_aiusagelog_user_and_more`
   → новые с 0065.
5. `manage.py check` — 0 ошибок (предупреждение про отсутствие `REDIS_URL`
   штатное для разработки).
6. Якоря — скрипт сверки нашёл 4 «расхождения», все ложные:
   - `templates/_feedback.html`: правило лежит внутри `<style>` (строка 30) как
     `.fb-btn svg { width: 15px; height: 15px; flex-shrink: 0; }` — в промпте
     записано сокращённо, дефект (размер значка только в позднем стиле) тот же.
   - `templates/_nav.html` (×2): скрипт принял `stroke-width="1.75"` за атрибут
     `width`. У обоих SVG есть только `viewBox="0 0 24 24"`, размеров нет.
   - `dead_test_widget_ids.txt`: 4 512 строк = 2 строки-шапки `#` + **4 510**
     строк данных (3 956 «варианты текстом в условии» + 554 «ни подпунктов, ни
     разметки»).
   Остальные якоря совпали дословно (игра, дуэль, поиск, чат, провайдеры ИИ,
   `core.run` с `provider/model/system/parse/log/check_budget`, `ProblemPart`,
   `answer_check`, `problem_types`, `entrypoint.sh` с `--max-requests 1000`,
   маршрут `api/feedback/`).

---

## Фаза 0. Ветка, журнал, Notion — ✅

- Ветка `feat/night-20260915` создана от `bae68c5`. Журнал заведён.
- Notion прочитан по протоколу: «Задачи» («В работе» и «Надо», первые 100
  строк), «Решения» — 10 последних по дате. Карточек решений от 15.09 не было.
  AI-поиск Notion на тарифе недоступен — искал обычным поиском по словам
  внутри каждой базы.
- **«Задачи» — 12 новых карточек (статус «Надо», ответственный Claude Code):**
  - Ф1 Игра: клавиши пробивают открытые окна — https://app.notion.com/p/3dcb11c92bc1816f85e6d76b6ea5cb9d
  - Ф2 Игра: фриз с горящей кнопкой — https://app.notion.com/p/3dcb11c92bc181c5bc56d47c46613f87
  - Ф3 Огромный значок в шапке и числа на графике — https://app.notion.com/p/3dcb11c92bc18145a34ccaff1bcc8bc8
  - Ф4 «Проблема или предложение»: варианты и снимок — https://app.notion.com/p/3dcb11c92bc181bab2eef9dd9fdad79a
  - Ф5 Кнопка «Плохая задача» — https://app.notion.com/p/3dcb11c92bc1816ca0ddea248ef3486b
  - Ф6 Умный поиск: прогрев, разбивка, плашка — https://app.notion.com/p/3dcb11c92bc181ec8baecd3ab0e4c222
  - Ф7 Поиск: анимация ожидания — https://app.notion.com/p/3dcb11c92bc181f1a026cf98ac960ad4
  - Ф9 Аналитика беты — https://app.notion.com/p/3dcb11c92bc181e89cb7c7f1f29e65a4
  - Ф10 Чат на задаче — https://app.notion.com/p/3dcb11c92bc181f78103c5992ce3b102
  - Ф11 Бесконечные тесты — https://app.notion.com/p/3dcb11c92bc181cd8286c92bfbdf934f
  - Ф12 Дуэль: синхронный старт — https://app.notion.com/p/3dcb11c92bc18109bdfbc22890404092
  - Ф13 Экраны Wecon Rush — https://app.notion.com/p/3dcb11c92bc181e4a9eff3438096d8d4
- **Ф8 — существующая карточка**, не дублировал: «Виджет теста не соберётся ни у
  одной из 8 992 переносимых задач» https://app.notion.com/p/3d9b11c92bc1817da7ecc8b2f544bbb2
  — комментарий «делается в ночной сессии 15.09, фаза 8».
- Комментарии-связки на смежных карточках (не переписывал): «Визуальная
  приёмка: макет wecon_rush_mockup.html…» (→ решение 6, фаза 13); «Фото
  рукописной работы ученика уходит в Anthropic…» (→ фаза 10 шлёт фото в
  GLM/Z.AI, вопрос остаётся открытым там); «Умный поиск открыт всем…» (→ фаза 6).
- **«Решения» — 7 карточек от 15.09.2026** (статус «Действует», источник —
  владелец в чате с Claude):
  1. Аналитика беты — своя таблица — https://app.notion.com/p/3dcb11c92bc181dd86eaca02f167cb4b
  2. Виджет тестов: варианты в ProblemPart — https://app.notion.com/p/3dcb11c92bc1815c8012e2e819ebb202
  3. Дуэль — строго синхронный старт — https://app.notion.com/p/3dcb11c92bc181638568f212137d460a
  4. Чат на задаче: режимы, файлы, GLM-5.3 — https://app.notion.com/p/3dcb11c92bc18163bef8ee0ca03157e9
  5. Бесконечные тесты внутри Wecon Rush — https://app.notion.com/p/3dcb11c92bc1811a847ff19bbb16af84
  6. Экраны Wecon Rush по списку без макета — https://app.notion.com/p/3dcb11c92bc181d4b8bbc2a40846010a
  7. Кнопка «Плохая задача» — https://app.notion.com/p/3dcb11c92bc18171a3acf9861e086c4c
- «Результаты» (`39bb11c9-2bc1-81da-91c5-e890da24d3c5`) — это обычная страница
  с разделами по датам, а не база; запись туда — в Фазе 14.
- **Замечено для владельца:** в «Решениях» карточка «Перенос корпуса на прод —
  весь видимый каталог минус подтверждённый брак…» от 13.09 заведена дважды
  (…81eab96af7c3968ec8b3 и …81058c5fe744a6a4fbda). В Notion ничего не удаляю —
  решать владельцу.

---

## Фаза 1. Игра: клавиши не пробивают модалы, забег умеет паузу — ✅

**Сделано** (фикс `1dd031c5`):
- `game/templates/game/game.html`: в начале глобального `keydown` два ранних
  выхода — `isTypingTarget` (INPUT/TEXTAREA/SELECT/contenteditable, кроме
  `#num-input` Классики, где Enter и пробел работают как раньше) и
  `modalIsOpen` (`.fb-back`, `.rp-back`, `dialog[open]`, `#quit-modal`,
  `#fmodal`, `#dmodal`, если не `hidden`).
- Пауза `rushPause` / `rushResume` рядом с `tick`: `timeLeft` на паузе не
  меняется, rAF отменён, сердцебиение снято и возвращается через
  `paintHud → paintAlarm`. Если забег кончился под окном — цикл не поднимается.
- Единое событие `weco:modal {open}`: шлют `_feedback.html` (в `build()` и
  `close()`) и окно выхода (`openQuit`/`closeQuit`); игра слушает и ставит
  или снимает паузу (закрылось одно окно, а под ним другое — пауза остаётся).

**Тест:** `game/tests/test_browser_modals.py` + раннер `browser_modals.mjs`
(Playwright в node, как у `test_design_canon`). Метка `serial`, причина в
комментарии: живой сервер + Chromium, допуск таймера 0,1 с. Блиц на пуле из
12 вопросов: открыть `.fb-btn` → «Проблема» → в textarea пробел, «1», Enter →
2 с. Остаток времени берётся у полосы `#time-fill` (`scaleX`), а не у цифр
таймера: выше 10 с цифры целые.

**Зубастость** (`game.html` и `_feedback.html` возвращены из `2d51fe58`):
красный по трём проверкам — `no_answers_sent` (ушёл 1 ответ),
`question_same` (вопрос сменился), `timer_paused` (t0 119,68 → t1 117,58 за
2 с). `timer_resumed` с дефектом зелёный — ожидаемо, таймер и так идёт. С
фиксом — зелёный, два прогона по 7,3 с.

**Быстрый круг:** `game` + `problems.tests.test_feedback`, шаг A — 788 тестов
за 88 с, OK. Логи — `reports/night_20260915/logs/f1_*.log`.

**Предположения и решения по ходу:**
1. `--scope-from-git` на изменённом ОБЩЕМ шаблоне (`templates/_feedback.html`)
   сам расширяет прогон до полного шага A — это ~112 мин на SQLite (замер
   08.09), а общие шаблоны задевают ещё фазы 3, 4, 5, 9. Фоновый полный
   прогон остановлен; быстрый круг гоняю явными лейблами: приложения
   изменённых файлов + тесты, которые ссылаются на изменённый общий шаблон
   (нашёл grep'ом). Полный прогон всех пяти джобов — в Фазе 14, как в промпте.
2. Окно выхода теперь ставит паузу (так в промпте). Это отменяет прежнее
   НАМЕРЕННОЕ поведение: комментарий в коде говорил «таймер идёт, иначе окно
   выхода стало бы бесплатной паузой». Комментарий переписан с датой решения.
3. В список открытых окон добавлен `#dmodal` («Бросить вызов») — тоже окно
   поверх игры, тот же дефект клавиш. В промпте его не было.
4. Окно фильтров из самого забега недоступно (открывается со старта и из окна
   дуэли) — паузу на нём не ставлю, только блок клавиш.
5. Серверное время ответа паузу не знает — бонус за скорость на вопросе с
   паузой сгорает (принято владельцем, комментарий у `rushPause`).
   **Найдено сверх промпта:** потолок длительности `_rank_run`
   (`duration × (1 + TIME_BONUS_CAP_FACTOR) + 60 с`, в Блице 300 с) меряет
   стенные часы — долгая пауза снимет забег с зачёта (`time_overrun`). Не
   чинил: учитывать паузу со слов клиента — ровно та дыра анти-чита, от
   которой потолок стоит. Вопрос владельцу — в отчёт.

**Осталось:** визуальная приёмка владельцем.

---

## Фаза 2. Игра: фриз с горящей кнопкой — ✅

**Сделано** (фикс `34046831`, правка текстов — в чекпоинте Фазы 2):
- `api()`: не-OK ответ → объект `{error, status}` (JSON-тело, если было);
  200 с не-JSON телом (страница входа после редиректа) → тоже объект. Сетевой
  сбой по-прежнему отклоняет промис.
- `postAnswer` → `answerFailed(d)`: `no_run` → оверлей «Раунд потерян» с
  кнопкой «На старт» (`/game/`); 409 → `advance()`; прочее и сетевой сбой →
  `inputLocked = false`, у Классики разблокируется поле, тост `#net-toast`
  «Нет связи. Нажмите ответ ещё раз» на 3 с.
- `prefetchNext()`: три повтора 300 / 800 / 1 600 мс, потом `prefetchFailed`;
  поколение цепочки `prefetchGen` гасит поздние ответы старой цепочки; `no_run`
  из буфера → `runLost`. `advance()`: ждёт буфер дольше 6 с — новая цепочка;
  `prefetchFailed` без буфера — тост с кнопкой «Повторить»; `runLost` — оверлей.
- `startRun()`: сетевой сбой → строка на стартовом экране «Нет связи: игра не
  началась. Попробуйте ещё раз.»
- Остальные вызовы `api()` просмотрены (всего 11): доска, статистика, итоги
  (`session/finish` уже с запасной сводкой), история, создание дуэли, панель
  рекордов — обработчики есть; счётчики фильтров без ответа просто остаются
  прежними, экран не замерзает — не трогал.
- Тост прозрачен для кликов (кликабельна только «Повторить»): иначе он мог бы
  закрыть собой вариант, который просят нажать ещё раз.

**Тест:** `game/tests/test_browser_freeze.py` + `browser_freeze.mjs` (serial,
причина в комментарии). Случай 1: первый ответ на `/game/api/answer/` — 502
страницей HTML → тост виден, через 300 мс повторный клик уходит на сервер
(перехватчик насчитал 2 вызова). Случай 2: `{"error": "Забег не начат",
"reason": "no_run"}` со статусом 400 → оверлей, «На старт» ведёт на `/game/`.

**Зубастость** (`game.html` из чекпоинта Фазы 1 `7389610c`): красные все четыре
проверки — тоста нет, повторный клик не ушёл (`calls: 1`), оверлея и кнопки
нет. С фиксом — зелёный, 4,0 с.

**Быстрый круг:** `--scope-from-git 7389610c` → лейбл `game`: шаг A — 763 теста
за 108,6 с OK, шаг B — 2 браузерных OK. **Первый прогон круга был красный** —
3 падения в `game.tests.test_wecon_texts`, исправлено (пункты 1–2 ниже).

**Предположения и решения по ходу:**
1. Текст оверлея из промпта «Забег потерян…» противоречит действующему решению
   владельца от 04.09 (слово «забег» на экране заменено на «раунд»), его держит
   `RoundNotRaceTests`. На экране — «Раунд потерян» / «Соединение или время
   ожидания. Начните новый.»
2. Длинное тире в видимых строках игры запрещено (`scripts/check_em_dash.py`,
   `EmDashTests`) — строка старта через двоеточие.
3. Тест `QuitModalTests.test_card_behind_is_blurred_and_timer_keeps_running`
   проверял СТАРОЕ решение (окно выхода без паузы) и после Фазы 1 зеленел
   впустую: искал `cancelAnimationFrame`, а пауза идёт событием. Переписан в
   `test_card_behind_is_blurred_and_round_pauses`: `openQuit` обязан объявить
   `weco:modal`. Зубастость: на `game.html` из `2d51fe58` утверждение ложно
   (красный), на фиксе истинно (зелёный).
4. `api()` превращает в объект и 200 с не-JSON телом — в коде промпта этого
   случая не было, но «HTML страницы входа» названа в корне дефекта.
5. У сервера два 409: «Вопрос уже отвечен» и «Забег уже завершён». Оба ведут в
   `advance()`, как в промпте. Второй бывает только при игре тем же раундом в
   двух вкладках: раунд не замерзает, а доигрывается до конца времени. Сервер
   не трогал.

**Осталось:** визуальная приёмка — выключить сеть посреди раунда.

---

## Фаза 3. Огромная иконка в шапке и подпись на графике — ✅

**Сделано** (фикс `cbecc135`):
- **3а.** Правила кнопки «Проблема или предложение» (`.fb-btn`, `.fb-btn svg`,
  `.fb-btn.is-busy`, медиа-запрос 1420, `.nav-panel .fb-btn …`,
  `.fb-btn--onpage`) вместе с комментарием-причиной перенесены из
  `_feedback.html` в новый партиал `templates/_fb_btn_style.html` — только CSS,
  без тега `<style>`. Партиал включён внутрь `<style>` шапки `_nav.html` (он
  стоит до разметки `<nav>`) и внутрь `registration/_auth_style.html` (он в
  `<head>` входа и регистрации). В `_feedback.html` остались стили окна,
  кружка Telegram и печати. У SVG значка — `width="15" height="15"` в
  `_nav.html` (×2), `login.html`, `register.html`.
- **3б.** Шкала очков графика «Динамика раунда» — чистые функции
  `scoreY`/`labelY` в `game/static/game/chart_math.js` (подключён `defer`
  рядом с `sound.js`): полоса `LABEL_PAD = 22` под подпись, подпись на 6 px
  выше точки. `chartCurve` зовёт `window.rushChartMath`.

**Тесты:**
- `problems/tests/test_fb_button_style.py`: на отрисованных `/catalog/`,
  `/game/`, `/calc2/`, `/login/`, `/register/`, `/student/`, `/profile/`
  правило `.fb-btn svg {` стоит раньше первой кнопки; у каждого значка кнопки
  есть атрибуты 15×15; в `_feedback.html` нет правил кнопки.
- `game/tests/test_chart_math.py`: node исполняет `chart_math.js` для очков
  [0, 10, 20], H = 130, P = 8 → подпись ≥ 12, точки в [8, 122]; страница
  рисует через модуль, старой формулы в ней нет.

**Зубастость:**
- 3а (5 шаблонов из `5599f36e`): красные все три — на `/catalog/` правило на
  позиции 110 014, кнопка на 82 177; на `/student/` 150 542 против 129 856; в
  `_feedback.html` снова `.fb-btn {`. С фиксом — зелёные.
- 3б (`LABEL_PAD = 8`, подпись на 5 px выше, `game.html` из `5599f36e`):
  подпись y = 3 при точках [122, 65, 8]; на странице нет подключения модуля.
  С фиксом — зелёные.

**Прогоны:** до коммита — шаг A 118 тестов OK (`test_fb_button_style`,
`test_chart_math`, `test_feedback`, `test_page_js`, `test_wecon_texts`,
статические проверки `test_design_canon`), шаг B — браузерный канон дизайна
`/calc2/` OK (37 с, без пропуска). Быстрый круг после правки сообщений тестов:
шаг A — 225 тестов за 26 с OK (лейблы фазы + все тесты шапки и входа).

**Предположения и решения по ходу:**
1. Экраны входа и регистрации шапку не подключают, а кнопка на них есть.
   Поэтому правила не «переехали в шапку», а стали партиалом, который оба
   места включают до разметки: одна копия правил, два места включения.
2. Сообщения утверждений в двух новых тестах переделаны на
   `assertTrue`/`assertFalse` с короткой строкой: первая версия при падении
   печатала весь HTML страницы (вывод проверки зубастости раздулся до 335 КБ).
3. Быстрый круг — явными лейблами (общие шаблоны расширили бы
   `--scope-from-git` до полного прогона): к тестам фазы добавлены все, кто
   смотрит в шапку и экраны входа — `config.tests.test_nav`,
   `problems.tests.test_obzor_nav`, `problems.tests.test_r15_builder`,
   `scripts.tests.test_scope_from_git`.

**Осталось:** приёмка глазами — обновить страницу на медленной сети (значок
сразу маленький); итог раунда — число над последней точкой видно целиком.

---

## Фаза 4. «Проблема или предложение»: варианты по экранам, снимок, события — ✅

**Сделано** (фикс `34a9728d`):
- **4а.** `problems/feedback_options.py`: списки заменены формулировками
  владельца, порядок сохранён; у `home` и `textbook` свои списки, из
  `FEEDBACK_ALIASES` они убраны; экран задачи остаётся алиасом каталога
  (жалобы на саму задачу — кнопка Фазы 5).
- **4б.** `grabShot()` отдаёт `{blob, note}`, `note` ∈ ok | timeout | error |
  nolib; таймаут 4 000 → 6 000 мс; форма шлёт `screenshot_note`. Поле
  `Feedback.screenshot_note` (`CharField(16)`, миграция
  `problems/0065_feedback_screenshot_note`); вьюха режет до 16 знаков; в
  админке столбец после «Снимок» и поле на странице записи.
- **4в.** `weco:modal` окно шлёт с Фазы 1 (`build()`/`close()`). Escape:
  слушатель окна закрывает его; до игры клавиша не доходит — игровой
  `keydown` выходит раньше по `modalIsOpen()` (Фаза 1), да и в Блице у Escape
  нет действия. Проверка добавлена в браузерный тест Фазы 1
  (`escape_closes_modal`).

**Миграция (скилл `weco-migration-safety`):**
- `showmigrations problems`: цепочка до 0064, новая 0065 сразу за ней;
- `makemigrations --check --dry-run` → «No changes detected»;
- с нуля на пустой SQLite — 117 миграций OK, включая 0065;
- копия рабочей базы: накат 0065 OK → откат к 0064 OK → повторный накат OK;
- PostgreSQL НЕ проверен: Docker не запущен (демона нет, порт 55432 закрыт) —
  проверка «с нуля на PostgreSQL» в Фазе 14. Рабочая `db.sqlite3` не
  мигрирована (перед утренней приёмкой нужен `migrate` — будет в отчёте).

**Тесты:** в `problems/tests/test_feedback.py` — свои списки у главной и
учебника; экран задачи = каталог; точные формулировки каталога и игры;
`screenshot_note` сохраняется и режется до 16; скрипт окна шлёт причину и
ждёт 6 с. В `game/tests/browser_modals.mjs` — проверка Escape.

**Зубастость** (по одному дефекту за раз, после каждого фикс возвращён):
- а) вьюха без поля → красный `test_screenshot_note_is_saved_and_trimmed`;
- б) старые списки → красные `test_home_and_textbook_have_their_own_lists`,
  `test_catalog_and_game_lists_are_the_owners_wording`;
- в) удалён алиас `problem` → красный `test_problem_screen_stays_a_catalog_alias`;
- г) `_feedback.html` из чекпоинта Фазы 3 → красный
  `test_script_sends_the_note_and_waits_six_seconds`;
- д) удалён слушатель Escape окна → красный `escape_closes_modal`.
- После возврата: 30 тестов + браузерный — OK.

**Быстрый круг:** `test_feedback`, `test_fb_button_style`, `test_media_route`
(админка обратной связи), `test_wecon_texts`, `test_page_js`,
`config.tests.test_nav` — шаг A, 155 тестов за 13,9 с, OK.

**Предположения:**
1. Промпт просит столбец в `list_display` после `has_shot`; поле показано и на
   странице записи (readonly) — иначе в карточке жалобы причины не видно.
2. Значение `screenshot_note` на сервере не сверяется со списком — промпт
   требует только обрезку; это подсказка разборщику, а не данные для логики.

**Осталось:** приёмка глазами — окно на каждом экране показывает свой список.
