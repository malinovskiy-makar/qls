# Журнал автономной ночной сессии 13.09.2026

Ветка на входе: `feat/corpus-transfer-selection` (33694969).
Модель: Opus 5, effort High. Владельца нет, стоп-гейты запрещены.

---

## ФАЗА 0 — сверка с реальностью (05:16–05:30)

**Git.** Незавершённых git-операций нет. Три worktree: `qls`
(`feat/corpus-transfer-selection`), `qls-models` (`feat/taxonomy-v2-openai-provider`),
`qls-olymp` (`feat/olympiad-text-dedup`) — параллельных сессий Claude Code в одной
папке нет. Локальный `main` = `17b2fe33`, на один коммит впереди `origin/main`
(`669801fe`). Прод стоит на `5b675b87` — предок обоих, отстаёт на два
документационных коммита.

**Локальная база (SQLite `db.sqlite3`).**

| Показатель | Значение |
|---|---|
| Problem всего | 41 307 |
| human_review = approved | 5 090 |
| human_review = defect | 1 536 |
| human_review пусто | 34 681 |
| needs_quality_review = True | 2 802 |
| hidden_pending_review = True | 26 832 |
| DupMark строк | 6 220 |
| DupMark групп | 2 751 |
| DupMark is_best = True | 953 |

Все три числа из промпта (41307 / 6220-2751-953) сошлись до единицы.
Статусы Problem: published 30 311, duplicate 5 393, draft 3 270, hidden 2 265,
archived 68.

**Прод (SSH, только чтение).** Контейнеры `nginx/postgres/redis/web/ws` — все
running. Рабочее дерево чистое (единственная запись — нетронутый untracked
архив `deploy/deploy_fixtures_approved.tar.gz` с прошлой сессии).
В боевой базе: **5 095 задач**, из них approved 5 090, **строк ProblemFigure — 0**
(известная карточка «На проде ноль строк ProblemFigure» подтверждена прямым
запросом). Код с последнего деплоя не менялся.

**GLM_API_KEY_PROD.** В `.env` есть, длина 49, хвост `Lt5836Cd`. Это **НЕ тот же
ключ**, что `GLM_API_KEY` (длина 49, хвост `Z7RRBEvn`) — тот и использовался в
экспериментах `reports/llm_search_eval`, где в документах фигурирует именно
имя `GLM_API_KEY`. Ключевого материала в `reports/` не нашлось ни по одному из
двух хвостов — утечки ключа в репозиторий нет. Фаза C не блокируется.

**Notion.** Штаб прочитан: база «Задачи» (статусы «Надо»/«В работе») и последние
8 записей «Решения». Доступен, работает.

---

## ФАЗА A — таксономия `problem_type`: одна точка правды (05:30–06:05)

### Что оказалось на самом деле

Промпт называл две поломки. Первая подтвердилась, вторая — нет, и ниже
написано, чем она оказалась.

**Живых виджетов теста в каталоге — ровно ноль, и причина в коде.** В банке
одновременно живут ДВА словаря названий типа. Прямой запрос к базе:

| `problem_type` | задач | словарь |
|---|---|---|
| `несколько_подвопросов` | 11 236 | v2 |
| `единственный_выбор` | 10 570 | v2 |
| `задача с развёрнутым ответом` | 7 777 | старый |
| `тест: короткий ответ` | 3 815 | v2 (§5.5 мастер-файла) |
| `множественный_выбор` | 2 801 | v2 |
| `верно_неверно` | 2 734 | v2 |
| `не_задача` | 1 545 | v2 |
| `сопоставление` | 19 | v2 |
| пусто | 810 | — |

Каталог сравнивал поле со строками СТАРОГО словаря
(`catalog/filters.py: TEST_TYPES`, `TEST_PREFIX`), и ни одна задача под них
не подходила. Игра работала только потому, что у `build_game_pool` случайно
оказались оба словаря сразу — два своих локальных `dict` рядом.

**«numeric = 0 в пуле игры» — это про ПРОД, и это ДАННЫЕ, а не код.**
Локально в пуле `numeric` = 491 сохранённых (500 по свежему сухому прогону),
всё работает. На боевом сервере `problem_type` содержит ровно три значения
старого словаря (`тест: один ответ` 846, `тест: верно/неверно` 348,
`тест: все верные` 271) и ни одного значения numeric-вида. Пул там,
соответственно, `single` 810, `boolean` 348, `multi` 246, `numeric` 0.
Причина — **(а) в корпусе прода физически нет задач такого типа**. Гейт
`build_game_pool` в порядке, чинить в нём нечего; numeric на бою появится
переносом корпуса (Фаза B), а не правкой кода. Задачи не выдумываю.

### Что сделано

Заведён `problems/problem_types.py` — единственная точка правды: строка
типа → вид теста (`single`/`boolean`/`multi`/`numeric`), оба словаря
названий равноправны. Новое имя типа теперь заводится в одном месте.

Потребителей нашлось **девять**, а не два (промпт допускал третий):

| файл | что сравнивалось со строками |
|---|---|
| `catalog/testplay.py` | префикс «тест» + точная строка «тест: все верные» |
| `catalog/filters.py` | `TEST_TYPES`, `TEST_PREFIX`, отбор в базе, счётчики |
| `catalog/views.py` | шесть мест: карточка, облачка, подписи |
| `catalog/hybrid.py` | аварийная деградация поиска |
| `catalog/semantic.py` | признак теста в индексе (искал префикс «тест:») |
| `game/…/build_game_pool.py` | два собственных словаря |
| `problems/hw_generator.py` | подбор в домашнюю работу |
| `problems/answer_check.py` | автопроверка ответа ученика |
| `problems/assignment_rows.py`, `models_platform.py`, `teacher/views.py` | форма ответа и признак теста в работе |

Отдельная находка: `answer_check` решал «отмечать несколько или один»
сравнением `ptype != 'тест: один ответ'`. Под v2 это означало, что
`единственный_выбор` проверялся как множественный выбор — то есть ученику
на домашке засчитывался бы неверный ответ. Теперь сравнение идёт по виду.

### Замер: сколько задач имеют РАБОЧИЙ интерактивный виджет

Считались видимые в каталоге задачи (`published`, без флага качества, без
`hidden_pending_review`, `content_status=ok`), у которых `game_of()`
возвращает игру.

| вид | видимых задач | с живым виджетом ДО | ПОСЛЕ |
|---|---|---|---|
| single | 3 771 | 0 | **826** |
| boolean | 1 219 | 0 | **345** |
| multi | 1 148 | 0 | **238** |
| numeric | 1 377 | 0 | 0 (по устройству, см. ниже) |
| **всего** | 7 515 | **0** | **1 409** |

Игровой пул (сухой прогон `build_game_pool`): `single` 3 429, `boolean` 607,
`multi` 618, `numeric` 500 — до правки столько же. Для игры правка
поведения не меняет вовсе: она снимает копию словаря, а не чинит отбор.
Именно это и было целью — одно место вместо двух.

**Почему `numeric` на каталоге остаётся нулём.** Виджет каталога —
ОТМЕТИТЬ ВАРИАНТ; у numeric вариантов нет вовсе, ответ вводится строкой.
Поля ввода короткого ответа в каталоге сегодня не существует ни в одном
виде: в `catalog/` нет ни `check_open_answer`, ни эндпоинта ответа, ни
разметки. Завести его — новая работа с новым API и новым экраном, а не
синхронизация словарей. **Развилка решена самостоятельно в пользу
консервативного варианта:** словари сведены, новый экран не выдуман;
1 377 видимых numeric-задач ждут отдельного решения владельца.

Второе честное число: из 3 771 видимых `single` виджет получают 826. Разница
— не код, а данные: у остальных либо нет подпунктов-вариантов, либо верные
метки не сопоставляются с метками вариантов. Это та же копилка, что
«буква ответа не сопоставилась» в отсеве игрового пула.

### Поведенческое изменение, записанное намеренно

Признак теста был «строка начинается на тест», стал «строка есть в словаре».
Для банка это ничего не меняет (проверено запросом: все восемь значений
покрыты, на проде все три — тоже). Меняется край: выдуманное значение вроде
голого `тест` тестом больше не считается. Так и задумано — новое имя типа
заводится в одном месте, а не подхватывается втихую по первым буквам.
Тест `catalog.tests.test_catalog_redesign` на этот случай переписан.

### Зависимости (пункт 5 промпта)

Промпт говорил «добавь pymorphy3 рядом с bm25s». По факту **в
`requirements/base.in` не было НИ ОДНОГО из них**: `bm25s` не значился
вообще ни в одном файле зависимостей, `pymorphy3` с его словарём лежал в
`dev.in`. При этом `catalog/rerank.py` зовёт `catalog/lexical_bm25.py` и при
сборке корпуса (`build_index`), и на каждом запросе (`bm25.search`), —
докстринг модуля «к каталогу не подключён» устарел. То есть при включённом
`SMART_SEARCH_RERANK` первый же боевой запрос падал бы на `import bm25s`,
а следом на `import pymorphy3`.

В `base.in` добавлены три пакета: `bm25s`, `pymorphy3`, `pymorphy3-dicts-ru`
(без словаря `MorphAnalyzer` не поднимается). Из `dev.in` пара pymorphy3
убрана — `dev.in` включает `base.in` строкой `-r base.in`, CI их по-прежнему
ставит. Локи пересобраны заявленным в `requirements/README.md` способом,
на Python 3.13: `base.txt`, `dev.txt`, `local.txt`.

⚠️ `local.txt` кроме трёх пакетов подтянул ещё `openai`, `httpx2`,
`httpcore2`, `truststore` — лок был **устаревшим относительно своего
исходника**: `openai` стоит в `local.in` с 27-й строки, а в локе его не было.
Пересборка привела лок в соответствие с источником. На прод `local.txt` не
едет.

---

## ФАЗА B — фильтр отбора корпуса: два пропущенных критерия (06:05–06:25)

Ветка `feat/corpus-transfer-selection` уже содержала посчитанный whitelist на
3 522 задачи (`reports/corpus_transfer_20260912/transfer_new_ids.txt`).
Скрипта отбора в репозитории нет — прошлая сессия считала разово и оставила
списки. Поэтому два новых критерия наложены на готовый список: это ровно то,
о чём просил промпт («добавь эти два шага в таблицу, не трогая уже
посчитанные»), и прежние шесть чисел от этого не смещаются.

### Воронка целиком

| шаг | − | осталось |
|---|---|---|
| старт: всё локально минус прод | | 36 212 |
| 1. `status = published` | 10 979 | 25 233 |
| 2. `content_status = ok` | 2 100 | 23 133 |
| 3. `answer_consistency = согласован` | 17 649 | 5 484 |
| 4. рендер-гейт (`content_format = markdown`) | 1 098 | 4 386 |
| 5. проигравший уверенной группы копий | 117 | 4 269 |
| 6. участник группы «на разбор» | 747 | 3 522 |
| **7. `human_review != 'defect'`** *(новое)* | **108** | 3 414 |
| **8. `needs_quality_review = False`** *(новое)* | **71** | **3 343** |

Считано по ВСЕМ 3 522, а не по подмножеству скрытых: `defect` 108,
`needs_quality_review` 76, пересечение 5, суммарно уходит 179.

**Довод прошлой сессии проверен и оказался верным по факту, но узким.**
Она писала: исключать смысла нет, эти задачи всё равно приедут скрытыми.
Прямой запрос: из 179 отсечённых **видимыми в каталоге были бы 0** — все
179 действительно лежат внутри 421 скрытых `hidden_pending_review`. То есть
ученик разницы не увидит. Но на сервере они лежали бы готовыми минами:
любое снятие `hidden_pending_review` (а такая карточка в «Надо» есть —
«один флаг прячет 12 555 годных задач») выпустило бы на публику 108 задач,
про которые человек уже сказал «брак». Решение владельца принято как есть.

### Итог переноса

| | было (12.09) | стало (13.09) |
|---|---|---|
| переносится задач | 3 522 | **3 343** |
| видимы в каталоге сразу | 3 101 | **3 101** |
| приедут скрытыми | 421 | 242 |
| станет на проде | 8 617 | **8 438** |

873 задачи «прошли рендер-гейт, но `content_format='plain'`» **не везём** —
решение владельца, повторено в промпте. Список остаётся в
`gate_would_pass_ids.txt` для отдельной сессии про `content_format`.

### Дамп

Пересобран той же командой с новыми списками:

```
manage.py dump_for_deploy --bank-only --outdir deploy_fixtures_transfer_v2 \
  --ids-file   reports/corpus_transfer_20260912/transfer_new_ids_v2.txt \
  --with-dupmark \
  --dupmark-scope-file reports/corpus_transfer_20260912/transfer_scope_ids_v2.txt
```

34 файла, 25 МБ после `gzip -9`, 27 879 объектов. Проверено разбором самих
файлов, а не доверием команде: `problems.problem` в дампе ровно 3 343 и
множество совпадает с whitelist до элемента, **висячих ссылок на Problem —
0**. Внутри: 11 337 подсказок, 3 343 привязки к источнику, 2 845 связей
особенностей, 1 099 подпунктов, 915 олимпиадных привязок, **409 картинок**,
2 пометки групп копий.

Файлы списков: `transfer_new_ids_v2.txt` (3 343),
`transfer_scope_ids_v2.txt` (8 438), `dropped_by_new_criteria_ids.txt` (179).
Старые файлы 12.09 не тронуты.

---

## ФАЗА C — умный поиск по-настоящему рабочий на проде (06:25–07:05)

### 1. Недоступный плотный сервис больше не стоит секунд

**Что нашлось вместо ожидаемого.** Промпт говорил про «20-секундный
таймаут». По коду таймаут один — `SEARCH_SERVICE_TIMEOUT`, умолчание 5 с, и
он у `urlopen` общий на дозвон и на чтение.

**Что сделано.** В `catalog/search_client.py` заведён короткоживущий
выключатель: первый отказ закрывает дверь на
`SEARCH_SERVICE_BREAKER_SECONDS` (30 с по умолчанию), и всё это время
поиск уходит по словам, не трогая сеть вовсе. Состояние процессное, а не в
общем кэше — намеренно: выключатель обязан работать и тогда, когда Redis
сам недоступен.

Замер (localhost, порт без слушателя — то же, что на бою: контейнера
`search` там нет):

| | 1-е | 2-е | 3-е | 4-е | сумма |
|---|---|---|---|---|---|
| было | 2,04 с | 2,04 с | 2,04 с | 2,05 с | **8,17 с** |
| стало | 2,03 с | 0,000 с | 0,000 с | 0,000 с | **2,03 с** |

**Отдельного таймаута на дозвон НЕТ, и это проверено, а не забыто.** Я
сперва сделал пробу через `socket.create_connection` с потолком 0,5 с.
Замер её опроверг: на машине владельца `create_connection` на заведомо
нероутируемые адреса (10.255.255.1, 192.0.2.1, 198.51.100.7) возвращает
УСПЕХ за миллисекунду — сетевой стек отвечает за несуществующего соседа.
Такая проба не отличает живой сервис от мёртвого и стоила бы лишнего
сокета на каждый запрос. Проба убрана, причина записана комментарием в
коде. Промпт допускал любой из двух способов («и/или»), взят проверяемый.

Тесты: `catalog.tests.test_search_service.BreakerTests` — четыре штуки,
включая «второе обращение к мёртвому сервису отвечает быстрее 0,05 с».

### 2. GLM — через общую дверь `core.run`

Было: `providers.get_provider('glm')` напрямую. Довод прошлой сессии был
верен про дверь (`core.run` — для профилей генерации, с общим поставщиком,
общей моделью и суточным лимитом ОБРАЩЕНИЙ на пользователя) и неверен про
вывод: расход поисковой сортировки просто не попадал в `AiUsageLog`, то
есть не был виден нигде рядом с остальными тратами, и потолка не имел.

У `core.run` появились переопределения — `provider` (готовый ОБЪЕКТ, а не
имя: так шов подмены в тестах остаётся один и совпадает с боевым путём),
`model`, `system`, `parse`, `log`, `check_budget`. Не задано ни одно —
старое поведение слово в слово.

Три вещи, которые пришлось решить по дороге, и все три — не косметика:

1. **`AiUsageLog.user` стал необязательным** (миграция `problems` 0064).
   Поиском пользуются НЕ ВХОДЯ, а строку расхода без пользователя записать
   было некуда — `core._log` такие строки молча не писал вовсе. Денежный
   потолок, считающий по этому журналу, не видел бы основную часть трат
   именно той функции, ради которой заводится. Заодно добавлен индекс
   `(kind, created_at)` — потолок суммирует по виду работы за сегодня.
2. **Одна строка расхода на поисковый запрос, а не на пачку.** Пачки идут
   в ПОТОКАХ, а соединение с базой в Django потоко-локальное, и закрыть
   его в рабочем потоке некому (`close_old_connections` висит на сигналах
   запроса и живёт в главном потоке). Девять воркеров по три пачки — и
   PostgreSQL упёрся бы в `max_connections` не от нагрузки, а от утечки.
   Поэтому `run(..., log=False)` в потоках, `core.record_usage(...)` один
   раз в потоке запроса.
3. **Потолок спрашивается один раз, тоже в потоке запроса**
   (`core.budget_exceeded`), по той же причине. Это всплыло само:
   тест упал с «database table is locked» — проверка потолка ходила в
   базу из каждого рабочего потока.

**Своей таблицы цен у модуля больше нет** — цена GLM переехала в
`settings.AI_PRICES` к остальным. Две таблицы цен разошлись бы так же
тихо, как разошлись два словаря `problem_type`.

### 3. Суточный денежный потолок

`AI_DAILY_COST_CAPS['search_rerank']` = **$0,50 в сутки**, переопределяется
`SEARCH_RERANK_DAILY_CAP_USD`. Выбран **ЖИВЫМ ЗАМЕРОМ**, не прикидкой:
один запрос сегодняшним путём (пул 50, одна пачка) стоит **$0,00134**.
Значит $0,50 — это **370 запросов в сутки**. Когда поднимут плотную ногу,
пул дорастёт до потолка 150, то есть трёх пачек, ~$0,004 за запрос — это
**125 запросов в сутки**.

Бета — несколько репетиторов со своими учениками; столько поисков руками
не набирают. Потолок здесь не ради экономии, а чтобы ошибка в коде или
чужой робот, дёргающий поиск в цикле, не выставили счёт на тысячи: $0,50 в
сутки — около $15 в месяц при худшем раскладе. Упёрлись — выдача ТИХО
уходит на базовый порядок (`status='fallback'`), поиск не ломается.

### 4. «Только сотрудникам» снято

`rerank.is_available()` требовала `is_staff`. Это была кнопка «показать
одному себе» в отсутствие второй кнопки; вторая есть — сам флаг
`SMART_SEARCH_RERANK`. Два выключателя подряд означали бы, что включённый
флаг всё равно ничего не меняет для тех, ради кого функцию и делали:
каталогом пользуются ученики, а поиском — и гости, не входя вовсе.
Тесты доступа переписаны: при включённом флаге сортировку получают все,
включая `AnonymousUser`; при выключенном — никто.

### 5. Живой end-to-end на боевом ключе

Путь как на проде: `SMART_SEARCH_RERANK=1`, `LEGS=bm25` (плотной ноги там
нет), гость, ключ `GLM_API_KEY_PROD`.

| | запрос 1 | запрос 2 |
|---|---|---|
| «монополия и ценовая дискриминация третьей степени» | ✓ | |
| «эластичность спроса по доходу для товаров Гиффена» | | ✓ |
| итог | rerank | rerank |
| пул | bm25: 50 | bm25: 50 |
| пачек | 1 | 1 |
| задержка целиком | **8,07 с** | **4,66 с** |
| из них модель | 5,94 с | 4,64 с |
| токенов вход / выход | 7 405 / 472 | 7 163 / 538 |
| стоимость | **$0,001348** | **$0,001343** |

Сборка bm25-корпуса — **24,9 с на 14 082 задачи**, один раз на процесс
(дальше живёт синглтоном с версией в общем кэше). На боевом сервере это
разовая плата первого поискового запроса после перезапуска контейнера,
и её стоит иметь в виду отдельно.

В `AiUsageLog` легли две строки: `kind='search_rerank'`, `user=None`,
`provider='glm'`, модель `glm-5.3-flash`. Истрачено за прогон **$0,00269**
при потолке $0,50.

### Тесты

`catalog.tests.test_rerank` — 32 (было 26): все прежние зелёные, поведение
не менялось, менялся маршрут. Новые: расход попадает в общий журнал без
пользователя; цена берётся из `AI_PRICES`; исчерпанный бюджет тихо уводит
на базовый порядок; без потолка ограничения нет; суточный лимит обращений
на пользователя к поиску не применяется. Плюс 4 теста выключателя в
`test_search_service`. Прогон `catalog` целиком — 377 тестов, зелено;
`problems.tests.test_ai_providers`, `test_eval_set_b`, `teacher`,
`student` — 120 тестов, зелено.
