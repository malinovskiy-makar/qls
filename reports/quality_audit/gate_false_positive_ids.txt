# Ложные срабатывания шлюза, ПРОВЕРЕННЫЕ ЧЕЛОВЕКОМ.
#
# Формат: id<TAB>причина. Строки с решёткой пропускаются.
#
# Сюда попадает только то, что человек открыл и посмотрел. Это не способ
# «замять» неудобный детектор: если срабатываний одного класса много,
# правильный ответ — чинить детектор, а не пополнять этот список.
#
# ── unpaired_dollar на денежных суммах (сессия 31.08.2026) ───────────────
# Обе карточки подтверждены человеком (human_review='approved') и БЫЛИ
# видны до точечного прогона шлюза 30.08. Непарный `$` у них — деньги, а
# не обрывок формулы: закрывающего `$` нет вовсе, поэтому KaTeX ничего не
# превращает в математику и на экране всё в порядке.
#
# Детектор при этом НЕ сужался, и это осознанно. Замер по всему банку:
# нечётный `$` у 163 задач, под правило «`$` перед цифрой — валюта»
# попали бы 46, и 41 из них — в источниках вне текущей работы. Менять
# приговор сорока одной чужой карточке заодно нельзя.
26724	unpaired_dollar: «Правительство также заплатило $2 в виде трансфертов населению» — доллары это деньги
29691	unpaired_dollar: «привязка к доллару: $35 могли быть обменены на одну унцию золота» — доллары это деньги
