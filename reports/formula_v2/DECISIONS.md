# DECISIONS.md — решения и расхождения сессии «формула v2»

Формат: что решил → почему → что отверг. Расхождения с заданием и с
`claude/API_RUN_MASTER_20260830.md` записываются здесь, а не подгоняются.

---

## Р-01. `plot` непусто у 11 867 задач, а не у 11 219 — задание устарело

**Факт.** `Problem.objects.exclude(plot='')` даёт 11 867 (пустых-или-пробельных
29 440). Задание называет 11 219, разница 648.

**Кто прав.** Банк. Число в задании снято 07.09 до слияния ветки обогащения
v2 в `main` (коммит `326c939`), после которого в банк доехала ещё часть
сюжетов. Пересчитывать нечего — расхождение в одну сторону и объяснимо.

**Последствий нет.** `plot` решением владельца 07.09 из формулы v2 убран
(раздел 3.6 задания), в спецификациях `v1`/`v2_core`/`v2_plus_blurb` он
берётся из поля как есть.

---

## Р-02. Манифест допрогона остаётся 1 740 задач, хотя 1 736 из них лежат в чистке 03.09

**Что требовало задание.** «Задачи, попавшие туда, из манифеста исключаются:
их статус поставил человек, а человек сильнее прогона. Ожидаю пересечение
около нуля».

**Факт.** Пересечение — 1 736 из 1 740, все через группу `needs_fix_ids`.

**Решение: манифест НЕ сокращается.** Основание — не догадка, а docstring
самой команды `problems/management/commands/content_cleanup.py`:

> «Решение владельца 03.09.2026 дословно: … **Ручного просмотра не будет** —
> безопасность обеспечивают проверки ЗДЕСЬ. Два входа, **оба из журнала
> прогона**, к API не обращаемся: `queue_not_a_problem.jsonl` … где модель
> поставила `task_nature = не_задача`; `queue_broken_text.jsonl` — задачи с
> серьёзными дефектами текста (`text_quality` = битый/серьёзные дефекты)».

То есть чистка 03.09 — **механическая, по вердиктам самого первого прогона**,
а не ручная. Посылка задания («статус поставил человек») к группе
`needs_fix_ids` не относится. Исключить её значило бы затянуть ровно тот
замкнутый круг, ради размыкания которого фаза 1 и существует: слабый прогон
пометил задачу битой → из-за пометки её не пускает сильный прогон → она
навсегда вне каталога. Совпадение 1 736 = 1 736 (число задач манифеста с
`content_status='needs_fix'`) это подтверждает точно: это одно и то же
множество, а не случайное пересечение.

**Что от чистки 03.09 действительно защищено, то и защищено.** Единственное
необратимое действие того дня — 23 удаления (`to_delete_ids`,
`deleted_not_a_problem.jsonl`). Пересечение с манифестом — **0**. Группы
`not_a_problem` (1 596) и `left_visible_ids` (24) тоже дают 0.

**Что отвергнуто.** Вариант «исключить 1 736 и прогнать 4 задачи» —
формально соответствует букве задания и полностью обессмысливает фазу.

---

## Р-03. Порядок и состав блоков: расхождения с `API_RUN_MASTER` §9

Задание (раздел 3.4) фиксирует состав и порядок как решение владельца от
07.09; `API_RUN_MASTER_20260830.md` §9 старше на неделю. Действуем по
заданию, расхождения фиксируем здесь.

| Что | §9 (30.08) | Список владельца 07.09 | Как сделано |
|---|---|---|---|
| `ai_blurb` | блок 4 | отсутствует | **не входит** в `v2`; цену меряет `v2_plus_blurb` |
| `plot` (сюжет) | блок 7 | убран явно | **не входит** в `v2` |
| Заголовок | «в отпечаток не добавляется» (§5.8, §9) | блок 16 | **входит** в `v2` |
| Решение (`solution`) | «решение и ответ не входят» (v1) | блок 7 | **входит** в `v2`, бюджет 800; цену меряет `v2_no_solution` |
| Порядок | «различающее вперёд», метаданные в голове | метаданные в хвосте | **как у владельца**; цену порядка меряет `v2_meta_first` |

Про заголовок стоит сказать отдельно: §5.8 запрещал его именно из-за
7 098 заголовков-обрубков. Список 07.09 берёт **`title_candidate ?? title`**,
а `title_candidate` заполнен у 41 307 задач из 41 307 и по инвариантам §11
не длиннее 40 символов и без цифр. Возражение §5.8 к этому источнику не
относится — оно относилось к старому `title`.

---

## Р-04. Три вещи, которые задание велело решить на месте

1. **Блок 15 «Сложность» пишется словом**, не цифрой: `Сложность: средняя.`
   Отображение 1→«очень лёгкая», 2→«лёгкая», 3→«средняя», 4→«трудная»,
   5→«очень трудная». Пусто → блока нет. Причина — голая цифра не значит для
   модели ничего и рискует смешаться с числами условия.
2. **`ai_blurb` по умолчанию из формулы выпал.** Список владельца 07.09 —
   полный и явный, поэтому в `v2` его нет. Это отдельной строкой в отчёте,
   чтобы владелец подтвердил или вернул; цену возврата меряет `v2_plus_blurb`.
3. **Порядок владельца (метаданные в хвост, решение перед условием)
   принят как есть.** Расхождение с принципом «различающее вперёд» не
   оспаривается, а меряется парой `v2` ↔ `v2_meta_first`.
