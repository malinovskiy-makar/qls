# PROGRESS.md — ход сессии «формула v2»

Все числа — в `NUMBERS.md`, решения и расхождения — в `DECISIONS.md`,
инструкция аренды — в `RUNBOOK_GPU.md`.

## Фаза 0. Ветка, сверка, бэкап — ГОТОВО

- Ветка `feat/embedding-formula-v2` от `main` (`326c939`). `main` не тронут.
- Проверено, на какую базу смотрит Django: `C:\Users\shipu\qls\db.sqlite3`.
- Бэкап `C:\Users\shipu\weconomics-data\backups\db_formula_v2_phase0_20260907.sqlite3`:
  размер сошёлся, `integrity_check: ok`, 41 307 задач. **Пригодился** — фаза 2.
- Таблица раздела 1 сверена: сошлось всё, кроме `plot` (Р-01).

## Фаза 1. Допрогон 1 740 задач — ГОТОВО

- Замкнутый круг разомкнут в коде: `--include-nonok`, `--ids-file`, честное
  предупреждение с тремя причинами. Проверено на живом банке: **без флага в
  прогон ушло бы 4 задачи из 1 740**, с флагом — 1 740.
- Прогон остановился сам на сторожe «пустые подсказки» (5,2 % при 5,0 %);
  разбор и решение владельца — Р-05. Дожат флагом.
- Итог: **1 740 = 1 047 + 693 + 0 невосстановленных**, $1,8991 из потолка $3.
  Хвостовой `run3b` закрыл 6 из 7 бракованных за $0,0172.
- Гейт §11: **все инварианты «должно быть 0» прошли**; шесть долевых порогов
  не сошлись и каждый разобран сравнением с run1 на тех же задачах.

## Фаза 2. Мерж и статусы — ГОТОВО

- ⚠️ **Первый мерж откачен из бэкапа фазы 0**: сверка отпечатка показала
  14 784 изменившиеся чужие задачи. Найдена ошибка в `merge_enrichment_v2`
  (Р-06), починена, закрыта пятью тестами.
- Повторный мерж: изменилось 1 734 задачи, из них вне манифеста ровно одна и
  та документированная (`LEGACY_CROSSWALK`). Отпечаток защищённых полей и
  отпечаток текстов — до = после.
- Повышение `content_status`: **559 задач `needs_fix` → `ok`, из них 452
  опубликованы**. Задач вне манифеста не тронуто (проверено кодом).
- **Каталог: 13 968 → 14 082 видимых задач.**

## Фаза 3. Формула v2 — ГОТОВО

- `problems/embedding_formula.py`: спецификация данными, **19 блоков**,
  **17 вариантов** замера, боевая `v2` — 16 блоков в порядке владельца.
- **`v1` воспроизводится символ в символ**: юнит-тест на краевых случаях плюс
  `--self-check 2500` по живому банку — **0 расхождений**.
- `problem_to_text` — тонкая обёртка над `build_text(p, ACTIVE_SPEC)`.
- `ACTIVE_SPEC_NAME = 'v1'` — не переключено, это шаг ранбука.
- `EMBEDDING_MAX_SEQ_LENGTH = 8192` заведена явно; пулинг BGE-M3 — CLS.

## Фаза 4. Вывоз, ввоз, офлайн-замер — ГОТОВО

- `embeddings_export_texts` (гейт допрогона, `--self-check`),
  `embeddings_export_queries`, `tools/gpu_encode.py`,
  `embeddings_check_build`, `embeddings_import_vectors`,
  `search_eval --vectors/--query-vectors`.
- Проверено на живом банке: вывоз 41 307 текстов за **32 секунды**,
  и **`search_eval --vectors` даёт числа, совпадающие с замером по базе бит
  в бит** (набор A, 2 619 запросов).

## Фаза 5. Ранбук аренды — ГОТОВО

`RUNBOOK_GPU.md` — десять шагов, от выбора машины до того, что обязательно
сделать после ввоза.

## Что осталось владельцу

1. **Приёмка мержа глазами** — `reports/formula_v2/accept3/merge_sample.html`.
2. **Пуш ветки** — коммиты сделаны, пуш за владельцем (правило 6).
3. **Задача id 55678** — единственная, не прошедшая допрогон; ложное
   срабатывание детектора цифр на «mp3» (Р-07). Пока она есть, боевой вывоз
   идёт с `--allow-run1-leftovers`.
4. **Аренда видеокарты** — по `RUNBOOK_GPU.md`.
5. **Полный прогон тестов** — эта сессия его не гоняла (204 минуты, прямой
   запрет задания). Гонялись затронутые модули.

---

# Сессия «заголовки и v2_lean» (07.09, продолжение)

## Фаза −1. Сверка с реальностью — СОШЛОСЬ ВСЁ

| Что | Ожидалось | Факт | Сошлось |
|---|---|---|---|
| Ветка | `feat/embedding-formula-v2`, дерево чисто | она, отслеживаемых правок 0 | да |
| Задач в банке | 41 307 | 41 307 | да |
| `content_status` ok / needs_fix / junk | 37 582 / 2 113 / 1 612 | 37 582 / 2 113 / 1 612 | да |
| Видимых в каталоге | 14 082 | 14 082 | да |
| `ACTIVE_SPEC_NAME` | `v1` | `v1` | да |
| Вариантов в `SPECS` | 17 | 17 | да |
| `enrichment_source` run2 / run3 / run1 / пусто | 37 342 / 1 739 / 1 344 / 882 | те же | да |
| `manage.py check` | 0 issues | 0 issues | да |
| `title_source` kept | 22 816 | 22 816 | да |
| `title_source` model-firstline | 12 862 | 12 862 | да |
| `title_source` model-empty | 2 444 | 2 444 | да |
| `title_source` model-broken | 2 303 | 2 303 | да |
| `title_source` литеральное | 882 | 882 | да |

Ни одного расхождения — останавливаться не пришлось.

## Фаза 0. Бэкап — ГОТОВО

`C:\Users\shipu\weconomics-data\backups\db_20260907_titles_lean_phase0.sqlite3`,
1 291 358 208 байт (совпал с исходником), `integrity_check: ok`, 41 307 задач.
Путь записан в `STATE.json` как `backup_titles_lean_phase0`.

## Фаза 1. `ai_blurb` вернулся в боевую формулу v2 — ГОТОВО

- `blurb` — четвёртый блок `v2`, бюджет 400, между понятиями и запросами.
  Порядок: темы → теги → понятия → **ai_blurb** → запросы → найти → дано →
  решение → условие → подпункты → подсказки → особенности → тип и характер →
  олимпиада → обоснование сложности → сложность → заголовок. **17 блоков.**
- `v2_plus_blurb` → **`v2_no_blurb`**, смысл перевёрнут (абляция СНИМАЕТ блок),
  версия прежняя — 103.
- Инварианты: `--self-check 2500` на живом банке — **0 расхождений**; `v2` —
  ровно 17 блоков, `blurb` на позиции 4; `v2_no_blurb` отличается от `v2`
  сравнением списков ровно одним блоком; наследование состава проверено
  подмножеством по всем вариантам, кроме `v1`, `v2_core` и `v2_lean`.

## Фаза 2. Вариант `v2_lean` — ГОТОВО

Версия 106, восемь блоков: заголовок → темы → теги → понятия → найти → дано →
условие → подпункты. Бюджеты: условие 400, подпункты 400 на каждый, найти 400,
дано 500. Медиана **1 093 символа (~364 токена)** — в ожидавшейся полосе
1 000–1 200. Инвариант «длиннее `v1`, короче `v2`» держится: 586 < 1 093 < 2 137.

## Фаза 3. Заголовки — КЛАССИФИКАЦИЯ ГОТОВА, ЗАМЕНА НЕ ПРИМЕНЕНА

Стоп-гейт: отчёт `reports/formula_v2/titles_review.html` собран, ответа
владельца нет — команда `apply_title_candidates` в базу не писала ни разу.

## Фаза 4. Баг «имя поля вместо значения» — ГОТОВО

Причина найдена и **воспроизведена**, а не предположена (см. `DECISIONS.md`,
Р-09). 882 задачи очищены, `title_candidate` непусто теперь ровно у 40 425.

## Фаза 5. Зубастость тестов — см. `NUMBERS.md`, таблица мутаций.

## Фаза 3.3. Замена применена — ГОТОВО (после ответа владельца)

**Критерий поправлен решением владельца 07.09:** «не трогаем задачи с длинным
названием, но с термином внутри». Клетка «длиннее 40, термин есть» (6 840)
снята, и **длина из корзины C выпала целиком** — осталось одно условие вместо
двух: есть термин словаря → оставляем, нет → заменяем.

| Корзина | было по первому критерию | стало |
|---|---|---|
| A | 12 473 | 12 473 |
| B | 318 | 318 |
| C | 16 338 | **9 498** |
| C-оставляем | 5 599 | **12 439** |
| **под замену** | 29 129 | **22 289** |

Пять инвариантов задания — проверены кодом ПОСЛЕ применения, независимо от
самой команды:

| Инвариант | Результат |
|---|---|
| 1. Защищённые поля до = после | **совпали с бэкапом фазы 0 побайтово** (`89c339ef…`) |
| 2. Изменившихся ровно столько, сколько насчитали корзины | 22 289 = 22 289, все id из A+B+C |
| 3. Заголовков, равных имени поля модели | **0** во всём банке |
| 4. Изменённых заголовков длиннее 40 символов | **0** |
| 5. `--revert` возвращает побайтово | **проверено настоящим прогоном на копии боевой базы** |
| (сверх задания) идемпотентность | второй проход даёт **0** под замену |

Снимок «было»: `reports/formula_v2/title_apply_backup.json` (22 289 задач).
Откат — `manage.py apply_title_candidates --revert --apply`.
