# PROGRESS.md — ход сессии «формула v2»

Все числа — в `NUMBERS.md`, решения и расхождения — в `DECISIONS.md`,
инструкция аренды — в `RUNBOOK_GPU.md`.

## Фаза 0. Ветка, сверка, бэкап — ГОТОВО

- Ветка `feat/embedding-formula-v2` от `main` (`326c939`). `main` не тронут.
- Проверено, на какую базу смотрит Django: `C:\Users\shipu\qls\db.sqlite3`.
- Бэкап `C:\Users\shipu\weconomics-data\backups\db_formula_v2_phase0_20260907.sqlite3`:
  размер сошёлся, `integrity_check: ok`, 41 307 задач. **Пригодился** — фаза 2.
- Таблица раздела 1 сверена: сошлось всё, кроме `plot` (Р-01).

## Фаза 1. Допрогон 1 740 задач — ГОТОВО

- Замкнутый круг разомкнут в коде: `--include-nonok`, `--ids-file`, честное
  предупреждение с тремя причинами. Проверено на живом банке: **без флага в
  прогон ушло бы 4 задачи из 1 740**, с флагом — 1 740.
- Прогон остановился сам на сторожe «пустые подсказки» (5,2 % при 5,0 %);
  разбор и решение владельца — Р-05. Дожат флагом.
- Итог: **1 740 = 1 047 + 693 + 0 невосстановленных**, $1,8991 из потолка $3.
  Хвостовой `run3b` закрыл 6 из 7 бракованных за $0,0172.
- Гейт §11: **все инварианты «должно быть 0» прошли**; шесть долевых порогов
  не сошлись и каждый разобран сравнением с run1 на тех же задачах.

## Фаза 2. Мерж и статусы — ГОТОВО

- ⚠️ **Первый мерж откачен из бэкапа фазы 0**: сверка отпечатка показала
  14 784 изменившиеся чужие задачи. Найдена ошибка в `merge_enrichment_v2`
  (Р-06), починена, закрыта пятью тестами.
- Повторный мерж: изменилось 1 734 задачи, из них вне манифеста ровно одна и
  та документированная (`LEGACY_CROSSWALK`). Отпечаток защищённых полей и
  отпечаток текстов — до = после.
- Повышение `content_status`: **559 задач `needs_fix` → `ok`, из них 452
  опубликованы**. Задач вне манифеста не тронуто (проверено кодом).
- **Каталог: 13 968 → 14 082 видимых задач.**

## Фаза 3. Формула v2 — ГОТОВО

- `problems/embedding_formula.py`: спецификация данными, **19 блоков**,
  **17 вариантов** замера, боевая `v2` — 16 блоков в порядке владельца.
- **`v1` воспроизводится символ в символ**: юнит-тест на краевых случаях плюс
  `--self-check 2500` по живому банку — **0 расхождений**.
- `problem_to_text` — тонкая обёртка над `build_text(p, ACTIVE_SPEC)`.
- `ACTIVE_SPEC_NAME = 'v1'` — не переключено, это шаг ранбука.
- `EMBEDDING_MAX_SEQ_LENGTH = 8192` заведена явно; пулинг BGE-M3 — CLS.

## Фаза 4. Вывоз, ввоз, офлайн-замер — ГОТОВО

- `embeddings_export_texts` (гейт допрогона, `--self-check`),
  `embeddings_export_queries`, `tools/gpu_encode.py`,
  `embeddings_check_build`, `embeddings_import_vectors`,
  `search_eval --vectors/--query-vectors`.
- Проверено на живом банке: вывоз 41 307 текстов за **32 секунды**,
  и **`search_eval --vectors` даёт числа, совпадающие с замером по базе бит
  в бит** (набор A, 2 619 запросов).

## Фаза 5. Ранбук аренды — ГОТОВО

`RUNBOOK_GPU.md` — десять шагов, от выбора машины до того, что обязательно
сделать после ввоза.

## Что осталось владельцу

1. **Приёмка мержа глазами** — `reports/formula_v2/accept3/merge_sample.html`.
2. **Пуш ветки** — коммиты сделаны, пуш за владельцем (правило 6).
3. **Задача id 55678** — единственная, не прошедшая допрогон; ложное
   срабатывание детектора цифр на «mp3» (Р-07). Пока она есть, боевой вывоз
   идёт с `--allow-run1-leftovers`.
4. **Аренда видеокарты** — по `RUNBOOK_GPU.md`.
5. **Полный прогон тестов** — эта сессия его не гоняла (204 минуты, прямой
   запрет задания). Гонялись затронутые модули.
