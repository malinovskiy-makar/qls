# Переранжирование умного поиска моделью — журнал сессии

Ветка `feat/smart-search-rerank` (от `feat/llm-search-eval`), 11.09.2026,
Claude Code. Числа — в `SUMMARY.md` этой же папки. Здесь — по фазам, что
сделано, что разошлось с заданием и почему.

---

## Фаза −1. Сверка с реальностью — расхождения

1. **`git status` не был чистым** на старте — посторонние нетронутые файлы
   (список в конце файла), не моего создания.
2. Ветку `feat/smart-search-rerank` создал не сразу — по спешке начал править
   прямо на `feat/llm-search-eval`, переключился в середине разведки
   (`git checkout -b`, изменения перенеслись без потерь).
3. `openai` и `bm25s` стоят в venv, но не зафиксированы ни в одном
   `requirements/*.txt` (только `openai` в `requirements/local.in`, `bm25s`
   нигде). Не чинил — вне рамок сессии, уже отслеживается в Notion (см. ниже).
4. Плотный поиск локально числится включённым (`SEMANTIC_SEARCH_ENABLED=1`
   по умолчанию), но реально недоступен: `search_service` требует HTTP-вызов
   в соседний контейнер, а Docker Desktop не был поднят в начале сессии.

## Фаза 0-1. Каркас и встраивание — сделано, коммит `7fa2e9b`

`catalog/rerank.py`, точка внедрения в `catalog/views.py:_catalog_context`,
настройки в `config/settings.py`. Подробности — `SUMMARY.md`.

**Архитектурное решение, принятое в сессии:** модуль обходит «одну дверь
наружу» (`problems/ai/core.run`) и зовёт `GLMProvider` напрямую. Причина —
докстринг в шапке `catalog/rerank.py`. Зафиксировано в Notion явно (см. ниже).

## Фаза 2. Тесты и зубастость — сделано

26 тестов, `catalog/tests/test_rerank.py`. Зубастость проверена дефект →
красный → откат → зелёный на пяти участках:
- `merge()` (сортировка по баллу) — при первом коммите каркаса;
- сборка пула с дедуп-группами (`collapse_dedup`);
- откат по таймауту (сужение `except` до `ValueError` — исключение
  перестаёт гаситься);
- нечитаемый ответ модели (искусственное проглатывание `ValueError` внутри
  `call_one` — тест ловит, что «мусор» стал тихо восприниматься как валидный
  ответ);
- кэш (отключение проверки `expires` — тест ловит повторный вызов `_run`).

Каждый раз: точечная правка → тест красный → правка снята → тест зелёный;
после отката `git diff -- catalog/rerank.py` пуст (совпадает с коммитом).

## Фаза 3. Живая проверка — сделано, $0,0043

`runserver` с `SMART_SEARCH_RERANK=1`, сессия сотрудника создана напрямую
через `django.contrib.sessions` (без смены пароля `admin`). Три запроса,
подмена ключа, флаг выключен — все три сценария подтвердились. Подробности
и топ-10 по темам — `SUMMARY.md`.

⚠️ Первый прогон трёх запросов дал мойбоке в логе для двух из трёх
(`curl --data-urlencode` на этой Windows/git-bash машине не кодирует
кириллицу в UTF-8 для аргументов командной строки) — это ловушка тестового
стенда, не баг приложения: те же запросы, закодированные вручную в UTF-8
percent-encoding, прошли штатно и дали `rerank` с осмысленным топ-10.

## Стоп-гейт — пройден, «да» владельца 11.09.2026

Уточнения владельца учтены построчно (см. коммиты выше и разделы ниже).

## Фаза 4. Задержка через код сайта — сделано частично, $0,128

91 запрос через `catalog.rerank.rerank()` (реальный код сайта, не скрипты
замера), `reports/smart_search_rerank/run_phase4.py`. Результат — bm25-only
конфигурация (= сегодняшняя боевая, `SEMANTIC_SEARCH_ENABLED` на проде не
влияет, там плотного сервиса нет вовсе). Все 91 — успех, ни одного отката.

**dense+bm25 не измерено.** Владелец разрешил прогнать второй раз, если
поднимется Docker — Docker Desktop поднялся в середине сессии, но
`docker-compose.dev.yml` намеренно не публикует порт `search` наружу
(комментарий в файле: «на этом стоит проверка изоляции», повторяет боевую
раскладку). Django на этой машине — не в Docker, поэтому даже с поднятым
контейнером достучаться нельзя, не сломав саму изоляцию, которую файл
специально проверяет. Строка оставлена пустой с этим уточнением, докер не
трогал, порт не публиковал.

## Фаза 5. Пул для прода — сделано, $0

Три состава пула посчитаны офлайн из уже готовых `rerank_glm-flash.jsonl` +
`pool.jsonl` (кто откуда в пуле) — без единого нового вызова модели, методом
из `reports/llm_search_eval/metrics.py` (зафиксирован 10.09.2026).
`reports/smart_search_rerank/run_phase5.py`. Таблица и вывод — `SUMMARY.md`.

Задержка «только bm25» — из живого прогона Фазы 4 (тот же код, та же
машина). Задержка «обе» — из `reports/llm_search_eval/latency.json`
(замер 10.09.2026 на своей машине с доступом к dense). Задержка «только
dense» не измерена по той же причине, что и в Фазе 4.

## Фаза 6. Отчёт и Notion

### Проверки перед сдачей (частичные — только затронутые модули, не полный CI)

- `git diff --stat -- '*.html' '*.css' '*.js'` — пусто, ни разу за всю сессию.
- `manage.py check` — чисто.
- `manage.py makemigrations --check --dry-run` — «No changes detected»
  (миграций в сессии не было, проверено на всякий случай).
- `ruff check catalog/rerank.py catalog/tests/test_rerank.py catalog/views.py
  config/settings.py` — чисто.
- `bandit -r catalog/rerank.py -ll` — без замечаний.
- `catalog.tests.test_rerank` (26) + `test_smart_catalog` +
  `test_semantic_degradation` + `test_search_service` + `test_lexical_bm25`
  (61, без единой правки) — зелёные.
- База не менялась: только временная `Session` для теста Фазы 3, создана и
  удалена явно (`Session.objects.filter(session_key=...).delete()`), пароль
  `admin` не трогал.
- Полный прогон `scripts/run_tests.py` (все 5 джобов CI) — НЕ делался: задание
  просило проверить только затронутые модули на этом шаге, не полный прогон.

### Notion

- Комментарий к карточке «Гибридный поиск нигде не подключён…»
  (`3d6b11c9-2bc1-815c-8c6a-d1f7e2ef5c8d`): bm25s теперь реально подключён
  (за флагом), `openai` тоже не в локах.
- Новая карточка «Сортировщик поиска зовёт GLMProvider в обход core.run —
  вернуть под общий учёт расходов и лимиты до выхода на прод»
  (`3d8b11c9-2bc1-811e-a604-ec52d53acf81`), направление «Каталог и поиск»,
  статус «Надо».
- Комментарий к карточке «`core.run()` принимает модель и уровень
  рассуждения…» (`3cdb11c9-2bc1-81a2-b259-c076685b095d`): второй обход,
  ссылка на новую карточку.
- Вторую карточку по openai/bm25s (пункт (а) поручения) НЕ заводил — это
  дубль двух уже существующих карточек («Гибридный поиск…» и «CI красный на
  feat/taxonomy-v2-openai-provider…», у второй ровно эта находка уже
  записана как «побочная, не трогалась»). Зафиксировал комментарием вместо
  третьей карточки — правило «дублей не плодить».
- Страница «Результаты» и разделы в карточках идей/решений — см. ссылки в
  конце финального отчёта сессии (чат).

---

## Посторонние файлы в git status (не тронуты)

```
.txt                                             3 012 байт, 31.08.2026
"New Text Document.txt"                          0 байт, 10.09.2026
kachat_vektora.ps1                               3 130 байт, 08.09.2026
session_c15_transcript_20260830.md               78 353 байт, 30.08.2026
claude/HANDOFF_OLYMPIAD_TEXT_DEDUP_20260902.md    23 373 байт, 02.09.2026
claude/RENDER_AUDIT_LEGACY_3000_20260829.md      162 376 байт, 29.08.2026
"Claude outputs/"                                17 файлов (PROMPT_*.md,
                                                  TEAM_WORKFLOW_*.md,
                                                  АРЕНДА_ВИДЕОКАРТЫ_ИНСТРУКЦИЯ.md,
                                                  kachat_vektora.ps1 — дубль
                                                  корневого), 08-09.09.2026
```

## Инструкция владельцу: как проверить локально

**1. `.env` теперь подхватывается сам.** `config/settings.py` читает
`.env` из корня проекта ДО остальных настроек (`_load_dotenv_if_present`),
если файл существует — `runserver` и любая команда `manage.py` видят
`GLM_API_KEY` без ручных танцев. Прод это не задевает: там `.env`-файла в
контейнере нет, переменные приходят через `docker compose` как раньше;
уже выставленная в оболочке переменная сильнее файла.

**2. Включить локально:**

```bash
SMART_SEARCH_RERANK=1 venv313/Scripts/python.exe manage.py runserver
```

(в PowerShell — `$env:SMART_SEARCH_RERANK=1` отдельной строкой перед
запуском). Видит только сотрудник (`is_staff=True`) при поиске в каталоге.

**3. Плотная нога (dense) требует Docker — РЕШЕНО 11.09.2026 (сессия
«локальная плотная нога умного поиска»):**

```bash
docker compose -f docker-compose.dev.yml -f docker-compose.dev.local.yml up -d search
```

Локальный оверрай `docker-compose.dev.local.yml` публикует `search` на
`127.0.0.1:8001` — только на loopback, никогда на `0.0.0.0` (тест
`DevLocalOverrideTests` в `catalog/tests/test_search_service.py` это
сторожит). Базовый `docker-compose.dev.yml` не тронут, проверка изоляции
(`ComposeIsolationTests`) продолжает читать его и красит тест, если там
появится `ports`. Затем в `.env`:

```
SEARCH_SERVICE_URL=http://127.0.0.1:8001
```

Подробности и разбор — `docs/EMBEDDINGS.md`, раздел «Плотная нога
локально, вне Docker»; разбор сессии — раздел «Сессия 2» ниже.

**4. Отличить bm25-only от dense+bm25 по строке лога.** Каждая строка в
консоли и в `reports/smart_search_log.jsonl` содержит `pool_by_leg` —
словарь `{'dense': N, 'bm25': M}`. `dense: 0` — плотная нога не сработала
(сервис недоступен или выключена настройкой); `dense: >0` — сработала.
Пример строки:

```
умный поиск: запрос='...' пул={'dense': 0, 'bm25': 50} пачек=1 модель=5.05с
всего=7.77с стоимость=$0.0014 итог=rerank
```

Здесь `dense: 0` — прогон шёл без плотной ноги (Фаза 4 первой сессии).
Появление `dense: >0` в той же строке означает, что плотный поиск
заработал по-настоящему — см. пример в разделе «Сессия 2» ниже.

---

## Сессия 2: локальная плотная нога умного поиска (11.09.2026, Claude Code)

Задача: сделать плотную ногу доступной локальному `runserver`, НЕ трогая
проверку изоляции и базовый `docker-compose.dev.yml`.

### Фаза −1. Сверка с реальностью

Картина совпала с описанной в задании построчно: `SEARCH_SERVICE_URL`
по умолчанию `http://search:8001` (DNS-имя, разрешимое только внутри сети
Docker), `search` в `docker-compose.dev.yml` без `ports:` (комментарий в
файле подтверждает — это проверяемое свойство), `catalog/semantic.py`
кодирует запрос ТОЛЬКО через HTTP (`search_client.encode_one`) — отката на
локальную модель нет, и это задокументировано как намеренное решение
(«ОТКАТА НА ЛОКАЛЬНУЮ МОДЕЛЬ ЗДЕСЬ НЕТ, И ЭТО ГЛАВНОЕ»). `get_model()` в
`catalog/semantic.py` жив в файле, но мёртв в пути живого поиска — вызывают
его только тесты. Вариант «оживить in-process режим» не рассматривался
всерьёз: он бы прямо противоречил зафиксированному в коде решению против
«мины №1» (ADR 0008, ADR 0012) — и локальное удобство не повод его
пересматривать. Расхождений с описанием не найдено, продолжил без стоп-гейта.

### Фаза 0. Override-файл

`docker-compose.dev.local.yml` (корень репозитория) — публикует `search`
на `127.0.0.1:8001`. Проверено вручную: контейнер поднят, `curl
http://127.0.0.1:8001/healthz` с хоста отвечает `200`, `docker compose ps`
показывает `127.0.0.1:8001->8001/tcp` (не `0.0.0.0`). `git diff --stat`
на `docker-compose.dev.yml`/`deploy/docker-compose.yml` — пусто, базовые
файлы не тронуты.

### Фаза 1. Тесты

`catalog/tests/test_search_service.py`: `DevLocalOverrideTests` (порт
только на 127.0.0.1 — зубастость проверена дефект `0.0.0.0` → красный →
откат → зелёный; оверрай не входит в список `ComposeIsolationTests.ФАЙЛЫ`)
и `LoopbackServiceReachableViaEnvTests` (игрушечный HTTP-сервис на
`127.0.0.1:0` без Docker, `SEARCH_SERVICE_URL` из настроек реально до него
достаёт — три теста, включая проверку через `catalog.semantic.embed_query`
целиком). 21 тест файла — зелёные.

### Фаза 2. `manage.py rerank_trace`

Новая команда (`catalog/management/commands/rerank_trace.py`) плюс
минимальная правка `catalog/rerank.py::_score_pool` — теперь возвращает
`(ranked, usage, scores)` вместо `(ranked, usage)`, третий элемент нужен
только трассировке, `_run` его игнорирует (`_scores`). Прогон на «задача
на неравенство, в группах меняется количество людей» — полный вывод в
`rerank_trace_neravenstvo_20260911.txt` (локальный файл, не в git — как и
остальные числа прогонов), сводка и разбор — в `SUMMARY.md`.

### Фаза 3. Живая проверка через runserver

`SMART_SEARCH_RERANK=1` + `SEARCH_SERVICE_URL=http://127.0.0.1:8001`,
сессия сотрудника — снова напрямую через `django.contrib.sessions` для
существующего `admin` (пароль не трогал, сессия удалена по окончании).
Запрос «задача на неравенство, в группах меняется количество людей»:
`X-Smart-Search: rerank`, строка лога
`пул={'dense': 50, 'bm25': 50}` (после `повтор2` — вариация текста для
обхода часового кэша), плашки деградации на странице нет (`ss-degraded`
отсутствует), топ-10 — все десять о неравенстве доходов (id и темы — в
`SUMMARY.md`).

⚠️ Первая попытка ушла в `dense: 0` — не баг раскладки, а стрельба по
чужому порту: на 8000 висел ОСИРОТЕВШИЙ `runserver` от предыдущего
запуска этой же сессии (два процесса на одном порту одновременно,
`netstat` показал оба). Погашен, поднят один чистый процесс — вторая
попытка сразу дала `dense: 50`.

### Фаза 4. Повтор 91 запроса с dense+bm25

`reports/smart_search_rerank/run_phase4.py`, без изменений в самом
скрипте — только окружение (`SEARCH_SERVICE_URL` на loopback). Прежние
результаты bm25-only сохранены отдельно
(`phase4_results_bm25only_20260911.jsonl`/`.json`) перед перезаписью.
91/91 успех, 0 откатов, $0,208. Таблица и разбор чисел — `SUMMARY.md`.

### Проверки перед сдачей (частичные — только затронутые модули)

- `ruff check catalog/rerank.py catalog/tests/test_search_service.py
  catalog/management/commands/rerank_trace.py` — чисто.
- `bandit -r catalog/rerank.py catalog/management/commands/rerank_trace.py -ll`
  — без замечаний.
- `catalog.tests.test_rerank` (26) + `test_smart_catalog` +
  `test_semantic_degradation` + `test_search_service` (теперь 21, было 13)
  + `test_lexical_bm25` — 92 теста, зелёные.
- `manage.py check` — чисто; `makemigrations --check --dry-run` — «No
  changes detected».
- `git diff --stat -- '*.html' '*.css' '*.js'` — пусто.
- База не менялась: только временная `Session` для Фазы 3, удалена явно.
- Полный прогон `scripts/run_tests.py` (пять джобов CI) — НЕ делался: как
  и в первой сессии, задание просило проверку только затронутых модулей.

### Стоимость сессии

Фаза 2 (`rerank_trace`, 2 пачки) + Фаза 3 (2 живых запроса через страницу)
+ Фаза 4 (91 запрос, $0,208) ≈ $0,22–0,24 суммарно — в пределах
согласованного бюджета (~$0,4 на сессию, с учётом первой сессии $0,132).
