# Журнал сессии: демо-ученик для витрины (`seed_showcase_student`)

> **Владелец:** Claude Code · **Дата:** 2026-09-21 · **Статус:** в работе
> Промпт: `claude/PROMPT_SHOWCASE_STUDENT_20260921.md` (знания проекта).
> Карточка в Notion: «Демо-ученик для витрины: команда seed_showcase_student и запуск на бою».

## 0. Где работаем и что проверено (стоп-гейт Фазы −1)

Первый заход остановился на стоп-гейте: приложение завело worktree не в
`~/Downloads/qls-demo`, а под `.claude/worktrees/`. Владелец решил остаться
здесь и поправил критерии проверок 1 и 2. Проверки повторены заново.

| # | Проверка | Ожидание (с поправкой владельца) | Факт | Итог |
|---|---|---|---|---|
| 1 | Папка и ветка | `~/Downloads/qls_platform/.claude/worktrees/showcase-student-demo-57b4b5`, ветка `claude/showcase-student-demo-57b4b5`, дерево чистое | совпало, `git status --short` пуст, HEAD `e586516e` | ✅ |
| 2 | База своя | `db.sqlite3` обычный файл, inode 129407281; у основной папки другой (101679301) | 129407281 против 101679301, оба «Regular File», 463 036 416 Б | ✅ |
| 3 | Миграции | `check` чисто; неприменённые — только записать | `showmigrations` (все приложения): неприменённых **нет**; `check`: «no issues» (предупреждение про `REDIS_URL` — норма локально) | ✅ |
| 4 | Толщина банка (видимые задачи) | ≥2000 с темой, ≥12 тем, ≥100 задач сложности 4–5 | **2146** с темой, **44** темы, **252** сложности 4–5 (видимых всего 5090) | ✅ |

`venv313` — символическая ссылка на общий venv основной папки
(`~/Downloads/qls_platform/venv313`). В него ничего не ставим (ограничение 5).

### Наблюдения, влияющие на генератор

- Запас по банку небольшой: **2146** против порога 2000. Разрешённые задачи
  выбираются ЗАПРОСОМ, а не по номерам.
- Сложность 4–5 сосредоточена в ~14 темах (Эластичность 38, КПВ 47,
  Неравенство 25, Совершенная конкуренция 22, Теория фирмы 14, Вмешательство
  государства 15, Финансы 15, Инфляция и безработица 15, Монополия и ценовая
  дискриминация 13, Рынок труда 10, ВВП 9, Монетарная политика 8, Олигополия 7,
  Международная торговля 6). В остальных ~30 темах сложных задач 0–2.
  Уровень владения темой «разобрался» требует ≥3 сложных решённых в теме (уточнить
  по коду в Фазе 0) — темы для `mastered` берём из этой четырнадцатки.
- У части видимых задач с темой `difficulty IS NULL` — такие задачи не годятся
  для истории с «сложностью»; брать только с непустой сложностью.
- `Topic.is_canonical` среди 44 тем видимого банка — 0 (локальная база; на бою
  может быть иначе). Канон разделов держит `problems/sections.py` — уточню в Фазе 0.
- В промпте фильтр назван `catalog/views.py::_visible`; такой функции нет —
  тот же фильтр (`status=PUBLISHED, needs_quality_review=False,
  hidden_pending_review=False, content_status=OK`) записан в
  `catalog/views.py::_visible_problem` (стр. 828).

## Развилки и принятые решения

| # | Развилка | Выбор | Почему |
|---|---|---|---|
| Р1 | `reports/*` целиком в `.gitignore` (строка 67), а править существующие файлы нельзя | `JOURNAL.md` и `RUNBOOK.md` кладём в git через `git add -f`, `.gitignore` не трогаем | Ограничение 2: только новые файлы. Уже добавленный файл остаётся отслеживаемым; если владельцу нужно «по-хорошему», отдельный коммит с исключением в `.gitignore` — хвост в отчёте |

## Фаза 0. Карта экрана статистики `/profile/stats/`

Источники: `problems/views_stats.py`, `problems/stats.py`, `problems/templates/platform/stats.html`.
Экран имеет 14 визуальных блоков (шапка и 4 карточки можно считать одним — 13).
Всё, что не переключается периодом, читается за `'all'`: рейтинг, радар, «Стоит вернуться», игра, динамика, достижения, рекорды.

| # | Блок | Откуда данные | Что должно существовать, чтобы блок не был пустым | Пустая строка на экране |
|---|---|---|---|---|
| 1 | Шапка: уровень, серия, опыт, недельная цель | `StudentProgressProfile`, `weekly_progress` | профиль после пересчёта; `weekly_goal` подобран под неделю | — (будут нули) |
| 2 | Четыре карточки (решено, доля верных, время, опыт) | `stats.overview` → `_problem_events` (без игры, с задачей); опыт из `DailySummary` | события `solved`/`failed` с задачей за период; для стрелки «к прошлому» — попытки и в окне 31–60 дней назад | «нет ответов», «за выбранный период» |
| 3 | Карта тем | `topic_breakdown` | события с `topic`; метки: <5 попыток «мало данных», ≥85% «мастер», ≥60% «разобрался», иначе «стоит подтянуть» | «Ещё нет решённых задач с темой. Реши несколько из каталога, и карта заполнится сама.» |
| 4 | Сильные и слабые темы | `strongest_weakest` (всегда `all`) | ≥5 попыток у темы; полные 5+5 — при ≥10 таких темах | «Ни одна тема пока не набрала 5 попыток…» / «Порог в 5 попыток набрала пока одна тема…» |
| 5 | Радар «Разделы экономики» | `section_radar`, тема → раздел по ТОЧНОМУ имени | попытки по темам всех пяти разделов: micro, macro, fin, math, other; неизвестное имя уходит в other | рисуется всегда, пустая ось — точка в центре |
| 6 | По сложности | `difficulty_split` по снимку `LearningEvent.difficulty` 1–5 | события с непустой `difficulty` | «Решённых задач пока мало, карта сложности заполнится сама.» |
| 7 | Активность (сетка, часы, дни недели) | `activity_grid`, `minutes_by_hour/weekday` по ВСЕМ событиям, МСК | события в разные дни/часы; шаг сессии ≤30 мин | подпись «Пока не из чего считать…» |
| 8 | Динамика уровня | `level_history` из `DailySummary` (`all`) | ≥2 дня, лучше ≥10 | «Реши первую задачу, и линия начнёт расти.» |
| 9 | Где решаешь | `source_split` (4 столбца, игра входит) | события с `source` catalog/homework/exam/game | пустого текста нет |
| 10 | Достижения | `EarnedAchievement` + справочник `Achievement` (29 шт.) + `rarity_map` | справочник из `seed_achievements`; выданные после пересчёта | пустого текста нет |
| 11 | Рекорды | `PersonalRecord` (3 вида; `fastest_homework` не пишется никем) | пересчёт; дата рекорда лежит в `payload['date']` | блок скрыт без записей |
| 12 | Стоит вернуться | `hardest_problems` (`all`) | ≥5 разных задач каталога хотя бы с одним `failed` | блок скрыт |
| 13 | Что отмечает преподаватель | `TeacherFeedback`+`MistakeTag` на `Submission` ученика; `StudentSkillProgress` | ≥3 метки с разным счётом, ≥3 навыка | блок скрыт |
| 14 | Игра | `game_stats`, `game_miss_topics` | события `source='game'` `solved`/`failed`, `payload['mode']`; для воронки — `failed` с темой ≥3 тем | «Ты ещё не играл. Попробовать Wecon Rush» |

### Что нужно карточке ученика у репетитора и «Обзору» группы

- Доступ: репетитор — `teacher` группы, в которой состоит ученик (`StudentGroup.teacher`); карточка ученика НЕ открывается редиректом,
  если у пары ровно одно занятие и оно индивидуальное — поэтому демо-ученик состоит в группе, не в `individual`.
- Работа попадает в историю ученика при `Assignment.author=tutor` И `students ∋ ученик`; в историю группы — при `Assignment.group=группа`. Нужны все три связи.
- «Оценка» и проценты: `Submission.problem_item` задан, `TeacherFeedback.score` не NULL.
- «Средняя сложность работ» читает ТОЛЬКО `WorkDifficulty` (шкала 1–10, ставит ученик).
- Матрица «ученики × темы» и рейтинг берут `LearningEvent.topic`; названия тем — из 23 канонических имён.
- Геймификации (серия, XP, уровень) на экране репетитора нет вообще (принцип).
- Блока «Что отмечает преподаватель» у репетитора нет — он на экране ученика (`stats.html:341`).

### Правила геймификации, важные для подбора истории

- XP дают только `solved`; база по сложности 1–5: 2 / 5 / 15 / 45 / 90 (NULL → 5); лёгкие (≤2 или NULL) затухают по локальному дню
  (первые 5 — ×1, 6–15 — ×0,5, дальше ×0,2); `exam` ×1,5; повторное решение той же задачи XP не даёт. Уровень L: порог `int(100·(L−1)^1,5)`
  (12 → 3648, 15 → 5238, 17 → 6400, 20 → 8281).
- День в серию: ≥3 события `attempted/failed/solved` ИЛИ сданная работа; две «заморозки» в месяц прощают пропуск до 2 дней;
  `current_streak` жив, только если последний зачтённый день — сегодня/вчера. День по Москве.
- Владение темой: `mastered` — решено ≥15, доля ≥0,80, из них ≥3 со сложностью ≥4 (по СОБЫТИЮ); `confident` — ≥8 и ≥0,60; `familiar` — ≥3.
- Достижений **29, а не 30** (в промпте «из 30» — округление): depth 6, consistency 7, skill 6, milestone 10.
  Все, кроме четырёх (`first_homework`, `homework_10`, `first_exam`, `exam_5`), выполняются одними `LearningEvent`; эти четыре читают `Submission`
  (`submitted/reviewed`, различные работы нужного `kind`). `ExamAttempt` и `TeacherFeedback` геймификация не читает.
- «Верных подряд» = подряд идущие `solved` без `failed` между ними по ВСЕМ источникам (включая игру), сортировка `created_at` без тайбрейка → метки времени нужны уникальные.
- `recalculate_gamification`: `DailySummary` и темы пересобирает, профиль перезаписывает (`weekly_goal` не трогает), `EarnedAchievement` и `PersonalRecord` НЕ удаляет
  и рекорды не снижает → перед пересчётом их нужно стереть самим.
- Без `seed_achievements` `check_achievements` ничего не выдаст.

### Ловушки (повторены решения из `seed_platform_demo._history`, а где они неполны — исправлены)

| Ловушка | Решение в этой команде |
|---|---|
| `LearningEvent.created_at` — `auto_now_add`, `bulk_create` перетирает | задуманные даты в отдельном списке → `bulk_update(objs, ['created_at'])` (не вызывает `pre_save`, `auto_now*` не срабатывает); тест 3 на это |
| в сиде история кончается ВЧЕРА (`range(90, stop, -1)` без 0) → `current_streak = 0` | история идёт до СЕГОДНЯ включительно; события «сегодня» зажаты `≤ now` |
| в сиде час ставится `now.replace(hour=…)` в UTC → «вечер» уезжает за МСК-полночь | моменты строятся как `make_aware(datetime.combine(локальная_дата, time(h, m)))` |
| `EarnedAchievement.earned_at` — `auto_now_add` | даты проставляются `.update()` после пересчёта |
| `PersonalRecord.achieved_at` — `auto_now` | человеческая дата — `payload['date']` (экран берёт её оттуда); `achieved_at` ставится `.update()` |
| `TeacherFeedback.reviewed_at`, `ExamAttempt.started_at` — `auto_now_add` | `.update()` после создания (в сиде `started_at` не правится, из-за чего «затрачено» выходит отрицательным) |
| кэш `stats_full_<pk>_<period>` 5 мин, `achv_rarity_map` 10 мин | `stats.invalidate(user)` + `cache.delete('achv_rarity_map')` |
| `log_event/log_problem_event` сразу начисляют XP «по сегодня» | события пишутся ORM-ом напрямую, начисление — только пересчётом |

| Р2 | Выбор темы/сложности: у 76 % видимых задач `Problem.difficulty = NULL` (реальная есть у 279 из 2146 видимых с темой, из них 239 — «5») | для событий по задачам без сложности сложность берётся из демо-распределения по роли темы (`DIFF_WEIGHTS`); реальная — когда она есть и подходит. Тот же приём у `seed_platform_demo` (`problem.difficulty or randint(1, 4)`) | Иначе решённых с 4–5 не набрать, а пять уровней не показать. Снимок сложности в событии законно может расходиться с полем задачи |
| Р3 | Тема события: `Problem.topics.first()` (как в `event_log`) против «первой канонической» | тема события = ПЕРВАЯ КАНОНИЧЕСКАЯ тема задачи (23 имени `CANONICAL_SECTION`); одна неканоническая тема даёт ось «Прочее» | Радар и «Обзор» группы группируют по точному имени. Первая каноническая совпадает с `_first_canonical_topic_id` у экрана репетитора |
| Р4 | Пересчёт не отнимает достижения и не снижает рекорды | перед пересчётом стираются `EarnedAchievement`, `PersonalRecord`, `DailySummary`, `StudentTopicProgress`, профиль демо-учеников | Иначе после смены истории остаются «привидения» прошлого прогона |
| Р5 | Даты достижений: точно или «равномерно по истории» | точно: реальные правила гоняются по префиксам истории в откатываемых `savepoint` (≤ ~40 проб на ученика); `weekly_goal_*` считаются отдельно, потому что правило зависит от «сегодня» | Дёшево (весь прогон ~7 с) и не дублирует правила геймификации |
| Р6 | Контрольные: писать через `exam_engine.grade_attempt` или напрямую | напрямую: `ExamAttempt` + `Submission` + `TeacherFeedback(reviewed_by=NULL)` + события `source='exam'` | `grade_attempt` через `log_event` сразу начисляет XP «по сегодня» и пишет `created_at=сейчас` |
| Р7 | Событие по ещё не проверенной домашке | `attempted`, а не `solved/failed` | Так делает боевой `_log_submission_event` |
| Д1 | ЦЕЛЬ «источники ≈45/40/15» недостижима вместе с требованием «10–12 работ по 5–8 задач и 2 контрольные, а события по работам — часть той же истории» (12×6,5 ≈ 78 событий ≈ 10 % от 794; две контрольные ≈ 22) | приоритет у согласованности «событие ↔ реальная сдача»: факт около 90/7/3 | Иначе разбивка «Где решаешь» разошлась бы с числом сданных работ (сам же промпт требует этого). Расхождение честно показано в таблице «факт против цели» |
| Д2 | Достижений 29, а не 30 | цели «из 30» читаются как «из 29»; 16–24 получено, ≥5 не получено остаются | Справочник `seed_achievements` = 29 |
| Д3 | Недельная цель `weekly_goal` подбирается под день недели запуска (ceil(сделано/0,8)) | пересчёт её не трогает, задаётся до него | Иначе 20 по умолчанию даёт полосу 0–30 % в понедельник. Значение зависит от дня, когда запущена команда → в рунбуке: перед показом запустить `--apply` ещё раз |

## План фаз

- S0 — разведка: карта блоков экрана (ниже), правила геймификации, образцы сида.
- S1 — каркас команды, пять аккаунтов, группа, `--purge`.
- S2 — история решений за 90 дней (первоисточник — `LearningEvent`).
- S3 — домашки, контрольные, проверки, навыки, оценки сложности, заметка.
- S4 — игра, `recalculate_gamification --user-id` ×4, даты достижений, кэш.
- S5 — тесты с зубастостью, числовые инварианты «факт против цели».
- S6 — рунбук, отчёт, Notion.
